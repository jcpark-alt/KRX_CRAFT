<?xml version="1.0" encoding="UTF-8"?>
<!-- SBChart 리치 옵션 예제 (bar): CHART_CONFIG 에 global/axis/grid/title/legend/tooltip/extend 다수 사용
  - 2026-08-26 SPCHART.md 회신 반영(문의 처리 요약: sbchart/md/SPCHART.md §1) : 코드 컨벤션 정리 + 색상 팔레트·툴팁 값 포맷·clear — var→const, fn_click→click
-->
<html xmlns="http://www.w3.org/1999/xhtml" xmlns:ev="http://www.w3.org/2001/xml-events"
      xmlns:w2="http://www.inswave.com/websquare" xmlns:xf="http://www.w3.org/2002/xforms">
<head meta_screenId="SPCHART_RICH_BAR" meta_screenName="SBChart 리치옵션 - 막대" meta_desc="옵션 다수 예제" meta_author="WRM" meta_date="2026.07.08">
    <w2:type>COMPONENT</w2:type>
    <w2:buildDate/>
    <w2:MSA/>
    <xf:model>
        <xf:instance><data xmlns=""/></xf:instance>
        <w2:dataCollection baseNode="map"/>
        <w2:workflowCollection/>
    </xf:model>
    <w2:layoutInfo/>
    <w2:publicInfo method=""/>
    <script lazy="false" type="text/javascript"><![CDATA[

scwin.CHART_DATA_URL ="/ui/sample/SBChart/COLUMNS/data/COLUMNS.json";   // data(type:bar, columns) 조회

// ---- 차트 공통 상수 (SPCHART.md 회신 §1 일반화) ----
scwin.CHART_COLORS = ["#E52528", "#1B64DA", "#0A8060", "#F5A623", "#7B1FA2"];   // ★ 시리즈 색상 팔레트 — 현장 조정

scwin.CHART_CONFIG = {
    "tooltip": {   // 마우스오버 표시값 — 천단위 콤마 — SPCHART.md §1-7
      "format": {
        "value": function (v, ratio, id, index) { return $c.num.formatNumber(v); }
      }
    },
    "color": { "pattern": scwin.CHART_COLORS },   // 시리즈 색상 명시 (미지정 시 라이브러리 기본색) — SPCHART.md §1-3
    global: {
        color:   { theme: "a11y" },                       // 색 테마
        padding: { top: 20, right: 30, bottom: 40, left: 60 },
        animation: { show: true, delay: 100, ease: "easeCubic" },
        onclick: function (data) {                         // 막대 클릭 이벤트
            scwin.click("클릭: " + data.name + " = " + data.value + " (idx " + data.index + ")");
        }
    },
    title: { text: "연도별 분기 실적", position: "top" },   // 제목
    axis: {
        y: {
            min: 0, max: 400,
            label: { text: "금액(만원)" },                 // Y축 라벨
            tick:  { format: function (v) { return v.toLocaleString(); } }  // Y눈금 포맷
        },
        x: { label: { text: "분기" } }                     // X축 라벨
    },
    grid: { y: { show: true } },                           // 가로 격자선
    legend: { show: true, position: "bottom" },            // 범례 하단
    tooltip: {                                             // 커스텀 툴팁
        custom: function (arr) {
            const d = arr[0];
            return '<div style="padding:8px;background:#222;color:#fff;border-left:4px solid #40bcd3;border-radius:4px;">'
                 + '<b>' + d.name + '</b><br/>' + Number(d.value).toLocaleString() + ' 만원</div>';
        }
    },
    extend: {
        bar: { width: 18, borderRadius: 4, topRadius: 4 }  // 막대 폭·모서리
    }
};

/**
 * 차트 컨테이너를 정리한다. 재조회 렌더 전 호출 — 이전 차트 잔존 방지 (SPCHART.md §1-5)
 * (resize 리스너까지 해제하려면 sb.chart.render 직접 호출로 인스턴스를 잡아 sb.chart.destroy 사용)
 */
scwin.clearChart = function (comp) {
    const el = document.getElementById(comp.getID());
    if (el) { el.innerHTML = ""; }
};

scwin.onpageload = async function () {
    const res = await $c.sbm.executeDynamic({ id: "sbm_sbchart", action: scwin.CHART_DATA_URL, method: "get", mediatype: "application/json", mode: "asynchronous", isProcessMsg: false });
    const data = res.responseJSON;
    scwin.clearChart(chartWrap);   // 재조회 대비 이전 차트 정리
    $c.ext.drawChartData("bar", data, chartWrap, scwin.CHART_CONFIG);
};
scwin.click = function (msg) { 
	$c.win.alert(msg)
};
    ]]></script>
</head>
<body ev:onpageload="scwin.onpageload">
        <!-- SAMPLE_DESC -->
    <xf:group style="padding:8px 12px;margin-bottom:8px;background:#eef6f3;border-left:4px solid #0a8060;border-radius:4px;">
        <w2:span label="리치옵션 - 막대 (bar)" style="display:block;font-weight:bold;color:#0a7a52;"/>
        <w2:span label="막대 차트에 global/axis/grid/legend/tooltip/extend 다수 옵션을 적용한 예제입니다." style="display:block;font-size:12px;color:#555;margin-top:2px;"/>
    </xf:group>
<w2:group id="resultBox" style="display:block;margin:6px 0;color:#0a8060;font-weight:bold;"/>
    <w2:group id="chartWrap" style="width:100%;height:440px;"/>
</body>
</html>
