<?xml version="1.0" encoding="UTF-8"?>
<html xmlns="http://www.w3.org/1999/xhtml"
    xmlns:ev="http://www.w3.org/2001/xml-events"
    xmlns:w2="http://www.inswave.com/websquare" xmlns:xf="http://www.w3.org/2002/xforms">
    <head meta_screenName="의무보유주식의 처분-목록" meta_screenId="ULDFIL52000">
        <w2:type>COMPONENT</w2:type>
        <w2:buildDate/>
        <w2:MSA/>
        <xf:model>
            <w2:dataCollection baseNode="map">
            	<w2:dataMap baseNode="map" id="dma_SearchReq" style="">
            		<w2:keyInfo>
            			<w2:key dataType="number" defaultValue="1" id="page" name="현재 페이지 번호 (기본값: 1)"></w2:key>
            			<w2:key dataType="number" defaultValue="10" id="size" name=" 페이지당 데이터개수"></w2:key>
            			<w2:key id="integUsrId" name="로그인ID" dataType="text"></w2:key>
            		</w2:keyInfo>
            		<w2:data use="false"></w2:data>
            	</w2:dataMap>
            	<w2:dataMap baseNode="map" id="dma_pageRes" style="">
            		<w2:keyInfo>
            			<w2:key dataType="number" id="page" name="현재 페이지 번호 (1부터 시작)"></w2:key>
            			<w2:key dataType="number" id="size" name="한페이지당 데이터 개수"></w2:key>
            			<w2:key dataType="number" id="totalCount" name="전체 데이터개수"></w2:key>
            			<w2:key dataType="number" id="totalPages" name="전체 페이지수"></w2:key>
            			<w2:key dataType="number" id="startRowNumDesc" name="현재 페이지의 역순 글번호 시작값"></w2:key>
            			<w2:key dataType="number" id="startRowNumAsc" name="현재 페이지의 정순 글번호 시작값"></w2:key>
            			<w2:key dataType="text" id="hasPrevPage" name="이전 페이지 존재 여부 (&lt; 버튼 활성화 여부)"></w2:key>
            			<w2:key dataType="number" id="prevPage" name="이전 페이지 번호(없으면 0)"></w2:key>
            			<w2:key dataType="text" id="hasNextPage" name="다음페이지존재여부(버튼활성화여부)"></w2:key>
            			<w2:key dataType="number" id="nextPage" name="다음 페이지 번호 (없으면 0)"></w2:key>
            			<w2:key dataType="number" id="startPage" name="현재 블럭의 시작 페이지 번호"></w2:key>
            			<w2:key dataType="number" id="endPage" name="현재블럭의 마지막 페이지번호"></w2:key>
            			<w2:key dataType="text" id="hasPrevBlockPage" name="이전 블럭 존재 여부 (&lt;&lt; 버튼 활성화 여부)"></w2:key>
            			<w2:key dataType="number" id="prevBlockPage" name="이전 블럭의 마지막 페이지 번호 (없으면 0)"></w2:key>
            			<w2:key dataType="text" id="hasNextBlockPage" name="다음 블럭 존재 여부 (&gt;&gt; 버튼 활성화 여부)"></w2:key>
            			<w2:key dataType="number" id="nextBlockPage" name="다음 블럭의 시작 페이지 번호 (없으면 0)"></w2:key>
            		</w2:keyInfo>
            	</w2:dataMap>
            	<w2:dataMap baseNode="map" id="dma_SearchRes" style="">
            		<w2:keyInfo>
            			<w2:key dataType="number" id="page" name="현재페이지"></w2:key>
            			<w2:key dataType="number" id="size" name="페이지 당 개수"></w2:key>
            			<w2:key dataType="number" id="totalCount" name="총 개수"></w2:key>
            			<w2:key dataType="number" id="totalPages" name="총 페이지 수"></w2:key>
            		</w2:keyInfo>
            	</w2:dataMap>
            	<w2:dataList id="dlt_prtdepoDisposalRes" baseNode="list" saveRemovedData="true" repeatNode="map">
            		<w2:columnInfo>
            			<w2:column id="bzProcsNo" name="상장심사_신청서 업무처리번호" dataType="text"></w2:column>
            			<w2:column id="isurCd" name="발행기관코드" dataType="text"></w2:column>
            			<w2:column id="comNm" name="법인명" dataType="text"></w2:column>
            			<w2:column id="prtdepoNo" name="의무보유인출 업무처리번호" dataType="text"></w2:column>
            			<w2:column id="prtdepoWithdApplSeq" name="의무보유인출신청일련번호" dataType="number"></w2:column>
            			<w2:column id="prtdepoId" name="의무보유 번호" dataType="text"></w2:column>
            			<w2:column id="shrhdNm" name="주주명(의무보유자명)" dataType="text"></w2:column>
            			<w2:column id="prtdepoWithdShrs" name="의무보유인출주식수" dataType="text"></w2:column>
            			<w2:column id="prtdepoWithdRsn" name="의무보유인출사유" dataType="text"></w2:column>
            			<w2:column id="lstApprovYn" name="최종승인여부(처리상태코드)" dataType="text"></w2:column>
            			<w2:column id="procsDdtm" name="처리일시, 접수일시(처리쪽 실행일시)" dataType="text"></w2:column>
            			<w2:column id="regDdtm" name="등록일시, 제출일시" dataType="text"></w2:column>
            			<w2:column id="listProcsStatCd" name="상장처리상태코드" dataType="text"></w2:column>
            			<w2:column id="lstApprovNm" name="상장처리상태" dataType="text"></w2:column>
            			<w2:column id="lstApprovYn_custom1" name="커스텀 상태" dataType="text"></w2:column>
            			<w2:column id="lstApprovYn_custom2" name="커스텀 비고" dataType="text"></w2:column>
            		</w2:columnInfo>
            	</w2:dataList>
            	<w2:dataMap baseNode="map" id="dma_submitReq">
            		<w2:keyInfo>
            			<w2:key id="bzProcsNo" name="업무처리번호" dataType="text"></w2:key>
            			<w2:key id="prtDepoNo" name="업무처리번호" dataType="text"></w2:key>
            			<w2:key id="searchType" name="조회타입" dataType="text"></w2:key>
            			<w2:key id="regId" name="등록자ID" dataType="text"></w2:key>
            		</w2:keyInfo>
            	</w2:dataMap>
            </w2:dataCollection>
            <w2:workflowCollection/>
        </xf:model>
        <w2:layoutInfo/>
        <w2:publicInfo method="scwin.onpageload,scwin.grd_list_oncellclick,scwin.write_onclick,scwin.search,scwin.setPaging,scwin.sendPrtDepoWithd,scwin.sbm_submit_submitdone,scwin.popupCallback,scwin.initPageSize,scwin.setStatNm,scwin.setPrtdepoNm,scwin.viewDetail,scwin.openAdjReqPop"/>
        <script lazy="false" type="text/javascript"><![CDATA[
///////// 1. 변수 및 선언 영역 /////////
scwin.pageIndex = 1;        // 현재 페이지
scwin.pageItemCnt = 10;     // 페이징 시 출력할 페이지 번호 개수

///////// 2. 초기화 영역 /////////
/**
 * @method
 * @name onpageload
 * @description 화면 로딩 시 초기 처리 — initPageSize, search 호출
 * @returns {void}
 * @hidden N
 */
scwin.onpageload = function() {
    try {
        dma_SearchReq.set("integUsrId",$c.session.getUserInfo("integUsrId"));
        scwin.initPageSize();
        // 통신 전 : paging 설정
        scwin.setPaging();
        scwin.search(1);
    } catch (ex) {
        $c.exception.handleError(ex, { context : "ULDFIL52000.onpageload" });
    }
};

///////// 3. 컴포넌트 이벤트 영역 /////////
/**
 * @method
 * @name grd_list_oncellclick
 * @description 컬럼 클릭 이벤트 (회사명, 상태, 비고 세팅)
 * @param {Number} rowIndex 행 인덱스
 * @param {Number} columnIndex 열 인덱스
 * @param {String} columnId 컬럼 id
 * @returns {void}
 * @hidden N
 */
scwin.grd_list_oncellclick = function (rowIndex, columnIndex, columnId) {
    try {
        let prtdepoNo = dlt_prtdepoDisposalRes.getCellData(rowIndex, "prtdepoNo");
        let lstApprovYn = dlt_prtdepoDisposalRes.getCellData(rowIndex, "lstApprovYn");
        let listProcsStatCd = dlt_prtdepoDisposalRes.getCellData(rowIndex, "listProcsStatCd");

        // 회사명
        if (columnId === "comNm") {
            scwin.viewDetail(prtdepoNo, lstApprovYn, listProcsStatCd);
        }

        // 상태
        if (columnId === "lstApprovYn_custom1" ) {    // 상태컬럼
            if(lstApprovYn === "0" && (listProcsStatCd ==="" || listProcsStatCd === "35" )) {
                scwin.viewDetail(prtdepoNo, lstApprovYn, listProcsStatCd);
            } else if (lstApprovYn === "3" && listProcsStatCd === "35" ) {
                scwin.viewDetail(prtdepoNo, lstApprovYn, listProcsStatCd);
            } else if ((lstApprovYn === "0" || listProcsStatCd === "7") && listProcsStatCd === "37") {
                scwin.viewDetail(prtdepoNo, lstApprovYn, listProcsStatCd);
            } else if (lstApprovYn === "7" && listProcsStatCd === "38" ) {
                scwin.viewDetail(prtdepoNo, lstApprovYn, listProcsStatCd);
            }  else {
                scwin.viewDetail(prtdepoNo, lstApprovYn, listProcsStatCd);
            }
        }

        // 비고
        if (columnId === "lstApprovYn_custom2" ) {    // 상태컬럼
            if(lstApprovYn === "0" && listProcsStatCd === "") {
                scwin.sendPrtDepoWithd(prtdepoNo, '2');
            } else if ((lstApprovYn === "0" || lstApprovYn === "3") && listProcsStatCd === "35") {
                scwin.sendPrtDepoWithd(prtdepoNo, '3');
            } else if ((lstApprovYn === "0" || listProcsStatCd === "7") && listProcsStatCd === "37") {
                scwin.sendPrtDepoWithd(prtdepoNo, '3');
            } else if (lstApprovYn === "7" && listProcsStatCd === "38" ) {
                scwin.sendPrtDepoWithd(prtdepoNo, '3');
            } else if (lstApprovYn === "3" && (listProcsStatCd === "33" || listProcsStatCd === "36" || listProcsStatCd === "37")) {
                scwin.openAdjReqPop(prtdepoNo, '150');
            }
        }
    } catch (ex) {
        $c.exception.handleError(ex, { context : "ULDFIL52000.grd_list_oncellclick" });
    }
};

/**
 * @method
 * @name write_onclick
 * @description 「신규작성」 버튼 클릭 — 작성 화면으로 이동한다.
 * @param {Object} e 이벤트 객체
 * @returns {void}
 * @hidden N
 */
scwin.write_onclick = function (e) {
    try {
        const data = {
            searchType: "0",
        };
        const opts = {
            id: "ULDFIL52100",
            popupName: "의무보유주식의 처분-작성화면",
            width: 900,
            height: 600,
            frameModal: "top",
            type: "pageFramePopup",
        };
        // $c.win.openPopup("/ui/lst/lstinvstg/ULDFIL52100.xml", opts, data);
        $c.win.moveUrl("/ui/lst/lstinvstg/ULDFIL52100.xml", data);
    } catch (ex) {
        $c.exception.handleError(ex, { context : "ULDFIL52000.write_onclick" });
    }
};

///////// 4. 서브미션 콜백 영역 /////////
/**
 * @method
 * @name search
 * @description 의무보유주식 처분 목록을 페이징 조회한다. 페이지 이동·페이지당 건수 변경 시 $c.sbm.setPagingInfo 가 이 함수를 pageFunction 으로 호출한다.
 * @param {Number} gPageNo 조회할 페이지 번호(기본 1)
 * @returns {Promise<void>}
 * @hidden N
 */
scwin.search = async function (gPageNo = 1) {
    scwin.pageIndex = gPageNo;
    dma_SearchReq.set("page", scwin.pageIndex);          // 현재 페이지
    dma_SearchReq.set("size", sel_size.getValue());      // 페이지당 데이터 개수

    const sbmOptions = {
        id : "sbm_prtdepoDisposa",
        method: "get",
        action : "/api/listing/listinvstg/prtdepo-disposal/select-list-paging",
        ref : "dma_SearchReq",
        target : "dma_SearchRes=body,dlt_prtdepoDisposalRes=body.content",
        gridview : "grd_list",
        isProcessMsg : false
    };

    const res = await $c.sbm.executeDynamic(sbmOptions);

    // 통신 후 : paging 설정(전체 건수로 페이지 리스트 렌더링·총건수·순번)
    scwin.setPaging(res);
};

/**
 * @method
 * @name sendPrtDepoWithd
 * @description 의무보유주식 처분 신청을 제출한다.
 * @param {String} prtDepoNo 식별자
 * @param {String} updateType 구분 코드
 * @returns {Promise<void>}
 * @hidden N
 */
scwin.sendPrtDepoWithd = async function (prtDepoNo, updateType) {

    let res = await $c.win.confirm($c.data.getMessage("com_crud_0198", "제출"));
    if(res){
        dma_submitReq.set("bzProcsNo", prtDepoNo); // 업무처리번호
        dma_submitReq.set("prtDepoNo", prtDepoNo); // 업무처리번호
        dma_submitReq.set("searchType", updateType); // 0:최초입력, 1:해당기업조회, 2:입력후저장내용 불러오기, 3:수정, 4:첨부파일, 5:최종승인 , 6:최종승인 반려
        dma_submitReq.set("regId", $c.session.getUserInfo("integUsrId")); // 등록자ID
        const sbmOptions = {
            id : "sbm_submit",
            method: "post",
            action : "/api/listing/listinvstg/prtdepo-disposal/submit",
            ref : "dma_submitReq",
            // target : "dma_submitRes=body",
            // gridview : "grd_list",
            isProcessMsg : false
        };

        const sbmRtn = await $c.sbm.executeDynamic(sbmOptions);
        await scwin.sbm_submit_submitdone(sbmRtn);
        }
};

/**
 * @method
 * @name sbm_submit_submitdone
 * @description 제출 통신 응답 후처리 — 결과 메시지 표시 후 목록을 재조회한다.
 * @param {Object} e 이벤트 객체
 * @returns {void}
 * @hidden N
 */
scwin.sbm_submit_submitdone = function(e) {
    $c.win.alert($c.data.getMessage("com_complete_0150", "제출"));
    scwin.search(1);
}

/**
 * @method
 * @name popupCallback
 * @description 수정요청 팝업이 닫힌 뒤 현재 페이지를 재조회한다.
 * @returns {void}
 * @hidden N
 */
scwin.popupCallback = function(){
    scwin.search(scwin.pageIndex);
}

///////// 5. 일반/업무 함수 영역 /////////
/**
 * @method
 * @name setPaging
 * @description 페이징 컴포넌트 설정 — 통신 전(인자 없음)에는 기본값 병합·조회 DataMap 컬럼 설정만 하고,
 *              통신 후(응답 전달)에는 전체 건수로 페이지 리스트를 렌더링하고 총건수·행 번호 시작값을 맞춘다.
 *              페이지 이동과 페이지당 건수 변경은 공통이 pageFunction(scwin.search) 에 바인딩한다.
 * @param {Object} result $c.sbm.executeDynamic 응답 객체(통신 전 호출이면 생략)
 * @returns {void}
 * @hidden N
 */
scwin.setPaging = function (result = {}) {
    const pagingInfo = {
        pageType : "P",                          // S/P/A Default=P
        rowNumVisble : "grd_list|asc",           // gridView 순번 시작 인덱스(현재 페이지 기준 오름차순)
        maxRowNum : 100,                         // gridView 최대 visibleRowNum
        pageListId : "pageList1",                // 페이지 리스트 아이디
        pageFunction : "scwin.search",           // 페이징 처리 함수(페이지 번호를 인자로 호출됨)
        currentPage : scwin.pageIndex,           // 현재 페이지
        totalCountId : "txt_totalCount",         // 총건수 출력 아이디
        recordPerPageId : sel_size,              // 페이지당 건수 컴포넌트
        pageSize : scwin.pageItemCnt             // 페이징 시 출력할 페이지 번호 개수
    };

    $c.sbm.setPagingInfo(pagingInfo, "dmaPaging", result.responseJSON?.body?.totalCount || 0);
};

/**
 * @method
 * @name initPageSize
 * @description 페이지 크기 셀렉트박스(sel_size)에 15/30/50/100 항목을 채운다.
 * @returns {void}
 * @hidden N
 */
scwin.initPageSize = function () {
    const sel_size = $p.getComponentById("sel_size");
    if (!sel_size) {
        return;
    }
    $c.data.comboCbDataSetDynamic(sel_size, [15, 30, 50, 100], 0);
};

/**
 * @method
 * @name setStatNm
 * @description 그리드 상태 컬럼 커스텀 포매터 — 승인여부·처리상태에 따라 작성중/수정가능 등 버튼 HTML 을 만든다.
 * @param {*} data 셀 값(원본 데이터)
 * @param {String} formattedData 표시 형식 문자열
 * @param {Number} rowIndex 행 인덱스
 * @param {Number} columnIndex 열 인덱스
 * @returns {String} 셀에 표시할 HTML
 * @hidden N
 */
scwin.setStatNm = function (data, formattedData, rowIndex, columnIndex) {
    let lstApprovYn = dlt_prtdepoDisposalRes.getCellData(rowIndex, "lstApprovYn");
    let listProcsStatCd = dlt_prtdepoDisposalRes.getCellData(rowIndex, "listProcsStatCd");
    let lstApprovNm     = dlt_prtdepoDisposalRes.getCellData(rowIndex, "lstApprovNm");

    if (columnIndex === 5 ) {    // 상태컬럼
        if(lstApprovYn === "0" && (listProcsStatCd ==="" || listProcsStatCd === "35" )) {
            dlt_prtdepoDisposalRes.setCellData(rowIndex, "lstApprovYn_custom1", "작성중");
            data = "<button>작성중</button>";
        } else if (lstApprovYn === "3" && listProcsStatCd === "35" ) {
            dlt_prtdepoDisposalRes.setCellData(rowIndex, "lstApprovYn_custom1", "수정가능");
            data = "<button>수정가능</button>";
        } else if ((lstApprovYn === "0" || listProcsStatCd === "7") && listProcsStatCd === "37") {
            dlt_prtdepoDisposalRes.setCellData(rowIndex, "lstApprovYn_custom1", "재작성");
            data = "<button>재작성</button>";
        } else if (lstApprovYn === "7" && listProcsStatCd === "38" ) {
            dlt_prtdepoDisposalRes.setCellData(rowIndex, "lstApprovYn_custom1", "재작성");
            data = "<button>재작성</button>";
        }  else {
            dlt_prtdepoDisposalRes.setCellData(rowIndex, "lstApprovYn_custom1", lstApprovNm);
        }
    }

    return data;
};

/**
 * @method
 * @name setPrtdepoNm
 * @description 그리드 비고 컬럼 커스텀 포매터 — 승인여부·처리상태에 따라 제출 버튼 HTML 을 만든다.
 * @param {*} data 셀 값(원본 데이터)
 * @param {String} formattedData 표시 형식 문자열
 * @param {Number} rowIndex 행 인덱스
 * @param {Number} columnIndex 열 인덱스
 * @returns {String} 셀에 표시할 HTML
 * @hidden N
 */
scwin.setPrtdepoNm = function (data, formattedData, rowIndex, columnIndex) {
    let lstApprovYn = dlt_prtdepoDisposalRes.getCellData(rowIndex, "lstApprovYn");
    let listProcsStatCd = dlt_prtdepoDisposalRes.getCellData(rowIndex, "listProcsStatCd");

    if (columnIndex === 6 ) {    // 상태컬럼
        if(lstApprovYn === "0" && listProcsStatCd === "") {
            dlt_prtdepoDisposalRes.setCellData(rowIndex, "lstApprovYn_custom2", "제출");
            data = "<button style='color: #ff0000; border-color: red;'>제출</button>";
        } else if ((lstApprovYn === "0" || lstApprovYn === "3") && listProcsStatCd === "35") {
            dlt_prtdepoDisposalRes.setCellData(rowIndex, "lstApprovYn_custom2", "제출");
            data = "<button style='color: #ff0000; border-color: red;'>제출</button>";
        } else if ((lstApprovYn === "0" || listProcsStatCd === "7") && listProcsStatCd === "37") {
            dlt_prtdepoDisposalRes.setCellData(rowIndex, "lstApprovYn_custom2", "제출");
            data = "<button style='color: #ff0000; border-color: red;'>제출</button>";
        } else if (lstApprovYn === "7" && listProcsStatCd === "38" ) {
            dlt_prtdepoDisposalRes.setCellData(rowIndex, "lstApprovYn_custom2", "제출");
            data = "<button style='color: #ff0000; border-color: red;'>제출</button>";
        } else if (lstApprovYn === "3" && (listProcsStatCd === "33" || listProcsStatCd === "36" || listProcsStatCd === "37")) {
            dlt_prtdepoDisposalRes.setCellData(rowIndex, "lstApprovYn_custom2", "수정요청");
            data = "<button>수정요청</button>";
        } else {
            dlt_prtdepoDisposalRes.setCellData(rowIndex, "lstApprovYn_custom2", "-");
        }
    }

    return data;
};

/**
 * @method
 * @name viewDetail
 * @description 상세내용 조회 팝업 searchType 0:최초입력, 1:해당기업조회, 2:입력후저장내용 불러오기, 3:수정, 4:첨부파일, 5:최종승인 , 6:최종승인 반려
 * @param {String} prtdepoNo 식별자
 * @param {String} approvYn 구분 코드
 * @param {String} procsStatCd 구분 코드
 * @returns {void}
 * @hidden N
 */
scwin.viewDetail = function (prtdepoNo, approvYn, procsStatCd) {

    if(approvYn === '0' || approvYn === '7'){// 작성중
        if(procsStatCd === '38')// 첨부파일 제출완료
            searchType = '5';
        else
            searchType = '2';
    }
    else if(approvYn === '2'){// 제출완료 - 수정불가능
        if(procsStatCd === '38')// 첨부파일 제출완료
            searchType = '5';
        else
            searchType = '1';
    }
    else if(approvYn === '3'){ // 접수완료 - 수정가능
        if(procsStatCd === '34')// 수정요청
            searchType = '1';
        else if(procsStatCd === '35')// 수정승인 - 수정가능
            searchType = '3';
        else  // 수정불가 - 항목수정불가, 첨부만 가능
            searchType = '1';
    }
    else if(approvYn === '8'){// 처분승인 - 수정불가능, 처분승인공문을 다운, 재의무보유첨부만 할 수 있다.
        searchType = '4';
    }
    else if(approvYn === '9'){// 최종승인 - 모두 불가능
        searchType = '5';
    }

    let screenId = "";
    if (searchType === "1" || searchType === "4" || searchType === "5") {
        screenId = "ULDFIL52101";
    } else {
        screenId = "ULDFIL52100";
    }

    const data = {
        prtDepoNo: prtdepoNo, // 의무보유주식의 처분 업무처리번호
        searchType: searchType,
    };
    const opts = {
        id: screenId,
        popupName: "의무보유주식의 처분-상세내용",
        width: 900,
        height: 700,
        frameModal: "top",
        type: "pageFramePopup",
    };
    // $c.win.openPopup("/ui/lst/lstinvstg/ULDFIL52101.xml", opts, data);
    $c.win.moveUrl("/ui/lst/lstinvstg/" + screenId + ".xml", data);
};

/**
 * @method
 * @name openAdjReqPop
 * @description 수정요청 팝업
 * @param {String} prtdepoNo 식별자
 * @param {String} listInvstgBzTpCd 구분 코드
 * @returns {Promise<void>}
 * @hidden N
 */
scwin.openAdjReqPop = async function(prtdepoNo, listInvstgBzTpCd) {
    const data = {
        bzProcsNo : prtdepoNo,
        listInvstgBzTpCd : listInvstgBzTpCd,
        listBzDtlTpCd : "51", // TODO: DB확인해보니 (23: 의무보유) 인듯한데 확인필요
        callbackFn: "scwin.popupCallback",
    };

    const options = {
        id: "ULDFIL59411",
        popupName: "상장공시제출시스템 수정요청",
        width: 900,
        height: 380,
        frameModal: "top"
    };

    const result = await $c.win.openPopup("/ui/lst/common/ULDFIL59411.xml", options, data);
}
]]></script>
    </head>
    <body ev:onpageload="scwin.onpageload">
    	<xf:group class="sub_contents" id="" meta_snippetCategory="00_화면시작" meta_snippetKeyComponent="true" meta_snippetName="0_01 페이지시작"
    		style="">
    		<w2:pageFrame src="/cm/xml/contentHeader.xml" style="" id="pfmContentHeader"></w2:pageFrame>
    		<xf:group class="titbox" id="" meta_snippetCategory="02_타이틀" meta_snippetKeyComponent="true" meta_snippetName="2_02 서브타이틀(h3)"
    			style="">
    			<xf:group class="rt" id="" style="">
    				<xf:group class="count" id="" style="">
    					<w2:textbox escape="false" id="" label="전체&amp;nbsp;" style=""></w2:textbox>
    					<w2:textbox class="num" id="txt_totalCount" label="0" style=""></w2:textbox>
    					<w2:textbox escape="false" id="" label="건" style=""></w2:textbox>
    				</xf:group>
    				<xf:select1 allOption="" appearance="minimal" chooseOption="" class="" direction="auto" disabled="false"
    					disabledClass="w2selectbox_disabled" id="sel_size"
    					metaev_onchange_desc="페이지 당 게시물 갯수를 지정" metaev_onchange_name="페이지 당 게시물 갯수" ref="data:dma_SearchReq.size" renderType=""
    					style="width: 100px;" submenuSize="auto">
    					<xf:choices></xf:choices>
    				</xf:select1>
    			</xf:group>
    		</xf:group>
    		<xf:group adaptiveThreshold="" class="gvwbox" id="" meta_snippetCategory="06_그리드" meta_snippetKeyComponent="true"
    			meta_snippetName="6_01 그리드" style="">
    			<w2:gridView autoFit="allColumn" checkReadOnlyOnPasteEnable="" class="gvw" dataList="data:dlt_prtdepoDisposalRes"
    				focusMode="row" id="grd_list" scrollByColumn="false" scrollByColumnAdaptive="false" style="" rowNumVisible="true"
    				rowNumHeaderValue="번호" rowNumWidth="50" ev:oncellclick="scwin.grd_list_oncellclick" visibleRowNum="20">
    				<w2:caption id="caption2" style="" value="this is a grid caption."></w2:caption>
    				<w2:header id="header2" style="">
    					<w2:row id="row3" style="">
    						<w2:column blockSelect="false" displayMode="label" id="column12" inputType="text" removeBorderStyle="false"
    							value="회사명" width="200">
    						</w2:column>
    						<w2:column blockSelect="false" displayMode="label" id="column11" inputType="text" removeBorderStyle="false"
    							value="회사코드" width="80">
    						</w2:column>
    						<w2:column blockSelect="false" displayMode="label" id="column10" inputType="text" removeBorderStyle="false"
    							value="의무보유자명" width="120">
    						</w2:column>
    						<w2:column blockSelect="false" displayMode="label" id="column9" inputType="text" removeBorderStyle="false"
    							value="처분주식수" width="100">
    						</w2:column>
    						<w2:column blockSelect="false" displayMode="label" id="column8" inputType="text" removeBorderStyle="false"
    							value="제출일" width="100">
    						</w2:column>
    						<w2:column blockSelect="false" displayMode="label" id="column7" inputType="text" removeBorderStyle="false"
    							value="상태" width="100">
    						</w2:column>
    						<w2:column blockSelect="false" displayMode="label" id="column6" inputType="text" removeBorderStyle="false"
    							value="비고" width="100">
    						</w2:column>
    					</w2:row>
    				</w2:header>
    				<w2:gBody id="gBody2" style="">
    					<w2:row id="row4" style="">
    						<w2:column blockSelect="false" displayMode="label" id="comNm" inputType="text" removeBorderStyle="false"
    							width="200" textAlign="left" style="cursor:pointer;">
    						</w2:column>
    						<w2:column blockSelect="false" displayMode="label" id="isurCd" inputType="text" removeBorderStyle="false"
    							width="80">
    						</w2:column>
    						<w2:column blockSelect="false" displayMode="label" id="shrhdNm" inputType="text" removeBorderStyle="false"
    							width="120" textAlign="left">
    						</w2:column>
    						<w2:column blockSelect="false" displayMode="label" id="prtdepoWithdShrs" inputType="text"
    							removeBorderStyle="false" width="100" textAlign="right">
    						</w2:column>
    						<w2:column blockSelect="false" displayMode="label" id="regDdtm" inputType="text" removeBorderStyle="false"
    							width="100">
    						</w2:column>
    						<w2:column blockSelect="false" displayMode="label" id="lstApprovYn_custom1" inputType="custom"
    							removeBorderStyle="false" width="100" customFormatter="scwin.setStatNm" style="cursor:pointer;">
    						</w2:column>
    						<w2:column blockSelect="false" displayMode="label" id="lstApprovYn_custom2" inputType="custom" removeBorderStyle="false"
    							width="100" customFormatter="scwin.setPrtdepoNm" style="cursor:pointer;">
    						</w2:column>
    					</w2:row>
    				</w2:gBody>
    			</w2:gridView>
    		</xf:group>
    		<xf:group class="pglbox" id="" meta_snippetCategory="06_그리드" meta_snippetKeyComponent="true" meta_snippetName="6_03 페이지리스트">
    			<w2:pageList class="pgl" displayButtonType="display" displayFormat="#" id="pageList1" pageSize="10" pagingType="0"
    				style="">
    			</w2:pageList>
    		</xf:group>
    		<xf:group class="btnbox" id="" meta_snippetCategory="08_버튼" meta_snippetKeyComponent="true" meta_snippetName="8_04 전체제어버튼"
    			style="">
    			<xf:group class="rt" id="" style="">
    				<xf:trigger class="btn_cm " id="btn_delete" style="display:none;" type="button">
    					<xf:label><![CDATA[삭제]]></xf:label>
    				</xf:trigger>
    				<xf:trigger class="btn_cm pt" ev:onclick="scwin.write_onclick" id="write" style="" type="button">
    					<xf:label><![CDATA[신규작성]]></xf:label>
    				</xf:trigger>
    			</xf:group>
    		</xf:group>
    	</xf:group>
    </body>
</html>
