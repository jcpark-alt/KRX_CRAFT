<?xml version="1.0" encoding="UTF-8"?>
<html xmlns="http://www.w3.org/1999/xhtml"
xmlns:ev="http://www.w3.org/2001/xml-events"
xmlns:w2="http://www.inswave.com/websquare"
xmlns:xf="http://www.w3.org/2002/xforms">
	<head meta_screenId="$c.cp" meta_screenName="cp">
		<w2:type>COMMON</w2:type>
		<w2:buildDate/>
		<w2:MSA/>
		<xf:model>
			<w2:dataCollection baseNode="map">
			</w2:dataCollection>
			<w2:workflowCollection>
			</w2:workflowCollection>
		</xf:model>
		<w2:mfeSubmission>
		</w2:mfeSubmission>
		<w2:layoutInfo>
		</w2:layoutInfo>
		<w2:publicInfo method="scwin.deleteAll,scwin.getColumnProp,scwin.getNameValueRow,scwin.getValueRow,scwin.isModified,scwin.nameMax,scwin.removeRow,scwin.setBroadCast,scwin.setCBData,scwin.setColumnProp,scwin.setFormat,scwin.valueOfIndex"/>
		<script type="text/javascript" lazy="false"><![CDATA[
scwin.onpageload = function() {
	
};

/**
 * @method
 * @name removeRow
 * @description
 * @param {Object} dataList dataCollection
 * @param {Number} idx rowIndex
 * @hidden N
 * @exception
 * @example
 */ 
scwin.removeRow = function(dataList,idx) {
    dataList.removeRow(idx);
};

/**
 * @method
 * @name isModified
 * @description
 * @param {Object} dcObjArr DataCollection 또는 배열
 * @returns {Boolean} 검사결과
 * @hidden N
 * @exception
 * @example
 */ 
scwin.isModified = function(dcObjArr) {
    return $c.data.isModified(dcObjArr);
};

/**
 * @method
 * @name deleteAll
 * @description
 * @param {Object} dataList DataCollection
 * @hidden N
 * @exception
 * @example
 */ 
scwin.deleteAll = function(dataList) {
    dataList.removeAll();
};

/**
 * @method
 * @name getValueRow
 * @description
 * @param {index} rowposition
 * @param {string} value
 * @hidden N
 * @exception
 * @example
 */ 
scwin.getValueRow = function(dataList,rowIdx,value) {
    
};

/**
 * @method
 * @name getNameValueRow
 * @description
 * @param
 * @hidden N
 * @exception
 * @example
 */ 
scwin.getNameValueRow = function(dataList,rowIdx,value) {

};

/**
 * @method
 * @name nameMax
 * @description
 * @param {Object} dataList Datacollection
 * @param {String} columnId 컬럼명
 * @param {Number} fromRow 시작Row
 * @param {Number} toRow 종료Row
 * @returns {Number} 최대값
 * @hidden N
 * @exception
 * @example
 * dts_listShareRoom.NameMax("col1", 0, dataList.getTotalRow() -1)
 */ 
scwin.nameMax = function(dataList,columnId, fromRow, toRow) {
    const distinctValues = dataList.getColumnDistinctDataArray(columnId, fromRow, toRow);
    return Math.max(...distinctValues);
};

/**
 * @method
 * @name getColumnProp
 * @description
 * @param
 * @hidden N
 * @exception
 * @example
 */ 
scwin.getColumnProp = function(grdObj,columnId,propertyName) {    //'Show'
    //mask 4, headchkeck 22, data 20, Suppress 11, Multiline 14,
    if (propertyName == "Show") {
        return grdObj.getColumnVisible(columnId, propertyValue);   //true, false
    } else if (propertyName == "Width") {
        return grdObj.getColumnWidth(columnId, propertyValue); //Number Width Pixel
    } else if (propertyName == "Name") {
        return grdObj.getHeaderValue(columnId, propertyValue); //header text value
    } else if (propertyName == "Edit") {
        return grdObj.getColumnReadOnly(columnId, propertyValue); //any, none
    }
};

/**
 * @method
 * @name setColumnProp
 * @description
 * @param {string} grdObj desc
 * @param {string} columnId desc
 * @param {string} propertyName desc
 * @param {string} propertyValue desc
 * @hidden N
 * @exception
 * @example
 */ 
scwin.setColumnProp = function(grdObj,columnId,propertyName,propertyValue) {   //'Show',true
    //mask 4, headchkeck 22, data 20, Suppress 11, Multiline 14,
    if (propertyName == "Show") {
        grdObj.setColumnVisible(columnId, propertyValue);   //true, false
    } else if (propertyName == "Width") {
        grdObj.setColumnWidth(columnId, propertyValue); //Number Width Pixel
    } else if (propertyName == "Name") {
        grdObj.setHeaderValue(columnId, propertyValue); //header text value
    } else if (propertyName == "Edit") {
        grdObj.setColumnReadOnly(columnId, propertyValue); //any, none
    }
};

/**
 * @method
 * @name setBroadCast
 * @description 그리드뷰의 dataCollection 동기화를 제어한다
 * @param {Object} grdObj 그리드뷰 컴포넌트
 * @param {Boolean} useYn 사용여부
 * @hidden N
 * @exception
 * @example
 *      $c.cp.setBroadCast(grd_list, false);
 *      dlt_list.setJSON(jsonData);
 *      $c.cp.setBroadCast(grd_list, true);
 */ 
scwin.setBroadCast = function(grdObj,useYn) {
    const dltObj = $p.getComponentById(grdObj.getDataList());
    dltObj.setBroadcast(useYn);
};

/**
 * @method
 * @name valueOfIndex
 * @description
 * @param {string} selectboxObj desc
 * @returns {String} 해당 index 값
 * @param {string} value desc
 * @param {string} index desc
 * @hidden N
 * @exception
 * @example
 */ 
scwin.valueOfIndex = function(selectboxObj,value,index) {
    /*
    if (index > -1 && selectboxObj.getSelectedIndex() != index) {
        selectboxObj.setSelectedIndex(index);
    }
    */

    if (selectboxObj.itemsetObj.value == value) {
        return selectboxObj.getValue();
    }
    //return selectboxObj.getText();
    return selectboxObj.getSelectedIndex();
};

/**
 * @method
 * @name setCBData
 * @description
 * @param
 * @hidden N
 * @exception
 * @example
 */ 
scwin.setCBData = function(selectboxObj,itemsetStr) { //"0^전체,1^미처리,2^처리중,3^처리완료,4^반송"

};

/**
 * @method
 * @name setFormat
 * @description
 * @param
 * @hidden N
 * @exception
 * @example
 */ 
scwin.setFormat = function(radioObj,itemsetStr) {   //"10^신규,20^처리중"

};]]></script>
	</head>
	<body ev:onpageload="scwin.onpageload">
	</body>
</html>
