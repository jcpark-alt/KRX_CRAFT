<?xml version="1.0" encoding="UTF-8"?>
<!-- 기능샘플: 값 구간별 스타일 (extend.line.sections)
  - 2026-08-26 SPCHART.md 회신 반영(문의 처리 요약: sbchart/md/SPCHART.md §1) : 코드 컨벤션 정리 + 색상 팔레트·툴팁 값 포맷·clear — var→const
  - 고급 기능 정의: sbchart/md/SBCHART_OPTIONS.md §9 — extend.line.sections — 값 구간별 선/영역/마커 스타일
-->
<html xmlns="http://www.w3.org/1999/xhtml" xmlns:ev="http://www.w3.org/2001/xml-events"
      xmlns:w2="http://www.inswave.com/websquare" xmlns:xf="http://www.w3.org/2002/xforms">
<head meta_screenId="SPCHART_SECTIONS" meta_screenName="구간별 임계색 (sections)" meta_desc="extend.line.sections" meta_author="WRM" meta_date="2026.07.09">
    <w2:type>COMPONENT</w2:type>
    <w2:buildDate/><w2:MSA/>
    <xf:model><xf:instance><data xmlns=""/></xf:instance><w2:dataCollection baseNode="map"/><w2:workflowCollection/></xf:model>
    <w2:layoutInfo/><w2:publicInfo method=""/>
    <script lazy="false" type="text/javascript"><![CDATA[
scwin.CHART_DATA_URL ="/ui/sample/SBChart/COLUMNS/data/COLUMNS.json";
// ---- 차트 공통 상수 (SPCHART.md 회신 §1 일반화) ----
scwin.CHART_COLORS = ["#E52528", "#1B64DA", "#0A8060", "#F5A623", "#7B1FA2"];   // ★ 시리즈 색상 팔레트 — 현장 조정

scwin.CHART_CONFIG = {
    "tooltip": {   // 마우스오버 표시값 — 천단위 콤마 — SPCHART.md §1-7
      "format": {
        "value": function (v, ratio, id, index) { return $c.num.formatNumber(v); }
      }
    },
    "color": { "pattern": scwin.CHART_COLORS },   // 시리즈 색상 명시 (미지정 시 라이브러리 기본색) — SPCHART.md §1-3
    tooltip: { grouped: false },
    extend: {
        line: {
            sections: [                                   // ★ 값 구간별로 선/영역/마커 스타일 변경
                { start: 0,   end: 100, line: { fillColor: "#2ecc71", strokeColor: "#2ecc71" } },   // 정상
                { start: 100, end: 200, line: { strokeStyle: "dashed", strokeColor: "#f1c40f" } },  // 경고
                { start: 200, end: 400,
                  line:   { fillColor: "#e74c3c", strokeColor: "#e74c3c" },                          // 위험
                  area:   { fillColor: "#f7c6c0" },
                  marker: { radius: 6, fillColor: "#fff", strokeColor: "#e74c3c", strokeWidth: 3 }
                }
            ]
        }
    }
};
/**
 * 차트 컨테이너를 정리한다. 재조회 렌더 전 호출 — 이전 차트 잔존 방지 (SPCHART.md §1-5)
 * (resize 리스너까지 해제하려면 sb.chart.render 직접 호출로 인스턴스를 잡아 sb.chart.destroy 사용)
 */
scwin.clearChart = function (comp) {
    const el = document.getElementById(comp.getID());
    if (el) { el.innerHTML = ""; }
};

scwin.onpageload = async function () {
    const res = await $c.sbm.executeDynamic({ id: "sbm_sbchart", action: scwin.CHART_DATA_URL, method: "get", mediatype: "application/json", mode: "asynchronous", isProcessMsg: false });
    const data = res.responseJSON;
    scwin.clearChart(chartWrap);   // 재조회 대비 이전 차트 정리
    $c.ext.drawChartData("area-spline", data, chartWrap, scwin.CHART_CONFIG);
};
    ]]></script>
</head>
<body ev:onpageload="scwin.onpageload">
    <!-- SAMPLE_DESC -->
    <xf:group style="padding:8px 12px;margin-bottom:8px;background:#eef6f3;border-left:4px solid #0a8060;border-radius:4px;">
        <w2:span label="값 구간별 임계 스타일 (extend.line.sections)" style="display:block;font-weight:bold;color:#0a7a52;"/>
        <w2:span label="값 범위(start~end)별로 선 색/점선/영역/마커를 달리해 정상·경고·위험 구간을 표현합니다." style="display:block;font-size:12px;color:#555;margin-top:2px;"/>
    </xf:group>
    <w2:group id="chartWrap" style="width:100%;height:440px;"/>
</body>
</html>
