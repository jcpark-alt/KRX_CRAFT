<?xml version="1.0" encoding="UTF-8"?>
<!--
  SBChart [treeRadial] 샘플 (WRM / SPA)
  - 라이브러리는 config.xml <engine><module> 로 전역 로드됨
  - 데이터는 /ui/sample/SBChart/HIER/data/HIER.json 를 WebSquare.net.ajax 로 조회(GET)하여 결합
  - 설정(CHART_CONFIG)은 원본 예제의 JS 를 그대로 유지 (포매터/콜백 함수 보존)
  - 컨테이너는 w2:group + getID() (다중 탭 동시표시 안전)
-->
<html xmlns="http://www.w3.org/1999/xhtml"
      xmlns:ev="http://www.w3.org/2001/xml-events"
      xmlns:w2="http://www.inswave.com/websquare"
      xmlns:xf="http://www.w3.org/2002/xforms">
<head meta_screenId="SPCHART_TREERADIAL" meta_screenName="SBChart treeRadial" meta_desc="SBChart treeRadial 조회형 샘플" meta_author="WRM" meta_date="2026.07.07">
    <w2:type>COMPONENT</w2:type>
    <w2:buildDate/>
    <w2:MSA/>
    <xf:model>
        <xf:instance>
            <data xmlns=""/>
        </xf:instance>
        <w2:dataCollection baseNode="map"/>
        <w2:workflowCollection/>
    </xf:model>
    <w2:layoutInfo/>
    <w2:publicInfo method=""/>
    <script lazy="false" type="text/javascript"><![CDATA[

// 데이터/옵션 분리: JSON 은 순수 데이터만, 아래 type+옵션은 스크립트가 보유 → 렌더시 조합
scwin.CHART_TYPE = "treeRadial";                    // 차트 타입 (스크립트)
scwin.CHART_DATA_URL ="/ui/sample/SBChart/HIER/data/HIER.json";             // 순수 데이터 JSON (type 없음)
scwin.CHART_CONFIG = {
  "extend": {   // 차트별 세부 옵션
    "treeRadial": {   // 방사트리 전용 옵션
      "textOnClick": function(parm) {
        console.log('textOnClick called : parm=',parm)
      }
    }
  }
};               // 옵션(global/axis/extend…) + dataOpts(keys/labels…)

scwin.onpageload = async function () {
    const res = await $c.sbm.executeDynamic({ id: "sbm_sbchart", action: scwin.CHART_DATA_URL, method: "get", mediatype: "application/json", mode: "asynchronous", isProcessMsg: false });
    const data = res.responseJSON;
    $c.ext.drawChartData(scwin.CHART_TYPE, data, chartWrap, scwin.CHART_CONFIG);
};

    ]]></script>
</head>

<body ev:onpageload="scwin.onpageload">
        <!-- SAMPLE_DESC -->
    <xf:group style="padding:8px 12px;margin-bottom:8px;background:#eef6f3;border-left:4px solid #0a8060;border-radius:4px;">
        <w2:span label="방사형 트리 (treeRadial)" style="display:block;font-weight:bold;color:#0a7a52;"/>
        <w2:span label="계층 구조를 방사형으로 펼칩니다." style="display:block;font-size:12px;color:#555;margin-top:2px;"/>
    </xf:group>
<w2:group id="chartWrap" style="width:100%;height:430px;"/>
</body>
</html>
