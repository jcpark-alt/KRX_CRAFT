<?xml version="1.0" encoding="UTF-8"?>
<html xmlns="http://www.w3.org/1999/xhtml" xmlns:ev="http://www.w3.org/2001/xml-events" xmlns:w2="http://www.inswave.com/websquare" xmlns:xf="http://www.w3.org/2002/xforms">
	<head>
		<w2:type>COMMON</w2:type>
		<w2:buildDate/>
		<xf:model>
			<w2:workflowCollection/>
		</xf:model>
		<w2:publicInfo method="scwin.cL,scwin.cM,scwin.cU,scwin.cV,scwin.cW,scwin.cX,scwin.cY,scwin.cZ,scwin.c$,scwin.da,scwin.cj = ct.exec(ck.toLowerCase()) || [], p.fn.load"/>
		<script type="text/javascript" lazy="false"><![CDATA[
/* ★Wcraft guide★    
스크립트 수작업 유의사항
 ASIS에서 문법에 어긋난 문장이 발견되었습니다. 이는 WCRAFT에서 X 로 변환합니다.  
 X 로 변환된 곳을 찾아 ASIS소스를 참고하여 문법에 맞도록 변환해주세요. 

----------------------------------------------------
[0] 기존 문법 오류
----------------------------------------------------
	> X 오류 확인 - 기존 소스 확인 필요

*/


/*! jQuery v1.8.2 jquery.com | jquery.org/license */
(function(a, b) {
    /**
     * @method
     * @name G
     * @description desc
     * @param {string} a desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function G(a) {
        var b = F[a] = {};
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        p.each(a.split(s), function(a, c) {
            b[c] = !0;
        }), b;
    };
    /**
     * @method
     * @name J
     * @description desc
     * @param {string} a desc
     * @param {string} c desc
     * @param {string} d desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function J(a, c, d) {
        if (d === b && a.nodeType === 1) {
            var e = "data-" + c.replace(I, "-$1").toLowerCase();
            d = a.getAttribute(e);
            if (typeof d == "string") {
                try {
                    d = d === "true" ? !0 : d === "false" ? !1 : d === "null" ? null : +d + "" === d ? +d : H.test(d) ? p.parseJSON(d) : d;
                } catch (f) {

                }
                p.data(a, scwin.c, d);
            } else d = b;
        }
        return d;
    };
    /**
     * @method
     * @name K
     * @description desc
     * @param {string} a desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function K(a) {
        var b;
        for (b in a) {
            if (b === "data" && p.isEmptyObject(a[b])) continue;
            if (b !== "toJSON") return !1;
        }
        return !0;
    };
    /**
     * @method
     * @name ba
     * @description desc
     * @param
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function ba() {
        return !1;
    };
    /**
     * @method
     * @name bb
     * @description desc
     * @param
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function bb() {
        return !0;
    };
    /**
     * @method
     * @name bh
     * @description desc
     * @param {string} a desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function bh(a) {
        return !a || !a.parentNode || a.parentNode.nodeType === 11;
    };
    /**
     * @method
     * @name bi
     * @description desc
     * @param {string} a desc
     * @param {string} b desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function bi(a, b) {
        do a = a[b]; while (a && a.nodeType !== 1);
        return a;
    };
    /**
     * @method
     * @name bj
     * @description desc
     * @param {string} a desc
     * @param {string} b desc
     * @param {string} c desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function bj(a, b, c) {
        b = b || 0;
        if (p.isFunction(b)) return p.grep(a, function(a, d) {
            var e = b.call(a, d, a);
            return e === scwin.c;
        });
        if (b.nodeType) return p.grep(a, function(a, d) {
            return a === b === scwin.c;
        });
        if (typeof b == "string") {
            var d = p.grep(a, function(a) {
                return a.nodeType === 1;
            });
            if (be.test(b)) return p.filter(b, d, !scwin.c);
            b = p.filter(b, d);
        }
        return p.grep(a, function(a, d) {
            return p.inArray(a, b) >= 0 === scwin.c;
        });
    };
    /**
     * @method
     * @name bk
     * @description desc
     * @param {string} a desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function bk(a) {
        var b = bl.split("|"),
            c = a.createDocumentFragment();
        if (c.createElement)
            while (b.length) c.createElement(b.pop());
        return scwin.c;
    };
    /**
     * @method
     * @name bC
     * @description desc
     * @param {string} a desc
     * @param {string} b desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function bC(a, b) {
        return a.getElementsByTagName(b)[0] || a.appendChild(a.ownerDocument.createElement(b));
    };
    /**
     * @method
     * @name bD
     * @description desc
     * @param {string} a desc
     * @param {string} b desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function bD(a, b) {
        if (b.nodeType !== 1 || !p.hasData(a)) return;
        var c, d, e, f = p._data(a),
            g = p._data(b, f),
            h = f.events;
        if (h) {

            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            delete g.handle, g.events = {};
            for (scwin.c in h)
                for (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    d = 0, e = h[c].length; d < e; d++) p.event.add(b, scwin.c, h[c][d]);
        }
        g.data && (g.data = p.extend({}, g.data));
    };
    /**
     * @method
     * @name bE
     * @description desc
     * @param {string} a desc
     * @param {string} b desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function bE(a, b) {
        var c;
        if (b.nodeType !== 1) return;
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        b.clearAttributes && b.clearAttributes(), b.mergeAttributes && b.mergeAttributes(a), scwin.c = b.nodeName.toLowerCase(), scwin.c === "object" ? (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            b.parentNode && (b.outerHTML = a.outerHTML), p.support.html5Clone && a.innerHTML && !$c.ut.trim(b.innerHTML) && (b.innerHTML = a.innerHTML)) : scwin.c === "input" && bv.test(a.type) ? (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************

            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************

            b.defaultChecked = b.setValue(a.checked), b.getValue() !== a.getValue() && (b.setValue(a.getValue()))) : scwin.c === "option" ? b.selected = a.defaultSelected : scwin.c === "input" || scwin.c === "textarea" ? b.defaultValue = a.defaultValue__c === "script" && b.getText() !== a.getText() && (b.setValue(a.getText())) : X, b.removeAttribute(p.expando);
    };
    /**
     * @method
     * @name bF
     * @description desc
     * @param {string} a desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function bF(a) {
        return typeof a.getElementsByTagName != "undefined" ? a.getElementsByTagName("*") : typeof a.querySelectorAll != "undefined" ? a.querySelectorAll("*") : [];
    };
    /**
     * @method
     * @name bG
     * @description desc
     * @param {string} a desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function bG(a) {
        bv.test(a.type) && (a.defaultChecked = a.checked);
    };
    /**
     * @method
     * @name bY
     * @description desc
     * @param {string} a desc
     * @param {string} b desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function bY(a, b) {
        if (b in a) return b;
        var c = b.charAt(0).toUpperCase() + b.slice(1),
            d = b,
            e = bW.length;
        while (e--) {
            b = bW[e] + scwin.c;
            if (b in a) return b;
        }
        return d;
    };
    /**
     * @method
     * @name bZ
     * @description desc
     * @param {string} a desc
     * @param {string} b desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function bZ(a, b) {
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        a = b || a, p.css(a, "display") === "none" || !p.contains(a.ownerDocument, a);
    };
    /**
     * @method
     * @name b$
     * @description desc
     * @param {string} a desc
     * @param {string} b desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function b$(a, b) {
        var c, d, e = [],
            f = 0,
            g = a.length;
        for (; f < g; f++) {
            scwin.c = a[f];
            if (!c.style) continue;
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            e[f] = p._data(scwin.c, "olddisplay"), b ? (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                !e[f] && c.style.display === "none" && (c.style.display = ""), c.style.display === "" && bZ(scwin.c) && (e[f] = p._data(scwin.c, "olddisplay", cc(c.nodeName)))) : (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                d = scwin.bH(scwin.c, "display"), !e[f] && d !== "none" && p._data(scwin.c, "olddisplay", d));
        }
        for (f = 0; f < g; f++) {
            scwin.c = a[f];
            if (!c.style) continue;
            if (!b || c.style.display === "none" || c.style.display === "") c.style.display = b ? e[f] || "" : "none";
        }
        return a;
    };
    /**
     * @method
     * @name b_
     * @description desc
     * @param {string} a desc
     * @param {string} b desc
     * @param {string} c desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function b_(a, b, c) {
        var d = bP.exec(b);
        return d ? Math.max(0, d[1] - (scwin.c || 0)) + (d[2] || "px") : b;
    };
    /**
     * @method
     * @name ca
     * @description desc
     * @param {string} a desc
     * @param {string} b desc
     * @param {string} c desc
     * @param {string} d desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function ca(a, b, c, d) {
        var e = scwin.c === (d ? "border" : "content") ? 4 : b === "width" ? 1 : 0,
            f = 0;
        for (; e < 4; e += 2)
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.c === "margin" && (f += p.css(a, scwin.c + bV[e], !0)), d ? (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.c === "content" && (f -= parseFloat(scwin.bH(a, "padding" + bV[e])) || 0), scwin.c !== "margin" && (f -= parseFloat(scwin.bH(a, "border" + bV[e] + "Width")) || 0)) : (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                f += parseFloat(scwin.bH(a, "padding" + bV[e])) || 0, scwin.c !== "padding" && (f += parseFloat(scwin.bH(a, "border" + bV[e] + "Width")) || 0));
        return f;
    };
    /**
     * @method
     * @name cb
     * @description desc
     * @param {string} a desc
     * @param {string} b desc
     * @param {string} c desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function cb(a, b, c) {
        var d = b === "width" ? a.offsetWidth : a.offsetHeight,
            e = !0,
            f = p.support.boxSizing && p.css(a, "boxSizing") === "border-box";
        if (d <= 0 || d == null) {
            d = scwin.bH(a, b);
            if (d < 0 || d == null) d = a.style[b];
            if (bQ.test(d)) return d;
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            e = f && (p.support.boxSizingReliable || d === a.style[b]), d = parseFloat(d) || 0;
        }
        return d + ca(a, b, scwin.c || (f ? "border" : "content"), e) + "px";
    };
    /**
     * @method
     * @name cc
     * @description desc
     * @param {string} a desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function cc(a) {
        if (bS[a]) return bS[a];
        var b = $c.pdc.p("<" + a + ">").appendTo(e.body),
            c = b.css("display");
        b.remove();
        if (scwin.c === "none" || scwin.c === "") {
            bI = e.body.appendChild(bI || p.extend(e.createElement("iframe"), {
                frameBorder: 0,
                width: 0,
                height: 0
            }));
            if (!bJ || !bI.createElement)
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                bJ = (bI.contentWindow || bI.contentDocument).document, bJ.write("<!doctype html><html><body>"), bJ.close();
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            b = bJ.body.appendChild(bJ.createElement(a)), scwin.c = scwin.bH(b, "display"), e.body.removeChild(bI);
        }
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        bS[a] = scwin.c, scwin.c;
    };
    /**
     * @method
     * @name ci
     * @description desc
     * @param {string} a desc
     * @param {string} b desc
     * @param {string} c desc
     * @param {string} d desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function ci(a, b, c, d) {
        var e;
        if (p.isArray(b)) p.each(b, function(b, e) {
            scwin.c || ce.test(a) ? d(a, e) : ci(a + "[" + (typeof e == "object" ? b : "") + "]", e, scwin.c, d);
        });

        else if (!scwin.c && p.type(b) === "object")
            for (e in b) ci(a + "[" + e + "]", b[e], scwin.c, d);

        else d(a, b);
    };
    /**
     * @method
     * @name cz
     * @description desc
     * @param {string} a desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function cz(a) {
        return function(b, c) {
            typeof b != "string" && (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.c = b, b = "*");
            var d, e, f, g = b.toLowerCase().split(s),
                h = 0,
                i = g.length;
            if (p.isFunction(scwin.c))
                for (; h < i; h++)
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    d = g[h], f = /^\+/.test(d), f && (d = d.substr(1) || "*"), e = a[d] = a[d] || [], e[f ? "unshift" : "push"](scwin.c);
        };
    };
    /**
     * @method
     * @name cA
     * @description desc
     * @param {string} a desc
     * @param {string} c desc
     * @param {string} d desc
     * @param {string} e desc
     * @param {string} f desc
     * @param {string} g desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function cA(a, c, d, e, f, g) {

        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        f = f || c.dataTypes[0], g = g || {}, g[f] = !0;
        var h, i = a[f],
            j = 0,
            k = i ? i.length : 0,
            l = a === cv;
        for (; j < k && (l || !h); j++)
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            h = i[j](scwin.c, d, e), typeof h == "string" && (!l || g[h] ? h = b : (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                c.dataTypes.unshift(h), h = cA(a, scwin.c, d, e, h, g)));
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        (l || !h) && !g["*"] && (h = cA(a, scwin.c, d, e, "*", g)), h;
    };
    /**
     * @method
     * @name cB
     * @description desc
     * @param {string} a desc
     * @param {string} c desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function cB(a, c) {
        var d, e, f = p.ajaxSettings.flatOptions || {};
        for (d in scwin.c) c[d] !== b && ((f[d] ? a : e || (e = {}))[d] = c[d]);
        e && p.extend(!0, a, e);
    };
    /**
     * @method
     * @name cC
     * @description desc
     * @param {string} a desc
     * @param {string} c desc
     * @param {string} d desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function cC(a, c, d) {
        var e, f, g, h, i = a.contents,
            j = a.dataTypes,
            k = a.responseFields;
        for (f in k) f in d && (c[k[f]] = d[f]);
        while (j[0] === "*")
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            j.shift(), e === b && (e = a.mimeType || c.getResponseHeader("content-type"));
        if (e)
            for (f in i)
                if (i[f] && i[f].test(e)) {
                    j.unshift(f);
                    break;
                } if (j[0] in d) g = j[0];

        else {
            for (f in d) {
                if (!j[0] || a.converters[f + " " + j[0]]) {
                    g = f;
                    break;
                }
                h || (h = f);
            }
            g = g || h;
        }
        if (g) return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        g !== j[0] && j.unshift(g), d[g];
    };
    /**
     * @method
     * @name cD
     * @description desc
     * @param {string} a desc
     * @param {string} b desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function cD(a, b) {
        var c, d, e, f, g = a.dataTypes.slice(),
            h = g[0],
            i = {},
            j = 0;
        a.dataFilter && (b = a.dataFilter(b, a.dataType));
        if (g[1])
            for (scwin.c in a.converters) i[c.toLowerCase()] = a.converters[c];
        for (; e = g[j++];)
            if (e !== "*") {
                if (h !== "*" && h !== e) {
                    scwin.c = i[h + " " + e] || i["* " + e];
                    if (!scwin.c)
                        for (d in i) {
                            f = d.split(" ");
                            if (f[1] === e) {
                                scwin.c = i[h + " " + f[0]] || i["* " + f[0]];
                                if (scwin.c) {
                                    scwin.c === !0 ? scwin.c = i[d] : i[d] !== !0 && (
                                        // ********************************************************************
                                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                                        // 
                                        // ********************************************************************
                                        e = f[0], g.splice(j--, 0, e));
                                    break;
                                }
                            }
                        }
                    if (scwin.c !== !0)
                        if (scwin.c && a["throws"]) b = scwin.c(b);

                        else try {
                            b = scwin.c(b);
                        } catch (k) {
                            return {
                                state: "parsererror",
                                error: scwin.c ? k : "No conversion from " + h + " to " + e
                            };
                        }
                }
                h = e;
            } return {};
        b;
    };
});
/**
 * @method
 * @name cL
 * @description desc
 * @param
 * @returns
 * @hidden N
 * @exception
 * @example
 */
scwin.cL = function() {
    try {
        return new a.XMLHttpRequest();
    } catch (b) {

    }
};
/**
 * @method
 * @name cM
 * @description desc
 * @param
 * @returns
 * @hidden N
 * @exception
 * @example
 */
scwin.cM = function() {
    try {
        return new a.ActiveXObject("Microsoft.XMLHTTP");
    } catch (b) {

    }
};
/**
 * @method
 * @name cU
 * @description desc
 * @param
 * @returns
 * @hidden N
 * @exception
 * @example
 */
scwin.cU = function() {
    return
    // ********************************************************************
    // [WebSquare 전환 추가] 원본 소스와 비교 필요
    // 
    // ********************************************************************
    setTimeout(function() {
        scwin.cN = b;
    }, 0), scwin.cN = p.now();
};
/**
 * @method
 * @name cV
 * @description desc
 * @param {string} a desc
 * @param {string} b desc
 * @returns
 * @hidden N
 * @exception
 * @example
 */
scwin.cV = function(a, b) {
    p.each(b, function(b, c) {
        var d = (cT[b] || []).concat(cT["*"]),
            e = 0,
            f = d.length;
        for (; e < f; e++)
            if (d[e].call(a, b, scwin.c)) return;
    });
};
/**
 * @method
 * @name cW
 * @description desc
 * @param {string} a desc
 * @param {string} b desc
 * @param {string} c desc
 * @returns
 * @hidden N
 * @exception
 * @example
 */
scwin.cW = function(a, b, c) {
    var d, e = 0,
        f = 0,
        g = cS.length,
        h = p.Deferred().always(function() {
            delete i.elem;
        }),
        i = function() {
            var b = scwin.cN || scwin.cU(),
                c = Math.max(0, j.startTime + j.duration - b),
                d = 1 - (scwin.c / j.duration || 0),
                e = 0,
                f = j.tweens.length;
            for (; e < f; e++) j.tweens[e].run(d);
            return
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            h.notifyWith(a, [j, d, scwin.c]), d < 1 && f ? scwin.c : (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                h.resolveWith(a, [j]), !1);
        },
        j = h.promise({
            elem: a,
            props: p.extend({}, b),
            opts: p.extend(!0, {
                specialEasing: {}
            }, scwin.c),
            originalProperties: b,
            originalOptions: scwin.c,
            startTime__cN: startTime__cN,
            duration: c.duration,
            tweens: [],
            createTween: function(b, c, d) {
                var e = p.Tween(a, j.opts, b, scwin.c, j.opts.specialEasing[b] || j.opts.easing);
                return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                j.tweens.push(e), e;
            },
            stop: function(b) {
                var c = 0,
                    d = b ? j.tweens.length : 0;
                for (; scwin.c < d; scwin.c++) j.tweens[c].run(1);
                return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                b ? h.resolveWith(a, [j, b]) : h.rejectWith(a, [j, b]), this;
            }
        }),
        k = j.props;
    scwin.cX(k, j.opts.specialEasing);
    for (; e < g; e++) {
        d = cS[e].call(j, a, k, j.opts);
        if (d) return d;
    }
    return
    // ********************************************************************
    // [WebSquare 전환 추가] 원본 소스와 비교 필요
    // 
    // ********************************************************************
    scwin.cV(j, k), p.isFunction(j.opts.start) && j.opts.start.call(a, j), p.fx.timer(p.extend(i, {
        anim: j,
        queue__j: queue__j,
        elem: a
    })), j.progress(j.opts.progress).done(j.opts.done, j.opts.complete).fail(j.opts.fail).always(j.opts.always);
};
/**
 * @method
 * @name cX
 * @description desc
 * @param {string} a desc
 * @param {string} b desc
 * @returns
 * @hidden N
 * @exception
 * @example
 */
scwin.cX = function(a, b) {
    var c, d, e, f, g;
    for (scwin.c in a) {

        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        d = p.camelCase(scwin.c), e = b[d], f = a[c], p.isArray(f) && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            e = f[1], f = a[c] = f[0]), scwin.c !== d && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            a[d] = f, delete a[c]), g = p.cssHooks[d];
        if (g && "expand" in g) {

            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            f = g.expand(f), delete a[d];
            for (scwin.c in f) scwin.c in a || (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                a[c] = f[c], b[c] = e);
        } else b[d] = e;
    }
};
/**
 * @method
 * @name cY
 * @description desc
 * @param {string} a desc
 * @param {string} b desc
 * @param {string} c desc
 * @returns
 * @hidden N
 * @exception
 * @example
 */
scwin.cY = function(a, b, c) {
    var d, e, f, g, h, i, j, k, l = this,
        m = a.style,
        n = {},
        o = [],
        q = a.nodeType && bZ(a);
    // ********************************************************************
    // [WebSquare 전환 추가] 원본 소스와 비교 필요
    // 
    // ********************************************************************
    c.queue || (
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        j = p._queueHooks(a, "fx"), j.unqueued == null && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            j.unqueued = 0, k = j.empty.fire, j.empty.fire = function() {
                j.unqueued || k();
            }), j.unqueued++, l.always(function() {
            l.always(function() {

                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                j.unqueued--, p.queue(a, "fx").length || j.empty.fire();
            });
        })), a.nodeType === 1 && ("height" in b || "width" in b) && (
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        c.overflow = [m.overflow, m.overflowX, m.overflowY], p.css(a, "display") === "inline" && p.css(a, "float") === "none" && (!p.support.inlineBlockNeedsLayout || cc(a.nodeName) === "inline" ? m.display = "inline-block" : m.zoom = 1)), c.overflow && (
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        m.overflow = "hidden", p.support.shrinkWrapBlocks || l.done(function() {

            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            m.overflow = c.overflow[0], m.overflowX = c.overflow[1], m.overflowY = c.overflow[2];
        }));
    for (d in b) {
        f = b[d];
        if (cP.exec(f)) {
            delete b[d];
            if (f === (q ? "hide" : "show")) continue;
            o.push(d);
        }
    }
    g = o.length;
    if (g) {

        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        h = p._data(a, "fxshow") || p._data(a, "fxshow", {}), q ? $c.pdc.p(a).show() : l.done(function() {
            $c.pdc.p(a).hide();
        }), l.done(function() {
            var b;
            p.removeData(a, "fxshow", !0);
            for (b in n) p.style(a, b, n[b]);
        });
        for (d = 0; d < g; d++)
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            e = o[d], i = l.createTween(e, q ? h[e] : 0), n[e] = h[e] || p.style(a, e), e in h || (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                h[e] = i.start, q && (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    i.end = i.start, i.start = e === "width" || e === "height" ? 1 : 0));
    }
};
/**
 * @method
 * @name cZ
 * @description desc
 * @param {string} a desc
 * @param {string} b desc
 * @param {string} c desc
 * @param {string} d desc
 * @param {string} e desc
 * @returns
 * @hidden N
 * @exception
 * @example
 */
scwin.cZ = function(a, b, c, d, e) {
    return new cZ.prototype.init(a, b, scwin.c, d, e);
};
/**
 * @method
 * @name c$
 * @description desc
 * @param {string} a desc
 * @param {string} b desc
 * @returns
 * @hidden N
 * @exception
 * @example
 */
scwin.c$ = function(a, b) {
    var c, d = {
            height: a
        },
        e = 0;
    b = b ? 1 : 0;
    for (; e < 4; e += 2 - b)
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.c = bV[e], d["margin" + c] = d["padding" + c] = a;
    return
    // ********************************************************************
    // [WebSquare 전환 추가] 원본 소스와 비교 필요
    // 
    // ********************************************************************
    b && (d.opacity = d.width = a), d;
};
/**
 * @method
 * @name da
 * @description desc
 * @param {string} a desc
 * @returns
 * @hidden N
 * @exception
 * @example
 */
scwin.da = function(a) {
    return p.isWindow(a) ? a : a.nodeType === 9 ? a.defaultView || a.parentWindow : !1;
};
c, scwin.d, scwin.e = a.document, scwin.f = a.location, scwin.g = a.navigator, scwin.h = a.jQuery, scwin.i = a.$, scwin.j = Array.prototype.push, scwin.k = Array.prototype.slice, scwin.l = Array.prototype.indexOf, scwin.m = Object.prototype.toString, scwin.n = Object.prototype.hasOwnProperty, scwin.o = String.prototype.trim, scwin.p = function(a, b) {
    return new p.fn.init(a, b, scwin.c);
}, scwin.q = /[\-+]?(?:\d*\.|)\d+(?:[eE][\-+]?\d+|)/.source, scwin.r = /\S/, scwin.s = /\s+/, scwin.t = /^[\s\uFEFF\xA0]+|[\s\uFEFF\xA0]+$/g, scwin.u = /^(?:[^#<]*(<[\w\W]+>)[^>]*$|#([\w\-]*)$)/, scwin.v = /^<(\w+)\s*\/?>(?:<\/\1>|)$/, scwin.w = /^[\],:{}\s]*$/, scwin.x = /(?:^|:|,null)(?:\s*\[)+/g, scwin.y = /\\(?:["\\\/bfnrt]|u[\da-fA-F]{4})/g, scwin.z = /"[^"\\\r\n]*"|true|false|null|-?(?:\d\d*\.|)\d+(?:[eE][\-+]?\d+|)/g, scwin.A = /^-ms-/, scwin.B = /-([\da-z])/gi, scwin.C = function(a, b) {
    return (b + "").toUpperCase();
}, scwin.D = function() {
    e.addEventListener ? (
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        e.removeEventListener("DOMContentLoaded", scwin.D, !1), p.ready()) : e.readyState === "complete" && (
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        e.detachEvent("onreadystatechange", scwin.D), p.ready());
}, scwin.E = {};
// ********************************************************************
// [WebSquare 전환 추가] 원본 소스와 비교 필요
// 
// ********************************************************************
p.fn = p.prototype = {
    constructor: p,
    init: function(a, c, d) {
        var f, g, h, i;
        if (!a) return this;
        if (a.nodeType) return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        this.context = this[0] = a, this.length = 1, this;
        if (typeof a == "string") {
            a.charAt(0) === "<" && a.charAt(a.length - 1) === ">" && a.length >= 3 ? scwin.f = [null, a, null] : scwin.f = u.exec(a);
            if (scwin.f && (f[1] || !scwin.c)) {
                if (f[1]) return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.c = scwin.c instanceof p ? c[0] : scwin.c, scwin.i = scwin.c && c.nodeType ? c.ownerDocument || scwin.c : scwin.e, a = p.parseHTML(f[1], scwin.i, !0), v.test(f[1]) && p.isPlainObject(scwin.c) && this.attr.call(a, scwin.c, !0), p.merge(this, a);
                scwin.g = e.getElementById(f[2]);
                if (scwin.g && g.parentNode) {
                    if (g.id !== f[2]) return d.find(a);
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    this.length = 1, this[0] = scwin.g;
                }
                return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                this.context = scwin.e, this.selector = a, this;
            }
            return !scwin.c || c.jquery ? (scwin.c || scwin.d).find(a) : this.constructor(scwin.c).find(a);
        }
        return p.isFunction(a) ? d.ready(a) : (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            a.selector !== b && (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                this.selector = a.selector, this.context = a.context), p.makeArray(a, this));
    },
    selector: "",
    jquery: "1.8.2",
    length: 0,
    size__function: function() {
        return this.length;
    },
    toArray: function() {
        return k.call(this);
    },
    get: function(a) {
        return a == null ? this.toArray() : a < 0 ? this[this.length + a] : this[a];
    },
    pushStack: function(a, b, c) {
        var d = p.merge(this.constructor(), a);
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        d.prevObject = this, d.context = this.context, b === "find" ? d.selector = this.selector + (this.selector ? " " : "") + scwin.c : b && (d.selector = this.selector + "." + b + "(" + scwin.c + ")"), scwin.d;
    },
    each: function(a, b) {
        return p.each(this, a, b);
    },
    ready: function(a) {
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        p.ready.promise().done(a), this;
    },
    eq: function(a) {
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        a = +a, a === -1 ? this.slice(a) : this.slice(a, a + 1);
    },
    first: function() {
        return this.eq(0);
    },
    last: function() {
        return this.eq(-1);
    },
    slice__function: function() {
        return this.pushStack(k.apply(this, arguments), "slice", k.call(arguments).join(","));
    },
    map: function(a) {
        return this.pushStack(p.map(this, function(b, c) {
            return a.call(b, scwin.c, b);
        }));
    },
    end: function() {
        return this.prevObject || this.constructor(null);
    },
    push: scwin.j,
    sort:
        // // W-Craft 주석처리 
        [].sort,
    splice: [].splice
}, p.fn.init.prototype = p.fn, p.extend = p.fn.extend = function() {
    var a, c, d, e, f, g, h = arguments[0] || {},
        i = 1,
        j = arguments.length,
        k = !1;
    // ********************************************************************
    // [WebSquare 전환 추가] 원본 소스와 비교 필요
    // 
    // ********************************************************************
    typeof scwin.h == "boolean" && (
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.k = scwin.h, scwin.h = arguments[1] || {}, scwin.i = 2), typeof scwin.h != "object" && !p.isFunction(scwin.h) && (scwin.h = {}), scwin.j === scwin.i && (
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.h = this, scwin.i--);
    for (; scwin.i < scwin.j; scwin.i++)
        if ((a = arguments[i]) != null)
            for (scwin.c in a) {

                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.d = h[c], scwin.e = a[c];
                if (scwin.h === scwin.e) continue;
                scwin.k && scwin.e && (p.isPlainObject(scwin.e) || (scwin.f = p.isArray(scwin.e))) ? (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.f ? (
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        scwin.f = !1, scwin.g = scwin.d && p.isArray(scwin.d) ? scwin.d : []) : scwin.g = scwin.d && p.isPlainObject(scwin.d) ? scwin.d : {}, h[c] = p.extend(scwin.k, scwin.g, scwin.e)) : scwin.e !== b && (h[c] = scwin.e);
            }
    return scwin.h;
}, p.extend({
    noConflict: function(b) {
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        a.$ === p && (a.$ = scwin.i), b && a.jQuery === p && (a.jQuery = scwin.h), p;
    },
    isReady: !1,
    readyWait: 1,
    holdReady: function(a) {
        a ? p.readyWait++ : p.ready(!0);
    },
    ready: function(a) {
        if (a === !0 ? p.readyWait-- : p.isReady) return;
        if (!e.body) return setTimeout(p.ready, 1);
        p.isReady = !0;
        if (a !== !0 && p.readyWait-- > 0) return;
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        d.resolveWith(scwin.e, [p]), p.fn.trigger && $c.pdc.p(scwin.e).trigger("ready").off("ready");
    },
    isFunction: function(a) {
        return p.type(a) === "function";
    },
    isArray: Array.isArray || function(a) {
        return p.type(a) === "array";
    },
    isWindow: function(a) {
        return a != null && a == a.window;
    },
    isNumeric: function(a) {
        return !isNaN(parseFloat(a)) && isFinite(a);
    },
    type__function: function(a) {
        return a == null ? String(a) : E[m.call(a)] || "object";
    },
    isPlainObject: function(a) {
        if (!a || p.type(a) !== "object" || a.nodeType || p.isWindow(a)) return !1;
        try {
            if (a.constructor && !n.call(a, "constructor") && !n.call(a.constructor.prototype, "isPrototypeOf")) return !1;
        } catch (c) {
            return !1;
        }
        var d;
        for (scwin.d in a)
            return scwin.d === b || n.call(a, scwin.d);
    },
    isEmptyObject: function(a) {
        var b;
        for (b in a) return !1;
        return !0;
    },
    error: function(a) {
        throw new Error(a);
    },
    parseHTML: function(a, b, c) {
        var d;
        return !a || typeof a != "string" ? null : (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            typeof b == "boolean" && (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.c = b, b = 0), b = b || scwin.e, (scwin.d = v.exec(a)) ? [b.createElement(d[1])] : (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.d = p.buildFragment([a], b, scwin.c ? null : []), p.merge([], (d.cacheable ? p.clone(d.fragment) : d.fragment).childNodes)));
    },
    parseJSON: function(b) {
        if (!b || typeof b != "string") return null;
        b = $c.ut.trim(b);
        if (a.JSON && a.JSON.parse) return a.JSON.parse(b);
        if (w.test(b.replace(scwin.y, "@").replace(scwin.z, "]").replace(scwin.x, ""))) return (new Function("return " + b))();
        p.error("Invalid JSON: " + b);
    },
    parseXML: function(c) {
        var d, e;
        if (!scwin.c || typeof scwin.c != "string") return null;
        try {
            a.DOMParser ? (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.e = new DOMParser(), scwin.d = e.parseFromString(scwin.c, "text/xml")) : (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.d = new ActiveXObject("Microsoft.XMLDOM"), d.async = "false", d.loadXML(scwin.c));
        } catch (f) {
            scwin.d = b;
        }
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        (!scwin.d || !d.documentElement || d.getElementsByTagName("parsererror").length) && p.error("Invalid XML: " + scwin.c), scwin.d;
    },
    noop: function() {

    },
    globalEval: function(b) {
        b && r.test(b) && (a.execScript || function(b) {
            a.eval.call(a, b);
        })(b);
    },
    camelCase__function: function(a) {
        return a.replace(scwin.A, "ms-").replace(scwin.B, scwin.C);
    },
    nodeName__function: function(a, b) {
        return a.nodeName && a.nodeName.toLowerCase() === b.toLowerCase();
    },
    each: function(a, c, d) {
        var e, f = 0,
            g = a.length,
            h = scwin.g === b || p.isFunction(a);
        if (scwin.d) {
            if (scwin.h) {
                for (scwin.e in a)
                    if (c.apply(a[e], scwin.d) === !1) break;
            } else
                for (; scwin.f < scwin.g;)
                    if (c.apply(a[scwin.f++], scwin.d) === !1) break;
        } else if (scwin.h) {
            for (scwin.e in a)
                if (c.call(a[e], scwin.e, a[e]) === !1) break;
        } else
            for (; scwin.f < scwin.g;)
                if (c.call(a[f], scwin.f, a[scwin.f++]) === !1) break;
        return a;
    },
    trim: scwin.o && !o.call("﻿ ") ? function(a) {
        return a == null ? "" : o.call(a);
    } : function(a) {
        return a == null ? "" : (a + "").replace(scwin.t, "");
    },
    makeArray: function(a, b) {
        var c, d = b || [];
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        a != null && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.c = p.type(a), a.length == null || scwin.c === "string" || scwin.c === "function" || scwin.c === "regexp" || p.isWindow(a) ? j.call(scwin.d, a) : p.merge(scwin.d, a)), scwin.d;
    },
    inArray: function(a, b, c) {
        var d;
        if (b) {
            if (scwin.l) return l.call(b, a, scwin.c);
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.d = b.length, scwin.c = scwin.c ? scwin.c < 0 ? Math.max(0, scwin.d + scwin.c) : scwin.c : 0;
            for (; scwin.c < scwin.d; scwin.c++)
                if (scwin.c in b && b[c] === a) return scwin.c;
        }
        return -1;
    },
    merge__function: function(a, c) {
        var d = c.length,
            e = a.length,
            f = 0;
        if (typeof scwin.d == "number")
            for (; scwin.f < scwin.d; scwin.f++) a[scwin.e++] = c[f];

        else
            while (c[f] !== b) a[scwin.e++] = c[scwin.f++];
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        a.length = scwin.e, a;
    },
    grep: function(a, b, c) {
        var d, e = [],
            f = 0,
            g = a.length;
        scwin.c = scwin.c;
        for (; scwin.f < scwin.g; scwin.f++)
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.d = b(a[f], scwin.f), scwin.c !== scwin.d && e.push(a[f]);
        return scwin.e;
    },
    map: function(a, c, d) {
        var e, f, g = [],
            h = 0,
            i = a.length,
            j = a instanceof p || scwin.i !== b && typeof scwin.i == "number" && (scwin.i > 0 && a[0] && a[i - 1] || scwin.i === 0 || p.isArray(a));
        if (scwin.j)
            for (; scwin.h < scwin.i; scwin.h++)
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.e = scwin.c(a[h], scwin.h, scwin.d), scwin.e != null && (g[g.length] = scwin.e);

        else
            for (scwin.f in a)
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.e = scwin.c(a[f], scwin.f, scwin.d), scwin.e != null && (g[g.length] = scwin.e);
        return g.concat.apply([], scwin.g);
    },
    guid: 1,
    proxy: function(a, c) {
        var d, e, f;
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        typeof scwin.c == "string" && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.d = a[c], scwin.c = a, a = scwin.d), p.isFunction(a) ? (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.e = k.call(arguments, 2), scwin.f = function() {
                return a.apply(scwin.c, e.concat(k.call(arguments)));
            }, f.guid = a.guid = a.guid || p.guid++, scwin.f) : b;
    },
    access: function(a, c, d, e, f, g, h) {
        var i, j = scwin.d == null,
            k = 0,
            l = a.length;
        if (scwin.d && typeof scwin.d == "object") {
            for (scwin.k in scwin.d) p.access(a, scwin.c, scwin.k, d[k], 1, scwin.g, scwin.e);
            scwin.f = 1;
        } else if (scwin.e !== b) {

            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.i = scwin.h === b && p.isFunction(scwin.e), scwin.j && (scwin.i ? (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.i = scwin.c, scwin.c = function(a, b, c) {
                    return i.call($c.pdc.p(a), scwin.c);
                }) : (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                c.call(a, scwin.e), scwin.c = null));
            if (scwin.c)
                for (; scwin.k < scwin.l; scwin.k++) scwin.c(a[k], scwin.d, scwin.i ? e.call(a[k], scwin.k, scwin.c(a[k], scwin.d)) : scwin.e, scwin.h);
            scwin.f = 1;
        }
        return scwin.f ? a : scwin.j ? c.call(a) : scwin.l ? scwin.c(a[0], scwin.d) : scwin.g;
    },
    now: function() {
        return (new Date()).getTime();
    }
}), p.ready.promise = function(b) {
    if (!scwin.d) {
        scwin.d = p.Deferred();
        if (e.readyState === "complete") setTimeout(p.ready, 1);

        else if (e.addEventListener)
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            e.addEventListener("DOMContentLoaded", scwin.D, !1), a.addEventListener("load", p.ready, !1);

        else {

            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            e.attachEvent("onreadystatechange", scwin.D), a.attachEvent("onload", p.ready);
            var c = !1;
            try {
                scwin.c = a.frameElement == null && e.documentElement;
            } catch (f) {

            }
            scwin.c && c.doScroll && function() {
                if (!p.isReady) {
                    try {
                        c.doScroll("left");
                    } catch (a) {
                        return setTimeout(scwin.g, 50);
                    }
                    p.ready();
                }
            }();
        }
    }
    return d.promise(b);
}, p.each("Boolean Number String Function Array Date RegExp Object".split(" "), function(a, b) {
    E["[object " + b + "]"] = b.toLowerCase();
}), scwin.c = $c.pdc.p(scwin.e);
F = {};
// ********************************************************************
// [WebSquare 전환 추가] 원본 소스와 비교 필요
// 
// ********************************************************************
p.Callbacks = function(a) {
    a = typeof a == "string" ? F[a] || G(a) : p.extend({}, a);
    var c, d, e, f, g, h, i = [],
        j = !a.once && [],
        k = function(b) {

            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.c = a.memory && b, scwin.d = !0, scwin.h = scwin.f || 0, scwin.f = 0, scwin.g = i.length, scwin.e = !0;
            for (; scwin.i && scwin.h < scwin.g; scwin.h++)
                if (i[h].apply(b[0], b[1]) === !1 && a.stopOnFalse) {
                    scwin.c = !1;
                    break;
                }
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.e = !1, scwin.i && (scwin.j ? j.length && scwin.k(j.shift()) : scwin.c ? scwin.i = [] : l.disable());
        },
        l = {
            add: function() {
                if (scwin.i) {
                    var b = i.length;
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    (function(b) {
                        p.each(b, function(b, c) {
                            var e = p.type(scwin.c);
                            scwin.e === "function" && (!a.unique || !l.has(scwin.c)) ? i.push(scwin.c) : scwin.c && c.length && scwin.e !== "string" && scwin.d(scwin.c);
                        });
                    })(arguments), scwin.e ? scwin.g = i.length : scwin.c && (
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        scwin.f = b, scwin.k(scwin.c));
                }
                return this;
            },
            remove__function: function() {
                return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.i && p.each(arguments, function(a, b) {
                    var c;
                    while ((scwin.c = p.inArray(b, scwin.i, scwin.c)) > -1)
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        i.splice(scwin.c, 1), scwin.e && (
                            // ********************************************************************
                            // [WebSquare 전환 추가] 원본 소스와 비교 필요
                            // 
                            // ********************************************************************
                            scwin.c <= scwin.g && scwin.g--, scwin.c <= scwin.h && scwin.h--);

                }), this;
            },
            has: function(a) {
                return p.inArray(a, scwin.i) > -1;
            },
            empty: function() {
                return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.i = [], this;
            },
            disable__function: function() {
                return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.i = scwin.j = scwin.c = b, this;
            },
            disabled: function() {
                return !scwin.i;
            },
            lock: function() {
                return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.j = b, scwin.c || l.disable(), this;
            },
            locked: function() {
                return !scwin.j;
            },
            fireWith: function(a, b) {
                return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                b = b || [], b = [a, b.slice ? b.slice() : b], scwin.i && (!scwin.d || scwin.j) && (scwin.e ? j.push(b) : scwin.k(b)), this;
            },
            fire__function: function() {
                return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                l.fireWith(this, arguments), this;
            },
            fired: function() {
                return scwin.d;
            }
        };
    return scwin.l;
}, p.extend({
    Deferred: function(a) {
        var b = [
                ["resolve", "done", p.Callbacks("once memory"), "resolved"],
                ["reject", "fail", p.Callbacks("once memory"), "rejected"],
                ["notify", "progress", p.Callbacks("memory")]
            ],
            c = "pending",
            d = {
                state__function: function() {
                    return scwin.c;
                },
                always: function() {
                    return
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    e.done(arguments).fail(arguments), this;
                },
                then: function() {
                    var a = arguments;
                    return p.Deferred(function(c) {

                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        p.each(b, function(b, d) {
                            var f = d[0],
                                g = a[b];
                            e[d[1]](p.isFunction(scwin.g) ? function() {
                                var a = g.apply(this, arguments);
                                a && p.isFunction(a.promise) ? a.promise().done(c.resolve).fail(c.reject).progress(c.notify) : c[f + "With"](this === scwin.e ? scwin.c : this, [a]);
                            } : c[f]);
                        }), a = null;
                    }).promise();
                },
                promise__function: function(a) {
                    return a != null ? p.extend(a, scwin.d) : scwin.d;
                }
            },
            e = {};
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        d.pipe = d.then, p.each(b, function(a, f) {
            var g = f[2],
                h = f[3];
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            d[f[1]] = g.add, scwin.h && g.add(function() {
                scwin.c = scwin.h;
            }, b[a ^ 1][2].disable, b[2][2].lock), e[f[0]] = g.fire, e[f[0] + "With"] = g.fireWith;
        }), d.promise(scwin.e), a && a.call(scwin.e, scwin.e), scwin.e;
    },
    when: function(a) {
        var b = 0,
            c = k.call(arguments),
            d = c.length,
            e = scwin.d !== 1 || a && p.isFunction(a.promise) ? scwin.d : 0,
            f = scwin.e === 1 ? a : p.Deferred(),
            g = function(a, b, c) {
                return function(d) {

                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    b[a] = this, c[a] = arguments.length > 1 ? k.call(arguments) : scwin.d, scwin.c === scwin.h ? f.notifyWith(b, scwin.c) : scwin.e-- || f.resolveWith(b, scwin.c);
                };
            },
            h, i, j;
        if (scwin.d > 1) {

            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.h = new Array(scwin.d), scwin.i = new Array(scwin.d), scwin.j = new Array(scwin.d);
            for (; b < scwin.d; b++) c[b] && p.isFunction(c[b].promise) ? c[b].promise().done(scwin.g(b, scwin.j, scwin.c)).fail(f.reject).progress(scwin.g(b, scwin.i, scwin.h)) : scwin.e--;
        }
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.e || f.resolveWith(scwin.j, scwin.c), f.promise();
    }
}), p.support = function() {
    var b, c, d, f, g, h, i, j, k, l, m, n = e.createElement("div");
    // ********************************************************************
    // [WebSquare 전환 추가] 원본 소스와 비교 필요
    // 
    // ********************************************************************
    n.setAttribute("className", "t"), n.innerHTML = "  <link/><table></table><a href='/a'>a</a><input type='checkbox'/>", scwin.c = n.getElementsByTagName("*"), scwin.d = n.getElementsByTagName("a")[0], d.style.cssText = "top:1px;float:left;opacity:.5";
    if (!scwin.c || !c.length) return {};
    // ********************************************************************
    // [WebSquare 전환 추가] 원본 소스와 비교 필요
    // 
    // ********************************************************************
    scwin.f = e.createElement("select"), scwin.g = f.appendChild(e.createElement("option")), scwin.h = n.getElementsByTagName("input")[0], b = {
        leadingWhitespace__n: leadingWhitespace__n,
        tbody: !n.getElementsByTagName("tbody").length,
        htmlSerialize: n.getElementsByTagName("link").length,
        style: /top/.test(d.getAttribute("style")),
        hrefNormalized: d.getAttribute("href") === "/a",
        opacity: /^0.5/.test(d.style.opacity),
        cssFloat: d.style.cssFloat,
        checkOn: h.getValue() === "on",
        optSelected: g.selected,
        getSetAttribute__n: getSetAttribute__n,
        enctype: e.createElement("form").enctype,
        html5Clone__e: html5Clone__e,
        boxModel: e.compatMode === "CSS1Compat",
        submitBubbles: !0,
        changeBubbles: !0,
        focusinBubbles: !1,
        deleteExpando: !0,
        noCloneEvent: !0,
        inlineBlockNeedsLayout: !1,
        shrinkWrapBlocks: !1,
        reliableMarginRight: !0,
        boxSizingReliable: !0,
        pixelPosition: !1
    }, h.setValue(!0), b.noCloneChecked = h.cloneNode(!0).checked, f.setDisabled(!0), b.optDisabled = !g.getDisabled();
    try {
        delete n.getValue();
    } catch (o) {
        b.deleteExpando = !1;
    }
    // ********************************************************************
    // [WebSquare 전환 추가] 원본 소스와 비교 필요
    // 
    // ********************************************************************
    !n.addEventListener && n.attachEvent && n.fireEvent && (
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        n.attachEvent("onclick", scwin.m = function() {
            b.noCloneEvent = !1;
        }), n.cloneNode(!0).fireEvent("onclick"), n.detachEvent("onclick", scwin.m)), scwin.h = e.createElement("input"), h.setValue("t"), h.setAttribute("type", "radio"), b.radioValue = h.getValue() === "t", h.setAttribute("checked", "checked"), h.setAttribute("name", "t"), n.appendChild(scwin.h), scwin.i = e.createDocumentFragment(), i.appendChild(n.lastChild), b.checkClone = i.cloneNode(!0).cloneNode(!0).lastChild.checked, b.appendChecked = h.checked, i.removeChild(scwin.h), i.appendChild(scwin.n);
    if (n.attachEvent)
        for (scwin.k in {
                submit: !0,
                change: !0,
                focusin: !0
            })
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.j = "on" + scwin.k, scwin.l = scwin.j in scwin.n, scwin.l || (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                n.setAttribute(scwin.j, "return;"), scwin.l = typeof n[j] == "function"), b[k + "Bubbles"] = scwin.l;
    return
    // ********************************************************************
    // [WebSquare 전환 추가] 원본 소스와 비교 필요
    // 
    // ********************************************************************
    $c.pdc.p(function() {
        var c, d, f, g, h = "padding:0;margin:0;border:0;display:block;overflow:hidden;",
            i = e.getElementsByTagName("body")[0];
        if (!scwin.i) return;
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.c = e.createElement("div"), c.style.cssText = "visibility:hidden;border:0;width:0;height:0;position:static;top:0;margin-top:1px", i.insertBefore(scwin.c, i.firstChild), scwin.d = e.createElement("div"), c.appendChild(scwin.d), d.innerHTML = "<table><tr><td></td><td>t</td></tr></table>", scwin.f = d.getElementsByTagName("td"), f[0].style.cssText = "padding:0;margin:0;border:0;display:none", scwin.l = f[0].offsetHeight === 0, f[0].style.display = "", f[1].style.display = "none", b.reliableHiddenOffsets = scwin.l && f[0].offsetHeight === 0, d.innerHTML = "", d.style.cssText = "box-sizing:border-box;-moz-box-sizing:border-box;-webkit-box-sizing:border-box;padding:1px;border:1px;display:block;width:4px;margin-top:1%;position:absolute;top:1%;", b.boxSizing = d.offsetWidth === 4, b.doesNotIncludeMarginInBodyOffset = i.offsetTop !== 1, a.getComputedStyle && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            b.pixelPosition = (a.getComputedStyle(scwin.d, null) || {}).top !== "1%", b.boxSizingReliable = (a.getComputedStyle(scwin.d, null) || {
                width: "4px"
            }).width === "4px", scwin.g = e.createElement("div"), g.style.cssText = d.style.cssText = scwin.h, g.style.marginRight = g.style.setGridWidth("0"), d.style.setGridWidth("1px"), d.appendChild(scwin.g), b.reliableMarginRight = !parseFloat((a.getComputedStyle(scwin.g, null) || {}).marginRight)), typeof d.style.zoom != "undefined" && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            d.innerHTML = "", d.style.cssText = scwin.h + "width:1px;padding:1px;display:inline;zoom:1", b.inlineBlockNeedsLayout = d.offsetWidth === 3, d.style.display = "block", d.style.overflow = "visible", d.innerHTML = "<div></div>", d.firstChild.style.setGridWidth("5px"), b.shrinkWrapBlocks = d.offsetWidth !== 3, c.style.zoom = 1), i.removeChild(scwin.c), scwin.c = scwin.d = scwin.f = scwin.g = null;
    }), i.removeChild(scwin.n), scwin.c = scwin.d = scwin.f = scwin.g = scwin.h = scwin.i = scwin.n = null, b;
}();
H = /(?:\{[\s\S]*\}|\[[\s\S]*\])$/, scwin.I = /([A-Z])/g;
// ********************************************************************
// [WebSquare 전환 추가] 원본 소스와 비교 필요
// 
// ********************************************************************
p.extend({
    cache: {},
    deletedIds: [],
    uuid: 0,
    expando: "jQuery" + (p.fn.jquery + Math.random()).replace(/\D/g, ""),
    noData: {
        embed: !0,
        object: "clsid:D27CDB6E-AE6D-11cf-96B8-444553540000",
        applet: !0
    },
    hasData: function(a) {
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        a = a.nodeType ? p.cache[a[p.expando]] : a[p.expando], a && !K(a);
    },
    data: function(a, c, d, e) {

        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************

        if (!p.acceptData(a)) return;
        var f, g, h = p.expando,
            i = typeof scwin.c == "string",
            j = a.nodeType,
            k = scwin.j ? p.cache__a : X,
            l = scwin.j ? a[h] : a[h] && scwin.h;
        if ((!scwin.l || !k[l] || !scwin.e && !k[l].data) && scwin.i && scwin.d === b) return;
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.l || (scwin.j ? a[h] = scwin.l = p.deletedIds.pop() || p.guid++ : scwin.l = scwin.h), k[l] || (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            k[l] = {}, scwin.j || (k[l].toJSON = p.noop));
        if (typeof scwin.c == "object" || typeof scwin.c == "function") scwin.e ? k[l] = p.extend(k[l], scwin.c) : k[l].data = p.extend(k[l].data, scwin.c);
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.f = k[l], scwin.e || (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            f.data || (f.data = {}), scwin.f = f.data), scwin.d !== b && (f[p.camelCase(scwin.c)] = scwin.d), scwin.i ? (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.g = f[c], scwin.g == null && (scwin.g = f[p.camelCase(scwin.c)])) : scwin.g = scwin.f, scwin.g;
    },
    removeData: function(a, b, c) {

        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************

        if (!p.acceptData(a)) return;
        var d, e, f, g = a.nodeType,
            h = scwin.g ? p.cache__a : X,
            i = scwin.g ? a[p.expando] : p.expando;
        if (!h[i]) return;
        if (b) {
            scwin.d = scwin.c ? h[i] : h[i].data;
            if (scwin.d) {
                p.isArray(b) || (b in scwin.d ? b = [b] : (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    b = p.camelCase(b), b in scwin.d ? b = [b] : b = b.split(" ")));
                for (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.e = 0, scwin.f = b.length; scwin.e < scwin.f; scwin.e++) delete d[b[e]];
                if (!(scwin.c ? K : p.isEmptyObject)(scwin.d)) return;
            }
        }
        if (!scwin.c) {
            delete h[i].data;
            if (!K(h[i])) return;
        }
        scwin.g ? p.cleanData([a], !0) : p.support.deleteExpando || scwin.h != h.window ? delete h[i] : h[i] = null;
    },
    _data: function(a, b, c) {
        return p.data(a, b, scwin.c, !0);
    },
    acceptData: function(a) {
        var b = a.nodeName && p.noData[a.nodeName.toLowerCase()];
        return !b || b !== !0 && a.getAttribute("classid") === b;
    }
}), p.fn.extend({
    data: function(a, c) {
        var d, e, f, g, h, i = this[0],
            j = 0,
            k = null;
        if (a === b) {
            if (this.length) {
                scwin.k = p.data(scwin.i);
                if (i.nodeType === 1 && !p._data(scwin.i, "parsedAttrs")) {
                    scwin.f = i.attributes;
                    for (scwin.h = f.length; scwin.j < scwin.h; scwin.j++)
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        scwin.g = f[j].name, g.indexOf("data-") || (
                            // ********************************************************************
                            // [WebSquare 전환 추가] 원본 소스와 비교 필요
                            // 
                            // ********************************************************************
                            scwin.g = p.camelCase(g.substring(5)), J(scwin.i, scwin.g, k[g]));
                    p._data(scwin.i, "parsedAttrs", !0);
                }
            }
            return scwin.k;
        }
        return typeof a == "object" ? this.each(function() {
            p.data(this, a);
        }) : (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.d = a.split(".", 2), d[1] = d[1] ? "." + d[1] : "", scwin.e = d[1] + "!", p.access(this, function(c) {
                if (scwin.c === b) return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.k = this.triggerHandler("getData" + scwin.e, [d[0]]), scwin.k === b && scwin.i && (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.k = p.data(scwin.i, a), scwin.k = J(scwin.i, a, scwin.k)), scwin.k === b && d[1] ? this.data(d[0]) : scwin.k;
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                d[1] = scwin.c, this.each(function() {
                    var b = $c.pdc.p(this);
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    b.triggerHandler("setData" + scwin.e, scwin.d), p.data(this, a, scwin.c), b.triggerHandler("changeData" + scwin.e, scwin.d);
                });
            }, null, scwin.c, arguments.length > 1, null, !1));
    },
    removeData: function(a) {
        return this.each(function() {
            p.removeData(this, a);
        });
    }
}), p.extend({
    queue__function: function(a, b, c) {
        var d;
        if (a) return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        b = (b || "fx") + "queue", scwin.d = p._data(a, b), scwin.c && (!scwin.d || p.isArray(scwin.c) ? scwin.d = p._data(a, b, p.makeArray(scwin.c)) : d.push(scwin.c)), scwin.d || [];
    },
    dequeue__function: function(a, b) {
        b = b || "fx";
        var c = p.queue(a, b),
            d = c.length,
            e = c.shift(),
            f = p._queueHooks(a, b),
            g = function() {
                p.dequeue(a, b);
            };
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.e === "inprogress" && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.e = c.shift(), scwin.d--), scwin.e && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            b === "fx" && c.unshift("inprogress"), delete f.stop, e.call(a, scwin.g, scwin.f)), !scwin.d && scwin.f && f.empty.fire();
    },
    _queueHooks: function(a, b) {
        var c = b + "queueHooks";
        return p._data(a, scwin.c) || p._data(a, scwin.c, {
            empty: p.Callbacks("once memory").add(function() {

                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                p.removeData(a, b + "queue", !0), p.removeData(a, scwin.c, !0);
            })
        });
    }
}), p.fn.extend({
    queue__function: function(a, c) {
        var d = 2;
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        typeof a != "string" && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.c = a, a = "fx", scwin.d--), arguments.length < scwin.d ? p.queue(this[0], a) : scwin.c === b ? this : this.each(function() {
            var b = p.queue(this, a, scwin.c);
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            p._queueHooks(this, a), a === "fx" && b[0] !== "inprogress" && p.dequeue(this, a);
        });
    },
    dequeue__function: function(a) {
        return this.each(function() {
            p.dequeue(this, a);
        });
    },
    delay: function(a, b) {
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        a = p.fx ? p.fx.speeds[a] || a : a, b = b || "fx", this.queue(b, function(b, c) {
            var d = setTimeout(b, a);
            c.stop = function() {
                clearTimeout(scwin.d);
            };
        });
    },
    clearQueue__function: function(a) {
        return this.queue(a || "fx", []);
    },
    promise__function: function(a, c) {
        var d, e = 1,
            f = p.Deferred(),
            g = this,
            h = this.length,
            i = function() {
                scwin.e-- || f.resolveWith(scwin.g, [scwin.g]);
            };
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        typeof a != "string" && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.c = a, a = b), a = a || "fx";
        while (scwin.h--)
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.d = p._data(g[h], a + "queueHooks"), scwin.d && d.empty && (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.e++, d.empty.add(scwin.i));
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.i(), f.promise(scwin.c);
    }
});
L, scwin.M, scwin.N, scwin.O = /[\t\r\n]/g, scwin.P = /\r/g, scwin.Q = /^(?:button|input)$/i, scwin.R = /^(?:button|input|object|select|textarea)$/i, scwin.S = /^a(?:rea|)$/i, scwin.T = /^(?:autofocus|autoplay|async|checked|controls|defer|disabled|hidden|loop|multiple|open|readonly|required|scoped|selected)$/i, scwin.U = p.support.getSetAttribute;
// ********************************************************************
// [WebSquare 전환 추가] 원본 소스와 비교 필요
// 
// ********************************************************************
p.fn.extend({
    attr: function(a, b) {
        return p.access(this, p.attr, a, b, arguments.length > 1);
    },
    removeAttr: function(a) {
        return this.each(function() {
            p.removeAttr(this, a);
        });
    },
    prop: function(a, b) {
        return p.access(this, p.prop, a, b, arguments.length > 1);
    },
    removeProp: function(a) {
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        a = p.propFix[a] || a, this.each(function() {
            try {

                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                this[a] = b, delete this[a];
            } catch (c) {

            }
        });
    },
    addClass: function(a) {
        var b, c, d, e, f, g, h;
        if (p.isFunction(a)) return this.each(function(b) {
            $c.pdc.p(this).addClass(a.call(this, b, this.className));
        });
        if (a && typeof a == "string") {
            b = a.split(scwin.s);
            for (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.c = 0, scwin.d = this.length; scwin.c < scwin.d; scwin.c++) {
                scwin.e = this[c];
                if (e.nodeType === 1)
                    if (!e.className && b.length === 1) e.addClass(a);

                    else {
                        scwin.f = " " + e.className + " ";
                        for (
                            // ********************************************************************
                            // [WebSquare 전환 추가] 원본 소스와 비교 필요
                            // 
                            // ********************************************************************
                            scwin.g = 0, scwin.h = b.length; scwin.g < scwin.h; scwin.g++) f.indexOf(" " + b[g] + " ") < 0 && (scwin.f += b[g] + " ");
                        e.addClass($c.ut.trim(scwin.f));
                    }
            }
        }
        return this;
    },
    removeClass: function(a) {
        var c, d, e, f, g, h, i;
        if (p.isFunction(a)) return this.each(function(b) {
            $c.pdc.p(this).removeClass(a.call(this, b, this.className));
        });
        if (a && typeof a == "string" || a === b) {
            scwin.c = (a || "").split(scwin.s);
            for (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.h = 0, scwin.i = this.length; scwin.h < scwin.i; scwin.h++) {
                scwin.e = this[h];
                if (e.nodeType === 1 && e.className) {
                    scwin.d = (" " + e.className + " ").replace(scwin.O, " ");
                    for (
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        scwin.f = 0, scwin.g = c.length; scwin.f < scwin.g; scwin.f++)
                        while (d.indexOf(" " + c[f] + " ") >= 0) scwin.d = d.replace(" " + c[f] + " ", " ");
                    e.addClass(a ? $c.ut.trim(scwin.d) : "");
                }
            }
        }
        return this;
    },
    toggleClass: function(a, b) {
        var c = typeof a,
            d = typeof b == "boolean";
        return p.isFunction(a) ? this.each(function(c) {
            $c.pdc.p(this).toggleClass(a.call(this, scwin.c, this.className, b), b);
        }) : this.each(function() {
            if (scwin.c === "string") {
                var e, f = 0,
                    g = $c.pdc.p(this),
                    h = b,
                    i = a.split(scwin.s);
                while (scwin.e = i[scwin.f++])
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.h = scwin.d ? scwin.h : !g.hasClass(scwin.e), g[scwin.h ? "addClass" : "removeClass"](scwin.e);

            } else if (scwin.c === "undefined" || scwin.c === "boolean")
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                this.className && p._data(this, "__className__", this.className), this.addClass(this.className || a === !1 ? "" : p._data(this, "__className__") || "");
        });
    },
    hasClass: function(a) {
        var b = " " + a + " ",
            c = 0,
            d = this.length;
        for (; scwin.c < scwin.d; scwin.c++)
            if (this[c].nodeType === 1 && (" " + this[c].className + " ").replace(scwin.O, " ").indexOf(b) >= 0) return !0;
        return !1;
    },
    val: function(a) {
        var c, d, e, f = this[0];
        if (!arguments.length) {
            if (scwin.f) return
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.c = p.valHooks[f.type] || p.valHooks[f.nodeName.toLowerCase()], scwin.c && "get" in scwin.c && (scwin.d = c.get(scwin.f, "value")) !== b ? scwin.d : (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.d = f.getValue(), typeof scwin.d == "string" ? d.replace(P, "") : scwin.d == null ? "" : scwin.d);
            return;
        }
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.e = p.isFunction(a), this.each(function(d) {
            var f, g = $c.pdc.p(this);
            if (this.nodeType !== 1) return;
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.e ? scwin.f = a.call(this, scwin.d, g.val()) : scwin.f = a, scwin.f == null ? scwin.f = "" : typeof scwin.f == "number" ? scwin.f += "" : p.isArray(scwin.f) && (scwin.f = p.map(scwin.f, function(a) {
                return a == null ? "" : a + "";
            })), scwin.c = p.valHooks[this.type] || p.valHooks[this.nodeName.toLowerCase()];
            if (!scwin.c || !("set" in scwin.c) || c.set(this, scwin.f, "value") === b) this.setValue(scwin.f);
        });
    }
}), p.extend({
    valHooks: {
        option: {
            get: function(a) {

                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************

                var b = a.attributes.getValue();
                return !b || b.specified ? a.value__a.getText() : X;
            }
        },
        select: {
            get: function(a) {
                var b, c, d, e, f = a.selectedIndex,
                    g = [],
                    h = a.options,
                    i = a.type === "select-one";
                if (scwin.f < 0) return null;
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.c = scwin.i ? scwin.f : 0, scwin.d = scwin.i ? scwin.f + 1 : h.length;
                for (; scwin.c < scwin.d; scwin.c++) {
                    scwin.e = h[c];
                    if (e.selected && (p.support.optDisabled ? !e.getDisabled() : e.getAttribute("disabled") === null) && (!e.parentNode.getDisabled() || !p.nodeName(e.parentNode, "optgroup"))) {
                        b = $c.pdc.p(scwin.e).val();
                        if (scwin.i) return b;
                        g.push(b);
                    }
                }
                return scwin.i && !g.length && h.length ? $c.pdc.p(h[f]).val() : scwin.g;
            },
            set: function(a, b) {
                var c = p.makeArray(b);
                return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                $c.pdc.p(a).find("option").each(function() {
                    this.selected = p.inArray($c.pdc.p(this).val(), scwin.c) >= 0;
                }), c.length || (a.selectedIndex = -1), scwin.c;
            }
        }
    },
    attrFn: {},
    attr: function(a, c, d, e) {
        var f, g, h, i = a.nodeType;
        if (!a || scwin.i === 3 || scwin.i === 8 || scwin.i === 2) return;
        if (scwin.e && p.isFunction(p.fn[c])) return $c.pdc.p(a)[c](scwin.d);
        if (typeof a.getAttribute == "undefined") return p.prop(a, scwin.c, scwin.d);
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.h = scwin.i !== 1 || !p.isXMLDoc(a), scwin.h && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.c = c.toLowerCase(), scwin.g = p.attrHooks[c] || (T.test(scwin.c) ? scwin.M : scwin.L));
        if (scwin.d !== b) {
            if (scwin.d === null) {
                p.removeAttr(a, scwin.c);
                return;
            }
            return scwin.g && "set" in scwin.g && scwin.h && (scwin.f = g.set(a, scwin.d, scwin.c)) !== b ? scwin.f : (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                a.setAttribute(scwin.c, scwin.d + ""), scwin.d);
        }
        return scwin.g && "get" in scwin.g && scwin.h && (scwin.f = g.get(a, scwin.c)) !== null ? scwin.f : (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.f = a.getAttribute(scwin.c), scwin.f === null ? b : scwin.f);
    },
    removeAttr: function(a, b) {
        var c, d, e, f, g = 0;
        if (b && a.nodeType === 1) {
            scwin.d = b.split(scwin.s);
            for (; scwin.g < d.length; scwin.g++)
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.e = d[g], scwin.e && (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************

                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************

                    scwin.c = p.propFix[e] || scwin.e, scwin.f = T.test(scwin.e), scwin.f || p.attr(a, scwin.e, ""), a.removeAttribute(scwin.U ? e__c : X), scwin.f && scwin.c in a && (a[c] = !1));
        }
    },
    attrHooks: {
        type: {
            set: function(a, b) {
                if (Q.test(a.nodeName) && a.parentNode) p.error("type property can't be changed");

                else if (!p.support.radioValue && b === "radio" && p.nodeName(a, "input")) {
                    var c = a.getValue();
                    return
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    a.setAttribute("type", b), scwin.c && (a.setValue(scwin.c)), b;
                }
            }
        },
        value: {
            get: function(a, b) {

                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************

                return scwin.L && p.nodeName(a, "button") ? L.get(a, b) : b in a ? a.value__null : X;
            },
            set: function(a, b, c) {
                if (scwin.L && p.nodeName(a, "button")) return L.set(a, b, scwin.c);
                a.setValue(b);
            }
        }
    },
    propFix: {
        tabindex: "tabIndex",
        readonly: "readOnly",
        "for": "htmlFor",
        "class": "className",
        maxlength: "maxLength",
        cellspacing: "cellSpacing",
        cellpadding: "cellPadding",
        rowspan: "rowSpan",
        colspan: "colSpan",
        usemap: "useMap",
        frameborder: "frameBorder",
        contenteditable: "contentEditable"
    },
    prop: function(a, c, d) {
        var e, f, g, h = a.nodeType;
        if (!a || scwin.h === 3 || scwin.h === 8 || scwin.h === 2) return;
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.g = scwin.h !== 1 || !p.isXMLDoc(a), scwin.g && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************

            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************

            scwin.c = p.propFix[c] || scwin.c, scwin.f = p.propHooks[c]), scwin.d !== b ? scwin.f && "set" in scwin.f && (scwin.e = f.set(a, scwin.d, scwin.c)) !== b ? e__a[c] = scwin.d : scwin.f && "get" in scwin.f && (scwin.e = f.get(a, scwin.c)) !== null ? e__a[c] : X : X;
    },
    propHooks: {
        tabIndex: {
            get: function(a) {
                var c = a.getAttributeNode("tabindex");
                return scwin.c && c.specified ? parseInt(c.getValue(), 10) : R.test(a.nodeName) || S.test(a.nodeName) && a.href ? 0 : b;
            }
        }
    }
}), scwin.M = {
    get: function(a, c) {
        var d, e = p.prop(a, scwin.c);
        return scwin.e === !0 || typeof scwin.e != "boolean" && (scwin.d = a.getAttributeNode(scwin.c)) && d.nodeValue !== !1 ? c.toLowerCase() : b;
    },
    set: function(a, b, c) {
        var d;
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        b === !1 ? p.removeAttr(a, scwin.c) : (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.d = p.propFix[c] || scwin.c, scwin.d in a && (a[d] = !0), a.setAttribute(scwin.c, c.toLowerCase())), scwin.c;
    }
}, scwin.U || (
    // ********************************************************************
    // [WebSquare 전환 추가] 원본 소스와 비교 필요
    // 
    // ********************************************************************
    scwin.N = {
        name: !0,
        id: !0,
        coords: !0
    }, scwin.L = p.valHooks.button = {
        get: function(a, c) {
            var d;
            return
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************

            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************

            scwin.d = a.getAttributeNode(scwin.c), scwin.d && (N[c] ? d.getValue() !== "" : d.specified) ? d.value__b : X;
        },
        set: function(a, b, c) {
            var d = a.getAttributeNode(scwin.c);
            return
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.d || (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.d = e.createAttribute(scwin.c), a.setAttributeNode(scwin.d)), d.setValue(b + "");
        }
    }, p.each(["width", "height"], function(a, b) {
        p.attrHooks[b] = p.extend(p.attrHooks[b], {
            set: function(a, c) {
                if (scwin.c === "") return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                a.setAttribute(b, "auto"), scwin.c;
            }
        });
    }), p.attrHooks.contenteditable = {
        get: L.get,
        set: function(a, b, c) {

            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            b === "" && (b = "false"), L.set(a, b, scwin.c);
        }
    }), p.support.hrefNormalized || p.each(["href", "src", "width", "height"], function(a, c) {
    p.attrHooks[c] = p.extend(p.attrHooks[c], {
        get: function(a) {
            var d = a.getAttribute(scwin.c, 2);
            return scwin.d === null ? b : scwin.d;
        }
    });
}), p.support.style || (p.attrHooks.style = {
    get: function(a) {
        return a.style.cssText.toLowerCase() || b;
    },
    set: function(a, b) {
        return a.style.cssText = b + "";
    }
}), p.support.optSelected || (p.propHooks.selected = p.extend(p.propHooks.selected, {
    get: function(a) {
        var b = a.parentNode;
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        b && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            b.selectedIndex, b.parentNode && b.parentNode.selectedIndex), null;
    }
})), p.support.enctype || (p.propFix.enctype = "encoding"), p.support.checkOn || p.each(["radio", "checkbox"], function() {
    p.valHooks[this] = {
        get: function(a) {
            return a.getAttribute("value") === null ? "on" : a.getValue();
        }
    };
}), p.each(["radio", "checkbox"], function() {
    p.valHooks[this] = p.extend(p.valHooks[this], {
        set: function(a, b) {
            if (p.isArray(b)) return a.setValue(p.inArray($c.pdc.p(a).val(), b) >= 0);
        }
    });
});
V = /^(?:textarea|input|select)$/i, scwin.W = /^([^\.]*|)(?:\.(.+)|)$/, scwin.X = /(?:^|\s)hover(\.\S+|)\b/, scwin.Y = /^key/, scwin.Z = /^(?:mouse|contextmenu)|click/, scwin.$ = /^(?:focusinfocus|focusoutblur)$/, scwin._ = function(a) {
    return p.event.special.hover ? a : a.replace(scwin.X, "mouseenter$1 mouseleave$1");
};
// ********************************************************************
// [WebSquare 전환 추가] 원본 소스와 비교 필요
// 
// ********************************************************************
p.event = {
        add: function(a, c, d, e, f) {
            var g, h, i, j, k, l, m, n, o, q, r;
            if (a.nodeType === 3 || a.nodeType === 8 || !scwin.c || !scwin.d || !(scwin.g = p._data(a))) return;
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            d.handler && (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.o = scwin.d, scwin.d = o.handler, scwin.f = o.selector), d.guid || (d.guid = p.guid++), scwin.i = g.events, scwin.i || (g.events = scwin.i = {}), scwin.h = g.handle, scwin.h || (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                g.handle = scwin.h = function(a) {
                    return typeof p != "undefined" && (!a || p.event.triggered !== a.type) ? p.event.dispatch.apply(h.elem, arguments) : b;
                }, h.elem = a), scwin.c = $c.ut.trim(scwin._(scwin.c)).split(" ");
            for (scwin.j = 0; scwin.j < c.length; scwin.j++) {

                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************

                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************

                scwin.k = W.exec(c[j]) || [], scwin.l = k[1], scwin.m = (k[2] || "").split(".").sort(), scwin.r = p.event.special[l] || {}, scwin.l = (scwin.f ? r.delegateType__r.bindType : X) || scwin.l, scwin.r = p.event.special[l] || {}, scwin.n = p.extend({
                    type__l: type__l,
                    origType__k: origType__k,
                    1: 1,
                    data: scwin.e,
                    handler: scwin.d,
                    guid: d.guid,
                    selector: scwin.f,
                    needsContext: scwin.f && p.expr.match.needsContext.test(scwin.f),
                    namespace__m: namespace__m
                }, scwin.o), scwin.q = i[l];
                if (!scwin.q) {

                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.q = i[l] = [], q.delegateCount = 0;
                    if (!r.setup || r.setup.call(a, scwin.e, scwin.m, scwin.h) === !1) a.addEventListener ? a.addEventListener(scwin.l, scwin.h, !1) : a.attachEvent && a.attachEvent("on" + scwin.l, scwin.h);
                }
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                r.add && (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    r.add.call(a, scwin.n), n.handler.guid || (n.handler.guid = d.guid)), scwin.f ? q.splice(q.delegateCount++, 0, scwin.n) : q.push(scwin.n), p.event.global[l] = !0;
            }
            a = null;
        },
        global: {},
        remove__function: function(a, b, c, d, e) {
            var f, g, h, i, j, k, l, m, n, o, q, r = p.hasData(a) && p._data(a);
            if (!scwin.r || !(scwin.m = r.events)) return;
            b = $c.ut.trim(scwin._(b || "")).split(" ");
            for (scwin.f = 0; scwin.f < b.length; scwin.f++) {

                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.g = W.exec(b[f]) || [], scwin.h = scwin.i = g[1], scwin.j = g[2];
                if (!scwin.h) {
                    for (scwin.h in scwin.m) p.event.remove(a, scwin.h + b[f], scwin.c, scwin.d, !0);
                    continue;
                }
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************

                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************

                scwin.n = p.event.special[h] || {}, scwin.h = (scwin.d ? n.delegateType__n.bindType : X) || scwin.h, scwin.o = m[h] || [], scwin.k = o.length, scwin.j = scwin.j ? new RegExp("(^|\\.)" + j.split(".").sort().join("\\.(?:.*\\.|)") + "(\\.|$)") : null;
                for (scwin.l = 0; scwin.l < o.length; scwin.l++)
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.q = o[l], (scwin.e || scwin.i === q.origType) && (!scwin.c || c.guid === q.guid) && (!scwin.j || j.test(q.namespace)) && (!scwin.d || scwin.d === q.selector || scwin.d === "**" && q.selector) && (
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        o.splice(scwin.l--, 1), q.selector && o.delegateCount--, n.remove && n.remove.call(a, scwin.q));
                o.length === 0 && scwin.k !== o.length && (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    (!n.teardown || n.teardown.call(a, scwin.j, r.handle) === !1) && p.removeEvent(a, scwin.h, r.handle), delete m[h]);
            }
            p.isEmptyObject(scwin.m) && (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                delete r.handle, p.removeData(a, "events", !0));
        },
        customEvent: {
            getData: !0,
            setData: !0,
            changeData: !0
        },
        trigger: function(c, d, f, g) {
            if (!scwin.f || f.nodeType !== 3 && f.nodeType !== 8) {
                var h, i, j, k, l, m, n, o, q, r, s = c.type || scwin.c,
                    t = [];
                if ($.test(scwin.s + p.event.triggered)) return;
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                s.indexOf("!") >= 0 && (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.s = s.slice(0, -1), scwin.i = !0), s.indexOf(".") >= 0 && (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.t = s.split("."), scwin.s = t.shift(), t.sort());
                if ((!scwin.f || p.event.customEvent[s]) && !p.event.global[s]) return;
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.c = typeof scwin.c == "object" ? c[p.expando] ? scwin.c : new p.Event(scwin.s, scwin.c) : new p.Event(scwin.s), c.type = scwin.s, c.isTrigger = !0, c.exclusive = scwin.i, c.namespace = t.join("."), c.namespace_re = c.namespace ? new RegExp("(^|\\.)" + t.join("\\.(?:.*\\.|)") + "(\\.|$)") : null, scwin.m = s.indexOf(":") < 0 ? "on" + scwin.s : "";
                if (!scwin.f) {
                    scwin.h = p.cache;
                    for (scwin.j in scwin.h) h[j].events && h[j].events[s] && p.event.trigger(scwin.c, scwin.d, h[j].handle.elem, !0);
                    return;
                }
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                c.result = b, c.target || (c.target = scwin.f), scwin.d = scwin.d != null ? p.makeArray(scwin.d) : [], d.unshift(scwin.c), scwin.n = p.event.special[s] || {};
                if (n.trigger && n.trigger.apply(scwin.f, scwin.d) === !1) return;
                scwin.q = [
                    [scwin.f, n.bindType || scwin.s]
                ];
                if (!scwin.g && !n.noBubble && !p.isWindow(scwin.f)) {

                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.r = n.delegateType || scwin.s, scwin.k = $.test(scwin.r + scwin.s) ? scwin.f : f.parentNode;
                    for (scwin.l = scwin.f; scwin.k; scwin.k = k.parentNode)
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        q.push([scwin.k, scwin.r]), scwin.l = scwin.k;
                    scwin.l === (f.ownerDocument || scwin.e) && q.push([l.defaultView || l.parentWindow || a, scwin.r]);
                }
                for (scwin.j = 0; scwin.j < q.length && !c.isPropagationStopped(); scwin.j++)
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.k = q[j][0], c.type = q[j][1], scwin.o = (p._data(scwin.k, "events") || {})[c.type] && p._data(scwin.k, "handle"), scwin.o && o.apply(scwin.k, scwin.d), scwin.o = scwin.m && k[m], scwin.o && p.acceptData(scwin.k) && o.apply && o.apply(scwin.k, scwin.d) === !1 && c.preventDefault();
                return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                c.type = scwin.s, !scwin.g && !c.isDefaultPrevented() && (!n._default || n._default.apply(f.ownerDocument, scwin.d) === !1) && (scwin.s !== "click" || !p.nodeName(scwin.f, "a")) && p.acceptData(scwin.f) && scwin.m && f[s] && (scwin.s !== "focus" && scwin.s !== "blur" || c.target.offsetWidth !== 0) && !p.isWindow(scwin.f) && (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.l = f[m], scwin.l && (f[m] = null), p.event.triggered = scwin.s, f[s](), p.event.triggered = b, scwin.l && (f[m] = scwin.l)), c.result;
            }
            return;
        },
        dispatch: function(c) {
            scwin.c = p.event.fix(scwin.c || a.event);
            var d, e, f, g, h, i, j, l, m, n, o = (p._data(this, "events") || {})[c.type] || [],
                q = o.delegateCount,
                r = k.call(arguments),
                s = !c.exclusive && !c.namespace,
                t = p.event.special[c.type] || {},
                u = [];
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            r[0] = scwin.c, c.delegateTarget = this;
            if (t.preDispatch && t.preDispatch.call(this, scwin.c) === !1) return;
            if (scwin.q && (!c.button || c.type !== "click"))
                for (scwin.f = c.target; scwin.f != this; scwin.f = f.parentNode || this)
                    if (f.getDisabled() !== !0 || c.type !== "click") {

                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        scwin.h = {}, scwin.j = [];
                        for (scwin.d = 0; scwin.d < scwin.q; scwin.d++)
                            // ********************************************************************
                            // [WebSquare 전환 추가] 원본 소스와 비교 필요
                            // 
                            // ********************************************************************
                            scwin.l = o[d], scwin.m = l.selector, h[m] === b && (h[m] = l.needsContext ? $c.pdc.p(scwin.m, this).index(scwin.f) >= 0 : p.find(scwin.m, this, null, [scwin.f]).length), h[m] && j.push(scwin.l);
                        j.length && u.push({
                            elem: scwin.f,
                            matches: scwin.j
                        });
                    } o.length > scwin.q && u.push({
                elem: this,
                matches: o.slice(scwin.q)
            });
            for (scwin.d = 0; scwin.d < u.length && !c.isPropagationStopped(); scwin.d++) {

                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.i = u[d], c.currentTarget = i.elem;
                for (scwin.e = 0; scwin.e < i.matches.length && !c.isImmediatePropagationStopped(); scwin.e++) {
                    scwin.l = i.matches[e];
                    if (scwin.s || !c.namespace && !l.namespace || c.namespace_re && c.namespace_re.test(l.namespace))
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        c.data = l.data, c.handleObj = scwin.l, scwin.g = ((p.event.special[l.origType] || {}).handle || l.handler).apply(i.elem, scwin.r), scwin.g !== b && (
                            // ********************************************************************
                            // [WebSquare 전환 추가] 원본 소스와 비교 필요
                            // 
                            // ********************************************************************
                            c.result = scwin.g, scwin.g === !1 && (
                                // ********************************************************************
                                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                                // 
                                // ********************************************************************
                                c.preventDefault(), c.stopPropagation()));
                }
            }
            return
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            t.postDispatch && t.postDispatch.call(this, scwin.c), c.result;
        },
        props: "attrChange attrName relatedNode srcElement altKey bubbles cancelable ctrlKey currentTarget eventPhase metaKey relatedTarget shiftKey target timeStamp view which".split(" "),
        fixHooks: {},
        keyHooks: {
            props: "char charCode key keyCode".split(" "),
            filter: function(a, b) {
                return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************

                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************

                a.which == null && (a.which = b.charCode != null ? b.charCode__b.keyCode : X), a;
            }
        },
        mouseHooks: {
            props: "button buttons clientX clientY fromElement offsetX offsetY pageX pageY screenX screenY toElement".split(" "),
            filter: function(a, c) {
                var d, f, g, h = c.button,
                    i = c.fromElement;
                return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                a.pageX == null && c.clientX != null && (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.d = a.target.ownerDocument || scwin.e, scwin.f = d.documentElement, scwin.g = d.body, a.pageX = c.clientX + (scwin.f && f.scrollLeft || scwin.g && g.scrollLeft || 0) - (scwin.f && f.clientLeft || scwin.g && g.clientLeft || 0), a.pageY = c.clientY + (scwin.f && f.scrollTop || scwin.g && g.scrollTop || 0) - (scwin.f && f.clientTop || scwin.g && g.clientTop || 0)), !a.relatedTarget && scwin.i && (a.relatedTarget = scwin.i === a.target ? c.toElement : scwin.i), !a.which && scwin.h !== b && (a.which = scwin.h & 1 ? 1 : scwin.h & 2 ? 3 : scwin.h & 4 ? 2 : 0), a;
            }
        },
        fix: function(a) {
            if (a[p.expando]) return a;
            var b, c, d = a,
                f = p.event.fixHooks[a.type] || {},
                g = f.props ? this.props.concat(f.props) : this.props;
            a = p.Event(scwin.d);
            for (b = g.length; b;)
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.c = g[b--], a[c] = d[c];
            return
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            a.target || (a.target = d.srcElement || scwin.e), a.target.nodeType === 3 && (a.target = a.target.parentNode), a.metaKey = a.metaKey, f.filter ? f.filter(a, scwin.d) : a;
        },
        special: {
            load: {
                noBubble: !0
            },
            focus: {
                delegateType: "focusin"
            },
            blur: {
                delegateType: "focusout"
            },
            beforeunload: {
                setup: function(a, b, c) {
                    p.isWindow(this) && (this.onbeforeunload = scwin.c);
                },
                teardown: function(a, b) {
                    this.onbeforeunload === b && (this.onbeforeunload = null);
                }
            }
        },
        simulate__function: function(a, b, c, d) {
            var e = p.extend(new p.Event(), scwin.c, {
                type__a: type__a,
                isSimulated: !0,
                originalEvent: {}
            });
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.d ? p.event.trigger(scwin.e, null, b) : p.event.dispatch.call(b, scwin.e), e.isDefaultPrevented() && c.preventDefault();
        }
    }, p.event.handle = p.event.dispatch, p.removeEvent = e.removeEventListener ? function(a, b, c) {
        a.removeEventListener && a.removeEventListener(b, scwin.c, !1);
    } : function(a, b, c) {
        var d = "on" + b;
        a.detachEvent && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            typeof a[d] == "undefined" && (a[d] = null), a.detachEvent(scwin.d, scwin.c));
    }, p.Event = function(a, b) {
        if (this instanceof p.Event)
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            a && a.type ? (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                this.originalEvent = a, this.type = a.type, this.isDefaultPrevented = a.defaultPrevented || a.returnValue === !1 || a.getPreventDefault && a.getPreventDefault() ? bb : ba) : this.type = a, b && p.extend(this, b), this.timeStamp = a && a.timeStamp || p.now(), this[p.expando] = !0;

        else return new p.Event(a, b);
    }, p.Event.prototype = {
        preventDefault: function() {
            this.isDefaultPrevented = bb;
            var a = this.originalEvent;
            if (!a) return;
            a.preventDefault ? a.preventDefault() : a.returnValue = !1;
        },
        stopPropagation: function() {
            this.isPropagationStopped = bb;
            var a = this.originalEvent;
            if (!a) return;
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            a.stopPropagation && a.stopPropagation(), a.cancelBubble = !0;
        },
        stopImmediatePropagation: function() {

            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            this.isImmediatePropagationStopped = bb, this.stopPropagation();
        },
        isDefaultPrevented: ba,
        isPropagationStopped: ba,
        isImmediatePropagationStopped: ba
    }, p.each({
        mouseenter: "mouseover",
        mouseleave: "mouseout"
    }, function(a, b) {
        p.event.special[a] = {
            delegateType__b: delegateType__b,
            bindType__b: bindType__b,
            handle__function: function(a) {
                var c, d = this,
                    e = a.relatedTarget,
                    f = a.handleObj,
                    g = f.selector;
                if (!scwin.e || scwin.e !== scwin.d && !p.contains(scwin.d, scwin.e))
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    a.type = f.origType, scwin.c = f.handler.apply(this, arguments), a.type = b;
                return scwin.c;
            }
        };
    }), p.support.submitBubbles || (p.event.special.submit = {
        setup: function() {
            if (p.nodeName(this, "form")) return !1;
            p.event.add(this, "click._submit keypress._submit", function(a) {
                var c = a.target,
                    d = p.nodeName(scwin.c, "input") || p.nodeName(scwin.c, "button") ? c.form : b;
                scwin.d && !p._data(scwin.d, "_submit_attached") && (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    p.event.add(scwin.d, "submit._submit", function(a) {
                        a._submit_bubble = !0;
                    }), p._data(scwin.d, "_submit_attached", !0));
            });
        },
        postDispatch: function(a) {
            a._submit_bubble && (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                delete a._submit_bubble, this.parentNode && !a.isTrigger && p.event.simulate("submit", this.parentNode, a, !0));
        },
        teardown: function() {
            if (p.nodeName(this, "form")) return !1;
            p.event.remove(this, "._submit");
        }
    }), p.support.changeBubbles || (p.event.special.change = {
        setup: function() {
            if (V.test(this.nodeName)) {
                if (this.type === "checkbox" || this.type === "radio")
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    p.event.add(this, "propertychange._change", function(a) {
                        a.originalEvent.propertyName === "checked" && (this._just_changed = !0);
                    }), p.event.add(this, "click._change", function(a) {

                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        this._just_changed && !a.isTrigger && (this._just_changed = !1), p.event.simulate("change", this, a, !0);
                    });
                return !1;
            }
            p.event.add(this, "beforeactivate._change", function(a) {
                var b = a.target;
                V.test(b.nodeName) && !p._data(b, "_change_attached") && (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    p.event.add(b, "change._change", function(a) {
                        this.parentNode && !a.isSimulated && !a.isTrigger && p.event.simulate("change", this.parentNode, a, !0);
                    }), p._data(b, "_change_attached", !0));
            });
        },
        handle__function: function(a) {
            var b = a.target;
            if (this !== b || a.isSimulated || a.isTrigger || b.type !== "radio" && b.type !== "checkbox") return a.handleObj.handler.apply(this, arguments);
        },
        teardown: function() {
            return
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            p.event.remove(this, "._change"), !V.test(this.nodeName);
        }
    }), p.support.focusinBubbles || p.each({
        focus: "focusin",
        blur: "focusout"
    }, function(a, b) {
        var c = 0,
            d = function(a) {
                p.event.simulate(b, a.target, p.event.fix(a), !0);
            };
        p.event.special[b] = {
            setup: function() {
                scwin.c++ === 0 && e.addEventListener(a, scwin.d, !0);
            },
            teardown: function() {
                scwin.c-- === 0 && e.removeEventListener(a, scwin.d, !0);
            }
        };
    }), p.fn.extend({
        on: function(a, c, d, e, f) {
            var g, h;
            if (typeof a == "object") {
                typeof scwin.c != "string" && (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.d = scwin.d || scwin.c, scwin.c = b);
                for (scwin.h in a) this.on(scwin.h, scwin.c, scwin.d, a[h], scwin.f);
                return this;
            }
            scwin.d == null && scwin.e == null ? (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.e = scwin.c, scwin.d = scwin.c = b) : scwin.e == null && (typeof scwin.c == "string" ? (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.e = scwin.d, scwin.d = b) : (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.e = scwin.d, scwin.d = scwin.c, scwin.c = b));
            if (scwin.e === !1) scwin.e = ba;

            else if (!scwin.e) return this;
            return
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.f === 1 && (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.g = scwin.e, scwin.e = function(a) {
                    return
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    $c.pdc.p().off(a), g.apply(this, arguments);
                }, e.guid = g.guid || (g.guid = p.guid++)), this.each(function() {
                p.event.add(this, a, scwin.e, scwin.d, scwin.c);
            });
        },
        one__function: function(a, b, c, d) {
            return this.on(a, b, scwin.c, scwin.d, 1);
        },
        off: function(a, c, d) {
            var e, f;
            if (a && a.preventDefault && a.handleObj) return
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************

            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************

            scwin.e = a.handleObj, $c.pdc.p(a.delegateTarget).off(e.namespace ? e.origType + "." + e.namespace__e.origType : X, e.selector, e.handler), this;
            if (typeof a == "object") {
                for (scwin.f in a) this.off(scwin.f, scwin.c, a[f]);
                return this;
            }
            if (scwin.c === !1 || typeof scwin.c == "function")
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.d = scwin.c, scwin.c = b;
            return
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.d === !1 && (scwin.d = ba), this.each(function() {
                p.event.remove(this, a, scwin.d, scwin.c);
            });
        },
        bind: function(a, b, c) {
            return this.on(a, null, b, scwin.c);
        },
        unbind: function(a, b) {
            return this.off(a, null, b);
        },
        live__function: function(a, b, c) {
            return
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            $c.pdc.p(this.context).on(a, this.selector, b, scwin.c), this;
        },
        die__function: function(a, b) {
            return
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            $c.pdc.p(this.context).off(a, this.selector || "**", b), this;
        },
        delegate__function: function(a, b, c, d) {
            return this.on(b, a, scwin.c, scwin.d);
        },
        undelegate__function: function(a, b, c) {
            return arguments.length === 1 ? this.off(a, "**") : this.off(b, a || "**", scwin.c);
        },
        trigger: function(a, b) {
            return this.each(function() {
                p.event.trigger(a, b, this);
            });
        },
        triggerHandler: function(a, b) {
            if (this[0]) return p.event.trigger(a, b, this[0], !0);
        },
        toggle__function: function(a) {
            var b = arguments,
                c = a.guid || p.guid++,
                d = 0,
                e = function(c) {
                    var e = (p._data(this, "lastToggle" + a.guid) || 0) % scwin.d;
                    return
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    p._data(this, "lastToggle" + a.guid, scwin.e + 1), c.preventDefault(), b[e].apply(this, arguments) || !1;
                };
            e.guid = scwin.c;
            while (scwin.d < b.length) b[scwin.d++].guid = scwin.c;
            return this.click(scwin.e);
        },
        hover: function(a, b) {
            return this.mouseenter(a).mouseleave(b || a);
        }
    }), p.each("blur focus focusin focusout load resize scroll unload click dblclick mousedown mouseup mousemove mouseover mouseout mouseenter mouseleave change select submit keydown keypress keyup error contextmenu".split(" "), function(a, b) {

        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        p.fn[b] = function(a, c) {
            return
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.c == null && (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.c = a, a = null), arguments.length > 0 ? this.on(b, null, a, scwin.c) : this.trigger(b);
        }, Y.test(b) && (p.event.fixHooks[b] = p.event.keyHooks), Z.test(b) && (p.event.fixHooks[b] = p.event.mouseHooks);
    }),
    function(a, b) {
        /**
         * @method
         * @name bc
         * @description desc
         * @param {string} a desc
         * @param {string} b desc
         * @param {string} c desc
         * @param {string} d desc
         * @returns
         * @hidden N
         * @exception
         * @example
         */
        function bc(a, b, c, d) {

            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.c = scwin.c || [], b = b || scwin.r;
            var e, f, i, j, k = b.nodeType;
            if (!a || typeof a != "string") return scwin.c;
            if (scwin.k !== 1 && scwin.k !== 9) return [];
            scwin.i = scwin.g(b);
            if (!scwin.i && !scwin.d)
                if (scwin.e = P.exec(a))
                    if (scwin.j = e[1]) {
                        if (scwin.k === 9) {
                            scwin.f = b.getElementById(scwin.j);
                            if (!scwin.f || !f.parentNode) return scwin.c;
                            if (f.id === scwin.j) return
                            // ********************************************************************
                            // [WebSquare 전환 추가] 원본 소스와 비교 필요
                            // 
                            // ********************************************************************
                            c.push(scwin.f), scwin.c;
                        } else if (b.ownerDocument && (scwin.f = b.ownerDocument.getElementById(scwin.j)) && scwin.h(b, scwin.f) && f.id === scwin.j) return
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        c.push(scwin.f), scwin.c;
                    }

            else {
                if (e[2]) return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                w.apply(scwin.c, x.call(b.getElementsByTagName(a), 0)), scwin.c;
                if ((scwin.j = e[3]) && scwin._ && b.getElementsByClassName) return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                w.apply(scwin.c, x.call(b.getElementsByClassName(scwin.j), 0)), scwin.c;
            }
            return bp(a.replace(scwin.L, "$1"), b, scwin.c, scwin.d, scwin.i);
        };
        /**
         * @method
         * @name bd
         * @description desc
         * @param {string} a desc
         * @returns
         * @hidden N
         * @exception
         * @example
         */
        function bd(a) {
            return function(b) {
                var c = b.nodeName.toLowerCase();
                return scwin.c === "input" && b.type === a;
            };
        };
        /**
         * @method
         * @name be
         * @description desc
         * @param {string} a desc
         * @returns
         * @hidden N
         * @exception
         * @example
         */
        function be(a) {
            return function(b) {
                var c = b.nodeName.toLowerCase();
                return (scwin.c === "input" || scwin.c === "button") && b.type === a;
            };
        };
        /**
         * @method
         * @name bf
         * @description desc
         * @param {string} a desc
         * @returns
         * @hidden N
         * @exception
         * @example
         */
        function bf(a) {
            return scwin.z(function(b) {
                return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                b = +b, scwin.z(function(c, d) {
                    var e, f = a([], c.length, b),
                        g = f.length;
                    while (scwin.g--) c[e = f[g]] && (c[e] = !(d[e] = c[e]));

                });
            });
        };
        /**
         * @method
         * @name bg
         * @description desc
         * @param {string} a desc
         * @param {string} b desc
         * @param {string} c desc
         * @returns
         * @hidden N
         * @exception
         * @example
         */
        function bg(a, b, c) {
            if (a === b) return scwin.c;
            var d = a.nextSibling;
            while (scwin.d) {
                if (scwin.d === b) return -1;
                scwin.d = d.nextSibling;
            }
            return 1;
        };
        /**
         * @method
         * @name bh
         * @description desc
         * @param {string} a desc
         * @param {string} b desc
         * @returns
         * @hidden N
         * @exception
         * @example
         */
        function bh(a, b) {
            var c, d, f, g, h, i, j, k = C[o][a];
            if (scwin.k) return b ? 0 : k.slice(0);
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.h = a, scwin.i = [], scwin.j = e.preFilter;
            while (scwin.h) {
                if (!scwin.c || (scwin.d = M.exec(scwin.h)))
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.d && (scwin.h = h.slice(d[0].length)), i.push(scwin.f = []);
                scwin.c = !1;
                if (scwin.d = N.exec(scwin.h))
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    f.push(scwin.c = new q(d.shift())), scwin.h = h.slice(c.length), c.type = d[0].replace(scwin.L, " ");
                for (scwin.g in e.filter)(scwin.d = W[g].exec(scwin.h)) && (!j[g] || (scwin.d = j[g](scwin.d, scwin.r, !0))) && (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    f.push(scwin.c = new q(d.shift())), scwin.h = h.slice(c.length), c.type = scwin.g, c.matches = scwin.d);
                if (!scwin.c) break;
            }
            return b ? h.length : scwin.h ? bc.error(a) : scwin.C(a, scwin.i).slice(0);
        };
        /**
         * @method
         * @name bi
         * @description desc
         * @param {string} a desc
         * @param {string} b desc
         * @param {string} d desc
         * @returns
         * @hidden N
         * @exception
         * @example
         */
        function bi(a, b, d) {
            var e = b.dir,
                f = scwin.d && b.dir === "parentNode",
                g = scwin.u++;
            return b.first ? function(b, c, d) {
                while (b = b[e])
                    if (scwin.f || b.nodeType === 1) return a(b, scwin.c, scwin.d);

            } : function(b, d, h) {
                if (!scwin.h) {
                    var i, j = scwin.t + " " + scwin.g + " ",
                        k = scwin.j + scwin.c;
                    while (b = b[e])
                        if (scwin.f || b.nodeType === 1) {
                            if ((scwin.i = b[o]) === scwin.k) return b.sizset;
                            if (typeof scwin.i == "string" && i.indexOf(scwin.j) === 0) {
                                if (b.sizset) return b;
                            } else {
                                b[o] = scwin.k;
                                if (a(b, scwin.d, scwin.h)) return
                                // ********************************************************************
                                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                                // 
                                // ********************************************************************
                                b.sizset = !0, b;
                                b.sizset = !1;
                            }
                        }

                } else
                    while (b = b[e])
                        if (scwin.f || b.nodeType === 1)
                            if (a(b, scwin.d, scwin.h)) return b;

            };
        };
        /**
         * @method
         * @name bj
         * @description desc
         * @param {string} a desc
         * @returns
         * @hidden N
         * @exception
         * @example
         */
        function bj(a) {
            return a.length > 1 ? function(b, c, d) {
                var e = a.length;
                while (scwin.e--)
                    if (!a[e](b, scwin.c, scwin.d)) return !1;
                return !0;
            } : a[0];
        };
        /**
         * @method
         * @name bk
         * @description desc
         * @param {string} a desc
         * @param {string} b desc
         * @param {string} c desc
         * @param {string} d desc
         * @param {string} e desc
         * @returns
         * @hidden N
         * @exception
         * @example
         */
        function bk(a, b, c, d, e) {
            var f, g = [],
                h = 0,
                i = a.length,
                j = b != null;
            for (; scwin.h < scwin.i; scwin.h++)
                if (scwin.f = a[h])
                    if (!scwin.c || scwin.c(scwin.f, scwin.d, scwin.e))
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        g.push(scwin.f), scwin.j && b.push(scwin.h);
            return scwin.g;
        };
        /**
         * @method
         * @name bl
         * @description desc
         * @param {string} a desc
         * @param {string} b desc
         * @param {string} c desc
         * @param {string} d desc
         * @param {string} e desc
         * @param {string} f desc
         * @returns
         * @hidden N
         * @exception
         * @example
         */
        function bl(a, b, c, d, e, f) {
            return
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.d && !d[o] && (scwin.d = scwin.bl(scwin.d)), scwin.e && !e[o] && (scwin.e = scwin.bl(scwin.e, scwin.f)), scwin.z(function(f, g, h, i) {
                if (scwin.f && scwin.e) return;
                var j, k, l, m = [],
                    n = [],
                    o = g.length,
                    p = scwin.f || bo(b || "*", h.nodeType ? [scwin.h] : scwin.h, [], scwin.f),
                    q = a && (scwin.f || !b) ? bk(p, scwin.m, a, scwin.h, scwin.i) : p,
                    r = scwin.c ? scwin.e || (scwin.f ? a : scwin.o || scwin.d) ? [] : scwin.g : scwin.q;
                scwin.c && scwin.c(scwin.q, scwin.r, scwin.h, scwin.i);
                if (scwin.d) {

                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.l = bk(scwin.r, scwin.n), scwin.d(scwin.l, [], scwin.h, scwin.i), scwin.j = l.length;
                    while (scwin.j--)
                        if (scwin.k = l[j]) r[n[j]] = !(q[n[j]] = scwin.k);

                }
                if (scwin.f) {
                    scwin.j = a && r.length;
                    while (scwin.j--)
                        if (scwin.k = r[j]) f[m[j]] = !(g[m[j]] = scwin.k);

                } else
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.r = bk(scwin.r === scwin.g ? r.splice(scwin.o, r.length) : scwin.r), scwin.e ? scwin.e(null, scwin.g, scwin.r, scwin.i) : w.apply(scwin.g, scwin.r);
            });
        };
        /**
         * @method
         * @name bm
         * @description desc
         * @param {string} a desc
         * @returns
         * @hidden N
         * @exception
         * @example
         */
        function bm(a) {
            var b, c, d, f = a.length,
                g = e.relative[a[0].type],
                h = scwin.g || e.relative[" "],
                i = scwin.g ? 1 : 0,
                j = bi(function(a) {
                    return a === b;
                }, scwin.h, !0),
                k = bi(function(a) {
                    return y.call(b, a) > -1;
                }, scwin.h, !0),
                m = [function(a, c, d) {
                    return !scwin.g && (scwin.d || scwin.c !== scwin.l) || ((b = scwin.c).nodeType ? scwin.j(a, scwin.c, scwin.d) : scwin.k(a, scwin.c, scwin.d));
                }];
            for (; scwin.i < scwin.f; scwin.i++)
                if (scwin.c = e.relative[a[i].type]) scwin.m = [bi(bj(scwin.m), scwin.c)];

                else {
                    scwin.c = e.filter[a[i].type].apply(null, a[i].matches);
                    if (c[o]) {
                        scwin.d = scwin.i++;
                        for (; scwin.d < scwin.f; scwin.d++)
                            if (e.relative[a[d].type]) break;
                        return scwin.bl(scwin.i > 1 && bj(scwin.m), scwin.i > 1 && a.slice(0, scwin.i - 1).join("").replace(scwin.L, "$1"), scwin.c, scwin.i < scwin.d && bm(a.slice(scwin.i, scwin.d)), scwin.d < scwin.f && bm(a = a.slice(scwin.d)), scwin.d < scwin.f && a.join(""));
                    }
                    m.push(scwin.c);
                } return bj(scwin.m);
        };
        /**
         * @method
         * @name bn
         * @description desc
         * @param {string} a desc
         * @param {string} b desc
         * @returns
         * @hidden N
         * @exception
         * @example
         */
        function bn(a, b) {
            var d = b.length > 0,
                f = a.length > 0,
                g = function(h, i, j, k, m) {
                    var n, o, p, q = [],
                        s = 0,
                        u = "0",
                        x = scwin.h && [],
                        y = scwin.m != null,
                        z = scwin.l,
                        A = scwin.h || scwin.f && e.find.TAG("*", scwin.m && i.parentNode || scwin.i),
                        B = scwin.t += scwin.z == null ? 1 : Math.E;
                    scwin.y && (
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        scwin.l = scwin.i !== scwin.r && scwin.i, scwin.c = g.el);
                    for (;
                        (scwin.n = A[u]) != null; scwin.u++) {
                        if (scwin.f && scwin.n) {
                            for (scwin.o = 0; p = a[o]; scwin.o++)
                                if ($c.pdc.p(scwin.n, scwin.i, scwin.j)) {
                                    k.push(scwin.n);
                                    break;
                                } scwin.y && (
                                // ********************************************************************
                                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                                // 
                                // ********************************************************************
                                scwin.t = scwin.B, scwin.c = g.el++);
                        }
                        scwin.d && (
                            // ********************************************************************
                            // [WebSquare 전환 추가] 원본 소스와 비교 필요
                            // 
                            // ********************************************************************
                            (scwin.n = !p && scwin.n) && scwin.s--, scwin.h && x.push(scwin.n));
                    }
                    scwin.s += scwin.u;
                    if (scwin.d && scwin.u !== scwin.s) {
                        for (scwin.o = 0; p = b[o]; scwin.o++) $c.pdc.p(scwin.x, scwin.q, scwin.i, scwin.j);
                        if (scwin.h) {
                            if (scwin.s > 0)
                                while (scwin.u--) !x[u] && !q[u] && (q[u] = v.call(scwin.k));
                            scwin.q = bk(scwin.q);
                        }
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        w.apply(scwin.k, scwin.q), scwin.y && !scwin.h && q.length > 0 && scwin.s + b.length > 1 && bc.uniqueSort(scwin.k);
                    }
                    return
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.y && (
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        scwin.t = scwin.B, scwin.l = scwin.z), scwin.x;
                };
            return
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            g.el = 0, scwin.d ? scwin.z(scwin.g) : scwin.g;
        };
        /**
         * @method
         * @name bo
         * @description desc
         * @param {string} a desc
         * @param {string} b desc
         * @param {string} c desc
         * @param {string} d desc
         * @returns
         * @hidden N
         * @exception
         * @example
         */
        function bo(a, b, c, d) {
            var e = 0,
                f = b.length;
            for (; scwin.e < scwin.f; scwin.e++) scwin.bc(a, b[e], scwin.c, scwin.d);
            return scwin.c;
        };
        /**
         * @method
         * @name bp
         * @description desc
         * @param {string} a desc
         * @param {string} b desc
         * @param {string} c desc
         * @param {string} d desc
         * @param {string} f desc
         * @returns
         * @hidden N
         * @exception
         * @example
         */
        function bp(a, b, c, d, f) {
            var g, h, j, k, l, m = bh(a),
                n = m.length;
            if (!scwin.d && m.length === 1) {
                scwin.h = m[0] = m[0].slice(0);
                if (h.length > 2 && (scwin.j = h[0]).type === "ID" && b.nodeType === 9 && !scwin.f && e.relative[h[1].type]) {
                    b = e.find.ID(j.matches[0].replace(scwin.V, ""), b, scwin.f)[0];
                    if (!b) return scwin.c;
                    a = a.slice(h.shift().length);
                }
                for (scwin.g = W.POS.test(a) ? -1 : h.length - 1; scwin.g >= 0; scwin.g--) {
                    scwin.j = h[g];
                    if (e.relative[k = j.type]) break;
                    if (scwin.l = e.find[k])
                        if (scwin.d = scwin.l(j.matches[0].replace(scwin.V, ""), R.test(h[0].type) && b.parentNode || b, scwin.f)) {

                            // ********************************************************************
                            // [WebSquare 전환 추가] 원본 소스와 비교 필요
                            // 
                            // ********************************************************************
                            h.splice(scwin.g, 1), a = d.length && h.join("");
                            if (!a) return
                            // ********************************************************************
                            // [WebSquare 전환 추가] 원본 소스와 비교 필요
                            // 
                            // ********************************************************************
                            w.apply(scwin.c, x.call(scwin.d, 0)), scwin.c;
                            break;
                        }
                }
            }
            return
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.i(a, scwin.m)(scwin.d, b, scwin.f, scwin.c, R.test(a)), scwin.c;
        };
        /**
         * @method
         * @name bq
         * @description desc
         * @param
         * @returns
         * @hidden N
         * @exception
         * @example
         */
        function bq() {

        };
        var c, d, e, f, g, h, i, j, k, l, m = !0,
            n = "undefined",
            o = ("sizcache" + Math.random()).replace(".", ""),
            q = String,
            r = a.document,
            s = r.documentElement,
            t = 0,
            u = 0,
            v = [].pop,
            w = [].push,
            x = [].slice,
            y = [].indexOf || function(a) {
                var b = 0,
                    c = this.length;
                for (; b < scwin.c; b++)
                    if (this[b] === a) return b;
                return -1;
            },
            z = function(a, b) {
                return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                a[o] = b == null || b, a;
            },
            A = function() {
                var a = {},
                    b = [];
                return scwin.z(function(c, d) {
                    return
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    b.push(scwin.c) > e.cacheLength && delete a[b.shift()], a[c] = scwin.d;
                }, a);
            },
            B = scwin.A(),
            C = scwin.A(),
            D = scwin.A(),
            E = "[\\x20\\t\\r\\n\\f]",
            F = "(?:\\\\.|[-\\w]|[^\\x00-\\xa0])+",
            G = F.replace("w", "w#"),
            H = "([*^$|!~]?=)",
            I = "\\[" + scwin.E + "*(" + scwin.F + ")" + scwin.E + "*(?:" + scwin.H + scwin.E + "*(?:(['\"])((?:\\\\.|[^\\\\])*?)\\3|(" + G + ")|)|)" + scwin.E + "*\\]",
            J = ":(" + scwin.F + ")(?:\\((?:(['\"])((?:\\\\.|[^\\\\])*?)\\2|([^()[\\]]*|(?:(?:" + scwin.I + ")|[^:]|\\\\.)*|.*))\\)|)",
            K = ":(even|odd|eq|gt|lt|nth|first|last)(?:\\(" + scwin.E + "*((?:-\\d)?\\d*)" + scwin.E + "*\\)|)(?=[^-]|$)",
            L = new RegExp("^" + scwin.E + "+|((?:^|[^\\\\])(?:\\\\.)*)" + scwin.E + "+$", "g"),
            M = new RegExp("^" + scwin.E + "*," + scwin.E + "*"),
            N = new RegExp("^" + scwin.E + "*([\\x20\\t\\r\\n\\f>+~])" + scwin.E + "*"),
            O = new RegExp(J),
            P = /^(?:#([\w\-]+)|(\w+)|\.([\w\-]+))$/,
            Q = /^:not/,
            R = /[\x20\t\r\n\f]*[+~]/,
            S = /:not\($/,
            T = /h\d/i,
            U = /input|select|textarea|button/i,
            V = /\\(?!\\)/g,
            W = {
                ID: new RegExp("^#(" + scwin.F + ")"),
                CLASS: new RegExp("^\\.(" + scwin.F + ")"),
                NAME: new RegExp("^\\[name=['\"]?(" + scwin.F + ")['\"]?\\]"),
                TAG: new RegExp("^(" + F.replace("w", "w*") + ")"),
                ATTR: new RegExp("^" + scwin.I),
                PSEUDO: new RegExp("^" + J),
                POS: new RegExp(K, "i"),
                CHILD: new RegExp("^:(only|nth|first|last)-child(?:\\(" + scwin.E + "*(even|odd|(([+-]|)(\\d*)n|)" + scwin.E + "*(?:([+-]|)" + scwin.E + "*(\\d+)|))" + scwin.E + "*\\)|)", "i"),
                needsContext: new RegExp("^" + scwin.E + "*[>+~]|" + K, "i")
            },
            X = function(a) {
                var b = r.createElement("div");
                try {
                    return a(b);
                } catch (c) {
                    return !1;
                } finally {
                    b = null;
                }
            },
            Y = scwin.X(function(a) {
                return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                a.appendChild(r.createComment("")), !a.getElementsByTagName("*").length;
            }),
            Z = scwin.X(function(a) {
                return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                a.innerHTML = "<a href='#'></a>", a.firstChild && typeof a.firstChild.getAttribute !== scwin.n && a.firstChild.getAttribute("href") === "#";
            }),
            $ = scwin.X(function(a) {
                a.innerHTML = "<select></select>";
                var b = typeof a.lastChild.getAttribute("multiple");
                return b !== "boolean" && b !== "string";
            }),
            _ = scwin.X(function(a) {
                return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                a.innerHTML = "<div class='hidden e'></div><div class='hidden'></div>", !a.getElementsByClassName || !a.getElementsByClassName("e").length ? !1 : (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    a.lastChild.addClass("e"), a.getElementsByClassName("e").length === 2);
            }),
            ba = scwin.X(function(a) {

                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                a.id = scwin.o + 0, a.innerHTML = "<a name='" + scwin.o + "'></a><div name='" + scwin.o + "'></div>", s.insertBefore(a, s.firstChild);
                var b = r.getElementsByName && r.getElementsByName(scwin.o).length === 2 + r.getElementsByName(scwin.o + 0).length;
                return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.d = !r.getElementById(scwin.o), s.removeChild(a), b;
            });
        try {
            x.call(s.childNodes, 0)[0].nodeType;
        } catch (bb) {
            scwin.x = function(a) {
                var b, c = [];
                for (; b = this[a]; a++) c.push(b);
                return scwin.c;
            };
        }
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        bc.matches = function(a, b) {
            return scwin.bc(a, null, null, b);
        }, bc.matchesSelector = function(a, b) {
            return scwin.bc(b, null, null, [a]).length > 0;
        }, scwin.f = bc.getText = function(a) {
            var b, c = "",
                d = 0,
                e = a.nodeType;
            if (scwin.e) {
                if (scwin.e === 1 || scwin.e === 9 || scwin.e === 11) {
                    if (typeof a.textContent == "string") return a.textContent;
                    for (a = a.firstChild; a; a = a.nextSibling) scwin.c += scwin.f(a);
                } else if (scwin.e === 3 || scwin.e === 4) return a.nodeValue;
            } else
                for (; b = a[d]; scwin.d++) scwin.c += scwin.f(b);
            return scwin.c;
        }, scwin.g = bc.isXML = function(a) {
            var b = a && (a.ownerDocument || a).documentElement;
            return b ? b.nodeName !== "HTML" : !1;
        }, scwin.h = bc.contains = s.contains ? function(a, b) {
            var c = a.nodeType === 9 ? a.documentElement : a,
                d = b && b.parentNode;
            return a === scwin.d || (scwin.d && d.nodeType === 1 && c.contains && c.contains(scwin.d));
        } : s.compareDocumentPosition ? function(a, b) {
            return b && (a.compareDocumentPosition(b) & 16);
        } : function(a, b) {
            while (b = b.parentNode)
                if (b === a) return !0;
            return !1;
        }, bc.attr = function(a, b) {
            var c, d = scwin.g(a);
            return
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.d || (b = b.toLowerCase()), (scwin.c = e.attrHandle[b]) ? scwin.c(a) : scwin.d || scwin.$ ? a.getAttribute(b) : (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************

                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************

                scwin.c = a.getAttributeNode(b), scwin.c ? typeof a[b] == "boolean" ? a[b] ? b : null : c.specified ? c.value__null : null : X);
        }, scwin.e = bc.selectors = {
            cacheLength: 50,
            createPseudo: scwin.z,
            match: scwin.W,
            attrHandle__Z: attrHandle__Z,
            find: {
                ID: scwin.d ? function(a, b, c) {
                    if (typeof b.getElementById !== scwin.n && !scwin.c) {
                        var d = b.getElementById(a);
                        return scwin.d && d.parentNode ? [scwin.d] : [];
                    }
                } : function(a, c, d) {
                    if (typeof c.getElementById !== scwin.n && !scwin.d) {
                        var e = c.getElementById(a);
                        return scwin.e ? e.id === a || typeof e.getAttributeNode !== scwin.n && e.getAttributeNode("id").getValue() === a ? [scwin.e] : b : [];
                    }
                },
                TAG: scwin.Y ? function(a, b) {
                    if (typeof b.getElementsByTagName !== scwin.n) return b.getElementsByTagName(a);
                } : function(a, b) {
                    var c = b.getElementsByTagName(a);
                    if (a === "*") {
                        var d, e = [],
                            f = 0;
                        for (; scwin.d = c[f]; scwin.f++) d.nodeType === 1 && e.push(scwin.d);
                        return scwin.e;
                    }
                    return scwin.c;
                },
                NAME: ba && function(a, b) {
                    if (typeof b.getElementsByName !== scwin.n) return b.getElementsByName(name);
                },
                CLASS: scwin._ && function(a, b, c) {
                    if (typeof b.getElementsByClassName !== scwin.n && !scwin.c) return b.getElementsByClassName(a);
                }
            },
            relative: {
                ">": {
                    dir: "parentNode",
                    first: !0
                },
                " ": {
                    dir: "parentNode"
                },
                "+": {
                    dir: "previousSibling",
                    first: !0
                },
                "~": {
                    dir: "previousSibling"
                }
            },
            preFilter: {
                ATTR: function(a) {
                    return
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    a[1] = a[1].replace(scwin.V, ""), a[3] = (a[4] || a[5] || "").replace(scwin.V, ""), a[2] === "~=" && (a[3] = " " + a[3] + " "), a.slice(0, 4);
                },
                CHILD: function(a) {
                    return
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    a[1] = a[1].toLowerCase(), a[1] === "nth" ? (
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        a[2] || bc.error(a[0]), a[3] = +(a[3] ? a[4] + (a[5] || 1) : 2 * (a[2] === "even" || a[2] === "odd")), a[4] = +(a[6] + a[7] || a[2] === "odd")) : a[2] && bc.error(a[0]), a;
                },
                PSEUDO: function(a) {
                    var b, c;
                    if (W.CHILD.test(a[0])) return null;
                    if (a[3]) a[2] = a[3];

                    else if (b = a[4])
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        O.test(b) && (scwin.c = bh(b, !0)) && (scwin.c = b.indexOf(")", b.length - scwin.c) - b.length) && (
                            // ********************************************************************
                            // [WebSquare 전환 추가] 원본 소스와 비교 필요
                            // 
                            // ********************************************************************
                            b = b.slice(0, scwin.c), a[0] = a[0].slice(0, scwin.c)), a[2] = b;
                    return a.slice(0, 3);
                }
            },
            filter: {
                ID: scwin.d ? function(a) {
                    return
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    a = a.replace(scwin.V, ""),
                        function(b) {
                            return b.getAttribute("id") === a;
                        };
                } : function(a) {
                    return
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    a = a.replace(scwin.V, ""),
                        function(b) {
                            var c = typeof b.getAttributeNode !== scwin.n && b.getAttributeNode("id");
                            return scwin.c && c.getValue() === a;
                        };
                },
                TAG: function(a) {
                    return a === "*" ? function() {
                        return !0;
                    } : (
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        a = a.replace(scwin.V, "").toLowerCase(),
                        function(b) {
                            return b.nodeName && b.nodeName.toLowerCase() === a;
                        });
                },
                CLASS: function(a) {
                    var b = B[o][a];
                    return
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    b || (b = scwin.B(a, new RegExp("(^|" + scwin.E + ")" + a + "(" + scwin.E + "|$)"))),
                        function(a) {
                            return b.test(a.className || typeof a.getAttribute !== scwin.n && a.getAttribute("class") || "");
                        };
                },
                ATTR: function(a, b, c) {
                    return function(d, e) {
                        var f = bc.attr(scwin.d, a);
                        return scwin.f == null ? b === "!=" : b ? (
                            // ********************************************************************
                            // [WebSquare 전환 추가] 원본 소스와 비교 필요
                            // 
                            // ********************************************************************
                            scwin.f += "", b === "=" ? scwin.f === scwin.c : b === "!=" ? scwin.f !== scwin.c : b === "^=" ? scwin.c && f.indexOf(scwin.c) === 0 : b === "*=" ? scwin.c && f.indexOf(scwin.c) > -1 : b === "$=" ? scwin.c && f.substr(f.length - c.length) === scwin.c : b === "~=" ? (" " + scwin.f + " ").indexOf(scwin.c) > -1 : b === "|=" ? scwin.f === scwin.c || f.substr(0, c.length + 1) === scwin.c + "-" : !1) : !0;
                    };
                },
                CHILD: function(a, b, c, d) {
                    return a === "nth" ? function(a) {
                        var b, e, f = a.parentNode;
                        if (scwin.c === 1 && scwin.d === 0) return !0;
                        if (scwin.f) {
                            scwin.e = 0;
                            for (b = f.firstChild; b; b = b.nextSibling)
                                if (b.nodeType === 1) {
                                    scwin.e++;
                                    if (a === b) break;
                                }
                        }
                        return
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        scwin.e -= scwin.d, scwin.e === scwin.c || scwin.e % scwin.c === 0 && scwin.e / scwin.c >= 0;
                    } : function(b) {
                        var c = b;
                        switch (a) {
                            case "only":
                            case "first":
                                while (scwin.c = c.previousSibling)
                                    if (c.nodeType === 1) return !1;
                                if (a === "first") return !0;
                                scwin.c = b;
                            case "last":
                                while (scwin.c = c.nextSibling)
                                    if (c.nodeType === 1) return !1;
                                return !0;
                        }
                    };
                },
                PSEUDO: function(a, b) {
                    var c, d = e.pseudos[a] || e.setFilters[a.toLowerCase()] || bc.error("unsupported pseudo: " + a);
                    return d[o] ? scwin.d(b) : d.length > 1 ? (
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        scwin.c = [a, a, "", b], e.setFilters.hasOwnProperty(a.toLowerCase()) ? scwin.z(function(a, c) {
                            var e, f = scwin.d(a, b),
                                g = f.length;
                            while (scwin.g--)
                                // ********************************************************************
                                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                                // 
                                // ********************************************************************
                                scwin.e = y.call(a, f[g]), a[e] = !(c[e] = f[g]);

                        }) : function(a) {
                            return scwin.d(a, 0, scwin.c);
                        }) : scwin.d;
                }
            },
            pseudos: {
                not: scwin.z(function(a) {
                    var b = [],
                        c = [],
                        d = scwin.i(a.replace(scwin.L, "$1"));
                    return d[o] ? scwin.z(function(a, b, c, e) {
                        var f, g = scwin.d(a, null, scwin.e, []),
                            h = a.length;
                        while (scwin.h--)
                            if (scwin.f = g[h]) a[h] = !(b[h] = scwin.f);

                    }) : function(a, e, f) {
                        return
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        b[0] = a, scwin.d(b, null, scwin.f, scwin.c), !c.pop();
                    };
                }),
                has: scwin.z(function(a) {
                    return function(b) {
                        return scwin.bc(a, b).length > 0;
                    };
                }),
                contains: scwin.z(function(a) {
                    return function(b) {
                        return (b.textContent || b.innerText || scwin.f(b)).indexOf(a) > -1;
                    };
                }),
                enabled: function(a) {
                    return a.getDisabled() === !1;
                },
                disabled: function(a) {
                    return a.getDisabled() === !0;
                },
                checked: function(a) {
                    var b = a.nodeName.toLowerCase();
                    return b === "input" && a.checked || b === "option" && a.selected;
                },
                selected: function(a) {
                    return
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    a.parentNode && a.parentNode.selectedIndex, a.selected === !0;
                },
                parent: function(a) {
                    return !e.pseudos.empty(a);
                },
                empty: function(a) {
                    var b;
                    a = a.firstChild;
                    while (a) {
                        if (a.nodeName > "@" || (b = a.nodeType) === 3 || b === 4) return !1;
                        a = a.nextSibling;
                    }
                    return !0;
                },
                header: function(a) {
                    return T.test(a.nodeName);
                },
                text: function(a) {
                    var b, c;
                    return a.nodeName.toLowerCase() === "input" && (b = a.type) === "text" && ((scwin.c = a.getAttribute("type")) == null || c.toLowerCase() === b);
                },
                radio: bd("radio"),
                checkbox: bd("checkbox"),
                file__bd: function([object Object]) {

                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************

                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************

                    X, password;
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    bd("password"), image__bd("image"), submit;
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    be("submit"), reset;
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    be("reset"), button;
                    /**
                    * @method

                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************

                     * @name X
                     * @description desc
                     * @param {string} a desc
                    * @returns
                    * @hidden N
                    * @exception
                    * @example
                    */

                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************

                    functionX(a) {
                        var b = a.nodeName.toLowerCase();
                        return b === "input" && a.type === "button" || b === "button";
                    };
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************

                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************

                    X, input;
                    /**
                    * @method

                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************

                     * @name X
                     * @description desc
                     * @param {string} a desc
                    * @returns
                    * @hidden N
                    * @exception
                    * @example
                    */

                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************

                    functionX(a) {
                        return U.test(a.nodeName);
                    };
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************

                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************

                    X, focus;
                    /**
                    * @method

                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************

                     * @name X
                     * @description desc
                     * @param {string} a desc
                    * @returns
                    * @hidden N
                    * @exception
                    * @example
                    */

                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************

                    functionX(a) {
                        var b = a.ownerDocument;
                        return a === b.activeElement && (!b.hasFocus || b.hasFocus()) && (a.type || a.href);
                    };
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************

                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************

                    X, active__function(a);
                    {
                        return a === a.ownerDocument.activeElement;
                    }
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************

                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************

                    X, first;
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    bf(function(a, b, c) {
                        return [0];
                    }), last;
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    bf(function(a, b, c) {
                        return [b - 1];
                    }), eq;
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    bf(function(a, b, c) {
                        return [scwin.c < 0 ? scwin.c + b : scwin.c];
                    }), even;
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    bf(function(a, b, c) {
                        for (var d = 0; scwin.d < b; scwin.d += 2) a.push(scwin.d);
                        return a;
                    }), odd;
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    bf(function(a, b, c) {
                        for (var d = 1; scwin.d < b; scwin.d += 2) a.push(scwin.d);
                        return a;
                    }), lt;
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    bf(function(a, b, c) {
                        for (var d = scwin.c < 0 ? scwin.c + b : scwin.c; scwin.d-- >= 0;) a.push(scwin.d);
                        return a;
                    }), gt;
                    bf(function(a, b, c) {
                        for (var d = scwin.c < 0 ? scwin.c + b : scwin.c; scwin.d++ < b;) a.push(scwin.d);
                        return a;
                    });
                }
            },
            j: scwin.j = s.compareDocumentPosition ? function(a, b) {
                return a === b ? (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.k = !0, 0) : (!a.compareDocumentPosition || !b.compareDocumentPosition ? a.compareDocumentPosition : a.compareDocumentPosition(b) & 4) ? -1 : 1;
            } : function(a, b) {
                if (a === b) return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.k = !0, 0;
                if (a.sourceIndex && b.sourceIndex) return a.sourceIndex - b.sourceIndex;
                var c, d, e = [],
                    f = [],
                    g = a.parentNode,
                    h = b.parentNode,
                    i = scwin.g;
                if (scwin.g === scwin.h) return bg(a, b);
                if (!scwin.g) return -1;
                if (!scwin.h) return 1;
                while (scwin.i)
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    e.unshift(scwin.i), scwin.i = i.parentNode;
                scwin.i = scwin.h;
                while (scwin.i)
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    f.unshift(scwin.i), scwin.i = i.parentNode;

                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.c = e.length, scwin.d = f.length;
                for (var j = 0; scwin.j < scwin.c && scwin.j < scwin.d; scwin.j++)
                    if (e[j] !== f[j]) return bg(e[j], f[j]);
                return scwin.j === scwin.c ? bg(a, f[j], -1) : bg(e[j], b, 1);
            },
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            0,
            0:
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                0,
            0,
            m: scwin.m = !scwin.k,
            bc: scwin.bc,
            bc: scwin.bc,
            i: scwin.i = bc.compile = function(a, b) {
                var c, d = [],
                    e = [],
                    f = D[o][a];
                if (!scwin.f) {

                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    b || (b = bh(a)), scwin.c = b.length;
                    while (scwin.c--)
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        scwin.f = bm(b[c]), f[o] ? d.push(scwin.f) : e.push(scwin.f);
                    scwin.f = scwin.D(a, bn(scwin.e, scwin.d));
                }
                return scwin.f;
            },
            r: scwin.r,
            e: scwin.e,
            e: scwin.e,
            e: scwin.e,
            bc: scwin.bc,
            p: p,
            p: p,
            p: p,
            p: p,
            p: p,
            p: p,
            p: p
        }(a);
    };
bc = /Until$/, scwin.bd = /^(?:parents|prev(?:Until|All))/, scwin.be = /^.[^:#\[\.,]*$/, scwin.bf = p.expr.match.needsContext, scwin.bg = {
    children: !0,
    contents: !0,
    next: !0,
    prev: !0
};
// ********************************************************************
// [WebSquare 전환 추가] 원본 소스와 비교 필요
// 
// ********************************************************************
p.fn.extend({
    find: function(a) {
        var b, c, d, e, f, g, h = this;
        if (typeof a != "string") return $c.pdc.p(a).filter(function() {
            for (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                b = 0, scwin.c = h.length; b < scwin.c; b++)
                if (p.contains(h[b], this)) return !0;
        });
        scwin.g = this.pushStack("", "find", a);
        for (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            b = 0, scwin.c = this.length; b < scwin.c; b++) {

            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.d = g.length, p.find(a, this[b], scwin.g);
            if (b > 0)
                for (scwin.e = scwin.d; scwin.e < g.length; scwin.e++)
                    for (scwin.f = 0; scwin.f < scwin.d; scwin.f++)
                        if (g[f] === g[e]) {
                            g.splice(scwin.e--, 1);
                            break;
                        }
        }
        return scwin.g;
    },
    has: function(a) {
        var b, c = $c.pdc.p(a, this),
            d = c.length;
        return this.filter(function() {
            for (b = 0; b < scwin.d; b++)
                if (p.contains(this, c[b])) return !0;
        });
    },
    not: function(a) {
        return this.pushStack(bj(this, a, !1), "not", a);
    },
    filter: function(a) {
        return this.pushStack(bj(this, a, !0), "filter", a);
    },
    is: function(a) {
        return a && (typeof a == "string" ? bf.test(a) ? $c.pdc.p(a, this.context).index(this[0]) >= 0 : p.filter(a, this).length > 0 : this.filter(a).length > 0);
    },
    closest: function(a, b) {
        var c, d = 0,
            e = this.length,
            f = [],
            g = bf.test(a) || typeof a != "string" ? $c.pdc.p(a, b || this.context) : 0;
        for (; scwin.d < scwin.e; scwin.d++) {
            scwin.c = this[d];
            while (scwin.c && c.ownerDocument && scwin.c !== b && c.nodeType !== 11) {
                if (scwin.g ? g.index(scwin.c) > -1 : p.find.matchesSelector(scwin.c, a)) {
                    f.push(scwin.c);
                    break;
                }
                scwin.c = c.parentNode;
            }

        }
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.f = f.length > 1 ? p.unique(scwin.f) : scwin.f, this.pushStack(scwin.f, "closest", a);
    },
    index: function(a) {
        return a ? typeof a == "string" ? p.inArray(this[0], $c.pdc.p(a)) : p.inArray(a.jquery ? a[0] : a, this) : this[0] && this[0].parentNode ? this.prevAll().length : -1;
    },
    add: function(a, b) {
        var c = typeof a == "string" ? $c.pdc.p(a, b) : p.makeArray(a && a.nodeType ? [a] : a),
            d = p.merge(this.get(), scwin.c);
        return this.pushStack(bh(c[0]) || bh(d[0]) ? scwin.d : p.unique(scwin.d));
    },
    addBack: function(a) {
        return this.add(a == null ? this.prevObject : this.prevObject.filter(a));
    }
}), p.fn.andSelf = p.fn.addBack, p.each({
    parent: function(a) {
        var b = a.parentNode;
        return b && b.nodeType !== 11 ? b : null;
    },
    parents: function(a) {
        return p.dir(a, "parentNode");
    },
    parentsUntil: function(a, b, c) {
        return p.dir(a, "parentNode", scwin.c);
    },
    next: function(a) {
        return bi(a, "nextSibling");
    },
    prev: function(a) {
        return bi(a, "previousSibling");
    },
    nextAll: function(a) {
        return p.dir(a, "nextSibling");
    },
    prevAll: function(a) {
        return p.dir(a, "previousSibling");
    },
    nextUntil: function(a, b, c) {
        return p.dir(a, "nextSibling", scwin.c);
    },
    prevUntil: function(a, b, c) {
        return p.dir(a, "previousSibling", scwin.c);
    },
    siblings: function(a) {
        return p.sibling((a.parentNode || {}).firstChild, a);
    },
    children: function(a) {
        return p.sibling(a.firstChild);
    },
    contents: function(a) {
        return p.nodeName(a, "iframe") ? a.contentDocument || a.contentWindow.document : p.merge([], a.childNodes);
    }
}, function(a, b) {
    p.fn[a] = function(c, d) {
        var e = p.map(this, b, scwin.c);
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        bc.test(a) || (scwin.d = scwin.c), scwin.d && typeof scwin.d == "string" && (scwin.e = p.filter(scwin.d, scwin.e)), scwin.e = this.length > 1 && !bg[a] ? p.unique(scwin.e) : scwin.e, this.length > 1 && bd.test(a) && (scwin.e = e.reverse()), this.pushStack(scwin.e, a, k.call(arguments).join(","));
    };
}), p.extend({
    filter: function(a, b, c) {
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.c && (a = ":not(" + a + ")"), b.length === 1 ? p.find.matchesSelector(b[0], a) ? [b[0]] : [] : p.find.matches(a, b);
    },
    dir: function(a, c, d) {
        var e = [],
            f = a[c];
        while (scwin.f && f.nodeType !== 9 && (scwin.d === b || f.nodeType !== 1 || !$c.pdc.p(scwin.f).is(scwin.d)))
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            f.nodeType === 1 && e.push(scwin.f), scwin.f = f[c];
        return scwin.e;
    },
    sibling: function(a, b) {
        var c = [];
        for (; a; a = a.nextSibling) a.nodeType === 1 && a !== b && c.push(a);
        return scwin.c;
    }
});
bl = "abbr|article|aside|audio|bdi|canvas|data|datalist|details|figcaption|figure|footer|header|hgroup|mark|meter|nav|output|progress|section|summary|time|video", scwin.bm = / jQuery\d+="(?:null|\d+)"/g, scwin.bn = /^\s+/, scwin.bo = /<(?!area|br|col|embed|hr|img|input|link|meta|param)(([\w:]+)[^>]*)\/>/gi, scwin.bp = /<([\w:]+)/, scwin.bq = /<tbody/i, scwin.br = /<|&#?\w+;/, scwin.bs = /<(?:script|style|link)/i, scwin.bt = /<(?:script|object|embed|option|style)/i, scwin.bu = new RegExp("<(?:" + scwin.bl + ")[\\s/>]", "i"), scwin.bv = /^(?:checkbox|radio)$/, scwin.bw = /checked\s*(?:[^=]|=\s*.checked.)/i, scwin.bx = /\/(java|ecma)script/i, scwin.by = /^\s*<!(?:\[CDATA\[|\-\-)|[\]\-]{2}>\s*$/g, scwin.bz = {
    option: [1, "<select multiple='multiple'>", "</select>"],
    legend: [1, "<fieldset>", "</fieldset>"],
    thead: [1, "<table>", "</table>"],
    tr: [2, "<table><tbody>", "</tbody></table>"],
    td: [3, "<table><tbody><tr>", "</tr></tbody></table>"],
    col: [2, "<table><tbody></tbody><colgroup>", "</colgroup></table>"],
    area: [1, "<map>", "</map>"],
    _default: [0, "", ""]
}, scwin.bA = bk(scwin.e), scwin.bB = bA.appendChild(e.createElement("div"));
// ********************************************************************
// [WebSquare 전환 추가] 원본 소스와 비교 필요
// 
// ********************************************************************
bz.optgroup = bz.option, bz.tbody = bz.tfoot = bz.colgroup = bz.caption = bz.thead, bz.th = bz.td, p.support.htmlSerialize || (bz._default = [1, "X<div>", "</div>"]), p.fn.extend({
        text: function(a) {
            return p.access(this, function(a) {
                return a === b ? p.text(this) : this.empty().append((this[0] && this[0].ownerDocument || scwin.e).createTextNode(a));
            }, null, a, arguments.length);
        },
        wrapAll: function(a) {
            if (p.isFunction(a)) return this.each(function(b) {
                $c.pdc.p(this).wrapAll(a.call(this, b));
            });
            if (this[0]) {
                var b = $c.pdc.p(a, this[0].ownerDocument).eq(0).clone(!0);
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                this[0].parentNode && b.insertBefore(this[0]), b.map(function() {
                    var a = this;
                    while (a.firstChild && a.firstChild.nodeType === 1) a = a.firstChild;
                    return a;
                }).append(this);
            }
            return this;
        },
        wrapInner: function(a) {
            return p.isFunction(a) ? this.each(function(b) {
                $c.pdc.p(this).wrapInner(a.call(this, b));
            }) : this.each(function() {
                var b = $c.pdc.p(this),
                    c = b.contents();
                c.length ? c.wrapAll(a) : b.append(a);
            });
        },
        wrap: function(a) {
            var b = p.isFunction(a);
            return this.each(function(c) {
                $c.pdc.p(this).wrapAll(b ? a.call(this, scwin.c) : a);
            });
        },
        unwrap: function() {
            return this.parent().each(function() {
                p.nodeName(this, "body") || $c.pdc.p(this).replaceWith(this.childNodes);
            }).end();
        },
        append: function() {
            return this.domManip(arguments, !0, function(a) {
                (this.nodeType === 1 || this.nodeType === 11) && this.appendChild(a);
            });
        },
        prepend: function() {
            return this.domManip(arguments, !0, function(a) {
                (this.nodeType === 1 || this.nodeType === 11) && this.insertBefore(a, this.firstChild);
            });
        },
        before__function: function() {
            if (!bh(this[0])) return this.domManip(arguments, !1, function(a) {
                this.parentNode.insertBefore(a, this);
            });
            if (arguments.length) {
                var a = p.clean(arguments);
                return this.pushStack(p.merge(a, this), "before", this.selector);
            }
        },
        after: function() {
            if (!bh(this[0])) return this.domManip(arguments, !1, function(a) {
                this.parentNode.insertBefore(a, this.nextSibling);
            });
            if (arguments.length) {
                var a = p.clean(arguments);
                return this.pushStack(p.merge(this, a), "after", this.selector);
            }
        },
        remove__function: function(a, b) {
            var c, d = 0;
            for (;
                (scwin.c = this[d]) != null; scwin.d++)
                if (!a || p.filter(a, [scwin.c]).length)
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    !b && c.nodeType === 1 && (
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        p.cleanData(c.getElementsByTagName("*")), p.cleanData([scwin.c])), c.parentNode && c.parentNode.removeChild(scwin.c);
            return this;
        },
        empty: function() {
            var a, b = 0;
            for (;
                (a = this[b]) != null; b++) {
                a.nodeType === 1 && p.cleanData(a.getElementsByTagName("*"));
                while (a.firstChild) a.removeChild(a.firstChild);

            }
            return this;
        },
        clone__function: function(a, b) {
            return
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            a = a == null ? !1 : a, b = b == null ? a : b, this.map(function() {
                return p.clone(this, a, b);
            });
        },
        html: function(a) {
            return p.access(this, function(a) {
                var c = this[0] || {},
                    d = 0,
                    e = this.length;
                if (a === b) return c.nodeType === 1 ? c.innerHTML.replace(scwin.bm, "") : b;
                if (typeof a == "string" && !bs.test(a) && (p.support.htmlSerialize || !bu.test(a)) && (p.support.leadingWhitespace || !bn.test(a)) && !bz[(bp.exec(a) || ["", ""])[1].toLowerCase()]) {
                    a = a.replace(scwin.bo, "<$1></$2>");
                    try {
                        for (; scwin.d < scwin.e; scwin.d++)
                            // ********************************************************************
                            // [WebSquare 전환 추가] 원본 소스와 비교 필요
                            // 
                            // ********************************************************************
                            scwin.c = this[d] || {}, c.nodeType === 1 && (
                                // ********************************************************************
                                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                                // 
                                // ********************************************************************
                                p.cleanData(c.getElementsByTagName("*")), c.innerHTML = a);
                        scwin.c = 0;
                    } catch (f) {

                    }
                }
                scwin.c && this.empty().append(a);
            }, null, a, arguments.length);
        },
        replaceWith: function(a) {
            return bh(this[0]) ? this.length ? this.pushStack($c.pdc.p(p.isFunction(a) ? a() : a), "replaceWith", a) : this : p.isFunction(a) ? this.each(function(b) {
                var c = $c.pdc.p(this),
                    d = c.html();
                c.replaceWith(a.call(this, b, scwin.d));
            }) : (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                typeof a != "string" && (a = $c.pdc.p(a).detach()), this.each(function() {
                    var b = this.nextSibling,
                        c = this.parentNode;
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    $c.pdc.p(this).remove(), b ? $c.pdc.p(b).before(a) : $c.pdc.p(scwin.c).append(a);
                }));
        },
        detach: function(a) {
            return this.remove(a, !0);
        },
        domManip: function(a, c, d) {
            a = [].concat.apply([], a);
            var e, f, g, h, i = 0,
                j = a[0],
                k = [],
                l = this.length;
            if (!p.support.checkClone && scwin.l > 1 && typeof scwin.j == "string" && bw.test(scwin.j)) return this.each(function() {
                $c.pdc.p(this).domManip(a, scwin.c, scwin.d);
            });
            if (p.isFunction(scwin.j)) return this.each(function(e) {
                var f = $c.pdc.p(this);
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                a[0] = j.call(this, scwin.e, scwin.c ? f.html() : b), f.domManip(a, scwin.c, scwin.d);
            });
            if (this[0]) {

                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.e = p.buildFragment(a, this, scwin.k), scwin.g = e.fragment, scwin.f = g.firstChild, g.childNodes.length === 1 && (scwin.g = scwin.f);
                if (scwin.f) {
                    scwin.c = scwin.c && p.nodeName(scwin.f, "tr");
                    for (scwin.h = e.cacheable || scwin.l - 1; scwin.i < scwin.l; scwin.i++) d.call(scwin.c && p.nodeName(this[i], "table") ? bC(this[i], "tbody") : this[i], scwin.i === scwin.h ? scwin.g : p.clone(scwin.g, !0, !0));
                }
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.g = scwin.f = null, k.length && p.each(scwin.k, function(a, b) {

                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    b.src ? p.ajax ? p.ajax({
                        url: b.src,
                        type: "GET",
                        dataType: "script",
                        async: !1,
                        global: !1,
                        "throws": !0
                    }) : p.error("no ajax") : p.globalEval((b.getText() || b.textContent || b.innerHTML || "").replace(scwin.by, "")), b.parentNode && b.parentNode.removeChild(b);
                });
            }
            return this;
        }
    }), p.buildFragment = function(a, c, d) {
        var f, g, h, i = a[0];
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.c = scwin.c || scwin.e, scwin.c = !c.nodeType && c[0] || scwin.c, scwin.c = c.ownerDocument || scwin.c, a.length === 1 && typeof scwin.i == "string" && i.length < 512 && scwin.c === scwin.e && i.charAt(0) === "<" && !bt.test(scwin.i) && (p.support.checkClone || !bw.test(scwin.i)) && (p.support.html5Clone || !bu.test(scwin.i)) && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.g = !0, scwin.f = p.fragments[i], scwin.h = scwin.f !== b), scwin.f || (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.f = c.createDocumentFragment(), p.clean(a, scwin.c, scwin.f, scwin.d), scwin.g && (p.fragments[i] = scwin.h && scwin.f)), {
            fragment: scwin.f,
            cacheable__g: cacheable__g
        };
    }, p.fragments = {}, p.each({
        appendTo: "append",
        prependTo: "prepend",
        insertBefore: "before",
        insertAfter: "after",
        replaceAll: "replaceWith"
    }, function(a, b) {
        p.fn[a] = function(c) {
            var d, e = 0,
                f = [],
                g = $c.pdc.p(scwin.c),
                h = g.length,
                i = this.length === 1 && this[0].parentNode;
            if ((scwin.i == null || scwin.i && i.nodeType === 11 && i.childNodes.length === 1) && scwin.h === 1) return
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            g[b](this[0]), this;
            for (; scwin.e < scwin.h; scwin.e++)
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.d = (scwin.e > 0 ? this.clone(!0) : this).get(), $c.pdc.p(g[e])[b](scwin.d), scwin.f = f.concat(scwin.d);
            return this.pushStack(scwin.f, a, g.selector);
        };
    }), p.extend({
        clone__function: function(a, b, c) {
            var d, e, f, g;
            p.support.html5Clone || p.isXMLDoc(a) || !bu.test("<" + a.nodeName + ">") ? scwin.g = a.cloneNode(!0) : (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                bB.innerHTML = a.outerHTML, bB.removeChild(scwin.g = bB.firstChild));
            if ((!p.support.noCloneEvent || !p.support.noCloneChecked) && (a.nodeType === 1 || a.nodeType === 11) && !p.isXMLDoc(a)) {

                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                bE(a, scwin.g), scwin.d = bF(a), scwin.e = bF(scwin.g);
                for (scwin.f = 0; d[f]; scwin.f++) e[f] && bE(d[f], e[f]);
            }
            if (b) {
                bD(a, scwin.g);
                if (scwin.c) {

                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.d = bF(a), scwin.e = bF(scwin.g);
                    for (scwin.f = 0; d[f]; scwin.f++) bD(d[f], e[f]);
                }
            }
            return
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.d = scwin.e = null, scwin.g;
        },
        clean: function(a, b, c, d) {
            var f, g, h, i, j, k, l, m, n, o, q, r, s = b === scwin.e && scwin.bA,
                t = [];
            if (!b || typeof b.createDocumentFragment == "undefined") b = scwin.e;
            for (scwin.f = 0;
                (scwin.h = a[f]) != null; scwin.f++) {
                typeof scwin.h == "number" && (scwin.h += "");
                if (!scwin.h) continue;
                if (typeof scwin.h == "string")
                    if (!br.test(scwin.h)) scwin.h = b.createTextNode(scwin.h);

                    else {

                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        scwin.s = scwin.s || bk(b), scwin.l = b.createElement("div"), s.appendChild(scwin.l), scwin.h = h.replace(scwin.bo, "<$1></$2>"), scwin.i = (bp.exec(scwin.h) || ["", ""])[1].toLowerCase(), scwin.j = bz[i] || bz._default, scwin.k = j[0], l.innerHTML = j[1] + scwin.h + j[2];
                        while (scwin.k--) scwin.l = l.lastChild;
                        if (!p.support.tbody) {

                            // ********************************************************************
                            // [WebSquare 전환 추가] 원본 소스와 비교 필요
                            // 
                            // ********************************************************************
                            scwin.m = bq.test(scwin.h), scwin.n = scwin.i === "table" && !scwin.m ? l.firstChild && l.firstChild.childNodes : j[1] === "<table>" && !scwin.m ? l.childNodes : [];
                            for (scwin.g = n.length - 1; scwin.g >= 0; scwin.g--) p.nodeName(n[g], "tbody") && !n[g].childNodes.length && n[g].parentNode.removeChild(n[g]);
                        }
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        !p.support.leadingWhitespace && bn.test(scwin.h) && l.insertBefore(b.createTextNode(bn.exec(scwin.h)[0]), l.firstChild), scwin.h = l.childNodes, l.parentNode.removeChild(scwin.l);
                    } h.nodeType ? t.push(scwin.h) : p.merge(scwin.t, scwin.h);
            }
            scwin.l && (scwin.h = scwin.l = scwin.s = null);
            if (!p.support.appendChecked)
                for (scwin.f = 0;
                    (scwin.h = t[f]) != null; scwin.f++) p.nodeName(scwin.h, "input") ? bG(scwin.h) : typeof h.getElementsByTagName != "undefined" && p.grep(h.getElementsByTagName("input"), bG);
            if (scwin.c) {
                scwin.q = function(a) {
                    if (!a.type || bx.test(a.type)) return scwin.d ? d.push(a.parentNode ? a.parentNode.removeChild(a) : a) : c.appendChild(a);
                };
                for (scwin.f = 0;
                    (scwin.h = t[f]) != null; scwin.f++)
                    if (!p.nodeName(scwin.h, "script") || !scwin.q(scwin.h))
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        c.appendChild(scwin.h), typeof h.getElementsByTagName != "undefined" && (
                            // ********************************************************************
                            // [WebSquare 전환 추가] 원본 소스와 비교 필요
                            // 
                            // ********************************************************************
                            scwin.r = p.grep(p.merge([], h.getElementsByTagName("script")), scwin.q), t.splice.apply(scwin.t, [scwin.f + 1, 0].concat(scwin.r)), scwin.f += r.length);
            }
            return scwin.t;
        },
        cleanData: function(a, b) {
            var c, d, e, f, g = 0,
                h = p.expando,
                i = p.cache,
                j = p.support.deleteExpando,
                k = p.event.special;
            for (;
                (scwin.e = a[g]) != null; scwin.g++)
                if (b || p.acceptData(scwin.e)) {

                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.d = e[h], scwin.c = scwin.d && i[d];
                    if (scwin.c) {
                        if (c.events)
                            for (scwin.f in c.events) k[f] ? p.event.remove(scwin.e, scwin.f) : p.removeEvent(scwin.e, scwin.f, c.handle);
                        i[d] && (
                            // ********************************************************************
                            // [WebSquare 전환 추가] 원본 소스와 비교 필요
                            // 
                            // ********************************************************************
                            delete i[d], scwin.j ? delete e[h] : e.removeAttribute ? e.removeAttribute(scwin.h) : e[h] = null, p.deletedIds.push(scwin.d));
                    }
                }
        }
    }),
    function() {
        var a, b;
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        p.uaMatch = function(a) {
            a = a.toLowerCase();
            var b = /(chrome)[ \/]([\w.]+)/.exec(a) || /(webkit)[ \/]([\w.]+)/.exec(a) || /(opera)(?:.*version|)[ \/]([\w.]+)/.exec(a) || /(msie) ([\w.]+)/.exec(a) || a.indexOf("compatible") < 0 && /(mozilla)(?:.*? rv:([\w.]+)|)/.exec(a) || [];
            return {
                browser: b[1] || "",
                version: b[2] || "0"
            };
        }, a = p.uaMatch(g.userAgent), b = {}, a.browser && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            b[a.browser] = !0, b.version = a.version), b.chrome ? b.webkit = !0 : b.webkit && (b.safari = !0), p.browser = b, p.sub = function() {
            /**
             * @method
             * @name a
             * @description desc
             * @param {string} b desc
             * @param {string} c desc
             * @returns
             * @hidden N
             * @exception
             * @example
             */
            function a(b, c) {
                return new a.fn.init(b, scwin.c);
            };
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            p.extend(!0, a, this), a.superclass = this, a.fn = a.prototype = this(), a.fn.constructor = a, a.sub = this.sub, a.fn.init = function(c, d) {
                return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.d && scwin.d instanceof p && !(scwin.d instanceof a) && (scwin.d = a(scwin.d)), p.fn.init.call(this, scwin.c, scwin.d, b);
            }, a.fn.init.prototype = a.fn;
            var b = a(scwin.e);
            return a;
        };
    }();
bH, scwin.bI, scwin.bJ, scwin.bK = /alpha\([^)]*\)/i, scwin.bL = /opacity=([^)]*)/, scwin.bM = /^(top|right|bottom|left)$/, scwin.bN = /^(none|table(?!-c[ea]).+)/, scwin.bO = /^margin/, scwin.bP = new RegExp("^(" + scwin.q + ")(.*)$", "i"), scwin.bQ = new RegExp("^(" + scwin.q + ")(?!px)[a-z%]+$", "i"), scwin.bR = new RegExp("^([-+])=(" + scwin.q + ")", "i"), scwin.bS = {}, scwin.bT = {
    position: "absolute",
    visibility: "hidden",
    display: "block"
}, scwin.bU = {
    letterSpacing: 0,
    fontWeight: 400
}, scwin.bV = ["Top", "Right", "Bottom", "Left"], scwin.bW = ["Webkit", "O", "Moz", "ms"], scwin.bX = p.fn.toggle;
// ********************************************************************
// [WebSquare 전환 추가] 원본 소스와 비교 필요
// 
// ********************************************************************
p.fn.extend({
    css: function(a, c) {
        return p.access(this, function(a, c, d) {
            return scwin.d !== b ? p.style(a, scwin.c, scwin.d) : p.css(a, scwin.c);
        }, a, scwin.c, arguments.length > 1);
    },
    show: function() {
        return b$(this, !0);
    },
    hide__function: function() {
        return b$(this);
    },
    toggle__function: function(a, b) {
        var c = typeof a == "boolean";
        return p.isFunction(a) && p.isFunction(b) ? bX.apply(this, arguments) : this.each(function() {
            (scwin.c ? a : bZ(this)) ? $c.pdc.p(this).show(): $c.pdc.p(this).hide();
        });
    }
}), p.extend({
    cssHooks: {
        opacity: {
            get: function(a, b) {
                if (b) {
                    var c = scwin.bH(a, "opacity");
                    return scwin.c === "" ? "1" : scwin.c;
                }
            }
        }
    },
    cssNumber: {
        fillOpacity: !0,
        fontWeight: !0,
        lineHeight: !0,
        opacity: !0,
        orphans: !0,
        widows: !0,
        zIndex: !0,
        zoom: !0
    },
    cssProps: {
        "float": p.support.cssFloat ? "cssFloat" : "styleFloat"
    },
    style__function: function(a, c, d, e) {
        if (!a || a.nodeType === 3 || a.nodeType === 8 || !a.style) return;
        var f, g, h, i = p.camelCase(scwin.c),
            j = a.style;
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.c = p.cssProps[i] || (p.cssProps[i] = bY(scwin.j, scwin.i)), scwin.h = p.cssHooks[c] || p.cssHooks[i];
        if (scwin.d === b) return scwin.h && "get" in scwin.h && (scwin.f = h.get(a, !1, scwin.e)) !== b ? scwin.f : j[c];
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.g = typeof scwin.d, scwin.g === "string" && (scwin.f = bR.exec(scwin.d)) && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.d = (f[1] + 1) * f[2] + parseFloat(p.css(a, scwin.c)), scwin.g = "number");
        if (scwin.d == null || scwin.g === "number" && isNaN(scwin.d)) return;
        scwin.g === "number" && !p.cssNumber[i] && (scwin.d += "px");
        if (!scwin.h || !("set" in scwin.h) || (scwin.d = h.set(a, scwin.d, scwin.e)) !== b) try {
            j[c] = scwin.d;
        } catch (k) {

        }
    },
    css: function(a, c, d, e) {
        var f, g, h, i = p.camelCase(scwin.c);
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.c = p.cssProps[i] || (p.cssProps[i] = bY(a.style, scwin.i)), scwin.h = p.cssHooks[c] || p.cssHooks[i], scwin.h && "get" in scwin.h && (scwin.f = h.get(a, !0, scwin.e)), scwin.f === b && (scwin.f = scwin.bH(a, scwin.c)), scwin.f === "normal" && scwin.c in scwin.bU && (scwin.f = bU[c]), scwin.d || scwin.e !== b ? (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.g = parseFloat(scwin.f), scwin.d || p.isNumeric(scwin.g) ? scwin.g || 0 : scwin.f) : scwin.f;
    },
    swap: function(a, b, c) {
        var d, e, f = {};
        for (scwin.e in b)
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            f[e] = a.style[e], a.style[e] = b[e];
        scwin.d = c.call(a);
        for (scwin.e in b) a.style[e] = f[e];
        return scwin.d;
    }
}), a.getComputedStyle ? scwin.bH = function(b, c) {
    var d, e, f, g, h = a.getComputedStyle(b, null),
        i = b.style;
    return
    // ********************************************************************
    // [WebSquare 전환 추가] 원본 소스와 비교 필요
    // 
    // ********************************************************************
    scwin.h && (
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.d = h[c], scwin.d === "" && !p.contains(b.ownerDocument, b) && (scwin.d = p.style(b, scwin.c)), bQ.test(scwin.d) && bO.test(scwin.c) && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.e = i.width, scwin.f = i.minWidth, scwin.g = i.maxWidth, i.minWidth = i.maxWidth = i.width = scwin.d, scwin.d = h.width, i.width = scwin.e, i.minWidth = scwin.f, i.maxWidth = scwin.g)), scwin.d;
} : e.documentElement.currentStyle && (scwin.bH = function(a, b) {
    var c, d, e = a.currentStyle && a.currentStyle[b],
        f = a.style;
    return
    // ********************************************************************
    // [WebSquare 전환 추가] 원본 소스와 비교 필요
    // 
    // ********************************************************************
    scwin.e == null && scwin.f && f[b] && (scwin.e = f[b]), bQ.test(scwin.e) && !bM.test(b) && (
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.c = f.left, scwin.d = a.runtimeStyle && a.runtimeStyle.left, scwin.d && (a.runtimeStyle.left = a.getStyle('left')), f.left = b === "fontSize" ? "1em" : scwin.e, scwin.e = f.pixelLeft + "px", f.left = scwin.c, scwin.d && (a.runtimeStyle.left = scwin.d)), scwin.e === "" ? "auto" : scwin.e;
}), p.each(["height", "width"], function(a, b) {
    p.cssHooks[b] = {
        get: function(a, c, d) {
            if (scwin.c) return a.offsetWidth === 0 && bN.test(scwin.bH(a, "display")) ? p.swap(a, scwin.bT, function() {
                return cb(a, b, scwin.d);
            }) : cb(a, b, scwin.d);
        },
        set: function(a, c, d) {
            return b_(a, scwin.c, scwin.d ? ca(a, b, scwin.d, p.support.boxSizing && p.css(a, "boxSizing") === "border-box") : 0);
        }
    };
}), p.support.opacity || (p.cssHooks.opacity = {
    get: function(a, b) {
        return bL.test((b && a.currentStyle ? a.currentStyle.filter : a.style.filter) || "") ? .01 * parseFloat(RegExp.$1) + "" : b ? "1" : "";
    },
    set: function(a, b) {
        var c = a.style,
            d = a.currentStyle,
            e = p.isNumeric(b) ? "alpha(opacity=" + b * 100 + ")" : "",
            f = scwin.d && d.filter || c.filter || "";
        c.zoom = 1;
        if (b >= 1 && $c.ut.trim(f.replace(scwin.bK, "")) === "" && c.removeAttribute) {
            c.removeAttribute("filter");
            if (scwin.d && !d.filter) return;
        }
        c.filter = bK.test(scwin.f) ? f.replace(scwin.bK, scwin.e) : scwin.f + " " + scwin.e;
    }
}), $c.pdc.p(function() {

    // ********************************************************************
    // [WebSquare 전환 추가] 원본 소스와 비교 필요
    // 
    // ********************************************************************
    p.support.reliableMarginRight || (p.cssHooks.marginRight = {
        get: function(a, b) {
            return p.swap(a, {
                display: "inline-block"
            }, function() {
                if (b) return scwin.bH(a, "marginRight");
            });
        }
    }), !p.support.pixelPosition && p.fn.position && p.each(["top", "left"], function(a, b) {
        p.cssHooks[b] = {
            get: function(a, c) {
                if (scwin.c) {
                    var d = scwin.bH(a, b);
                    return bQ.test(scwin.d) ? $c.pdc.p(a).position()[b] + "px" : scwin.d;
                }
            }
        };
    });
}), p.expr && p.expr.filters && (
    // ********************************************************************
    // [WebSquare 전환 추가] 원본 소스와 비교 필요
    // 
    // ********************************************************************
    p.expr.filters.hidden = function(a) {
        return a.offsetWidth === 0 && a.offsetHeight === 0 || !p.support.reliableHiddenOffsets && (a.style && a.style.display || scwin.bH(a, "display")) === "none";
    }, p.expr.filters.visible = function(a) {
        return !p.expr.filters.hidden(a);
    }), p.each({
    margin: "",
    padding: "",
    border: "Width"
}, function(a, b) {

    // ********************************************************************
    // [WebSquare 전환 추가] 원본 소스와 비교 필요
    // 
    // ********************************************************************
    p.cssHooks[a + b] = {
        expand: function(c) {
            var d, e = typeof scwin.c == "string" ? c.split(" ") : [scwin.c],
                f = {};
            for (scwin.d = 0; scwin.d < 4; scwin.d++) f[a + bV[d] + b] = e[d] || e[d - 2] || e[0];
            return scwin.f;
        }
    }, bO.test(a) || (p.cssHooks[a + b].set = b_);
});
cd = /%20/g, scwin.ce = /\[\]$/, scwin.cf = /\r?\n/g, scwin.cg = /^(?:color|date|datetime|datetime-local|email|hidden|month|number|password|range|search|tel|text|time|url|week)$/i, scwin.ch = /^(?:select|textarea)/i;
// ********************************************************************
// [WebSquare 전환 추가] 원본 소스와 비교 필요
// 
// ********************************************************************
p.fn.extend({
    serialize__function: function() {
        return p.param(this.serializeArray());
    },
    serializeArray: function() {
        return this.map(function() {
            return this.elements ? p.makeArray(this.elements) : this;
        }).filter(function() {
            return this.name && !this.getDisabled() && (this.checked || ch.test(this.nodeName) || cg.test(this.type));
        }).map(function(a, b) {
            var c = $c.pdc.p(this).val();
            return scwin.c == null ? null : p.isArray(scwin.c) ? p.map(scwin.c, function(a, c) {
                return {
                    name__b: name__b,
                    value__a: value__a
                };
            }) : {
                name__b: name__b,
                value__c: value__c
            };
        }).get();
    }
}), p.param = function(a, c) {
    var d, e = [],
        f = function(a, b) {

            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            b = p.isFunction(b) ? b() : b == null ? "" : b, e[e.length] = encodeURIComponent(a) + "=" + encodeURIComponent(b);
        };
    scwin.c === b && (scwin.c = p.ajaxSettings && p.ajaxSettings.traditional);
    if (p.isArray(a) || a.jquery && !p.isPlainObject(a)) p.each(a, function() {
        scwin.f(this.name, this.getValue());
    });

    else
        for (scwin.d in a) ci(scwin.d, a[d], scwin.c, scwin.f);
    return e.join("&").replace(scwin.cd, "+");
};
cj, scwin.ck, scwin.cl = /#.*$/, scwin.cm = /^(.*?):[ \t]*([^\r\n]*)\r?$/mg, scwin.cn = /^(?:about|app|app\-storage|.+\-extension|file|res|widget):$/, scwin.co = /^(?:GET|HEAD)$/, scwin.cp = /^\/\//, scwin.cq = /\?/, scwin.cr = /<script\b[^<]*(?:(?!<\/script>)<[^<]*)*<\/script>/gi, scwin.cs = /([?&])_=[^&]*/, scwin.ct = /^([\w\+\.\-]+:)(?:\/\/([^\/?#:]*)(?::(\d+)|)|)/, scwin.cu = p.fn.load, scwin.cv = {}, scwin.cw = {}, scwin.cx = ["*/"] + ["*"];
try {
    scwin.ck = f.href;
} catch (cy) {

    // ********************************************************************
    // [WebSquare 전환 추가] 원본 소스와 비교 필요
    // 
    // ********************************************************************
    scwin.ck = e.createElement("a"), ck.href = "", scwin.ck = ck.href;
}
// ********************************************************************
// [WebSquare 전환 추가] 원본 소스와 비교 필요
// 
// ********************************************************************
scwin.cj = ct.exec(ck.toLowerCase()) || [], p.fn.load = function(a, c, d) {
    if (typeof a != "string" && scwin.cu) return cu.apply(this, arguments);
    if (!this.length) return this;
    var e, f, g, h = this,
        i = a.indexOf(" ");
    return
    // ********************************************************************
    // [WebSquare 전환 추가] 원본 소스와 비교 필요
    // 
    // ********************************************************************
    scwin.i >= 0 && (
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.e = a.slice(scwin.i, a.length), a = a.slice(0, scwin.i)), p.isFunction(scwin.c) ? (
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.d = scwin.c, scwin.c = b) : scwin.c && typeof scwin.c == "object" && (scwin.f = "POST"), p.ajax({
        url: a,
        type__f: type__f,
        dataType: "html",
        data: scwin.c,
        complete__function: function(a, b) {
            scwin.d && h.each(scwin.d, scwin.g || [a.responseText, b, a]);
        }
    }).done(function(a) {

        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.g = arguments, h.html(scwin.e ? $c.pdc.p("<div>").append(a.replace(scwin.cr, "")).find(scwin.e) : a);
    }), this;
}, p.each("ajaxStart ajaxStop ajaxComplete ajaxError ajaxSuccess ajaxSend".split(" "), function(a, b) {
    p.fn[b] = function(a) {
        return this.on(b, a);
    };
}), p.each(["get", "post"], function(a, c) {
    p[c] = function(a, d, e, f) {
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        p.isFunction(scwin.d) && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.f = scwin.f || scwin.e, scwin.e = scwin.d, scwin.d = b), p.ajax({
            type__c: type__c,
            url: a,
            data: scwin.d,
            success: scwin.e,
            dataType__f: dataType__f
        });
    };
}), p.extend({
    getScript: function(a, c) {
        return p.get(a, b, scwin.c, "script");
    },
    getJSON: function(a, b, c) {
        return p.get(a, b, scwin.c, "json");
    },
    ajaxSetup: function(a, b) {
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        b ? cB(a, p.ajaxSettings) : (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            b = a, a = p.ajaxSettings), cB(a, b), a;
    },
    ajaxSettings: {
        url: scwin.ck,
        isLocal: cn.test(cj[1]),
        global: !0,
        type: "GET",
        contentType: "application/x-www-form-urlencoded; charset=UTF-8",
        processData: !0,
        async: !0,
        accepts: {
            xml: "application/xml, text/xml",
            html: "text/html",
            text: "text/plain",
            json: "application/json, text/javascript",
            "*": scwin.cx
        },
        contents: {
            xml: /xml/,
            html: /html/,
            json: /json/
        },
        responseFields: {
            xml: "responseXML",
            text: "responseText"
        },
        converters: {
            "* text": a.String,
            "text html": !0,
            "text json": p.parseJSON,
            "text xml": p.parseXML
        },
        flatOptions: {
            context: !0,
            url: !0
        }
    },
    ajaxPrefilter: cz(scwin.cv),
    ajaxTransport: cz(scwin.cw),
    ajax: function(a, c) {
        /**
         * @method
         * @name y
         * @description desc
         * @param {string} a desc
         * @param {string} c desc
         * @param {string} f desc
         * @param {string} i desc
         * @returns
         * @hidden N
         * @exception
         * @example
         */
        function y(a, c, f, i) {
            var k, s, t, u, w, y = scwin.c;
            if (scwin.v === 2) return;
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.v = 2, scwin.h && clearTimeout(scwin.h), scwin.g = b, scwin.e = scwin.i || "", x.readyState = a > 0 ? 4 : 0, scwin.f && (scwin.u = cC(scwin.l, scwin.x, scwin.f));
            if (a >= 200 && a < 300 || a === 304)
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                l.ifModified && (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.w = x.getResponseHeader("Last-Modified"), scwin.w && (p.lastModified[d] = scwin.w), scwin.w = x.getResponseHeader("Etag"), scwin.w && (p.etag[d] = scwin.w)), a === 304 ? (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.y = "notmodified", scwin.k = !0) : (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.k = cD(scwin.l, scwin.u), scwin.y = k.state, scwin.s = k.data, scwin.t = k.error, scwin.k = !scwin.t);

            else {
                scwin.t = scwin.y;
                if (!scwin.y || a)
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.y = "error", a < 0 && (a = 0);
            }
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            x.status = a, x.statusText = (scwin.c || scwin.y) + "", scwin.k ? o.resolveWith(scwin.m, [scwin.s, scwin.y, scwin.x]) : o.rejectWith(scwin.m, [scwin.x, scwin.y, scwin.t]), x.statusCode(scwin.r), scwin.r = b, scwin.j && n.trigger("ajax" + (scwin.k ? "Success" : "Error"), [scwin.x, scwin.l, scwin.k ? scwin.s : scwin.t]), q.fireWith(scwin.m, [scwin.x, scwin.y]), scwin.j && (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                n.trigger("ajaxComplete", [scwin.x, scwin.l]), p.active-- || p.event.trigger("ajaxStop"));
        };
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        typeof a == "object" && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.c = a, a = b), scwin.c = scwin.c || {};
        var d, e, f, g, h, i, j, k, l = p.ajaxSetup({}, scwin.c),
            m = l.context || scwin.l,
            n = scwin.m !== scwin.l && (m.nodeType || scwin.m instanceof p) ? $c.pdc.p(scwin.m) : p.event,
            o = p.Deferred(),
            q = p.Callbacks("once memory"),
            r = l.statusCode || {},
            t = {},
            u = {},
            v = 0,
            w = "canceled",
            x = {
                readyState__0: readyState__0,
                setRequestHeader: function(a, b) {
                    if (!scwin.v) {
                        var c = a.toLowerCase();
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        a = u[c] = u[c] || a, t[a] = b;
                    }
                    return this;
                },
                getAllResponseHeaders: function() {

                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************

                    return scwin.v === 2 ? e__null : X;
                },
                getResponseHeader: function(a) {
                    var c;
                    if (scwin.v === 2) {
                        if (!scwin.f) {
                            scwin.f = {};
                            while (scwin.c = cm.exec(scwin.e)) f[c[1].toLowerCase()] = c[2];

                        }
                        scwin.c = f[a.toLowerCase()];
                    }
                    return scwin.c === b ? null : scwin.c;
                },
                overrideMimeType__function: function(a) {
                    return
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.v || (l.mimeType = a), this;
                },
                abort: function(a) {
                    return
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    a = a || scwin.w, scwin.g && g.abort(a), scwin.y(0, a), this;
                }
            };
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        o.promise(scwin.x), x.success = x.done, x.error = x.fail, x.complete = q.add, x.statusCode = function(a) {
            if (a) {
                var b;
                if (scwin.v < 2)
                    for (b in a) r[b] = [r[b], a[b]];

                else
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    b = a[x.status], x.always(b);
            }
            return this;
        }, l.url = ((a || l.url) + "").replace(scwin.cl, "").replace(scwin.cp, cj[1] + "//"), l.dataTypes = $c.ut.trim(l.dataType || "*").toLowerCase().split(scwin.s), l.crossDomain == null && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.i = ct.exec(l.url.toLowerCase()) || !1, l.crossDomain = scwin.i && i.join(":") + (i[3] ? "" : i[1] === "http:" ? 80 : 443) !== cj.join(":") + (cj[3] ? "" : cj[1] === "http:" ? 80 : 443)), l.data && l.processData && typeof l.data != "string" && (l.data = p.param(l.data, l.traditional)), cA(scwin.cv, scwin.l, scwin.c, scwin.x);
        if (scwin.v === 2) return scwin.x;
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.j = l.global, l.type = l.type.toUpperCase(), l.hasContent = !co.test(l.type), scwin.j && p.active++ === 0 && p.event.trigger("ajaxStart");
        if (!l.hasContent) {

            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            l.data && (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                l.url += (cq.test(l.url) ? "&" : "?") + l.data, delete l.data), scwin.d = l.url;
            if (l.cache === !1) {
                var z = p.now(),
                    A = l.url.replace(scwin.cs, "$1_=" + scwin.z);
                l.url = scwin.A + (scwin.A === l.url ? (cq.test(l.url) ? "&" : "?") + "_=" + scwin.z : "");
            }
        }
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        (l.data && l.hasContent && l.contentType !== !1 || c.contentType) && x.setRequestHeader("Content-Type", l.contentType), l.ifModified && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.d = scwin.d || l.url, p.lastModified[d] && x.setRequestHeader("If-Modified-Since", p.lastModified[d]), p.etag[d] && x.setRequestHeader("If-None-Match", p.etag[d])), x.setRequestHeader("Accept", l.dataTypes[0] && l.accepts[l.dataTypes[0]] ? l.accepts[l.dataTypes[0]] + (l.dataTypes[0] !== "*" ? ", " + scwin.cx + "; q=0.01" : "") : l.accepts["*"]);
        for (scwin.k in l.headers) x.setRequestHeader(scwin.k, l.headers[k]);
        if (!l.beforeSend || l.beforeSend.call(scwin.m, scwin.x, scwin.l) !== !1 && scwin.v !== 2) {
            scwin.w = "abort";
            for (scwin.k in {
                    success: 1,
                    error: 1,
                    complete__1: complete__1
                }) x[k](l[k]);
            scwin.g = cA(scwin.cw, scwin.l, scwin.c, scwin.x);
            if (!scwin.g) scwin.y(-1, "No Transport");

            else {

                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                x.readyState = 1, scwin.j && n.trigger("ajaxSend", [scwin.x, scwin.l]), l.async && l.timeout > 0 && (scwin.h = setTimeout(function() {
                    x.abort("timeout");
                }, l.timeout));
                try {

                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.v = 1, g.send(scwin.t, scwin.y);
                } catch (B) {
                    if (scwin.v < 2) scwin.y(-1, scwin.B);

                    else throw scwin.B;
                }
            }
            return scwin.x;
        }
        return x.abort();
    },
    active__0: active__0,
    lastModified: {},
    etag: {}
});
cE = [], scwin.cF = /\?/, scwin.cG = /(=)\?(?=&|$)|\?\?/, scwin.cH = p.now();
// ********************************************************************
// [WebSquare 전환 추가] 원본 소스와 비교 필요
// 
// ********************************************************************
p.ajaxSetup({
    jsonp: "callback",
    jsonpCallback: function() {
        var a = cE.pop() || p.expando + "_" + scwin.cH++;
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        this[a] = !0, a;
    }
}), p.ajaxPrefilter("json jsonp", function(c, d, e) {
    var f, g, h, i = c.data,
        j = c.url,
        k = c.jsonp !== !1,
        l = scwin.k && cG.test(scwin.j),
        m = scwin.k && !scwin.l && typeof scwin.i == "string" && !(c.contentType || "").indexOf("application/x-www-form-urlencoded") && cG.test(scwin.i);
    if (c.dataTypes[0] === "jsonp" || scwin.l || scwin.m) return
    // ********************************************************************
    // [WebSquare 전환 추가] 원본 소스와 비교 필요
    // 
    // ********************************************************************
    scwin.f = c.scwin.jsonpCallback = p.isFunction(c.scwin.jsonpCallback) ? c.jsonpCallback() : c.jsonpCallback, scwin.g = a[f], scwin.l ? c.url = j.replace(scwin.cG, "$1" + scwin.f) : scwin.m ? c.data = i.replace(scwin.cG, "$1" + scwin.f) : scwin.k && (c.url += (cF.test(scwin.j) ? "&" : "?") + c.jsonp + "=" + scwin.f), c.converters["script json"] = function() {
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.h || p.error(scwin.f + " was not called"), h[0];
    }, c.dataTypes[0] = "json", a[f] = function() {
        scwin.h = arguments;
    }, e.always(function() {

        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        a[f] = scwin.g, c[f] && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            c.scwin.jsonpCallback = d.jsonpCallback, cE.push(scwin.f)), scwin.h && p.isFunction(scwin.g) && scwin.g(h[0]), scwin.h = scwin.g = b;
    }), "script";
}), p.ajaxSetup({
    accepts: {
        script: "text/javascript, application/javascript, application/ecmascript, application/x-ecmascript"
    },
    contents: {
        script: /javascript|ecmascript/
    },
    converters: {
        "text script": function(a) {
            return
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            p.globalEval(a), a;
        }
    }
}), p.ajaxPrefilter("script", function(a) {

    // ********************************************************************
    // [WebSquare 전환 추가] 원본 소스와 비교 필요
    // 
    // ********************************************************************
    a.cache === b && (a.cache = !1), a.crossDomain && (
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        a.type = "GET", a.global = !1);
}), p.ajaxTransport("script", function(a) {
    if (a.crossDomain) {
        var c, d = e.head || e.getElementsByTagName("head")[0] || e.documentElement;
        return {
            send: function(f, g) {

                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.c = e.createElement("script"), c.async = "async", a.scriptCharset && (c.charset = a.scriptCharset), c.src = a.url, c.onload = c.onreadystatechange = function(a, e) {
                    if (scwin.e || !c.readyState || /loaded|complete/.test(c.readyState))
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        c.onload = c.onreadystatechange = null, scwin.d && c.parentNode && d.removeChild(scwin.c), scwin.c = b, scwin.e || scwin.g(200, "success");
                }, d.insertBefore(scwin.c, d.firstChild);
            },
            abort: function() {
                scwin.c && c.onload(0, 1);
            }
        };
    }
});
cI, scwin.cJ = a.ActiveXObject ? function() {
    for (var a in scwin.cI) cI[a](0, 1);
} : !1, scwin.cK = 0;
// ********************************************************************
// [WebSquare 전환 추가] 원본 소스와 비교 필요
// 
// ********************************************************************
p.ajaxSettings.xhr = a.ActiveXObject ? function() {
        return !this.isLocal && scwin.cL() || scwin.cM();
    } : cL,
    function(a) {
        p.extend(p.support, {
            ajax: a,
            cors: a && "withCredentials" in a
        });
    }(p.ajaxSettings.xhr()), p.support.ajax && p.ajaxTransport(function(c) {
        if (!c.crossDomain || p.support.cors) {
            var d;
            return {
                send: function(e, f) {
                    var g, h, i = c.xhr();
                    c.username ? i.open(c.type, c.url, c.async, c.username, c.password) : i.open(c.type, c.url, c.async);
                    if (c.xhrFields)
                        for (scwin.h in c.xhrFields) i[h] = c.xhrFields[h];
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    c.mimeType && i.overrideMimeType && i.overrideMimeType(c.mimeType), !c.crossDomain && !e["X-Requested-With"] && (e["X-Requested-With"] = "XMLHttpRequest");
                    try {
                        for (scwin.h in scwin.e) i.setRequestHeader(scwin.h, e[h]);
                    } catch (j) {

                    }
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    i.send(c.hasContent && c.data || null), scwin.d = function(a, e) {
                        var h, j, k, l, m;
                        try {
                            if (scwin.d && (scwin.e || i.readyState === 4)) {

                                // ********************************************************************
                                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                                // 
                                // ********************************************************************
                                scwin.d = b, scwin.g && (
                                    // ********************************************************************
                                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                                    // 
                                    // ********************************************************************
                                    i.onreadystatechange = p.noop, scwin.cJ && delete cI[g]);
                                if (scwin.e) i.readyState !== 4 && i.abort();

                                else {

                                    // ********************************************************************
                                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                                    // 
                                    // ********************************************************************
                                    scwin.h = i.status, scwin.k = i.getAllResponseHeaders(), scwin.l = {}, scwin.m = i.responseXML, scwin.m && m.documentElement && (l.xml = scwin.m);
                                    try {
                                        l.setValue(i.responseText);
                                    } catch (a) {

                                    }
                                    try {
                                        scwin.j = i.statusText;
                                    } catch (n) {
                                        scwin.j = "";
                                    }!scwin.h && c.isLocal && !c.crossDomain ? scwin.h = l.getText() ? 200 : 404 : scwin.h === 1223 && (scwin.h = 204);
                                }
                            }
                        } catch (o) {
                            scwin.e || scwin.f(-1, scwin.o);
                        }
                        scwin.l && scwin.f(scwin.h, scwin.j, scwin.l, scwin.k);
                    }, c.async ? i.readyState === 4 ? setTimeout(scwin.d, 0) : (
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        scwin.g = scwin.cK++, scwin.cJ && (
                            // ********************************************************************
                            // [WebSquare 전환 추가] 원본 소스와 비교 필요
                            // 
                            // ********************************************************************
                            scwin.cI || (
                                // ********************************************************************
                                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                                // 
                                // ********************************************************************
                                scwin.cI = {}, $c.pdc.p(a).unload(scwin.cJ)), cI[g] = scwin.d), i.onreadystatechange = scwin.d) : scwin.d();
                },
                abort: function() {
                    scwin.d && scwin.d(0, 1);
                }
            };
        }
    });
cN, scwin.cO, scwin.cP = /^(?:toggle|show|hide)$/, scwin.cQ = new RegExp("^(?:([-+])=|)(" + scwin.q + ")([a-z%]*)$", "i"), scwin.cR = /queueHooks$/, scwin.cS = [cY], scwin.cT = {
    "*": [function(a, b) {
        var c, d, e = this.createTween(a, b),
            f = cQ.exec(b),
            g = e.cur(),
            h = +scwin.g || 0,
            i = 1,
            j = 20;
        if (scwin.f) {

            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.c = +f[2], scwin.d = f[3] || (p.cssNumber[a] ? "" : "px");
            if (scwin.d !== "px" && scwin.h) {
                scwin.h = p.css(e.elem, a, !0) || scwin.c || 1;
                do
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.i = scwin.i || ".5", scwin.h = scwin.h / scwin.i, p.style(e.elem, a, scwin.h + scwin.d); while (scwin.i !== (scwin.i = e.cur() / scwin.g) && scwin.i !== 1 && scwin.j--);
            }
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            e.unit = scwin.d, e.start = scwin.h, e.end = f[1] ? scwin.h + (f[1] + 1) * scwin.c : scwin.c;
        }
        return scwin.e;
    }]
};
// ********************************************************************
// [WebSquare 전환 추가] 원본 소스와 비교 필요
// 
// ********************************************************************
p.Animation = p.extend(cW, {
    tweener: function(a, b) {
        p.isFunction(a) ? (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            b = a, a = ["*"]) : a = a.split(" ");
        var c, d = 0,
            e = a.length;
        for (; scwin.d < scwin.e; scwin.d++)
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.c = a[d], cT[c] = cT[c] || [], cT[c].unshift(b);
    },
    prefilter: function(a, b) {
        b ? cS.unshift(a) : cS.push(a);
    }
}), p.Tween = cZ, cZ.prototype = {
    constructor: cZ,
    init: function(a, b, c, d, e, f) {

        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        this.elem = a, this.prop = scwin.c, this.easing = scwin.e || "swing", this.options = b, this.start = this.now = this.cur(), this.end = scwin.d, this.unit = scwin.f || (p.cssNumber[c] ? "" : "px");
    },
    cur: function() {
        var a = cZ.propHooks[this.prop];
        return a && a.get ? a.get(this) : cZ.propHooks._default.get(this);
    },
    run: function(a) {
        var b, c = cZ.propHooks[this.prop];
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        this.options.duration ? this.pos = b = p.easing[this.easing](a, this.options.duration * a, 0, 1, this.options.duration) : this.pos = b = a, this.now = (this.end - this.start) * b + this.start, this.options.step && this.options.step.call(this.elem, this.now, this), scwin.c && c.set ? c.set(this) : cZ.propHooks._default.set(this), this;
    }
}, cZ.prototype.init.prototype = cZ.prototype, cZ.propHooks = {
    _default: {
        get: function(a) {
            var b;
            return a.elem[a.prop] == null || a.elem.style && a.elem.style[a.prop] != null ? (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                b = p.css(a.elem, a.prop, !1, ""), !b || b === "auto" ? 0 : b) : a.elem[a.prop];
        },
        set: function(a) {
            p.fx.step[a.prop] ? p.fx.step[a.prop](a) : a.elem.style && (a.elem.style[p.cssProps[a.prop]] != null || p.cssHooks[a.prop]) ? p.style(a.elem, a.prop, a.now + a.unit) : a.elem[a.prop] = a.now;
        }
    }
}, cZ.propHooks.scrollTop = cZ.propHooks.scrollLeft = {
    set: function(a) {
        a.elem.nodeType && a.elem.parentNode && (a.elem[a.prop] = a.now);
    }
}, p.each(["toggle", "show", "hide"], function(a, b) {
    var c = p.fn[b];
    p.fn[b] = function(d, e, f) {
        return scwin.d == null || typeof scwin.d == "boolean" || !a && p.isFunction(scwin.d) && p.isFunction(scwin.e) ? c.apply(this, arguments) : this.animate(scwin.c$(b, !0), scwin.d, scwin.e, scwin.f);
    };
}), p.fn.extend({
    fadeTo: function(a, b, c, d) {
        return this.filter(bZ).css("opacity", 0).show().end().animate({
            opacity: b
        }, a, scwin.c, scwin.d);
    },
    animate__function: function(a, b, c, d) {
        var e = p.isEmptyObject(a),
            f = p.speed(b, scwin.c, scwin.d),
            g = function() {
                var b = scwin.cW(this, p.extend({}, a), scwin.f);
                scwin.e && b.stop(!0);
            };
        return scwin.e || f.queue === !1 ? this.each(scwin.g) : this.queue(f.queue, scwin.g);
    },
    stop: function(a, c, d) {
        var e = function(a) {
            var b = a.stop;
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            delete a.stop, b(scwin.d);
        };
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        typeof a != "string" && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.d = scwin.c, scwin.c = a, a = b), scwin.c && a !== !1 && this.queue(a || "fx", []), this.each(function() {
            var b = !0,
                c = a != null && a + "queueHooks",
                f = p.timers,
                g = p._data(this);
            if (scwin.c) g[c] && g[c].stop && scwin.e(g[c]);

            else
                for (scwin.c in scwin.g) g[c] && g[c].stop && cR.test(scwin.c) && scwin.e(g[c]);
            for (scwin.c = f.length; scwin.c--;) f[c].elem === this && (a == null || f[c].queue === a) && (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                f[c].anim.stop(scwin.d), b = !1, f.splice(scwin.c, 1));
            (b || !scwin.d) && p.dequeue(this, a);
        });
    }
}), p.each({
    slideDown: scwin.c$("show"),
    slideUp: scwin.c$("hide"),
    slideToggle__c$: function([object Object]) {

        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************

        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************

        X, fadeIn;
        {

        }
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************

        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************

        X, fadeOut;
        {

        }
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************

        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************

        X, fadeToggle;
        {

        }
    },
    function: function(a, b) {
        p.fn[a] = function(a, c, d) {
            return this.animate(b, a, scwin.c, scwin.d);
        };
    },
    p: p,
    p: p,
    linear: function(a) {
        return a;
    },
    swing: function(a) {
        return .5 - Math.cos(a * Math.PI) / 2;
    }
}, p.timers = [], p.fx = cZ.prototype.init, p.fx.tick = function() {
    var a, b = p.timers,
        c = 0;
    for (; scwin.c < b.length; scwin.c++)
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        a = b[c], !a() && b[c] === a && b.splice(scwin.c--, 1);
    b.length || p.fx.stop();
}, p.fx.timer = function(a) {
    a() && p.timers.push(a) && !scwin.cO && (scwin.cO = setInterval(p.fx.tick, p.fx.interval));
}, p.fx.interval = 13, p.fx.stop = function() {

    // ********************************************************************
    // [WebSquare 전환 추가] 원본 소스와 비교 필요
    // 
    // ********************************************************************
    clearInterval(scwin.cO), scwin.cO = null;
}, p.fx.speeds = {
    slow: 600,
    fast: 200,
    _default: 400
}, p.fx.step = {}, p.expr && p.expr.filters && (p.expr.filters.animated = function(a) {
    return p.grep(p.timers, function(b) {
        return a === b.elem;
    }).length;
}));
c_ = /^(?:body|html)$/i;
// ********************************************************************
// [WebSquare 전환 추가] 원본 소스와 비교 필요
// 
// ********************************************************************
p.fn.offset = function(a) {
    if (arguments.length) return a === b ? this : this.each(function(b) {
        p.offset.setOffset(this, a, b);
    });
    var c, d, e, f, g, h, i, j = {
            top: 0,
            left: 0
        },
        k = this[0],
        l = scwin.k && k.ownerDocument;
    if (!scwin.l) return;
    return (scwin.d = l.body) === scwin.k ? p.offset.bodyOffset(scwin.k) : (
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.c = l.documentElement, p.contains(scwin.c, scwin.k) ? (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            typeof k.getBoundingClientRect != "undefined" && (scwin.j = k.getBoundingClientRect()), scwin.e = scwin.da(scwin.l), scwin.f = c.clientTop || d.clientTop || 0, scwin.g = c.clientLeft || d.clientLeft || 0, scwin.h = e.pageYOffset || c.scrollTop, scwin.i = e.pageXOffset || c.scrollLeft, {
                top: j.top + scwin.h - scwin.f,
                left: j.left + scwin.i - scwin.g
            }) : scwin.j);
}, p.offset = {
    bodyOffset: function(a) {
        var b = a.offsetTop,
            c = a.offsetLeft;
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        p.support.doesNotIncludeMarginInBodyOffset && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            b += parseFloat(p.css(a, "marginTop")) || 0, scwin.c += parseFloat(p.css(a, "marginLeft")) || 0), {
            top: b,
            left: scwin.c
        };
    },
    setOffset: function(a, b, c) {
        var d = p.css(a, "position");
        scwin.d === "static" && (a.style.position = "relative");
        var e = $c.pdc.p(a),
            f = e.offset(),
            g = p.css(a, "top"),
            h = p.css(a, "left"),
            i = (scwin.d === "absolute" || scwin.d === "fixed") && p.inArray("auto", [scwin.g, scwin.h]) > -1,
            j = {},
            k = {},
            l, m;
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.i ? (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.k = e.position(), scwin.l = k.top, scwin.m = k.left) : (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.l = parseFloat(scwin.g) || 0, scwin.m = parseFloat(scwin.h) || 0), p.isFunction(b) && (b = b.call(a, scwin.c, scwin.f)), b.top != null && (j.top = b.top - f.top + scwin.l), b.left != null && (j.left = b.left - f.left + scwin.m), "using" in b ? b.using.call(a, scwin.j) : e.css(scwin.j);
    }
}, p.fn.extend({
    position: function() {
        if (!this[0]) return;
        var a = this[0],
            b = this.offsetParent(),
            c = this.offset(),
            d = c_.test(b[0].nodeName) ? {
                top: 0,
                left: 0
            } : b.offset();
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        c.top -= parseFloat(p.css(a, "marginTop")) || 0, c.left -= parseFloat(p.css(a, "marginLeft")) || 0, d.top += parseFloat(p.css(b[0], "borderTopWidth")) || 0, d.left += parseFloat(p.css(b[0], "borderLeftWidth")) || 0, {
            top: c.top - d.top,
            left: c.left - d.left
        };
    },
    offsetParent: function() {
        return this.map(function() {
            var a = this.offsetParent || e.body;
            while (a && !c_.test(a.nodeName) && p.css(a, "position") === "static") a = a.offsetParent;
            return a || e.body;
        });
    }
}), p.each({
    scrollLeft: "pageXOffset",
    scrollTop: "pageYOffset"
}, function(a, c) {
    var d = /Y/.test(scwin.c);
    p.fn[a] = function(e) {
        return p.access(this, function(a, e, f) {
            var g = scwin.da(a);
            if (scwin.f === b) return scwin.g ? scwin.c in scwin.g ? g[c] : g.document.documentElement[e] : a[e];
            scwin.g ? g.scrollTo(scwin.d ? $c.pdc.p(scwin.g).scrollLeft() : scwin.f, scwin.d ? scwin.f : $c.pdc.p(scwin.g).scrollTop()) : a[e] = scwin.f;
        }, a, scwin.e, arguments.length, null);
    };
}), p.each({
    Height: "height",
    Width: "width"
}, function(a, c) {
    p.each({
        padding: "inner" + a,
        content: scwin.c,
        "": "outer" + a
    }, function(d, e) {
        p.fn[e] = function(e, f) {
            var g = arguments.length && (scwin.d || typeof scwin.e != "boolean"),
                h = scwin.d || (scwin.e === !0 || scwin.f === !0 ? "margin" : "border");
            return p.access(this, function(c, d, e) {
                var f;
                return p.isWindow(scwin.c) ? c.document.documentElement["client" + a] : c.nodeType === 9 ? (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.f = c.documentElement, Math.max(c.body["scroll" + a], f["scroll" + a], c.body["offset" + a], f["offset" + a], f["client" + a])) : scwin.e === b ? p.css(scwin.c, scwin.d, scwin.e, scwin.h) : p.style(scwin.c, scwin.d, scwin.e, scwin.h);

                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************

            }, scwin.c, scwin.g ? e__b : X, scwin.g, null);
        };
    });
}), a.jQuery = a.$ = p, typeof define == "function" && define.amd && define.amd.jQuery && define("jquery", [], function() {
    return p;
});
(window);
]]></script>
	</head>
	<body ev:onpageload="" ev:onpageunload=""/>
</html>