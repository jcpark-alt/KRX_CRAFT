# 🚀 WebSquare5 공통함수(gcc) 이관 및 리팩토링 분석 요청서

너는 WebSquare AI 프레임워크 기반의 소스 코드를 분석하고 시스템 아키텍처를 재설계하는 **시니어 소프트웨어 엔지니어이자 코드 분석 전문가**야. 
내가 제공하는 디렉토리 구조와 분석 규칙을 바탕으로, 기존 업무 화면/모듈(`src/as-is/mgt`)의 함수들을 추출하고 공통 모듈(`src/gcc`)로 이관 및 통폐합하기 위한 리팩토링 가이드(MD 파일)를 작성해줘.

---

## 1. 분석 대상 및 디렉토리 구조
- **AS-IS 소스 위치:** `D:\workspace\W_Craft_gcc_20260529\src\mgt` (이하 **mgt 폴더**)
- **TO-BE 공통 위치:** `D:\workspace\W_Craft_gcc_20260529\src\gcc` (이하 **gcc 폴더**)
- **파일 형태:** WebSquare5 화면 또는 스크립트 파일 (`.xml` 내 `<script>` 태그 안의 JavaScript 함수 정의부)

---

## 2. 핵심 분석 및 매핑 가이드라인 (중요)

`mgt` 폴더 내의 XML 파일에서 함수 목록을 추출할 때, 다음 분류 규칙을 최우선으로 적용하여 필터링해줘.

### 🚫 [A 그룹] 이관 제외 및 삭제 대상 함수
아래 5가지 조건 중 하나라도 해당하면 **[삭제 대상]**으로 분류하고, `gcc` 공통함수 매핑 대상에서 완전히 제외해.

1. **달력(Calendar) 관련 함수:** Calendar 컴포넌트 제어, 날짜 선택/연산 등 달력 기능과 관련된 함수 전체.
2. **UI 레이아웃 제어 함수:** 컴포넌트의 크기 조절(`resize`), 위치 변경(`position` - left, top, right, bottom) 관련 함수 전체.
3. **패널 스타일 제어 함수:** 패널(Panel) 컴포넌트의 `style` 속성을 직접 수정하거나 관리하는 함수 전체.
4. **파일 다운로드 함수:** 엑셀 파일 다운로드(Excel Download) 및 일반 파일 다운로드 관련 함수 전체.
5. **진행바(Progress) 관련 함수:** 진행 상태 표시바(Progress Bar) 생성·애니메이션·제어 등 진행바 UI 기능과 관련된 함수 전체.

### 🔄 [B 그룹] 공통함수(gcc) 매핑 및 추가 대상 함수
[A 그룹]에 해당하지 않는 정상 함수들을 대상으로 진행해.

1. **기존 gcc 매핑 함수:** `mgt`에서 추출한 함수의 기능과 동일하거나 유사한 기능을 수행하는 함수가 이미 `gcc` 폴더의 공통 XML 파일에 존재하는 경우.
   - **조치:** `gcc` 폴더 내에 매핑되는 해당 공통 XML 파일에 함수를 추가/보완하도록 기록.
2. **신규 업무공통 함수 (미매핑):** [A 그룹]이 아니면서, 기존 `gcc` 공통함수 파일 어디에도 매핑되지 않는 고유한 비즈니스 공통 기능인 경우.
   - **조치:** 별도의 '업무공통함수'군으로 분류하여 독자적인 API 목록으로 관리.
3. **세션(Session) 관련 함수:** 세션 점검·만료 처리·세션 제어 등 세션 관리 기능인 경우.
   - **조치:** 신규 공통 모듈 `gcc/session.xml`(`$c.session`)을 생성하여 이관한다. (예: `sessionCheck` → `$c.session.sessionCheck`)

---

## 3. 최종 출력 요구사항 (Markdown 포맷)

출력 결과물은 다음 **2가지 마크다운 문서 형태**로 나누어 직관적인 테이블(Table) 구조로 작성해줘.

### 파일 1: `mgt_function_analysis_report.md`
> `mgt` 폴더에서 추출된 전체 함수 목록의 분석 결과와 이관/삭제 여부를 기록하는 마스터 리포트

#### [형식 예시]
## 📊 mgt 폴더 함수 추출 및 분류 마스터 테이블

| 원본 파일명 | 함수명 | 기능 설명 | 분류 결과 | 매핑 대상 (TO-BE) | 비고 / 삭제 사유 |
| :--- | :--- | :--- | :---: | :--- | :--- |
| `mgt_sample.xml` | `scwin.downloadExcel` | 조회 결과를 엑셀로 다운로드 | **삭제 대상** | N/A | [A그룹-4] 엑셀 다운로드 함수 제외 규칙 적용 |
| `mgt_sample.xml` | `scwin.adjustLayout` | 메인 패널의 높이 및 위치 조절 | **삭제 대상** | N/A | [A그룹-2] 레이아웃/Position 제어 함수 제외 |
| `mgt_util.xml` | `scwin.validateEmail` | 이메일 형식 유효성 체크 | **공통 이관** | `src/gcc/validate.xml` | 기존 gcc 공통 파일에 추가 매핑 가능 |
| `mgt_biz.xml` | `scwin.calculateKrxIndex` | KRX 고유 지수 커스텀 계산 로직 | **업무공통** | 별도 관리 파일로 이동 | 기존 gcc에 없는 신규 업무 공통 기능 |

---

### 파일 2: `gcc_mapping_and_biz_common.md`
> 공통함수로 편입될 함수들과 별도 관리할 신규 업무공통함수 리스트를 정리한 명세서

#### [형식 예시]
## 🛠️ 1. 기존 gcc 공통함수 추가 매핑 대상 리스트
- **대상 파일:** `src/gcc/validate.xml`
  - 추가할 함수: `scwin.validateEmail` (설명: 이메일 유효성 체크 기능 보완)
- **대상 파일:** `src/gcc/util.xml`
  - 추가할 함수: ...

## 📂 2. 신규 업무공통함수 관리 리스트 (미매핑 대상)
> 기존 gcc에 존재하지 않아 별도로 관리 및 추가 구현이 필요한 업무 공통 API 목록

| 함수명 | 입력 파라미터 | 반환값 | 기능 요약 및 구현 가이드 |
| :--- | :--- | :--- | :--- |
| `scwin.calculateKrxIndex` | `(Array data, String type)` | `Number` | KRX 거래소 표준 지수식에 맞춘 데이터 연산 및 포맷팅 수행 |

---

## 4. API 문서 생성 위치
D:\workspace\W_Craft_gcc_20260529\src\docs\api 폴더 아래에 신규 폴더에 생성해줘.
예) D:\workspace\W_Craft_gcc_20260529\src\docs\api\mgt

준비가 완료되었다면, 내가 입력하거나 업로드하는 `src/as-is/mgt` 소스 코드 내용을 기반으로 위 규칙들을 엄격히 적용하여 API 마크다운 문서를 작성해줘.