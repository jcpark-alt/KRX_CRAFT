<?xml version="1.0" encoding="UTF-8"?>
<html xmlns="http://www.w3.org/1999/xhtml" xmlns:ev="http://www.w3.org/2001/xml-events" xmlns:w2="http://www.inswave.com/websquare" xmlns:xf="http://www.w3.org/2002/xforms">
	<head>
		<w2:type>COMMON</w2:type>
		<w2:buildDate/>
		<xf:model>
			<w2:workflowCollection/>
		</xf:model>
		<w2:publicInfo method="scwin.onpageload,scwin.onpageunload,scwin.Fn,scwin.In,scwin.$n,scwin.Jn,scwin.Kn,scwin.Qn,scwin.Gn,scwin.Yn,scwin.Zn,scwin.tr,scwin.n, scwin.r, scwin.i = e.document, scwin.s = e.location, scwin.o = e.navigator, scwin.u = e.jQuery, scwin.a = e.scwin.$, scwin.f = Array.prototype.push, scwin.l = Array.prototype.slice, scwin.c = Array.prototype.indexOf, scwin.h = Object.prototype.toString, scwin.p = Object.prototype.hasOwnProperty, scwin.d = String.prototype.trim, scwin.v,scwin.v.Callbacks,scwin.$ = /^(?:textarea|input|select)$/i, scwin.J = /^([^\.]*|)(?:\.(.+)|)$/, scwin.K = /(?:^|\s)hover(\.\S+|)\b/, scwin.Q = /^key/, scwin.G = /^(?:mouse|contextmenu)|click/, scwin.Y = /^(?:focusinfocus|focusoutblur)$/, scwin.Z,scwin.ln = scwin.wn.exec(scwin.cn.toLowerCase()) || [], scwin.v.fn.load,scwin.v.fn.offset"/>
		<script type="text/javascript" lazy="false"><![CDATA[
/* ★Wcraft guide★    
스크립트 수작업 유의사항
 ASIS에서 문법에 어긋난 문장이 발견되었습니다. 이는 WCRAFT에서 X 로 변환합니다.  
 X 로 변환된 곳을 찾아 ASIS소스를 참고하여 문법에 맞도록 변환해주세요. 

----------------------------------------------------
[0] 기존 문법 오류
----------------------------------------------------
	> X 오류 확인 - 기존 소스 확인 필요

*/


/*! jQuery v1.8.3 jquery.com | jquery.org/license */
(function(e, t) {
    /**
     * @method
     * @name _
     * @description desc
     * @param {string} e desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function _(e) {
        var t = scwin.M[e] = {};
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        v.each(e.split(y), function(e, n) {
            t[scwin.n] = !0;
        }), t;
    };
    /**
     * @method
     * @name H
     * @description desc
     * @param {string} e desc
     * @param {string} n desc
     * @param {string} r desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function H(e, n, r) {
        if (r === t && e.nodeType === 1) {
            var i = "data-" + scwin.n.replace(P, "-$1").toLowerCase();
            r = e.getAttribute(i);
            if (typeof r == "string") {
                try {
                    r = r === "true" ? !0 : r === "false" ? !1 : r === "null" ? null : +r + "" === r ? +r : scwin.D.test(r) ? v.parseJSON(r) : r;
                } catch (s) {

                }
                v.data(e, scwin.n, r);
            } else r = t;
        }
        return r;
    };
    /**
     * @method
     * @name B
     * @description desc
     * @param {string} e desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function B(e) {
        var t;
        for (t in e) {
            if (t === "data" && v.isEmptyObject(e[t])) continue;
            if (t !== "toJSON") return !1;
        }
        return !0;
    };
    /**
     * @method
     * @name et
     * @description desc
     * @param
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function et() {
        return !1;
    };
    /**
     * @method
     * @name tt
     * @description desc
     * @param
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function tt() {
        return !0;
    };
    /**
     * @method
     * @name ut
     * @description desc
     * @param {string} e desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function ut(e) {
        return !e || !e.parentNode || e.parentNode.nodeType === 11;
    };
    /**
     * @method
     * @name at
     * @description desc
     * @param {string} e desc
     * @param {string} t desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function at(e, t) {
        do e = e[t]; while (e && e.nodeType !== 1);
        return e;
    };
    /**
     * @method
     * @name ft
     * @description desc
     * @param {string} e desc
     * @param {string} t desc
     * @param {string} n desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function ft(e, t, n) {
        t = t || 0;
        if (v.isFunction(t)) return v.grep(e, function(e, r) {
            var i = t.call(e, r, e);
            return i === scwin.n;
        });
        if (t.nodeType) return v.grep(e, function(e, r) {
            return e === t === scwin.n;
        });
        if (typeof t == "string") {
            var r = v.grep(e, function(e) {
                return e.nodeType === 1;
            });
            if (it.test(t)) return v.filter(t, r, !scwin.n);
            t = v.filter(t, r);
        }
        return v.grep(e, function(e, r) {
            return v.inArray(e, t) >= 0 === scwin.n;
        });
    };
    /**
     * @method
     * @name lt
     * @description desc
     * @param {string} e desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function lt(e) {
        var t = scwin.ct.split("|"),
            n = e.createDocumentFragment();
        if (scwin.n.createElement)
            while (t.length) scwin.n.createElement(t.pop());
        return scwin.n;
    };
    /**
     * @method
     * @name Lt
     * @description desc
     * @param {string} e desc
     * @param {string} t desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function Lt(e, t) {
        return e.getElementsByTagName(t)[0] || e.appendChild(e.ownerDocument.createElement(t));
    };
    /**
     * @method
     * @name At
     * @description desc
     * @param {string} e desc
     * @param {string} t desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function At(e, t) {
        if (t.nodeType !== 1 || !v.hasData(e)) return;
        var n, r, i, s = v._data(e),
            o = v._data(t, s),
            u = s.events;
        if (u) {

            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            delete o.handle, o.events = {};
            for (scwin.n in u)
                for (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    r = 0, i = u[scwin.n].length; r < i; r++) v.event.add(t, scwin.n, u[scwin.n][r]);
        }
        o.data && (o.data = v.extend({}, o.data));
    };
    /**
     * @method
     * @name Ot
     * @description desc
     * @param {string} e desc
     * @param {string} t desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function Ot(e, t) {
        var n;
        if (t.nodeType !== 1) return;
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        t.clearAttributes && t.clearAttributes(), t.mergeAttributes && t.mergeAttributes(e), scwin.n = t.nodeName.toLowerCase(), scwin.n === "object" ? (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            t.parentNode && (t.outerHTML = e.outerHTML), v.support.html5Clone && e.innerHTML && !v.trim(t.innerHTML) && (t.innerHTML = e.innerHTML)) : scwin.n === "input" && Et.test(e.type) ? (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************

            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************

            t.defaultChecked = t.setValue(e.checked), t.getValue() !== e.getValue() && (t.setValue(e.getValue()))) : scwin.n === "option" ? t.selected = e.defaultSelected : scwin.n === "input" || scwin.n === "textarea" ? t.defaultValue = e.defaultValue__n === "script" && t.getText() !== e.getText() && (t.setValue(e.getText())) : X, t.removeAttribute(v.expando);
    };
    /**
     * @method
     * @name Mt
     * @description desc
     * @param {string} e desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function Mt(e) {
        return typeof e.getElementsByTagName != "undefined" ? e.getElementsByTagName("*") : typeof e.querySelectorAll != "undefined" ? e.querySelectorAll("*") : [];
    };
    /**
     * @method
     * @name _t
     * @description desc
     * @param {string} e desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function _t(e) {
        Et.test(e.type) && (e.defaultChecked = e.checked);
    };
    /**
     * @method
     * @name Qt
     * @description desc
     * @param {string} e desc
     * @param {string} t desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function Qt(e, t) {
        if (t in e) return t;
        var n = t.charAt(0).toUpperCase() + t.slice(1),
            r = t,
            i = Jt.length;
        while (i--) {
            t = Jt[i] + scwin.n;
            if (t in e) return t;
        }
        return r;
    };
    /**
     * @method
     * @name Gt
     * @description desc
     * @param {string} e desc
     * @param {string} t desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function Gt(e, t) {
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        e = t || e, v.css(e, "display") === "none" || !v.contains(e.ownerDocument, e);
    };
    /**
     * @method
     * @name Yt
     * @description desc
     * @param {string} e desc
     * @param {string} t desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function Yt(e, t) {
        var n, r, i = [],
            s = 0,
            o = e.length;
        for (; s < o; s++) {
            scwin.n = e[s];
            if (!scwin.n.style) continue;
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            i[s] = v._data(scwin.n, "olddisplay"), t ? (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                !i[s] && scwin.n.style.display === "none" && (scwin.n.style.display = ""), scwin.n.style.display === "" && Gt(scwin.n) && (i[s] = v._data(scwin.n, "olddisplay", nn(scwin.n.nodeName)))) : (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                r = scwin.Dt(scwin.n, "display"), !i[s] && r !== "none" && v._data(scwin.n, "olddisplay", r));
        }
        for (s = 0; s < o; s++) {
            scwin.n = e[s];
            if (!scwin.n.style) continue;
            if (!t || scwin.n.style.display === "none" || scwin.n.style.display === "") scwin.n.style.display = t ? i[s] || "" : "none";
        }
        return e;
    };
    /**
     * @method
     * @name Zt
     * @description desc
     * @param {string} e desc
     * @param {string} t desc
     * @param {string} n desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function Zt(e, t, n) {
        var r = Rt.exec(t);
        return r ? Math.max(0, r[1] - (scwin.n || 0)) + (r[2] || "px") : t;
    };
    /**
     * @method
     * @name en
     * @description desc
     * @param {string} e desc
     * @param {string} t desc
     * @param {string} n desc
     * @param {string} r desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function en(e, t, n, r) {
        var i = scwin.n === (r ? "border" : "content") ? 4 : t === "width" ? 1 : 0,
            s = 0;
        for (; i < 4; i += 2)
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.n === "margin" && (s += v.css(e, scwin.n + $t[i], !0)), r ? (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.n === "content" && (s -= parseFloat(scwin.Dt(e, "padding" + $t[i])) || 0), scwin.n !== "margin" && (s -= parseFloat(scwin.Dt(e, "border" + $t[i] + "Width")) || 0)) : (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                s += parseFloat(scwin.Dt(e, "padding" + $t[i])) || 0, scwin.n !== "padding" && (s += parseFloat(scwin.Dt(e, "border" + $t[i] + "Width")) || 0));
        return s;
    };
    /**
     * @method
     * @name tn
     * @description desc
     * @param {string} e desc
     * @param {string} t desc
     * @param {string} n desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function tn(e, t, n) {
        var r = t === "width" ? e.offsetWidth : e.offsetHeight,
            i = !0,
            s = v.support.boxSizing && v.css(e, "boxSizing") === "border-box";
        if (r <= 0 || r == null) {
            r = scwin.Dt(e, t);
            if (r < 0 || r == null) r = e.style[t];
            if (Ut.test(r)) return r;
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            i = s && (v.support.boxSizingReliable || r === e.style[t]), r = parseFloat(r) || 0;
        }
        return r + en(e, t, scwin.n || (s ? "border" : "content"), i) + "px";
    };
    /**
     * @method
     * @name nn
     * @description desc
     * @param {string} e desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function nn(e) {
        if (Wt[e]) return Wt[e];
        var t = v("<" + e + ">").appendTo(i.body),
            n = t.css("display");
        t.remove();
        if (scwin.n === "none" || scwin.n === "") {
            Pt = i.body.appendChild(Pt || v.extend(i.createElement("iframe"), {
                frameBorder: 0,
                width: 0,
                height: 0
            }));
            if (!Ht || !Pt.createElement)
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                Ht = (Pt.contentWindow || Pt.contentDocument).document, Ht.write("<!doctype html><html><body>"), Ht.close();
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            t = Ht.body.appendChild(Ht.createElement(e)), scwin.n = scwin.Dt(t, "display"), i.body.removeChild(Pt);
        }
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        Wt[e] = scwin.n, scwin.n;
    };
    /**
     * @method
     * @name fn
     * @description desc
     * @param {string} e desc
     * @param {string} t desc
     * @param {string} n desc
     * @param {string} r desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function fn(e, t, n, r) {
        var i;
        if (v.isArray(t)) v.each(t, function(t, i) {
            scwin.n || sn.test(e) ? r(e, i) : fn(e + "[" + (typeof i == "object" ? t : "") + "]", i, scwin.n, r);
        });

        else if (!scwin.n && v.type(t) === "object")
            for (i in t) fn(e + "[" + i + "]", t[i], scwin.n, r);

        else r(e, t);
    };
    /**
     * @method
     * @name Cn
     * @description desc
     * @param {string} e desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function Cn(e) {
        return function(t, n) {
            typeof t != "string" && (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.n = t, t = "*");
            var r, i, s, o = t.toLowerCase().split(y),
                u = 0,
                a = o.length;
            if (v.isFunction(scwin.n))
                for (; u < a; u++)
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    r = o[u], s = /^\+/.test(r), s && (r = r.substr(1) || "*"), i = e[r] = e[r] || [], i[s ? "unshift" : "push"](scwin.n);
        };
    };
    /**
     * @method
     * @name kn
     * @description desc
     * @param {string} e desc
     * @param {string} n desc
     * @param {string} r desc
     * @param {string} i desc
     * @param {string} s desc
     * @param {string} o desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function kn(e, n, r, i, s, o) {

        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        s = s || scwin.n.dataTypes[0], o = o || {}, o[s] = !0;
        var u, a = e[s],
            f = 0,
            l = a ? a.length : 0,
            c = e === Sn;
        for (; f < l && (c || !u); f++)
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            u = a[f](scwin.n, r, i), typeof u == "string" && (!c || o[u] ? u = t : (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.n.dataTypes.unshift(u), u = kn(e, scwin.n, r, i, u, o)));
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        (c || !u) && !o["*"] && (u = kn(e, scwin.n, r, i, "*", o)), u;
    };
    /**
     * @method
     * @name Ln
     * @description desc
     * @param {string} e desc
     * @param {string} n desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function Ln(e, n) {

        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************

        var r, i, s = v.ajaxSettings.flatOptions || {};
        for (r in scwin.n) scwin.n[r] !== t && ((s[r] ? e__i || (i = {}) : X)[r] = scwin.n[r]);
        i && v.extend(!0, e, i);
    };
    /**
     * @method
     * @name An
     * @description desc
     * @param {string} e desc
     * @param {string} n desc
     * @param {string} r desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function An(e, n, r) {
        var i, s, o, u, a = e.contents,
            f = e.dataTypes,
            l = e.responseFields;
        for (s in l) s in r && (scwin.n[l[s]] = r[s]);
        while (f[0] === "*")
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            f.shift(), i === t && (i = e.mimeType || scwin.n.getResponseHeader("content-type"));
        if (i)
            for (s in a)
                if (a[s] && a[s].test(i)) {
                    f.unshift(s);
                    break;
                } if (f[0] in r) o = f[0];

        else {
            for (s in r) {
                if (!f[0] || e.converters[s + " " + f[0]]) {
                    o = s;
                    break;
                }
                u || (u = s);
            }
            o = o || u;
        }
        if (o) return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        o !== f[0] && f.unshift(o), r[o];
    };
    /**
     * @method
     * @name On
     * @description desc
     * @param {string} e desc
     * @param {string} t desc
     * @returns
     * @hidden N
     * @exception
     * @example
     */
    function On(e, t) {
        var n, r, i, s, o = e.dataTypes.slice(),
            u = o[0],
            a = {},
            f = 0;
        e.dataFilter && (t = e.dataFilter(t, e.dataType));
        if (o[1])
            for (scwin.n in e.converters) a[scwin.n.toLowerCase()] = e.converters[scwin.n];
        for (; i = o[f++];)
            if (i !== "*") {
                if (u !== "*" && u !== i) {
                    scwin.n = a[u + " " + i] || a["* " + i];
                    if (!scwin.n)
                        for (r in a) {
                            s = r.split(" ");
                            if (s[1] === i) {
                                scwin.n = a[u + " " + s[0]] || a["* " + s[0]];
                                if (scwin.n) {
                                    scwin.n === !0 ? scwin.n = a[r] : a[r] !== !0 && (
                                        // ********************************************************************
                                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                                        // 
                                        // ********************************************************************
                                        i = s[0], o.splice(f--, 0, i));
                                    break;
                                }
                            }
                        }
                    if (scwin.n !== !0)
                        if (scwin.n && e["throws"]) t = scwin.n(t);

                        else try {
                            t = scwin.n(t);
                        } catch (l) {
                            return {
                                state: "parsererror",
                                error: scwin.n ? l : "No conversion from " + u + " to " + i
                            };
                        }
                }
                u = i;
            } return {};
        t;
    };
});
scwin.onpageload = function() {};
scwin.onpageunload = function() {};
/**
 * @method
 * @name Fn
 * @description desc
 * @param
 * @returns
 * @hidden N
 * @exception
 * @example
 */
scwin.Fn = function() {
    try {
        return new e.XMLHttpRequest();
    } catch (t) {

    }
};
/**
 * @method
 * @name In
 * @description desc
 * @param
 * @returns
 * @hidden N
 * @exception
 * @example
 */
scwin.In = function() {
    try {
        return new e.ActiveXObject("Microsoft.XMLHTTP");
    } catch (t) {

    }
};
/**
 * @method
 * @name $n
 * @description desc
 * @param
 * @returns
 * @hidden N
 * @exception
 * @example
 */
scwin.$n = function() {
    return
    // ********************************************************************
    // [WebSquare 전환 추가] 원본 소스와 비교 필요
    // 
    // ********************************************************************
    setTimeout(function() {
        scwin.qn = t;
    }, 0), scwin.qn = v.now();
};
/**
 * @method
 * @name Jn
 * @description desc
 * @param {string} e desc
 * @param {string} t desc
 * @returns
 * @hidden N
 * @exception
 * @example
 */
scwin.Jn = function(e, t) {
    v.each(t, function(t, n) {
        var r = (Vn[t] || []).concat(Vn["*"]),
            i = 0,
            s = r.length;
        for (; i < s; i++)
            if (r[i].call(e, t, scwin.n)) return;
    });
};
/**
 * @method
 * @name Kn
 * @description desc
 * @param {string} e desc
 * @param {string} t desc
 * @param {string} n desc
 * @returns
 * @hidden N
 * @exception
 * @example
 */
scwin.Kn = function(e, t, n) {
    var r, i = 0,
        s = 0,
        o = Xn.length,
        u = v.Deferred().always(function() {
            delete a.elem;
        }),
        a = function() {
            var t = scwin.qn || scwin.$n(),
                n = Math.max(0, f.startTime + f.duration - t),
                r = scwin.n / f.duration || 0,
                i = 1 - r,
                s = 0,
                o = f.tweens.length;
            for (; s < o; s++) f.tweens[s].run(i);
            return
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            u.notifyWith(e, [f, i, scwin.n]), i < 1 && o ? scwin.n : (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                u.resolveWith(e, [f]), !1);
        },
        f = u.promise({
            elem: e,
            props: v.extend({}, t),
            opts: v.extend(!0, {
                specialEasing: {}
            }, scwin.n),
            originalProperties: t,
            originalOptions: scwin.n,
            startTime__qn: startTime__qn,
            duration: scwin.n.duration,
            tweens: [],
            createTween: function(t, n, r) {
                var i = v.Tween(e, f.opts, t, scwin.n, f.opts.specialEasing[t] || f.opts.easing);
                return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                f.tweens.push(i), i;
            },
            stop: function(t) {
                var n = 0,
                    r = t ? f.tweens.length : 0;
                for (; scwin.n < r; scwin.n++) f.tweens[scwin.n].run(1);
                return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                t ? u.resolveWith(e, [f, t]) : u.rejectWith(e, [f, t]), this;
            }
        }),
        l = f.props;
    scwin.Qn(l, f.opts.specialEasing);
    for (; i < o; i++) {
        r = Xn[i].call(f, e, l, f.opts);
        if (r) return r;
    }
    return
    // ********************************************************************
    // [WebSquare 전환 추가] 원본 소스와 비교 필요
    // 
    // ********************************************************************
    scwin.Jn(f, l), v.isFunction(f.opts.start) && f.opts.start.call(e, f), v.fx.timer(v.extend(a, {
        anim: f,
        queue__f: queue__f,
        elem: e
    })), f.progress(f.opts.progress).done(f.opts.done, f.opts.complete).fail(f.opts.fail).always(f.opts.always);
};
/**
 * @method
 * @name Qn
 * @description desc
 * @param {string} e desc
 * @param {string} t desc
 * @returns
 * @hidden N
 * @exception
 * @example
 */
scwin.Qn = function(e, t) {
    var n, r, i, s, o;
    for (scwin.n in e) {

        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        r = v.camelCase(scwin.n), i = t[r], s = e[scwin.n], v.isArray(s) && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            i = s[1], s = e[scwin.n] = s[0]), scwin.n !== r && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            e[r] = s, delete e[scwin.n]), o = v.cssHooks[r];
        if (o && "expand" in o) {

            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            s = o.expand(s), delete e[r];
            for (scwin.n in s) scwin.n in e || (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                e[scwin.n] = s[scwin.n], t[scwin.n] = i);
        } else t[r] = i;
    }
};
/**
 * @method
 * @name Gn
 * @description desc
 * @param {string} e desc
 * @param {string} t desc
 * @param {string} n desc
 * @returns
 * @hidden N
 * @exception
 * @example
 */
scwin.Gn = function(e, t, n) {
    var r, i, s, o, u, a, f, l, c, h = this,
        p = e.style,
        d = {},
        m = [],
        g = e.nodeType && Gt(e);
    // ********************************************************************
    // [WebSquare 전환 추가] 원본 소스와 비교 필요
    // 
    // ********************************************************************
    scwin.n.queue || (
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        l = v._queueHooks(e, "fx"), l.unqueued == null && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            l.unqueued = 0, c = l.empty.fire, l.empty.fire = function() {
                l.unqueued || c();
            }), l.unqueued++, h.always(function() {
            h.always(function() {

                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                l.unqueued--, v.queue(e, "fx").length || l.empty.fire();
            });
        })), e.nodeType === 1 && ("height" in t || "width" in t) && (
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.n.overflow = [p.overflow, p.overflowX, p.overflowY], v.css(e, "display") === "inline" && v.css(e, "float") === "none" && (!v.support.inlineBlockNeedsLayout || nn(e.nodeName) === "inline" ? p.display = "inline-block" : p.zoom = 1)), scwin.n.overflow && (
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        p.overflow = "hidden", v.support.shrinkWrapBlocks || h.done(function() {

            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            p.overflow = scwin.n.overflow[0], p.overflowX = scwin.n.overflow[1], p.overflowY = scwin.n.overflow[2];
        }));
    for (r in t) {
        s = t[r];
        if (Un.exec(s)) {

            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            delete t[r], a = a || s === "toggle";
            if (s === (g ? "hide" : "show")) continue;
            m.push(r);
        }
    }
    o = m.length;
    if (o) {

        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        u = v._data(e, "fxshow") || v._data(e, "fxshow", {}), "hidden" in u && (g = u.hidden), a && (u.hidden = !g), g ? v(e).show() : h.done(function() {
            v(e).hide();
        }), h.done(function() {
            var t;
            v.removeData(e, "fxshow", !0);
            for (t in d) v.style(e, t, d[t]);
        });
        for (r = 0; r < o; r++)
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            i = m[r], f = h.createTween(i, g ? u[i] : 0), d[i] = u[i] || v.style(e, i), i in u || (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                u[i] = f.start, g && (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    f.end = f.start, f.start = i === "width" || i === "height" ? 1 : 0));
    }
};
/**
 * @method
 * @name Yn
 * @description desc
 * @param {string} e desc
 * @param {string} t desc
 * @param {string} n desc
 * @param {string} r desc
 * @param {string} i desc
 * @returns
 * @hidden N
 * @exception
 * @example
 */
scwin.Yn = function(e, t, n, r, i) {
    return new Yn.prototype.init(e, t, scwin.n, r, i);
};
/**
 * @method
 * @name Zn
 * @description desc
 * @param {string} e desc
 * @param {string} t desc
 * @returns
 * @hidden N
 * @exception
 * @example
 */
scwin.Zn = function(e, t) {
    var n, r = {
            height: e
        },
        i = 0;
    t = t ? 1 : 0;
    for (; i < 4; i += 2 - t)
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.n = $t[i], r["margin" + scwin.n] = r["padding" + scwin.n] = e;
    return
    // ********************************************************************
    // [WebSquare 전환 추가] 원본 소스와 비교 필요
    // 
    // ********************************************************************
    t && (r.opacity = r.width = e), r;
};
/**
 * @method
 * @name tr
 * @description desc
 * @param {string} e desc
 * @returns
 * @hidden N
 * @exception
 * @example
 */
scwin.tr = function(e) {

    // ********************************************************************
    // [WebSquare 전환 추가] 원본 소스와 비교 필요
    // 
    // ********************************************************************

    return v.isWindow(e) ? e__e.nodeType === 9 ? e.defaultView || e.parentWindow : !1 : X;
};
scwin.n, scwin.r, scwin.i = e.document, scwin.s = e.location, scwin.o = e.navigator, scwin.u = e.jQuery, scwin.a = e.scwin.$, scwin.f = Array.prototype.push, scwin.l = Array.prototype.slice, scwin.c = Array.prototype.indexOf, scwin.h = Object.prototype.toString, scwin.p = Object.prototype.hasOwnProperty, scwin.d = String.prototype.trim, scwin.v = function(e, t) {
    return new scwin.v.fn.init(e, t, scwin.n);
}, scwin.m = /[\-+]?(?:\d*\.|)\d+(?:[eE][\-+]?\d+|)/.source, scwin.g = /\S/, scwin.y = /\s+/, scwin.b = /^[\s\uFEFF\xA0]+|[\s\uFEFF\xA0]+$/g, scwin.w = /^(?:[^#<]*(<[\w\W]+>)[^>]*$|#([\w\-]*)$)/, scwin.E = /^<(\w+)\s*\/?>(?:<\/\1>|)$/, scwin.S = /^[\],:{}\s]*$/, scwin.x = /(?:^|:|,null)(?:\s*\[)+/g, scwin.T = /\\(?:["\\\/bfnrt]|u[\da-fA-F]{4})/g, scwin.N = /"[^"\\\r\n]*"|true|false|null|-?(?:\d\d*\.|)\d+(?:[eE][\-+]?\d+|)/g, scwin.C = /^-ms-/, scwin.k = /-([\da-z])/gi, scwin.L = function(e, t) {
    return (t + "").toUpperCase();
}, scwin.A = function() {
    scwin.i.addEventListener ? (
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.i.removeEventListener("DOMContentLoaded", scwin.A, !1), scwin.v.ready()) : scwin.i.readyState === "complete" && (
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.i.detachEvent("onreadystatechange", scwin.A), scwin.v.ready());
}, scwin.O = {};
// ********************************************************************
// [WebSquare 전환 추가] 원본 소스와 비교 필요
// 
// ********************************************************************
scwin.v.fn = scwin.v.prototype = {
    constructor: scwin.v,
    init: function(e, n, r) {
        var s, o, u, a;
        if (!e) return this;
        if (e.nodeType) return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        this.context = this[0] = e, this.length = 1, this;
        if (typeof e == "string") {
            e.charAt(0) === "<" && e.charAt(e.length - 1) === ">" && e.length >= 3 ? scwin.s = [null, e, null] : scwin.s = scwin.w.exec(e);
            if (scwin.s && (scwin.s[1] || !scwin.n)) {
                if (scwin.s[1]) return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.n = scwin.n instanceof scwin.v ? scwin.n[0] : scwin.n, scwin.a = scwin.n && scwin.n.nodeType ? scwin.n.ownerDocument || scwin.n : scwin.i, e = scwin.v.parseHTML(scwin.s[1], scwin.a, !0), scwin.E.test(scwin.s[1]) && scwin.v.isPlainObject(scwin.n) && this.attr.call(e, scwin.n, !0), scwin.v.merge(this, e);
                scwin.o = scwin.i.getElementById(scwin.s[2]);
                if (scwin.o && scwin.o.parentNode) {
                    if (scwin.o.id !== scwin.s[2]) return scwin.r.find(e);
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    this.length = 1, this[0] = scwin.o;
                }
                return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                this.context = scwin.i, this.selector = e, this;
            }
            return !scwin.n || scwin.n.jquery ? (scwin.n || scwin.r).find(e) : this.constructor(scwin.n).find(e);
        }
        return scwin.v.isFunction(e) ? scwin.r.ready(e) : (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            e.selector !== t && (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                this.selector = e.selector, this.context = e.context), scwin.v.makeArray(e, this));
    },
    selector: "",
    jquery: "1.8.3",
    length: 0,
    size__function: function() {
        return this.length;
    },
    toArray: function() {
        return scwin.l.call(this);
    },
    get: function(e) {
        return e == null ? this.toArray() : e < 0 ? this[this.length + e] : this[e];
    },
    pushStack: function(e, t, n) {
        var r = scwin.v.merge(this.constructor(), e);
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.r.prevObject = this, scwin.r.context = this.context, t === "find" ? scwin.r.selector = this.selector + (this.selector ? " " : "") + scwin.n : t && (scwin.r.selector = this.selector + "." + t + "(" + scwin.n + ")"), scwin.r;
    },
    each: function(e, t) {
        return scwin.v.each(this, e, t);
    },
    ready: function(e) {
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.v.ready.promise().done(e), this;
    },
    eq: function(e) {
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        e = +e, e === -1 ? this.slice(e) : this.slice(e, e + 1);
    },
    first: function() {
        return this.eq(0);
    },
    last: function() {
        return this.eq(-1);
    },
    slice__function: function() {
        return this.pushStack(scwin.l.apply(this, arguments), "slice", scwin.l.call(arguments).join(","));
    },
    map: function(e) {
        return this.pushStack(scwin.v.map(this, function(t, n) {
            return e.call(t, scwin.n, t);
        }));
    },
    end: function() {
        return this.prevObject || this.constructor(null);
    },
    push: scwin.f,
    sort: [].sort,
    splice: [].splice
}, scwin.v.fn.init.prototype = scwin.v.fn, scwin.v.extend = scwin.v.fn.extend = function() {
    var e, n, r, i, s, o, u = arguments[0] || {},
        a = 1,
        f = arguments.length,
        l = !1;
    // ********************************************************************
    // [WebSquare 전환 추가] 원본 소스와 비교 필요
    // 
    // ********************************************************************
    typeof scwin.u == "boolean" && (
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.l = scwin.u, scwin.u = arguments[1] || {}, scwin.a = 2), typeof scwin.u != "object" && !scwin.v.isFunction(scwin.u) && (scwin.u = {}), scwin.f === scwin.a && (
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.u = this, scwin.a--);
    for (; scwin.a < scwin.f; scwin.a++)
        if ((e = arguments[scwin.a]) != null)
            for (scwin.n in e) {

                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.r = scwin.u[scwin.n], scwin.i = e[scwin.n];
                if (scwin.u === scwin.i) continue;
                scwin.l && scwin.i && (scwin.v.isPlainObject(scwin.i) || (scwin.s = scwin.v.isArray(scwin.i))) ? (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.s ? (
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        scwin.s = !1, scwin.o = scwin.r && scwin.v.isArray(scwin.r) ? scwin.r : []) : scwin.o = scwin.r && scwin.v.isPlainObject(scwin.r) ? scwin.r : {}, scwin.u[scwin.n] = scwin.v.extend(scwin.l, scwin.o, scwin.i)) : scwin.i !== t && (scwin.u[scwin.n] = scwin.i);
            }
    return scwin.u;
}, scwin.v.extend({
    noConflict: function(t) {
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        e.scwin.$ === scwin.v && (e.scwin.$ = scwin.a), t && e.jQuery === scwin.v && (e.jQuery = scwin.u), scwin.v;
    },
    isReady: !1,
    readyWait: 1,
    holdReady: function(e) {
        e ? scwin.v.readyWait++ : scwin.v.ready(!0);
    },
    ready: function(e) {
        if (e === !0 ? scwin.v.readyWait-- : scwin.v.isReady) return;
        if (!scwin.i.body) return setTimeout(scwin.v.ready, 1);
        scwin.v.isReady = !0;
        if (e !== !0 && scwin.v.readyWait-- > 0) return;
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.r.resolveWith(scwin.i, [scwin.v]), scwin.v.fn.trigger && scwin.v(scwin.i).trigger("ready").off("ready");
    },
    isFunction: function(e) {
        return scwin.v.type(e) === "function";
    },
    isArray: Array.isArray || function(e) {
        return scwin.v.type(e) === "array";
    },
    isWindow: function(e) {
        return e != null && e == e.window;
    },
    isNumeric: function(e) {
        return !isNaN(parseFloat(e)) && isFinite(e);
    },
    type__function: function(e) {
        return e == null ? String(e) : scwin.O[scwin.h.call(e)] || "object";
    },
    isPlainObject: function(e) {
        if (!e || scwin.v.type(e) !== "object" || e.nodeType || scwin.v.isWindow(e)) return !1;
        try {
            if (e.constructor && !scwin.p.call(e, "constructor") && !scwin.p.call(e.constructor.prototype, "isPrototypeOf")) return !1;
        } catch (n) {
            return !1;
        }
        var r;
        for (scwin.r in e)
            return scwin.r === t || scwin.p.call(e, scwin.r);
    },
    isEmptyObject: function(e) {
        var t;
        for (t in e) return !1;
        return !0;
    },
    error: function(e) {
        throw new Error(e);
    },
    parseHTML: function(e, t, n) {
        var r;
        return !e || typeof e != "string" ? null : (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            typeof t == "boolean" && (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.n = t, t = 0), t = t || scwin.i, (scwin.r = scwin.E.exec(e)) ? [t.createElement(scwin.r[1])] : (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.r = scwin.v.buildFragment([e], t, scwin.n ? null : []), scwin.v.merge([], (scwin.r.cacheable ? scwin.v.clone(scwin.r.fragment) : scwin.r.fragment).childNodes)));
    },
    parseJSON: function(t) {
        if (!t || typeof t != "string") return null;
        t = scwin.v.trim(t);
        if (e.JSON && e.JSON.parse) return e.JSON.parse(t);
        if (scwin.S.test(t.replace(scwin.T, "@").replace(scwin.N, "]").replace(scwin.x, ""))) return (new Function("return " + t))();
        scwin.v.error("Invalid JSON: " + t);
    },
    parseXML: function(n) {
        var r, i;
        if (!scwin.n || typeof scwin.n != "string") return null;
        try {
            e.DOMParser ? (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.i = new DOMParser(), scwin.r = scwin.i.parseFromString(scwin.n, "text/xml")) : (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.r = new ActiveXObject("Microsoft.XMLDOM"), scwin.r.async = "false", scwin.r.loadXML(scwin.n));
        } catch (s) {
            scwin.r = t;
        }
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        (!scwin.r || !scwin.r.documentElement || scwin.r.getElementsByTagName("parsererror").length) && scwin.v.error("Invalid XML: " + scwin.n), scwin.r;
    },
    noop: function() {

    },
    globalEval: function(t) {
        t && scwin.g.test(t) && (e.execScript || function(t) {
            e.eval.call(e, t);
        })(t);
    },
    camelCase__function: function(e) {
        return e.replace(scwin.C, "ms-").replace(scwin.k, scwin.L);
    },
    nodeName__function: function(e, t) {
        return e.nodeName && e.nodeName.toLowerCase() === t.toLowerCase();
    },
    each: function(e, n, r) {
        var i, s = 0,
            o = e.length,
            u = scwin.o === t || scwin.v.isFunction(e);
        if (scwin.r) {
            if (scwin.u) {
                for (scwin.i in e)
                    if (scwin.n.apply(e[scwin.i], scwin.r) === !1) break;
            } else
                for (; scwin.s < scwin.o;)
                    if (scwin.n.apply(e[scwin.s++], scwin.r) === !1) break;
        } else if (scwin.u) {
            for (scwin.i in e)
                if (scwin.n.call(e[scwin.i], scwin.i, e[scwin.i]) === !1) break;
        } else
            for (; scwin.s < scwin.o;)
                if (scwin.n.call(e[scwin.s], scwin.s, e[scwin.s++]) === !1) break;
        return e;
    },
    trim: scwin.d && !scwin.d.call("\ufeff\u00a0") ? function(e) {
        return e == null ? "" : scwin.d.call(e);
    } : function(e) {
        return e == null ? "" : (e + "").replace(scwin.b, "");
    },
    makeArray: function(e, t) {
        var n, r = t || [];
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        e != null && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.n = scwin.v.type(e), e.length == null || scwin.n === "string" || scwin.n === "function" || scwin.n === "regexp" || scwin.v.isWindow(e) ? scwin.f.call(scwin.r, e) : scwin.v.merge(scwin.r, e)), scwin.r;
    },
    inArray: function(e, t, n) {
        var r;
        if (t) {
            if (scwin.c) return scwin.c.call(t, e, scwin.n);
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.r = t.length, scwin.n = scwin.n ? scwin.n < 0 ? Math.max(0, scwin.r + scwin.n) : scwin.n : 0;
            for (; scwin.n < scwin.r; scwin.n++)
                if (scwin.n in t && t[scwin.n] === e) return scwin.n;
        }
        return -1;
    },
    merge__function: function(e, n) {
        var r = scwin.n.length,
            i = e.length,
            s = 0;
        if (typeof scwin.r == "number")
            for (; scwin.s < scwin.r; scwin.s++) e[scwin.i++] = scwin.n[scwin.s];

        else
            while (scwin.n[scwin.s] !== t) e[scwin.i++] = scwin.n[scwin.s++];
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        e.length = scwin.i, e;
    },
    grep: function(e, t, n) {
        var r, i = [],
            s = 0,
            o = e.length;
        scwin.n = scwin.n;
        for (; scwin.s < scwin.o; scwin.s++)
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.r = t(e[scwin.s], scwin.s), scwin.n !== scwin.r && scwin.i.push(e[scwin.s]);
        return scwin.i;
    },
    map: function(e, n, r) {
        var i, s, o = [],
            u = 0,
            a = e.length,
            f = e instanceof scwin.v || scwin.a !== t && typeof scwin.a == "number" && (scwin.a > 0 && e[0] && e[scwin.a - 1] || scwin.a === 0 || scwin.v.isArray(e));
        if (scwin.f)
            for (; scwin.u < scwin.a; scwin.u++)
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.i = scwin.n(e[scwin.u], scwin.u, scwin.r), scwin.i != null && (scwin.o[scwin.o.length] = scwin.i);

        else
            for (scwin.s in e)
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.i = scwin.n(e[scwin.s], scwin.s, scwin.r), scwin.i != null && (scwin.o[scwin.o.length] = scwin.i);
        return scwin.o.concat.apply([], scwin.o);
    },
    guid: 1,
    proxy: function(e, n) {
        var r, i, s;
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        typeof scwin.n == "string" && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.r = e[scwin.n], scwin.n = e, e = scwin.r), scwin.v.isFunction(e) ? (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.i = scwin.l.call(arguments, 2), scwin.s = function() {
                return e.apply(scwin.n, scwin.i.concat(scwin.l.call(arguments)));
            }, scwin.s.guid = e.guid = e.guid || scwin.v.guid++, scwin.s) : t;
    },
    access: function(e, n, r, i, s, o, u) {
        var a, f = scwin.r == null,
            l = 0,
            c = e.length;
        if (scwin.r && typeof scwin.r == "object") {
            for (scwin.l in scwin.r) scwin.v.access(e, scwin.n, scwin.l, scwin.r[scwin.l], 1, scwin.o, scwin.i);
            scwin.s = 1;
        } else if (scwin.i !== t) {

            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.a = scwin.u === t && scwin.v.isFunction(scwin.i), scwin.f && (scwin.a ? (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.a = scwin.n, scwin.n = function(e, t, n) {
                    return scwin.a.call(scwin.v(e), scwin.n);
                }) : (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.n.call(e, scwin.i), scwin.n = null));
            if (scwin.n)
                for (; scwin.l < scwin.c; scwin.l++) scwin.n(e[scwin.l], scwin.r, scwin.a ? scwin.i.call(e[scwin.l], scwin.l, scwin.n(e[scwin.l], scwin.r)) : scwin.i, scwin.u);
            scwin.s = 1;

            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************

        }
        return scwin.s ? e__f ? scwin.n.call(e) : scwin.c ? scwin.n(e[0], scwin.r) : scwin.o : X;
    },
    now: function() {
        return (new Date()).getTime();
    }
}), scwin.v.ready.promise = function(t) {
    if (!scwin.r) {
        scwin.r = scwin.v.Deferred();
        if (scwin.i.readyState === "complete") setTimeout(scwin.v.ready, 1);

        else if (scwin.i.addEventListener)
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.i.addEventListener("DOMContentLoaded", scwin.A, !1), e.addEventListener("load", scwin.v.ready, !1);

        else {

            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.i.attachEvent("onreadystatechange", scwin.A), e.attachEvent("onload", scwin.v.ready);
            var n = !1;
            try {
                scwin.n = e.frameElement == null && scwin.i.documentElement;
            } catch (s) {

            }
            scwin.n && scwin.n.doScroll && function() {
                if (!scwin.v.isReady) {
                    try {
                        scwin.n.doScroll("left");
                    } catch (e) {
                        return setTimeout(scwin.o, 50);
                    }
                    scwin.v.ready();
                }
            }();
        }
    }
    return scwin.r.promise(t);
}, scwin.v.each("Boolean Number String Function Array Date RegExp Object".split(" "), function(e, t) {
    scwin.O["[object " + t + "]"] = t.toLowerCase();
}), scwin.n = scwin.v(scwin.i);
scwin.M = {};
// ********************************************************************
// [WebSquare 전환 추가] 원본 소스와 비교 필요
// 
// ********************************************************************
scwin.v.Callbacks = function(e) {
    e = typeof e == "string" ? scwin.M[e] || _(e) : scwin.v.extend({}, e);
    var n, r, i, s, o, u, a = [],
        f = !e.once && [],
        l = function(t) {

            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.n = e.memory && t, scwin.r = !0, scwin.u = scwin.s || 0, scwin.s = 0, scwin.o = scwin.a.length, scwin.i = !0;
            for (; scwin.a && scwin.u < scwin.o; scwin.u++)
                if (scwin.a[scwin.u].apply(t[0], t[1]) === !1 && e.stopOnFalse) {
                    scwin.n = !1;
                    break;
                }
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.i = !1, scwin.a && (scwin.f ? scwin.f.length && scwin.l(scwin.f.shift()) : scwin.n ? scwin.a = [] : scwin.c.disable());
        },
        c = {
            add: function() {
                if (scwin.a) {
                    var t = scwin.a.length;
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    (function(t) {
                        scwin.v.each(t, function(t, n) {
                            var i = scwin.v.type(scwin.n);
                            scwin.i === "function" ? (!e.unique || !scwin.c.has(scwin.n)) && scwin.a.push(scwin.n) : scwin.n && scwin.n.length && scwin.i !== "string" && scwin.r(scwin.n);
                        });
                    })(arguments), scwin.i ? scwin.o = scwin.a.length : scwin.n && (
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        scwin.s = t, scwin.l(scwin.n));
                }
                return this;
            },
            remove__function: function() {
                return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.a && scwin.v.each(arguments, function(e, t) {
                    var n;
                    while ((scwin.n = scwin.v.inArray(t, scwin.a, scwin.n)) > -1)
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        scwin.a.splice(scwin.n, 1), scwin.i && (
                            // ********************************************************************
                            // [WebSquare 전환 추가] 원본 소스와 비교 필요
                            // 
                            // ********************************************************************
                            scwin.n <= scwin.o && scwin.o--, scwin.n <= scwin.u && scwin.u--);

                }), this;
            },
            has: function(e) {
                return scwin.v.inArray(e, scwin.a) > -1;
            },
            empty: function() {
                return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.a = [], this;
            },
            disable__function: function() {
                return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.a = scwin.f = scwin.n = t, this;
            },
            disabled: function() {
                return !scwin.a;
            },
            lock: function() {
                return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.f = t, scwin.n || scwin.c.disable(), this;
            },
            locked: function() {
                return !scwin.f;
            },
            fireWith: function(e, t) {
                return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                t = t || [], t = [e, t.slice ? t.slice() : t], scwin.a && (!scwin.r || scwin.f) && (scwin.i ? scwin.f.push(t) : scwin.l(t)), this;
            },
            fire__function: function() {
                return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.c.fireWith(this, arguments), this;
            },
            fired: function() {
                return scwin.r;
            }
        };
    return scwin.c;
}, scwin.v.extend({
    Deferred: function(e) {
        var t = [
                ["resolve", "done", scwin.v.Callbacks("once memory"), "resolved"],
                ["reject", "fail", scwin.v.Callbacks("once memory"), "rejected"],
                ["notify", "progress", scwin.v.Callbacks("memory")]
            ],
            n = "pending",
            r = {
                state__function: function() {
                    return scwin.n;
                },
                always: function() {
                    return
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.i.done(arguments).fail(arguments), this;
                },
                then: function() {
                    var e = arguments;
                    return scwin.v.Deferred(function(n) {

                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        scwin.v.each(t, function(t, r) {
                            var s = scwin.r[0],
                                o = e[t];
                            scwin.i[scwin.r[1]](scwin.v.isFunction(scwin.o) ? function() {
                                var e = scwin.o.apply(this, arguments);
                                e && scwin.v.isFunction(e.promise) ? e.promise().done(scwin.n.resolve).fail(scwin.n.reject).progress(scwin.n.notify) : scwin.n[scwin.s + "With"](this === scwin.i ? scwin.n : this, [e]);
                            } : scwin.n[scwin.s]);
                        }), e = null;
                    }).promise();
                },
                promise__function: function(e) {
                    return e != null ? scwin.v.extend(e, scwin.r) : scwin.r;
                }
            },
            i = {};
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.r.pipe = scwin.r.then, scwin.v.each(t, function(e, s) {
            var o = scwin.s[2],
                u = scwin.s[3];
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.r[scwin.s[1]] = scwin.o.add, scwin.u && scwin.o.add(function() {
                scwin.n = scwin.u;
            }, t[e ^ 1][2].disable, t[2][2].lock), scwin.i[scwin.s[0]] = scwin.o.fire, scwin.i[scwin.s[0] + "With"] = scwin.o.fireWith;
        }), scwin.r.promise(scwin.i), e && e.call(scwin.i, scwin.i), scwin.i;
    },
    when: function(e) {

        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************

        var t = 0,
            n = scwin.l.call(arguments),
            r = scwin.n.length,
            i = scwin.r !== 1 || e && scwin.v.isFunction(e.promise) ? scwin.r : 0,
            s = scwin.i === 1 ? e__v.Deferred() : X,
            o = function(e, t, n) {
                return function(r) {

                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    t[e] = this, scwin.n[e] = arguments.length > 1 ? scwin.l.call(arguments) : scwin.r, scwin.n === scwin.u ? scwin.s.notifyWith(t, scwin.n) : scwin.i-- || scwin.s.resolveWith(t, scwin.n);
                };
            },
            u, a, f;
        if (scwin.r > 1) {

            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.u = new Array(scwin.r), scwin.a = new Array(scwin.r), scwin.f = new Array(scwin.r);
            for (; t < scwin.r; t++) scwin.n[t] && scwin.v.isFunction(scwin.n[t].promise) ? scwin.n[t].promise().done(scwin.o(t, scwin.f, scwin.n)).fail(scwin.s.reject).progress(scwin.o(t, scwin.a, scwin.u)) : scwin.i--;
        }
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.i || scwin.s.resolveWith(scwin.f, scwin.n), scwin.s.promise();
    }
}), scwin.v.support = function() {
    var t, n, r, s, o, u, a, f, l, c, h, p = scwin.i.createElement("div");
    // ********************************************************************
    // [WebSquare 전환 추가] 원본 소스와 비교 필요
    // 
    // ********************************************************************
    scwin.p.setAttribute("className", "t"), scwin.p.innerHTML = "  <link/><table></table><a href='/a'>a</a><input type='checkbox'/>", scwin.n = scwin.p.getElementsByTagName("*"), scwin.r = scwin.p.getElementsByTagName("a")[0];
    if (!scwin.n || !scwin.r || !scwin.n.length) return {};
    // ********************************************************************
    // [WebSquare 전환 추가] 원본 소스와 비교 필요
    // 
    // ********************************************************************
    scwin.s = scwin.i.createElement("select"), scwin.o = scwin.s.appendChild(scwin.i.createElement("option")), scwin.u = scwin.p.getElementsByTagName("input")[0], scwin.r.style.cssText = "top:1px;float:left;opacity:.5", t = {
        leadingWhitespace__p: leadingWhitespace__p,
        tbody: !scwin.p.getElementsByTagName("tbody").length,
        htmlSerialize: scwin.p.getElementsByTagName("link").length,
        style: /top/.test(scwin.r.getAttribute("style")),
        hrefNormalized: scwin.r.getAttribute("href") === "/a",
        opacity: /^0.5/.test(scwin.r.style.opacity),
        cssFloat: scwin.r.style.cssFloat,
        checkOn: scwin.u.getValue() === "on",
        optSelected: scwin.o.selected,
        getSetAttribute__p: getSetAttribute__p,
        enctype: scwin.i.createElement("form").enctype,
        html5Clone__i: html5Clone__i,
        boxModel: scwin.i.compatMode === "CSS1Compat",
        submitBubbles: !0,
        changeBubbles: !0,
        focusinBubbles: !1,
        deleteExpando: !0,
        noCloneEvent: !0,
        inlineBlockNeedsLayout: !1,
        shrinkWrapBlocks: !1,
        reliableMarginRight: !0,
        boxSizingReliable: !0,
        pixelPosition: !1
    }, scwin.u.setValue(!0), t.noCloneChecked = scwin.u.cloneNode(!0).checked, scwin.s.setDisabled(!0), t.optDisabled = !scwin.o.getDisabled();
    try {
        delete scwin.p.getValue();
    } catch (d) {
        t.deleteExpando = !1;
    }
    // ********************************************************************
    // [WebSquare 전환 추가] 원본 소스와 비교 필요
    // 
    // ********************************************************************
    !scwin.p.addEventListener && scwin.p.attachEvent && scwin.p.fireEvent && (
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.p.attachEvent("onclick", scwin.h = function() {
            t.noCloneEvent = !1;
        }), scwin.p.cloneNode(!0).fireEvent("onclick"), scwin.p.detachEvent("onclick", scwin.h)), scwin.u = scwin.i.createElement("input"), scwin.u.setValue("t"), scwin.u.setAttribute("type", "radio"), t.radioValue = scwin.u.getValue() === "t", scwin.u.setAttribute("checked", "checked"), scwin.u.setAttribute("name", "t"), scwin.p.appendChild(scwin.u), scwin.a = scwin.i.createDocumentFragment(), scwin.a.appendChild(scwin.p.lastChild), t.checkClone = scwin.a.cloneNode(!0).cloneNode(!0).lastChild.checked, t.appendChecked = scwin.u.checked, scwin.a.removeChild(scwin.u), scwin.a.appendChild(scwin.p);
    if (scwin.p.attachEvent)
        for (scwin.l in {
                submit: !0,
                change: !0,
                focusin: !0
            })
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.f = "on" + scwin.l, scwin.c = scwin.f in scwin.p, scwin.c || (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.p.setAttribute(scwin.f, "return;"), scwin.c = typeof scwin.p[scwin.f] == "function"), t[scwin.l + "Bubbles"] = scwin.c;
    return
    // ********************************************************************
    // [WebSquare 전환 추가] 원본 소스와 비교 필요
    // 
    // ********************************************************************
    scwin.v(function() {
        var n, r, s, o, u = "padding:0;margin:0;border:0;display:block;overflow:hidden;",
            a = scwin.i.getElementsByTagName("body")[0];
        if (!scwin.a) return;
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.n = scwin.i.createElement("div"), scwin.n.style.cssText = "visibility:hidden;border:0;width:0;height:0;position:static;top:0;margin-top:1px", scwin.a.insertBefore(scwin.n, scwin.a.firstChild), scwin.r = scwin.i.createElement("div"), scwin.n.appendChild(scwin.r), scwin.r.innerHTML = "<table><tr><td></td><td>t</td></tr></table>", scwin.s = scwin.r.getElementsByTagName("td"), scwin.s[0].style.cssText = "padding:0;margin:0;border:0;display:none", scwin.c = scwin.s[0].offsetHeight === 0, scwin.s[0].style.display = "", scwin.s[1].style.display = "none", t.reliableHiddenOffsets = scwin.c && scwin.s[0].offsetHeight === 0, scwin.r.innerHTML = "", scwin.r.style.cssText = "box-sizing:border-box;-moz-box-sizing:border-box;-webkit-box-sizing:border-box;padding:1px;border:1px;display:block;width:4px;margin-top:1%;position:absolute;top:1%;", t.boxSizing = scwin.r.offsetWidth === 4, t.doesNotIncludeMarginInBodyOffset = scwin.a.offsetTop !== 1, e.getComputedStyle && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            t.pixelPosition = (e.getComputedStyle(scwin.r, null) || {}).top !== "1%", t.boxSizingReliable = (e.getComputedStyle(scwin.r, null) || {
                width: "4px"
            }).width === "4px", scwin.o = scwin.i.createElement("div"), scwin.o.style.cssText = scwin.r.style.cssText = scwin.u, scwin.o.style.marginRight = scwin.o.style.setGridWidth("0"), scwin.r.style.setGridWidth("1px"), scwin.r.appendChild(scwin.o), t.reliableMarginRight = !parseFloat((e.getComputedStyle(scwin.o, null) || {}).marginRight)), typeof scwin.r.style.zoom != "undefined" && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.r.innerHTML = "", scwin.r.style.cssText = scwin.u + "width:1px;padding:1px;display:inline;zoom:1", t.inlineBlockNeedsLayout = scwin.r.offsetWidth === 3, scwin.r.style.display = "block", scwin.r.style.overflow = "visible", scwin.r.innerHTML = "<div></div>", scwin.r.firstChild.style.setGridWidth("5px"), t.shrinkWrapBlocks = scwin.r.offsetWidth !== 3, scwin.n.style.zoom = 1), scwin.a.removeChild(scwin.n), scwin.n = scwin.r = scwin.s = scwin.o = null;
    }), scwin.a.removeChild(scwin.p), scwin.n = scwin.r = scwin.s = scwin.o = scwin.u = scwin.a = scwin.p = null, t;
}();
scwin.D = /(?:\{[\s\S]*\}|\[[\s\S]*\])$/, scwin.P = /([A-Z])/g;
// ********************************************************************
// [WebSquare 전환 추가] 원본 소스와 비교 필요
// 
// ********************************************************************
scwin.v.extend({
    cache: {},
    deletedIds: [],
    uuid: 0,
    expando: "jQuery" + (scwin.v.fn.jquery + Math.random()).replace(/\D/g, ""),
    noData: {
        embed: !0,
        object: "clsid:D27CDB6E-AE6D-11cf-96B8-444553540000",
        applet: !0
    },
    hasData: function(e) {
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        e = e.nodeType ? scwin.v.cache[e[scwin.v.expando]] : e[scwin.v.expando], e && !B(e);
    },
    data: function(e, n, r, i) {

        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************

        if (!scwin.v.acceptData(e)) return;
        var s, o, u = scwin.v.expando,
            a = typeof scwin.n == "string",
            f = e.nodeType,
            l = scwin.f ? scwin.v.cache__e : X,
            c = scwin.f ? e[scwin.u] : e[scwin.u] && scwin.u;
        if ((!scwin.c || !scwin.l[scwin.c] || !scwin.i && !scwin.l[scwin.c].data) && scwin.a && scwin.r === t) return;
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.c || (scwin.f ? e[scwin.u] = scwin.c = scwin.v.deletedIds.pop() || scwin.v.guid++ : scwin.c = scwin.u), scwin.l[scwin.c] || (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.l[scwin.c] = {}, scwin.f || (scwin.l[scwin.c].toJSON = scwin.v.noop));
        if (typeof scwin.n == "object" || typeof scwin.n == "function") scwin.i ? scwin.l[scwin.c] = scwin.v.extend(scwin.l[scwin.c], scwin.n) : scwin.l[scwin.c].data = scwin.v.extend(scwin.l[scwin.c].data, scwin.n);
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.s = scwin.l[scwin.c], scwin.i || (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.s.data || (scwin.s.data = {}), scwin.s = scwin.s.data), scwin.r !== t && (scwin.s[scwin.v.camelCase(scwin.n)] = scwin.r), scwin.a ? (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.o = scwin.s[scwin.n], scwin.o == null && (scwin.o = scwin.s[scwin.v.camelCase(scwin.n)])) : scwin.o = scwin.s, scwin.o;
    },
    removeData: function(e, t, n) {

        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************

        if (!scwin.v.acceptData(e)) return;
        var r, i, s, o = e.nodeType,
            u = scwin.o ? scwin.v.cache__e : X,
            a = scwin.o ? e[scwin.v.expando] : scwin.v.expando;
        if (!scwin.u[scwin.a]) return;
        if (t) {
            scwin.r = scwin.n ? scwin.u[scwin.a] : scwin.u[scwin.a].data;
            if (scwin.r) {
                scwin.v.isArray(t) || (t in scwin.r ? t = [t] : (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    t = scwin.v.camelCase(t), t in scwin.r ? t = [t] : t = t.split(" ")));
                for (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.i = 0, scwin.s = t.length; scwin.i < scwin.s; scwin.i++) delete scwin.r[t[scwin.i]];
                if (!(scwin.n ? B : scwin.v.isEmptyObject)(scwin.r)) return;
            }
        }
        if (!scwin.n) {
            delete scwin.u[scwin.a].data;
            if (!B(scwin.u[scwin.a])) return;
        }
        scwin.o ? scwin.v.cleanData([e], !0) : scwin.v.support.deleteExpando || scwin.u != scwin.u.window ? delete scwin.u[scwin.a] : scwin.u[scwin.a] = null;
    },
    _data: function(e, t, n) {
        return scwin.v.data(e, t, scwin.n, !0);
    },
    acceptData: function(e) {
        var t = e.nodeName && scwin.v.noData[e.nodeName.toLowerCase()];
        return !t || t !== !0 && e.getAttribute("classid") === t;
    }
}), scwin.v.fn.extend({
    data: function(e, n) {
        var r, i, s, o, u, a = this[0],
            f = 0,
            l = null;
        if (e === t) {
            if (this.length) {
                scwin.l = scwin.v.data(scwin.a);
                if (scwin.a.nodeType === 1 && !scwin.v._data(scwin.a, "parsedAttrs")) {
                    scwin.s = scwin.a.attributes;
                    for (scwin.u = scwin.s.length; scwin.f < scwin.u; scwin.f++)
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        scwin.o = scwin.s[scwin.f].name, scwin.o.indexOf("data-") || (
                            // ********************************************************************
                            // [WebSquare 전환 추가] 원본 소스와 비교 필요
                            // 
                            // ********************************************************************
                            scwin.o = scwin.v.camelCase(scwin.o.substring(5)), H(scwin.a, scwin.o, scwin.l[scwin.o]));
                    scwin.v._data(scwin.a, "parsedAttrs", !0);
                }
            }
            return scwin.l;
        }
        return typeof e == "object" ? this.each(function() {
            scwin.v.data(this, e);
        }) : (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.r = e.split(".", 2), scwin.r[1] = scwin.r[1] ? "." + scwin.r[1] : "", scwin.i = scwin.r[1] + "!", scwin.v.access(this, function(n) {
                if (scwin.n === t) return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.l = this.triggerHandler("getData" + scwin.i, [scwin.r[0]]), scwin.l === t && scwin.a && (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.l = scwin.v.data(scwin.a, e), scwin.l = H(scwin.a, e, scwin.l)), scwin.l === t && scwin.r[1] ? this.data(scwin.r[0]) : scwin.l;
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.r[1] = scwin.n, this.each(function() {
                    var t = scwin.v(this);
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    t.triggerHandler("setData" + scwin.i, scwin.r), scwin.v.data(this, e, scwin.n), t.triggerHandler("changeData" + scwin.i, scwin.r);
                });
            }, null, scwin.n, arguments.length > 1, null, !1));
    },
    removeData: function(e) {
        return this.each(function() {
            scwin.v.removeData(this, e);
        });
    }
}), scwin.v.extend({
    queue__function: function(e, t, n) {
        var r;
        if (e) return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        t = (t || "fx") + "queue", scwin.r = scwin.v._data(e, t), scwin.n && (!scwin.r || scwin.v.isArray(scwin.n) ? scwin.r = scwin.v._data(e, t, scwin.v.makeArray(scwin.n)) : scwin.r.push(scwin.n)), scwin.r || [];
    },
    dequeue__function: function(e, t) {
        t = t || "fx";
        var n = scwin.v.queue(e, t),
            r = scwin.n.length,
            i = scwin.n.shift(),
            s = scwin.v._queueHooks(e, t),
            o = function() {
                scwin.v.dequeue(e, t);
            };
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.i === "inprogress" && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.i = scwin.n.shift(), scwin.r--), scwin.i && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            t === "fx" && scwin.n.unshift("inprogress"), delete scwin.s.stop, scwin.i.call(e, scwin.o, scwin.s)), !scwin.r && scwin.s && scwin.s.empty.fire();
    },
    _queueHooks: function(e, t) {
        var n = t + "queueHooks";
        return scwin.v._data(e, scwin.n) || scwin.v._data(e, scwin.n, {
            empty: scwin.v.Callbacks("once memory").add(function() {

                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.v.removeData(e, t + "queue", !0), scwin.v.removeData(e, scwin.n, !0);
            })
        });
    }
}), scwin.v.fn.extend({
    queue__function: function(e, n) {
        var r = 2;
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        typeof e != "string" && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.n = e, e = "fx", scwin.r--), arguments.length < scwin.r ? scwin.v.queue(this[0], e) : scwin.n === t ? this : this.each(function() {
            var t = scwin.v.queue(this, e, scwin.n);
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.v._queueHooks(this, e), e === "fx" && t[0] !== "inprogress" && scwin.v.dequeue(this, e);
        });
    },
    dequeue__function: function(e) {
        return this.each(function() {
            scwin.v.dequeue(this, e);
        });
    },
    delay: function(e, t) {
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************

        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************

        e = scwin.v.fx ? scwin.v.fx.speeds[e] || e__e : X, t = t || "fx", this.queue(t, function(t, n) {
            var r = setTimeout(t, e);
            scwin.n.stop = function() {
                clearTimeout(scwin.r);
            };
        });
    },
    clearQueue__function: function(e) {
        return this.queue(e || "fx", []);
    },
    promise__function: function(e, n) {
        var r, i = 1,
            s = scwin.v.Deferred(),
            o = this,
            u = this.length,
            a = function() {
                scwin.i-- || scwin.s.resolveWith(scwin.o, [scwin.o]);
            };
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        typeof e != "string" && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.n = e, e = t), e = e || "fx";
        while (scwin.u--)
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.r = scwin.v._data(scwin.o[scwin.u], e + "queueHooks"), scwin.r && scwin.r.empty && (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.i++, scwin.r.empty.add(scwin.a));
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.a(), scwin.s.promise(scwin.n);
    }
});
scwin.j, scwin.F, scwin.I, scwin.q = /[\t\r\n]/g, scwin.R = /\r/g, scwin.U = /^(?:button|input)$/i, scwin.z = /^(?:button|input|object|select|textarea)$/i, scwin.W = /^a(?:rea|)$/i, scwin.X = /^(?:autofocus|autoplay|async|checked|controls|defer|disabled|hidden|loop|multiple|open|readonly|required|scoped|selected)$/i, scwin.V = scwin.v.support.getSetAttribute;
// ********************************************************************
// [WebSquare 전환 추가] 원본 소스와 비교 필요
// 
// ********************************************************************
scwin.v.fn.extend({
    attr: function(e, t) {
        return scwin.v.access(this, scwin.v.attr, e, t, arguments.length > 1);
    },
    removeAttr: function(e) {
        return this.each(function() {
            scwin.v.removeAttr(this, e);
        });
    },
    prop: function(e, t) {
        return scwin.v.access(this, scwin.v.prop, e, t, arguments.length > 1);
    },
    removeProp: function(e) {
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        e = scwin.v.propFix[e] || e, this.each(function() {
            try {

                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                this[e] = t, delete this[e];
            } catch (n) {

            }
        });
    },
    addClass: function(e) {
        var t, n, r, i, s, o, u;
        if (scwin.v.isFunction(e)) return this.each(function(t) {
            scwin.v(this).addClass(e.call(this, t, this.className));
        });
        if (e && typeof e == "string") {
            t = e.split(scwin.y);
            for (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.n = 0, scwin.r = this.length; scwin.n < scwin.r; scwin.n++) {
                scwin.i = this[scwin.n];
                if (scwin.i.nodeType === 1)
                    if (!scwin.i.className && t.length === 1) scwin.i.addClass(e);

                    else {
                        scwin.s = " " + scwin.i.className + " ";
                        for (
                            // ********************************************************************
                            // [WebSquare 전환 추가] 원본 소스와 비교 필요
                            // 
                            // ********************************************************************
                            scwin.o = 0, scwin.u = t.length; scwin.o < scwin.u; scwin.o++) scwin.s.indexOf(" " + t[scwin.o] + " ") < 0 && (scwin.s += t[scwin.o] + " ");
                        scwin.i.addClass(scwin.v.trim(scwin.s));
                    }
            }
        }
        return this;
    },
    removeClass: function(e) {
        var n, r, i, s, o, u, a;
        if (scwin.v.isFunction(e)) return this.each(function(t) {
            scwin.v(this).removeClass(e.call(this, t, this.className));
        });
        if (e && typeof e == "string" || e === t) {
            scwin.n = (e || "").split(scwin.y);
            for (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.u = 0, scwin.a = this.length; scwin.u < scwin.a; scwin.u++) {
                scwin.i = this[scwin.u];
                if (scwin.i.nodeType === 1 && scwin.i.className) {
                    scwin.r = (" " + scwin.i.className + " ").replace(scwin.q, " ");
                    for (
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        scwin.s = 0, scwin.o = scwin.n.length; scwin.s < scwin.o; scwin.s++)
                        while (scwin.r.indexOf(" " + scwin.n[scwin.s] + " ") >= 0) scwin.r = scwin.r.replace(" " + scwin.n[scwin.s] + " ", " ");
                    scwin.i.addClass(e ? scwin.v.trim(scwin.r) : "");
                }
            }
        }
        return this;
    },
    toggleClass: function(e, t) {
        var n = typeof e,
            r = typeof t == "boolean";
        return scwin.v.isFunction(e) ? this.each(function(n) {
            scwin.v(this).toggleClass(e.call(this, scwin.n, this.className, t), t);
        }) : this.each(function() {
            if (scwin.n === "string") {
                var i, s = 0,
                    o = scwin.v(this),
                    u = t,
                    a = e.split(scwin.y);
                while (scwin.i = scwin.a[scwin.s++])
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.u = scwin.r ? scwin.u : !scwin.o.hasClass(scwin.i), scwin.o[scwin.u ? "addClass" : "removeClass"](scwin.i);

            } else if (scwin.n === "undefined" || scwin.n === "boolean")
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                this.className && scwin.v._data(this, "__className__", this.className), this.addClass(this.className || e === !1 ? "" : scwin.v._data(this, "__className__") || "");
        });
    },
    hasClass: function(e) {
        var t = " " + e + " ",
            n = 0,
            r = this.length;
        for (; scwin.n < scwin.r; scwin.n++)
            if (this[scwin.n].nodeType === 1 && (" " + this[scwin.n].className + " ").replace(scwin.q, " ").indexOf(t) >= 0) return !0;
        return !1;
    },
    val: function(e) {
        var n, r, i, s = this[0];
        if (!arguments.length) {
            if (scwin.s) return
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.n = scwin.v.valHooks[scwin.s.type] || scwin.v.valHooks[scwin.s.nodeName.toLowerCase()], scwin.n && "get" in scwin.n && (scwin.r = scwin.n.get(scwin.s, "value")) !== t ? scwin.r : (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.r = scwin.s.getValue(), typeof scwin.r == "string" ? scwin.r.replace(scwin.R, "") : scwin.r == null ? "" : scwin.r);
            return;
        }
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.i = scwin.v.isFunction(e), this.each(function(r) {
            var s, o = scwin.v(this);
            if (this.nodeType !== 1) return;
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.i ? scwin.s = e.call(this, scwin.r, scwin.o.val()) : scwin.s = e, scwin.s == null ? scwin.s = "" : typeof scwin.s == "number" ? scwin.s += "" : scwin.v.isArray(scwin.s) && (scwin.s = scwin.v.map(scwin.s, function(e) {
                return e == null ? "" : e + "";
            })), scwin.n = scwin.v.valHooks[this.type] || scwin.v.valHooks[this.nodeName.toLowerCase()];
            if (!scwin.n || !("set" in scwin.n) || scwin.n.set(this, scwin.s, "value") === t) this.setValue(scwin.s);
        });
    }
}), scwin.v.extend({
    valHooks: {
        option: {
            get: function(e) {

                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************

                var t = e.attributes.getValue();
                return !t || t.specified ? e.value__e.getText() : X;
            }
        },
        select: {
            get: function(e) {
                var t, n, r = e.options,
                    i = e.selectedIndex,
                    s = e.type === "select-one" || scwin.i < 0,
                    o = scwin.s ? null : [],
                    u = scwin.s ? scwin.i + 1 : scwin.r.length,
                    a = scwin.i < 0 ? scwin.u : scwin.s ? scwin.i : 0;
                for (; scwin.a < scwin.u; scwin.a++) {
                    scwin.n = scwin.r[scwin.a];
                    if ((scwin.n.selected || scwin.a === scwin.i) && (scwin.v.support.optDisabled ? !scwin.n.getDisabled() : scwin.n.getAttribute("disabled") === null) && (!scwin.n.parentNode.getDisabled() || !scwin.v.nodeName(scwin.n.parentNode, "optgroup"))) {
                        t = scwin.v(scwin.n).val();
                        if (scwin.s) return t;
                        scwin.o.push(t);
                    }
                }
                return scwin.o;
            },
            set: function(e, t) {
                var n = scwin.v.makeArray(t);
                return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.v(e).find("option").each(function() {
                    this.selected = scwin.v.inArray(scwin.v(this).val(), scwin.n) >= 0;
                }), scwin.n.length || (e.selectedIndex = -1), scwin.n;
            }
        }
    },
    attrFn: {},
    attr: function(e, n, r, i) {
        var s, o, u, a = e.nodeType;
        if (!e || scwin.a === 3 || scwin.a === 8 || scwin.a === 2) return;
        if (scwin.i && scwin.v.isFunction(scwin.v.fn[scwin.n])) return scwin.v(e)[scwin.n](scwin.r);
        if (typeof e.getAttribute == "undefined") return scwin.v.prop(e, scwin.n, scwin.r);
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.u = scwin.a !== 1 || !scwin.v.isXMLDoc(e), scwin.u && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.n = scwin.n.toLowerCase(), scwin.o = scwin.v.attrHooks[scwin.n] || (scwin.X.test(scwin.n) ? scwin.F : scwin.j));
        if (scwin.r !== t) {
            if (scwin.r === null) {
                scwin.v.removeAttr(e, scwin.n);
                return;
            }
            return scwin.o && "set" in scwin.o && scwin.u && (scwin.s = scwin.o.set(e, scwin.r, scwin.n)) !== t ? scwin.s : (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                e.setAttribute(scwin.n, scwin.r + ""), scwin.r);
        }
        return scwin.o && "get" in scwin.o && scwin.u && (scwin.s = scwin.o.get(e, scwin.n)) !== null ? scwin.s : (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.s = e.getAttribute(scwin.n), scwin.s === null ? t : scwin.s);
    },
    removeAttr: function(e, t) {
        var n, r, i, s, o = 0;
        if (t && e.nodeType === 1) {
            scwin.r = t.split(scwin.y);
            for (; scwin.o < scwin.r.length; scwin.o++)
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.i = scwin.r[scwin.o], scwin.i && (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.n = scwin.v.propFix[scwin.i] || scwin.i, scwin.s = scwin.X.test(scwin.i), scwin.s || scwin.v.attr(e, scwin.i, ""), e.removeAttribute(scwin.V ? scwin.i : scwin.n), scwin.s && scwin.n in e && (e[scwin.n] = !1));
        }
    },
    attrHooks: {
        type: {
            set: function(e, t) {
                if (scwin.U.test(e.nodeName) && e.parentNode) scwin.v.error("type property can't be changed");

                else if (!scwin.v.support.radioValue && t === "radio" && scwin.v.nodeName(e, "input")) {
                    var n = e.getValue();
                    return
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    e.setAttribute("type", t), scwin.n && (e.setValue(scwin.n)), t;
                }
            }
        },
        value: {
            get: function(e, t) {

                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************

                return scwin.j && scwin.v.nodeName(e, "button") ? scwin.j.get(e, t) : t in e ? e.value__null : X;
            },
            set: function(e, t, n) {
                if (scwin.j && scwin.v.nodeName(e, "button")) return scwin.j.set(e, t, scwin.n);
                e.setValue(t);
            }
        }
    },
    propFix: {
        tabindex: "tabIndex",
        readonly: "readOnly",
        "for": "htmlFor",
        "class": "className",
        maxlength: "maxLength",
        cellspacing: "cellSpacing",
        cellpadding: "cellPadding",
        rowspan: "rowSpan",
        colspan: "colSpan",
        usemap: "useMap",
        frameborder: "frameBorder",
        contenteditable: "contentEditable"
    },
    prop: function(e, n, r) {
        var i, s, o, u = e.nodeType;
        if (!e || scwin.u === 3 || scwin.u === 8 || scwin.u === 2) return;
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.o = scwin.u !== 1 || !scwin.v.isXMLDoc(e), scwin.o && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.n = scwin.v.propFix[scwin.n] || scwin.n, scwin.s = scwin.v.propHooks[scwin.n]), scwin.r !== t ? scwin.s && "set" in scwin.s && (scwin.i = scwin.s.set(e, scwin.r, scwin.n)) !== t ? scwin.i : e[scwin.n] = scwin.r : scwin.s && "get" in scwin.s && (scwin.i = scwin.s.get(e, scwin.n)) !== null ? scwin.i : e[scwin.n];
    },
    propHooks: {
        tabIndex: {
            get: function(e) {
                var n = e.getAttributeNode("tabindex");
                return scwin.n && scwin.n.specified ? parseInt(scwin.n.getValue(), 10) : scwin.z.test(e.nodeName) || scwin.W.test(e.nodeName) && e.href ? 0 : t;
            }
        }
    }
}), scwin.F = {
    get: function(e, n) {
        var r, i = scwin.v.prop(e, scwin.n);
        return scwin.i === !0 || typeof scwin.i != "boolean" && (scwin.r = e.getAttributeNode(scwin.n)) && scwin.r.nodeValue !== !1 ? scwin.n.toLowerCase() : t;
    },
    set: function(e, t, n) {
        var r;
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        t === !1 ? scwin.v.removeAttr(e, scwin.n) : (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.r = scwin.v.propFix[scwin.n] || scwin.n, scwin.r in e && (e[scwin.r] = !0), e.setAttribute(scwin.n, scwin.n.toLowerCase())), scwin.n;
    }
}, scwin.V || (
    // ********************************************************************
    // [WebSquare 전환 추가] 원본 소스와 비교 필요
    // 
    // ********************************************************************
    scwin.I = {
        name: !0,
        id: !0,
        coords: !0
    }, scwin.j = scwin.v.valHooks.button = {
        get: function(e, n) {
            var r;
            return
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************

            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************

            scwin.r = e.getAttributeNode(scwin.n), scwin.r && (scwin.I[scwin.n] ? scwin.r.getValue() !== "" : scwin.r.specified) ? scwin.r.value__t : X;
        },
        set: function(e, t, n) {
            var r = e.getAttributeNode(scwin.n);
            return
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.r || (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.r = scwin.i.createAttribute(scwin.n), e.setAttributeNode(scwin.r)), scwin.r.setValue(t + "");
        }
    }, scwin.v.each(["width", "height"], function(e, t) {
        scwin.v.attrHooks[t] = scwin.v.extend(scwin.v.attrHooks[t], {
            set: function(e, n) {
                if (scwin.n === "") return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                e.setAttribute(t, "auto"), scwin.n;
            }
        });
    }), scwin.v.attrHooks.contenteditable = {
        get: scwin.j.get,
        set: function(e, t, n) {

            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            t === "" && (t = "false"), scwin.j.set(e, t, scwin.n);
        }
    }), scwin.v.support.hrefNormalized || scwin.v.each(["href", "src", "width", "height"], function(e, n) {
    scwin.v.attrHooks[scwin.n] = scwin.v.extend(scwin.v.attrHooks[scwin.n], {
        get: function(e) {
            var r = e.getAttribute(scwin.n, 2);
            return scwin.r === null ? t : scwin.r;
        }
    });
}), scwin.v.support.style || (scwin.v.attrHooks.style = {
    get: function(e) {
        return e.style.cssText.toLowerCase() || t;
    },
    set: function(e, t) {
        return e.style.cssText = t + "";
    }
}), scwin.v.support.optSelected || (scwin.v.propHooks.selected = scwin.v.extend(scwin.v.propHooks.selected, {
    get: function(e) {
        var t = e.parentNode;
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        t && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            t.selectedIndex, t.parentNode && t.parentNode.selectedIndex), null;
    }
})), scwin.v.support.enctype || (scwin.v.propFix.enctype = "encoding"), scwin.v.support.checkOn || scwin.v.each(["radio", "checkbox"], function() {
    scwin.v.valHooks[this] = {
        get: function(e) {
            return e.getAttribute("value") === null ? "on" : e.getValue();
        }
    };
}), scwin.v.each(["radio", "checkbox"], function() {
    scwin.v.valHooks[this] = scwin.v.extend(scwin.v.valHooks[this], {
        set: function(e, t) {
            if (scwin.v.isArray(t)) return e.setValue(scwin.v.inArray(scwin.v(e).val(), t) >= 0);
        }
    });
});
scwin.$ = /^(?:textarea|input|select)$/i, scwin.J = /^([^\.]*|)(?:\.(.+)|)$/, scwin.K = /(?:^|\s)hover(\.\S+|)\b/, scwin.Q = /^key/, scwin.G = /^(?:mouse|contextmenu)|click/, scwin.Y = /^(?:focusinfocus|focusoutblur)$/, scwin.Z = function(e) {

    // ********************************************************************
    // [WebSquare 전환 추가] 원본 소스와 비교 필요
    // 
    // ********************************************************************

    return scwin.v.event.special.hover ? e__e.replace(scwin.K, "mouseenter$1 mouseleave$1") : X;
};
// ********************************************************************
// [WebSquare 전환 추가] 원본 소스와 비교 필요
// 
// ********************************************************************
scwin.v.event = {
        add: function(e, n, r, i, s) {
            var o, u, a, f, l, c, h, p, d, m, g;
            if (e.nodeType === 3 || e.nodeType === 8 || !scwin.n || !scwin.r || !(scwin.o = scwin.v._data(e))) return;
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.r.handler && (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.d = scwin.r, scwin.r = scwin.d.handler, scwin.s = scwin.d.selector), scwin.r.guid || (scwin.r.guid = scwin.v.guid++), scwin.a = scwin.o.events, scwin.a || (scwin.o.events = scwin.a = {}), scwin.u = scwin.o.handle, scwin.u || (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.o.handle = scwin.u = function(e) {
                    return typeof scwin.v == "undefined" || e && scwin.v.event.triggered === e.type ? t : scwin.v.event.dispatch.apply(scwin.u.elem, arguments);
                }, scwin.u.elem = e), scwin.n = scwin.v.trim(scwin.Z(scwin.n)).split(" ");
            for (scwin.f = 0; scwin.f < scwin.n.length; scwin.f++) {

                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************

                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************

                scwin.l = scwin.J.exec(scwin.n[scwin.f]) || [], scwin.c = scwin.l[1], scwin.h = (scwin.l[2] || "").split(".").sort(), scwin.g = scwin.v.event.special[scwin.c] || {}, scwin.c = (scwin.s ? scwin.g.delegateType__g.bindType : X) || scwin.c, scwin.g = scwin.v.event.special[scwin.c] || {}, scwin.p = scwin.v.extend({
                    type__c: type__c,
                    origType__l: origType__l,
                    1: 1,
                    data: scwin.i,
                    handler: scwin.r,
                    guid: scwin.r.guid,
                    selector: scwin.s,
                    needsContext: scwin.s && scwin.v.expr.match.needsContext.test(scwin.s),
                    namespace__h: namespace__h
                }, scwin.d), scwin.m = scwin.a[scwin.c];
                if (!scwin.m) {

                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.m = scwin.a[scwin.c] = [], scwin.m.delegateCount = 0;
                    if (!scwin.g.setup || scwin.g.setup.call(e, scwin.i, scwin.h, scwin.u) === !1) e.addEventListener ? e.addEventListener(scwin.c, scwin.u, !1) : e.attachEvent && e.attachEvent("on" + scwin.c, scwin.u);
                }
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.g.add && (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.g.add.call(e, scwin.p), scwin.p.handler.guid || (scwin.p.handler.guid = scwin.r.guid)), scwin.s ? scwin.m.splice(scwin.m.delegateCount++, 0, scwin.p) : scwin.m.push(scwin.p), scwin.v.event.global[scwin.c] = !0;
            }
            e = null;
        },
        global: {},
        remove__function: function(e, t, n, r, i) {
            var s, o, u, a, f, l, c, h, p, d, m, g = scwin.v.hasData(e) && scwin.v._data(e);
            if (!scwin.g || !(scwin.h = scwin.g.events)) return;
            t = scwin.v.trim(scwin.Z(t || "")).split(" ");
            for (scwin.s = 0; scwin.s < t.length; scwin.s++) {

                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.o = scwin.J.exec(t[scwin.s]) || [], scwin.u = scwin.a = scwin.o[1], scwin.f = scwin.o[2];
                if (!scwin.u) {
                    for (scwin.u in scwin.h) scwin.v.event.remove(e, scwin.u + t[scwin.s], scwin.n, scwin.r, !0);
                    continue;
                }
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************

                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************

                scwin.p = scwin.v.event.special[scwin.u] || {}, scwin.u = (scwin.r ? scwin.p.delegateType__p.bindType : X) || scwin.u, scwin.d = scwin.h[scwin.u] || [], scwin.l = scwin.d.length, scwin.f = scwin.f ? new RegExp("(^|\\.)" + scwin.f.split(".").sort().join("\\.(?:.*\\.|)") + "(\\.|$)") : null;
                for (scwin.c = 0; scwin.c < scwin.d.length; scwin.c++)
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.m = scwin.d[scwin.c], (scwin.i || scwin.a === scwin.m.origType) && (!scwin.n || scwin.n.guid === scwin.m.guid) && (!scwin.f || scwin.f.test(scwin.m.namespace)) && (!scwin.r || scwin.r === scwin.m.selector || scwin.r === "**" && scwin.m.selector) && (
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        scwin.d.splice(scwin.c--, 1), scwin.m.selector && scwin.d.delegateCount--, scwin.p.remove && scwin.p.remove.call(e, scwin.m));
                scwin.d.length === 0 && scwin.l !== scwin.d.length && (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    (!scwin.p.teardown || scwin.p.teardown.call(e, scwin.f, scwin.g.handle) === !1) && scwin.v.removeEvent(e, scwin.u, scwin.g.handle), delete scwin.h[scwin.u]);
            }
            scwin.v.isEmptyObject(scwin.h) && (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                delete scwin.g.handle, scwin.v.removeData(e, "events", !0));
        },
        customEvent: {
            getData: !0,
            setData: !0,
            changeData: !0
        },
        trigger: function(n, r, s, o) {
            if (!scwin.s || scwin.s.nodeType !== 3 && scwin.s.nodeType !== 8) {
                var u, a, f, l, c, h, p, d, m, g, y = scwin.n.type || scwin.n,
                    b = [];
                if (scwin.Y.test(scwin.y + scwin.v.event.triggered)) return;
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.y.indexOf("!") >= 0 && (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.y = scwin.y.slice(0, -1), scwin.a = !0), scwin.y.indexOf(".") >= 0 && (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.b = scwin.y.split("."), scwin.y = scwin.b.shift(), scwin.b.sort());
                if ((!scwin.s || scwin.v.event.customEvent[scwin.y]) && !scwin.v.event.global[scwin.y]) return;
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.n = typeof scwin.n == "object" ? scwin.n[scwin.v.expando] ? scwin.n : new scwin.v.Event(scwin.y, scwin.n) : new scwin.v.Event(scwin.y), scwin.n.type = scwin.y, scwin.n.isTrigger = !0, scwin.n.exclusive = scwin.a, scwin.n.namespace = scwin.b.join("."), scwin.n.namespace_re = scwin.n.namespace ? new RegExp("(^|\\.)" + scwin.b.join("\\.(?:.*\\.|)") + "(\\.|$)") : null, scwin.h = scwin.y.indexOf(":") < 0 ? "on" + scwin.y : "";
                if (!scwin.s) {
                    scwin.u = scwin.v.cache;
                    for (scwin.f in scwin.u) scwin.u[scwin.f].events && scwin.u[scwin.f].events[scwin.y] && scwin.v.event.trigger(scwin.n, scwin.r, scwin.u[scwin.f].handle.elem, !0);
                    return;
                }
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.n.result = t, scwin.n.target || (scwin.n.target = scwin.s), scwin.r = scwin.r != null ? scwin.v.makeArray(scwin.r) : [], scwin.r.unshift(scwin.n), scwin.p = scwin.v.event.special[scwin.y] || {};
                if (scwin.p.trigger && scwin.p.trigger.apply(scwin.s, scwin.r) === !1) return;
                scwin.m = [
                    [scwin.s, scwin.p.bindType || scwin.y]
                ];
                if (!scwin.o && !scwin.p.noBubble && !scwin.v.isWindow(scwin.s)) {

                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.g = scwin.p.delegateType || scwin.y, scwin.l = scwin.Y.test(scwin.g + scwin.y) ? scwin.s : scwin.s.parentNode;
                    for (scwin.c = scwin.s; scwin.l; scwin.l = scwin.l.parentNode)
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        scwin.m.push([scwin.l, scwin.g]), scwin.c = scwin.l;
                    scwin.c === (scwin.s.ownerDocument || scwin.i) && scwin.m.push([scwin.c.defaultView || scwin.c.parentWindow || e, scwin.g]);
                }
                for (scwin.f = 0; scwin.f < scwin.m.length && !scwin.n.isPropagationStopped(); scwin.f++)
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.l = scwin.m[scwin.f][0], scwin.n.type = scwin.m[scwin.f][1], scwin.d = (scwin.v._data(scwin.l, "events") || {})[scwin.n.type] && scwin.v._data(scwin.l, "handle"), scwin.d && scwin.d.apply(scwin.l, scwin.r), scwin.d = scwin.h && scwin.l[scwin.h], scwin.d && scwin.v.acceptData(scwin.l) && scwin.d.apply && scwin.d.apply(scwin.l, scwin.r) === !1 && scwin.n.preventDefault();
                return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.n.type = scwin.y, !scwin.o && !scwin.n.isDefaultPrevented() && (!scwin.p._default || scwin.p._default.apply(scwin.s.ownerDocument, scwin.r) === !1) && (scwin.y !== "click" || !scwin.v.nodeName(scwin.s, "a")) && scwin.v.acceptData(scwin.s) && scwin.h && scwin.s[scwin.y] && (scwin.y !== "focus" && scwin.y !== "blur" || scwin.n.target.offsetWidth !== 0) && !scwin.v.isWindow(scwin.s) && (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.c = scwin.s[scwin.h], scwin.c && (scwin.s[scwin.h] = null), scwin.v.event.triggered = scwin.y, scwin.s[scwin.y](), scwin.v.event.triggered = t, scwin.c && (scwin.s[scwin.h] = scwin.c)), scwin.n.result;
            }
            return;
        },
        dispatch: function(n) {
            scwin.n = scwin.v.event.fix(scwin.n || e.event);
            var r, i, s, o, u, a, f, c, h, p, d = (scwin.v._data(this, "events") || {})[scwin.n.type] || [],
                m = scwin.d.delegateCount,
                g = scwin.l.call(arguments),
                y = !scwin.n.exclusive && !scwin.n.namespace,
                b = scwin.v.event.special[scwin.n.type] || {},
                w = [];
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.g[0] = scwin.n, scwin.n.delegateTarget = this;
            if (scwin.b.preDispatch && scwin.b.preDispatch.call(this, scwin.n) === !1) return;
            if (scwin.m && (!scwin.n.button || scwin.n.type !== "click"))
                for (scwin.s = scwin.n.target; scwin.s != this; scwin.s = scwin.s.parentNode || this)
                    if (scwin.s.getDisabled() !== !0 || scwin.n.type !== "click") {

                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        scwin.u = {}, scwin.f = [];
                        for (scwin.r = 0; scwin.r < scwin.m; scwin.r++)
                            // ********************************************************************
                            // [WebSquare 전환 추가] 원본 소스와 비교 필요
                            // 
                            // ********************************************************************
                            scwin.c = scwin.d[scwin.r], scwin.h = scwin.c.selector, scwin.u[scwin.h] === t && (scwin.u[scwin.h] = scwin.c.needsContext ? scwin.v(scwin.h, this).index(scwin.s) >= 0 : scwin.v.find(scwin.h, this, null, [scwin.s]).length), scwin.u[scwin.h] && scwin.f.push(scwin.c);
                        scwin.f.length && scwin.w.push({
                            elem: scwin.s,
                            matches: scwin.f
                        });
                    } scwin.d.length > scwin.m && scwin.w.push({
                elem: this,
                matches: scwin.d.slice(scwin.m)
            });
            for (scwin.r = 0; scwin.r < scwin.w.length && !scwin.n.isPropagationStopped(); scwin.r++) {

                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.a = scwin.w[scwin.r], scwin.n.currentTarget = scwin.a.elem;
                for (scwin.i = 0; scwin.i < scwin.a.matches.length && !scwin.n.isImmediatePropagationStopped(); scwin.i++) {
                    scwin.c = scwin.a.matches[scwin.i];
                    if (scwin.y || !scwin.n.namespace && !scwin.c.namespace || scwin.n.namespace_re && scwin.n.namespace_re.test(scwin.c.namespace))
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        scwin.n.data = scwin.c.data, scwin.n.handleObj = scwin.c, scwin.o = ((scwin.v.event.special[scwin.c.origType] || {}).handle || scwin.c.handler).apply(scwin.a.elem, scwin.g), scwin.o !== t && (
                            // ********************************************************************
                            // [WebSquare 전환 추가] 원본 소스와 비교 필요
                            // 
                            // ********************************************************************
                            scwin.n.result = scwin.o, scwin.o === !1 && (
                                // ********************************************************************
                                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                                // 
                                // ********************************************************************
                                scwin.n.preventDefault(), scwin.n.stopPropagation()));
                }
            }
            return
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.b.postDispatch && scwin.b.postDispatch.call(this, scwin.n), scwin.n.result;
        },
        props: "attrChange attrName relatedNode srcElement altKey bubbles cancelable ctrlKey currentTarget eventPhase metaKey relatedTarget shiftKey target timeStamp view which".split(" "),
        fixHooks: {},
        keyHooks: {
            props: "char charCode key keyCode".split(" "),
            filter: function(e, t) {
                return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************

                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************

                e.which == null && (e.which = t.charCode != null ? t.charCode__t.keyCode : X), e;
            }
        },
        mouseHooks: {
            props: "button buttons clientX clientY fromElement offsetX offsetY pageX pageY screenX screenY toElement".split(" "),
            filter: function(e, n) {
                var r, s, o, u = scwin.n.button,
                    a = scwin.n.fromElement;
                return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                e.pageX == null && scwin.n.clientX != null && (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.r = e.target.ownerDocument || scwin.i, scwin.s = scwin.r.documentElement, scwin.o = scwin.r.body, e.pageX = scwin.n.clientX + (scwin.s && scwin.s.scrollLeft || scwin.o && scwin.o.scrollLeft || 0) - (scwin.s && scwin.s.clientLeft || scwin.o && scwin.o.clientLeft || 0), e.pageY = scwin.n.clientY + (scwin.s && scwin.s.scrollTop || scwin.o && scwin.o.scrollTop || 0) - (scwin.s && scwin.s.clientTop || scwin.o && scwin.o.clientTop || 0)), !e.relatedTarget && scwin.a && (e.relatedTarget = scwin.a === e.target ? scwin.n.toElement : scwin.a), !e.which && scwin.u !== t && (e.which = scwin.u & 1 ? 1 : scwin.u & 2 ? 3 : scwin.u & 4 ? 2 : 0), e;
            }
        },
        fix: function(e) {
            if (e[scwin.v.expando]) return e;
            var t, n, r = e,
                s = scwin.v.event.fixHooks[e.type] || {},
                o = scwin.s.props ? this.props.concat(scwin.s.props) : this.props;
            e = scwin.v.Event(scwin.r);
            for (t = scwin.o.length; t;)
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.n = scwin.o[t--], e[scwin.n] = scwin.r[scwin.n];
            return
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            e.target || (e.target = scwin.r.srcElement || scwin.i), e.target.nodeType === 3 && (e.target = e.target.parentNode), e.metaKey = e.metaKey, scwin.s.filter ? scwin.s.filter(e, scwin.r) : e;
        },
        special: {
            load: {
                noBubble: !0
            },
            focus: {
                delegateType: "focusin"
            },
            blur: {
                delegateType: "focusout"
            },
            beforeunload: {
                setup: function(e, t, n) {
                    scwin.v.isWindow(this) && (this.onbeforeunload = scwin.n);
                },
                teardown: function(e, t) {
                    this.onbeforeunload === t && (this.onbeforeunload = null);
                }
            }
        },
        simulate__function: function(e, t, n, r) {
            var i = scwin.v.extend(new scwin.v.Event(), scwin.n, {
                type__e: type__e,
                isSimulated: !0,
                originalEvent: {}
            });
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.r ? scwin.v.event.trigger(scwin.i, null, t) : scwin.v.event.dispatch.call(t, scwin.i), scwin.i.isDefaultPrevented() && scwin.n.preventDefault();
        }
    }, scwin.v.event.handle = scwin.v.event.dispatch, scwin.v.removeEvent = scwin.i.removeEventListener ? function(e, t, n) {
        e.removeEventListener && e.removeEventListener(t, scwin.n, !1);
    } : function(e, t, n) {
        var r = "on" + t;
        e.detachEvent && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            typeof e[scwin.r] == "undefined" && (e[scwin.r] = null), e.detachEvent(scwin.r, scwin.n));
    }, scwin.v.Event = function(e, t) {
        if (!(this instanceof scwin.v.Event)) return new scwin.v.Event(e, t);
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        e && e.type ? (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            this.originalEvent = e, this.type = e.type, this.isDefaultPrevented = e.defaultPrevented || e.returnValue === !1 || e.getPreventDefault && e.getPreventDefault() ? tt : et) : this.type = e, t && scwin.v.extend(this, t), this.timeStamp = e && e.timeStamp || scwin.v.now(), this[scwin.v.expando] = !0;
    }, scwin.v.Event.prototype = {
        preventDefault: function() {
            this.isDefaultPrevented = tt;
            var e = this.originalEvent;
            if (!e) return;
            e.preventDefault ? e.preventDefault() : e.returnValue = !1;
        },
        stopPropagation: function() {
            this.isPropagationStopped = tt;
            var e = this.originalEvent;
            if (!e) return;
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            e.stopPropagation && e.stopPropagation(), e.cancelBubble = !0;
        },
        stopImmediatePropagation: function() {

            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            this.isImmediatePropagationStopped = tt, this.stopPropagation();
        },
        isDefaultPrevented: et,
        isPropagationStopped: et,
        isImmediatePropagationStopped: et
    }, scwin.v.each({
        mouseenter: "mouseover",
        mouseleave: "mouseout"
    }, function(e, t) {
        scwin.v.event.special[e] = {
            delegateType__t: delegateType__t,
            bindType__t: bindType__t,
            handle__function: function(e) {
                var n, r = this,
                    i = e.relatedTarget,
                    s = e.handleObj,
                    o = scwin.s.selector;
                if (!scwin.i || scwin.i !== scwin.r && !scwin.v.contains(scwin.r, scwin.i))
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    e.type = scwin.s.origType, scwin.n = scwin.s.handler.apply(this, arguments), e.type = t;
                return scwin.n;
            }
        };
    }), scwin.v.support.submitBubbles || (scwin.v.event.special.submit = {
        setup: function() {
            if (scwin.v.nodeName(this, "form")) return !1;
            scwin.v.event.add(this, "click._submit keypress._submit", function(e) {
                var n = e.target,
                    r = scwin.v.nodeName(scwin.n, "input") || scwin.v.nodeName(scwin.n, "button") ? scwin.n.form : t;
                scwin.r && !scwin.v._data(scwin.r, "_submit_attached") && (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.v.event.add(scwin.r, "submit._submit", function(e) {
                        e._submit_bubble = !0;
                    }), scwin.v._data(scwin.r, "_submit_attached", !0));
            });
        },
        postDispatch: function(e) {
            e._submit_bubble && (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                delete e._submit_bubble, this.parentNode && !e.isTrigger && scwin.v.event.simulate("submit", this.parentNode, e, !0));
        },
        teardown: function() {
            if (scwin.v.nodeName(this, "form")) return !1;
            scwin.v.event.remove(this, "._submit");
        }
    }), scwin.v.support.changeBubbles || (scwin.v.event.special.change = {
        setup: function() {
            if (scwin.$.test(this.nodeName)) {
                if (this.type === "checkbox" || this.type === "radio")
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.v.event.add(this, "propertychange._change", function(e) {
                        e.originalEvent.propertyName === "checked" && (this._just_changed = !0);
                    }), scwin.v.event.add(this, "click._change", function(e) {

                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        this._just_changed && !e.isTrigger && (this._just_changed = !1), scwin.v.event.simulate("change", this, e, !0);
                    });
                return !1;
            }
            scwin.v.event.add(this, "beforeactivate._change", function(e) {
                var t = e.target;
                scwin.$.test(t.nodeName) && !scwin.v._data(t, "_change_attached") && (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.v.event.add(t, "change._change", function(e) {
                        this.parentNode && !e.isSimulated && !e.isTrigger && scwin.v.event.simulate("change", this.parentNode, e, !0);
                    }), scwin.v._data(t, "_change_attached", !0));
            });
        },
        handle__function: function(e) {
            var t = e.target;
            if (this !== t || e.isSimulated || e.isTrigger || t.type !== "radio" && t.type !== "checkbox") return e.handleObj.handler.apply(this, arguments);
        },
        teardown: function() {
            return
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.v.event.remove(this, "._change"), !scwin.$.test(this.nodeName);
        }
    }), scwin.v.support.focusinBubbles || scwin.v.each({
        focus: "focusin",
        blur: "focusout"
    }, function(e, t) {
        var n = 0,
            r = function(e) {
                scwin.v.event.simulate(t, e.target, scwin.v.event.fix(e), !0);
            };
        scwin.v.event.special[t] = {
            setup: function() {
                scwin.n++ === 0 && scwin.i.addEventListener(e, scwin.r, !0);
            },
            teardown: function() {
                scwin.n-- === 0 && scwin.i.removeEventListener(e, scwin.r, !0);
            }
        };
    }), scwin.v.fn.extend({
        on: function(e, n, r, i, s) {
            var o, u;
            if (typeof e == "object") {
                typeof scwin.n != "string" && (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.r = scwin.r || scwin.n, scwin.n = t);
                for (scwin.u in e) this.on(scwin.u, scwin.n, scwin.r, e[scwin.u], scwin.s);
                return this;
            }
            scwin.r == null && scwin.i == null ? (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.i = scwin.n, scwin.r = scwin.n = t) : scwin.i == null && (typeof scwin.n == "string" ? (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.i = scwin.r, scwin.r = t) : (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.i = scwin.r, scwin.r = scwin.n, scwin.n = t));
            if (scwin.i === !1) scwin.i = et;

            else if (!scwin.i) return this;
            return
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.s === 1 && (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.o = scwin.i, scwin.i = function(e) {
                    return
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.v().off(e), scwin.o.apply(this, arguments);
                }, scwin.i.guid = scwin.o.guid || (scwin.o.guid = scwin.v.guid++)), this.each(function() {
                scwin.v.event.add(this, e, scwin.i, scwin.r, scwin.n);
            });
        },
        one__function: function(e, t, n, r) {
            return this.on(e, t, scwin.n, scwin.r, 1);
        },
        off: function(e, n, r) {
            var i, s;
            if (e && e.preventDefault && e.handleObj) return
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************

            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************

            scwin.i = e.handleObj, scwin.v(e.delegateTarget).off(scwin.i.namespace ? scwin.i.origType + "." + scwin.i.namespace__i.origType : X, scwin.i.selector, scwin.i.handler), this;
            if (typeof e == "object") {
                for (scwin.s in e) this.off(scwin.s, scwin.n, e[scwin.s]);
                return this;
            }
            if (scwin.n === !1 || typeof scwin.n == "function")
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.r = scwin.n, scwin.n = t;
            return
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.r === !1 && (scwin.r = et), this.each(function() {
                scwin.v.event.remove(this, e, scwin.r, scwin.n);
            });
        },
        bind: function(e, t, n) {
            return this.on(e, null, t, scwin.n);
        },
        unbind: function(e, t) {
            return this.off(e, null, t);
        },
        live__function: function(e, t, n) {
            return
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.v(this.context).on(e, this.selector, t, scwin.n), this;
        },
        die__function: function(e, t) {
            return
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.v(this.context).off(e, this.selector || "**", t), this;
        },
        delegate__function: function(e, t, n, r) {
            return this.on(t, e, scwin.n, scwin.r);
        },
        undelegate__function: function(e, t, n) {
            return arguments.length === 1 ? this.off(e, "**") : this.off(t, e || "**", scwin.n);
        },
        trigger: function(e, t) {
            return this.each(function() {
                scwin.v.event.trigger(e, t, this);
            });
        },
        triggerHandler: function(e, t) {
            if (this[0]) return scwin.v.event.trigger(e, t, this[0], !0);
        },
        toggle__function: function(e) {
            var t = arguments,
                n = e.guid || scwin.v.guid++,
                r = 0,
                i = function(n) {
                    var i = (scwin.v._data(this, "lastToggle" + e.guid) || 0) % scwin.r;
                    return
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.v._data(this, "lastToggle" + e.guid, scwin.i + 1), scwin.n.preventDefault(), t[scwin.i].apply(this, arguments) || !1;
                };
            scwin.i.guid = scwin.n;
            while (scwin.r < t.length) t[scwin.r++].guid = scwin.n;
            return this.click(scwin.i);
        },
        hover: function(e, t) {
            return this.mouseenter(e).mouseleave(t || e);
        }
    }), scwin.v.each("blur focus focusin focusout load resize scroll unload click dblclick mousedown mouseup mousemove mouseover mouseout mouseenter mouseleave change select submit keydown keypress keyup error contextmenu".split(" "), function(e, t) {

        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.v.fn[t] = function(e, n) {
            return
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.n == null && (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.n = e, e = null), arguments.length > 0 ? this.on(t, null, e, scwin.n) : this.trigger(t);
        }, scwin.Q.test(t) && (scwin.v.event.fixHooks[t] = scwin.v.event.keyHooks), scwin.G.test(t) && (scwin.v.event.fixHooks[t] = scwin.v.event.mouseHooks);
    }),
    function(e, t) {
        /**
         * @method
         * @name nt
         * @description desc
         * @param {string} e desc
         * @param {string} t desc
         * @param {string} n desc
         * @param {string} r desc
         * @returns
         * @hidden N
         * @exception
         * @example
         */
        function nt(e, t, n, r) {

            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.n = scwin.n || [], t = t || scwin.g;
            var i, s, a, f, l = t.nodeType;
            if (!e || typeof e != "string") return scwin.n;
            if (scwin.l !== 1 && scwin.l !== 9) return [];
            scwin.a = scwin.o(t);
            if (!scwin.a && !scwin.r)
                if (scwin.i = scwin.R.exec(e))
                    if (scwin.f = scwin.i[1]) {
                        if (scwin.l === 9) {
                            scwin.s = t.getElementById(scwin.f);
                            if (!scwin.s || !scwin.s.parentNode) return scwin.n;
                            if (scwin.s.id === scwin.f) return
                            // ********************************************************************
                            // [WebSquare 전환 추가] 원본 소스와 비교 필요
                            // 
                            // ********************************************************************
                            scwin.n.push(scwin.s), scwin.n;
                        } else if (t.ownerDocument && (scwin.s = t.ownerDocument.getElementById(scwin.f)) && scwin.u(t, scwin.s) && scwin.s.id === scwin.f) return
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        scwin.n.push(scwin.s), scwin.n;
                    }

            else {
                if (scwin.i[2]) return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.S.apply(scwin.n, scwin.x.call(t.getElementsByTagName(e), 0)), scwin.n;
                if ((scwin.f = scwin.i[3]) && scwin.Z && t.getElementsByClassName) return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.S.apply(scwin.n, scwin.x.call(t.getElementsByClassName(scwin.f), 0)), scwin.n;
            }
            return vt(e.replace(scwin.j, "$1"), t, scwin.n, scwin.r, scwin.a);
        };
        /**
         * @method
         * @name rt
         * @description desc
         * @param {string} e desc
         * @returns
         * @hidden N
         * @exception
         * @example
         */
        function rt(e) {
            return function(t) {
                var n = t.nodeName.toLowerCase();
                return scwin.n === "input" && t.type === e;
            };
        };
        /**
         * @method
         * @name it
         * @description desc
         * @param {string} e desc
         * @returns
         * @hidden N
         * @exception
         * @example
         */
        function it(e) {
            return function(t) {
                var n = t.nodeName.toLowerCase();
                return (scwin.n === "input" || scwin.n === "button") && t.type === e;
            };
        };
        /**
         * @method
         * @name st
         * @description desc
         * @param {string} e desc
         * @returns
         * @hidden N
         * @exception
         * @example
         */
        function st(e) {
            return scwin.N(function(t) {
                return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                t = +t, scwin.N(function(n, r) {
                    var i, s = e([], scwin.n.length, t),
                        o = scwin.s.length;
                    while (scwin.o--) scwin.n[scwin.i = scwin.s[scwin.o]] && (scwin.n[scwin.i] = !(scwin.r[scwin.i] = scwin.n[scwin.i]));

                });
            });
        };
        /**
         * @method
         * @name ot
         * @description desc
         * @param {string} e desc
         * @param {string} t desc
         * @param {string} n desc
         * @returns
         * @hidden N
         * @exception
         * @example
         */
        function ot(e, t, n) {
            if (e === t) return scwin.n;
            var r = e.nextSibling;
            while (scwin.r) {
                if (scwin.r === t) return -1;
                scwin.r = scwin.r.nextSibling;
            }
            return 1;
        };
        /**
         * @method
         * @name ut
         * @description desc
         * @param {string} e desc
         * @param {string} t desc
         * @returns
         * @hidden N
         * @exception
         * @example
         */
        function ut(e, t) {
            var n, r, s, o, u, a, f, l = scwin.L[scwin.d][e + " "];
            if (scwin.l) return t ? 0 : scwin.l.slice(0);
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.u = e, scwin.a = [], scwin.f = scwin.i.preFilter;
            while (scwin.u) {
                if (!scwin.n || (scwin.r = scwin.F.exec(scwin.u)))
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.r && (scwin.u = scwin.u.slice(scwin.r[0].length) || scwin.u), scwin.a.push(scwin.s = []);
                scwin.n = !1;
                if (scwin.r = scwin.I.exec(scwin.u))
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.s.push(scwin.n = new m(scwin.r.shift())), scwin.u = scwin.u.slice(scwin.n.length), scwin.n.type = scwin.r[0].replace(scwin.j, " ");
                for (scwin.o in scwin.i.filter)(scwin.r = scwin.J[scwin.o].exec(scwin.u)) && (!scwin.f[scwin.o] || (scwin.r = scwin.f[scwin.o](scwin.r))) && (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.s.push(scwin.n = new m(scwin.r.shift())), scwin.u = scwin.u.slice(scwin.n.length), scwin.n.type = scwin.o, scwin.n.matches = scwin.r);
                if (!scwin.n) break;
            }
            return t ? scwin.u.length : scwin.u ? scwin.nt.error(e) : scwin.L(e, scwin.a).slice(0);
        };
        /**
         * @method
         * @name at
         * @description desc
         * @param {string} e desc
         * @param {string} t desc
         * @param {string} r desc
         * @returns
         * @hidden N
         * @exception
         * @example
         */
        function at(e, t, r) {
            var i = t.dir,
                s = scwin.r && t.dir === "parentNode",
                o = scwin.w++;
            return t.first ? function(t, n, r) {
                while (t = t[scwin.i])
                    if (scwin.s || t.nodeType === 1) return e(t, scwin.n, scwin.r);

            } : function(t, r, u) {
                if (!scwin.u) {
                    var a, f = scwin.b + " " + scwin.o + " ",
                        l = scwin.f + scwin.n;
                    while (t = t[scwin.i])
                        if (scwin.s || t.nodeType === 1) {
                            if ((scwin.a = t[scwin.d]) === scwin.l) return t.sizset;
                            if (typeof scwin.a == "string" && scwin.a.indexOf(scwin.f) === 0) {
                                if (t.sizset) return t;
                            } else {
                                t[scwin.d] = scwin.l;
                                if (e(t, scwin.r, scwin.u)) return
                                // ********************************************************************
                                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                                // 
                                // ********************************************************************
                                t.sizset = !0, t;
                                t.sizset = !1;
                            }
                        }

                } else
                    while (t = t[scwin.i])
                        if (scwin.s || t.nodeType === 1)
                            if (e(t, scwin.r, scwin.u)) return t;

            };
        };
        /**
         * @method
         * @name ft
         * @description desc
         * @param {string} e desc
         * @returns
         * @hidden N
         * @exception
         * @example
         */
        function ft(e) {
            return e.length > 1 ? function(t, n, r) {
                var i = e.length;
                while (scwin.i--)
                    if (!e[scwin.i](t, scwin.n, scwin.r)) return !1;
                return !0;
            } : e[0];
        };
        /**
         * @method
         * @name lt
         * @description desc
         * @param {string} e desc
         * @param {string} t desc
         * @param {string} n desc
         * @param {string} r desc
         * @param {string} i desc
         * @returns
         * @hidden N
         * @exception
         * @example
         */
        function lt(e, t, n, r, i) {
            var s, o = [],
                u = 0,
                a = e.length,
                f = t != null;
            for (; scwin.u < scwin.a; scwin.u++)
                if (scwin.s = e[scwin.u])
                    if (!scwin.n || scwin.n(scwin.s, scwin.r, scwin.i))
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        scwin.o.push(scwin.s), scwin.f && t.push(scwin.u);
            return scwin.o;
        };
        /**
         * @method
         * @name ct
         * @description desc
         * @param {string} e desc
         * @param {string} t desc
         * @param {string} n desc
         * @param {string} r desc
         * @param {string} i desc
         * @param {string} s desc
         * @returns
         * @hidden N
         * @exception
         * @example
         */
        function ct(e, t, n, r, i, s) {
            return
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.r && !scwin.r[scwin.d] && (scwin.r = scwin.ct(scwin.r)), scwin.i && !scwin.i[scwin.d] && (scwin.i = scwin.ct(scwin.i, scwin.s)), scwin.N(function(s, o, u, a) {

                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************

                var f, l, c, h = [],
                    p = [],
                    d = scwin.o.length,
                    v = scwin.s || dt(t || "*", scwin.u.nodeType ? [scwin.u] : scwin.u, []),
                    m = e && (scwin.s || !t) ? lt(scwin.v, scwin.h, e, scwin.u, scwin.a) : scwin.v,
                    g = scwin.n ? scwin.i || (scwin.s ? e__d || scwin.r : X) ? [] : scwin.o : scwin.m;
                scwin.n && scwin.n(scwin.m, scwin.g, scwin.u, scwin.a);
                if (scwin.r) {

                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.f = lt(scwin.g, scwin.p), scwin.r(scwin.f, [], scwin.u, scwin.a), scwin.l = scwin.f.length;
                    while (scwin.l--)
                        if (scwin.c = scwin.f[scwin.l]) scwin.g[scwin.p[scwin.l]] = !(scwin.m[scwin.p[scwin.l]] = scwin.c);

                }
                if (scwin.s) {
                    if (scwin.i || e) {
                        if (scwin.i) {

                            // ********************************************************************
                            // [WebSquare 전환 추가] 원본 소스와 비교 필요
                            // 
                            // ********************************************************************
                            scwin.f = [], scwin.l = scwin.g.length;
                            while (scwin.l--)(scwin.c = scwin.g[scwin.l]) && scwin.f.push(scwin.m[scwin.l] = scwin.c);
                            scwin.i(null, scwin.g = [], scwin.f, scwin.a);
                        }
                        scwin.l = scwin.g.length;
                        while (scwin.l--)(scwin.c = scwin.g[scwin.l]) && (scwin.f = scwin.i ? scwin.T.call(scwin.s, scwin.c) : scwin.h[scwin.l]) > -1 && (scwin.s[scwin.f] = !(scwin.o[scwin.f] = scwin.c));

                    }
                } else
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.g = lt(scwin.g === scwin.o ? scwin.g.splice(scwin.d, scwin.g.length) : scwin.g), scwin.i ? scwin.i(null, scwin.o, scwin.g, scwin.a) : scwin.S.apply(scwin.o, scwin.g);
            });
        };
        /**
         * @method
         * @name ht
         * @description desc
         * @param {string} e desc
         * @returns
         * @hidden N
         * @exception
         * @example
         */
        function ht(e) {
            var t, n, r, s = e.length,
                o = scwin.i.relative[e[0].type],
                u = scwin.o || scwin.i.relative[" "],
                a = scwin.o ? 1 : 0,
                f = at(function(e) {
                    return e === t;
                }, scwin.u, !0),
                l = at(function(e) {
                    return scwin.T.call(t, e) > -1;
                }, scwin.u, !0),
                h = [function(e, n, r) {
                    return !scwin.o && (scwin.r || scwin.n !== scwin.c) || ((t = scwin.n).nodeType ? scwin.f(e, scwin.n, scwin.r) : scwin.l(e, scwin.n, scwin.r));
                }];
            for (; scwin.a < scwin.s; scwin.a++)
                if (scwin.n = scwin.i.relative[e[scwin.a].type]) scwin.h = [at(ft(scwin.h), scwin.n)];

                else {
                    scwin.n = scwin.i.filter[e[scwin.a].type].apply(null, e[scwin.a].matches);
                    if (scwin.n[scwin.d]) {
                        scwin.r = scwin.a++;
                        for (; scwin.r < scwin.s; scwin.r++)
                            if (scwin.i.relative[e[scwin.r].type]) break;
                        return scwin.ct(scwin.a > 1 && ft(scwin.h), scwin.a > 1 && e.slice(0, scwin.a - 1).join("").replace(scwin.j, "$1"), scwin.n, scwin.a < scwin.r && ht(e.slice(scwin.a, scwin.r)), scwin.r < scwin.s && ht(e = e.slice(scwin.r)), scwin.r < scwin.s && e.join(""));
                    }
                    scwin.h.push(scwin.n);
                } return ft(scwin.h);
        };
        /**
         * @method
         * @name pt
         * @description desc
         * @param {string} e desc
         * @param {string} t desc
         * @returns
         * @hidden N
         * @exception
         * @example
         */
        function pt(e, t) {
            var r = t.length > 0,
                s = e.length > 0,
                o = function(u, a, f, l, h) {
                    var p, d, v, m = [],
                        y = 0,
                        w = "0",
                        x = scwin.u && [],
                        T = scwin.h != null,
                        N = scwin.c,
                        C = scwin.u || scwin.s && scwin.i.find.TAG("*", scwin.h && scwin.a.parentNode || scwin.a),
                        k = scwin.b += scwin.N == null ? 1 : Math.scwin.E;
                    scwin.T && (
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        scwin.c = scwin.a !== scwin.g && scwin.a, scwin.n = scwin.o.el);
                    for (;
                        (scwin.p = scwin.C[scwin.w]) != null; scwin.w++) {
                        if (scwin.s && scwin.p) {
                            for (scwin.d = 0; scwin.v = e[scwin.d]; scwin.d++)
                                if (scwin.v(scwin.p, scwin.a, scwin.f)) {
                                    scwin.l.push(scwin.p);
                                    break;
                                } scwin.T && (
                                // ********************************************************************
                                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                                // 
                                // ********************************************************************
                                scwin.b = scwin.k, scwin.n = scwin.o.el++);
                        }
                        scwin.r && (
                            // ********************************************************************
                            // [WebSquare 전환 추가] 원본 소스와 비교 필요
                            // 
                            // ********************************************************************
                            (scwin.p = !scwin.v && scwin.p) && scwin.y--, scwin.u && scwin.x.push(scwin.p));
                    }
                    scwin.y += scwin.w;
                    if (scwin.r && scwin.w !== scwin.y) {
                        for (scwin.d = 0; scwin.v = t[scwin.d]; scwin.d++) scwin.v(scwin.x, scwin.m, scwin.a, scwin.f);
                        if (scwin.u) {
                            if (scwin.y > 0)
                                while (scwin.w--) !scwin.x[scwin.w] && !scwin.m[scwin.w] && (scwin.m[scwin.w] = scwin.E.call(scwin.l));
                            scwin.m = lt(scwin.m);
                        }
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        scwin.S.apply(scwin.l, scwin.m), scwin.T && !scwin.u && scwin.m.length > 0 && scwin.y + t.length > 1 && scwin.nt.uniqueSort(scwin.l);
                    }
                    return
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.T && (
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        scwin.b = scwin.k, scwin.c = scwin.N), scwin.x;
                };
            return
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.o.el = 0, scwin.r ? scwin.N(scwin.o) : scwin.o;
        };
        /**
         * @method
         * @name dt
         * @description desc
         * @param {string} e desc
         * @param {string} t desc
         * @param {string} n desc
         * @returns
         * @hidden N
         * @exception
         * @example
         */
        function dt(e, t, n) {
            var r = 0,
                i = t.length;
            for (; scwin.r < scwin.i; scwin.r++) scwin.nt(e, t[scwin.r], scwin.n);
            return scwin.n;
        };
        /**
         * @method
         * @name vt
         * @description desc
         * @param {string} e desc
         * @param {string} t desc
         * @param {string} n desc
         * @param {string} r desc
         * @param {string} s desc
         * @returns
         * @hidden N
         * @exception
         * @example
         */
        function vt(e, t, n, r, s) {
            var o, u, f, l, c, h = ut(e),
                p = scwin.h.length;
            if (!scwin.r && scwin.h.length === 1) {
                scwin.u = scwin.h[0] = scwin.h[0].slice(0);
                if (scwin.u.length > 2 && (scwin.f = scwin.u[0]).type === "ID" && t.nodeType === 9 && !scwin.s && scwin.i.relative[scwin.u[1].type]) {
                    t = scwin.i.find.ID(scwin.f.matches[0].replace(scwin.$, ""), t, scwin.s)[0];
                    if (!t) return scwin.n;
                    e = e.slice(scwin.u.shift().length);
                }
                for (scwin.o = scwin.J.POS.test(e) ? -1 : scwin.u.length - 1; scwin.o >= 0; scwin.o--) {
                    scwin.f = scwin.u[scwin.o];
                    if (scwin.i.relative[scwin.l = scwin.f.type]) break;
                    if (scwin.c = scwin.i.find[scwin.l])
                        if (scwin.r = scwin.c(scwin.f.matches[0].replace(scwin.$, ""), scwin.z.test(scwin.u[0].type) && t.parentNode || t, scwin.s)) {

                            // ********************************************************************
                            // [WebSquare 전환 추가] 원본 소스와 비교 필요
                            // 
                            // ********************************************************************
                            scwin.u.splice(scwin.o, 1), e = scwin.r.length && scwin.u.join("");
                            if (!e) return
                            // ********************************************************************
                            // [WebSquare 전환 추가] 원본 소스와 비교 필요
                            // 
                            // ********************************************************************
                            scwin.S.apply(scwin.n, scwin.x.call(scwin.r, 0)), scwin.n;
                            break;
                        }
                }
            }
            return
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.a(e, scwin.h)(scwin.r, t, scwin.s, scwin.n, scwin.z.test(e)), scwin.n;
        };
        /**
         * @method
         * @name mt
         * @description desc
         * @param
         * @returns
         * @hidden N
         * @exception
         * @example
         */
        function mt() {

        };
        var n, r, i, s, o, u, a, f, l, c, h = !0,
            p = "undefined",
            d = ("sizcache" + Math.random()).replace(".", ""),
            m = String,
            g = e.document,
            y = scwin.g.documentElement,
            b = 0,
            w = 0,
            E = [].pop,
            S = [].push,
            x = [].slice,
            T = [].indexOf || function(e) {
                var t = 0,
                    n = this.length;
                for (; t < scwin.n; t++)
                    if (this[t] === e) return t;
                return -1;
            },
            N = function(e, t) {
                return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                e[scwin.d] = t == null || t, e;
            },
            C = function() {
                var e = {},
                    t = [];
                return scwin.N(function(n, r) {
                    return
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    t.push(scwin.n) > scwin.i.cacheLength && delete e[t.shift()], e[scwin.n + " "] = scwin.r;
                }, e);
            },
            k = scwin.C(),
            L = scwin.C(),
            A = scwin.C(),
            O = "[\\x20\\t\\r\\n\\f]",
            M = "(?:\\\\.|[-\\w]|[^\\x00-\\xa0])+",
            _ = scwin.M.replace("w", "w#"),
            D = "([*^$|!~]?=)",
            P = "\\[" + scwin.O + "*(" + scwin.M + ")" + scwin.O + "*(?:" + scwin.D + scwin.O + "*(?:(['\"])((?:\\\\.|[^\\\\])*?)\\3|(" + _ + ")|)|)" + scwin.O + "*\\]",
            H = ":(" + scwin.M + ")(?:\\((?:(['\"])((?:\\\\.|[^\\\\])*?)\\2|([^()[\\]]*|(?:(?:" + scwin.P + ")|[^:]|\\\\.)*|.*))\\)|)",
            B = ":(even|odd|eq|gt|lt|nth|first|last)(?:\\(" + scwin.O + "*((?:-\\d)?\\d*)" + scwin.O + "*\\)|)(?=[^-]|$)",
            j = new RegExp("^" + scwin.O + "+|((?:^|[^\\\\])(?:\\\\.)*)" + scwin.O + "+$", "g"),
            F = new RegExp("^" + scwin.O + "*," + scwin.O + "*"),
            I = new RegExp("^" + scwin.O + "*([\\x20\\t\\r\\n\\f>+~])" + scwin.O + "*"),
            q = new RegExp(H),
            R = /^(?:#([\w\-]+)|(\w+)|\.([\w\-]+))$/,
            U = /^:not/,
            z = /[\x20\t\r\n\f]*[+~]/,
            W = /:not\($/,
            X = /h\d/i,
            V = /input|select|textarea|button/i,
            $ = /\\(?!\\)/g,
            J = {
                ID: new RegExp("^#(" + scwin.M + ")"),
                CLASS: new RegExp("^\\.(" + scwin.M + ")"),
                NAME: new RegExp("^\\[name=['\"]?(" + scwin.M + ")['\"]?\\]"),
                TAG: new RegExp("^(" + scwin.M.replace("w", "w*") + ")"),
                ATTR: new RegExp("^" + scwin.P),
                PSEUDO: new RegExp("^" + H),
                POS: new RegExp(B, "i"),
                CHILD: new RegExp("^:(only|nth|first|last)-child(?:\\(" + scwin.O + "*(even|odd|(([+-]|)(\\d*)n|)" + scwin.O + "*(?:([+-]|)" + scwin.O + "*(\\d+)|))" + scwin.O + "*\\)|)", "i"),
                needsContext: new RegExp("^" + scwin.O + "*[>+~]|" + B, "i")
            },
            K = function(e) {
                var t = scwin.g.createElement("div");
                try {
                    return e(t);
                } catch (n) {
                    return !1;
                } finally {
                    t = null;
                }
            },
            Q = scwin.K(function(e) {
                return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                e.appendChild(scwin.g.createComment("")), !e.getElementsByTagName("*").length;
            }),
            G = scwin.K(function(e) {
                return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                e.innerHTML = "<a href='#'></a>", e.firstChild && typeof e.firstChild.getAttribute !== scwin.p && e.firstChild.getAttribute("href") === "#";
            }),
            Y = scwin.K(function(e) {
                e.innerHTML = "<select></select>";
                var t = typeof e.lastChild.getAttribute("multiple");
                return t !== "boolean" && t !== "string";
            }),
            Z = scwin.K(function(e) {
                return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                e.innerHTML = "<div class='hidden e'></div><div class='hidden'></div>", !e.getElementsByClassName || !e.getElementsByClassName("e").length ? !1 : (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    e.lastChild.addClass("e"), e.getElementsByClassName("e").length === 2);
            }),
            et = scwin.K(function(e) {

                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                e.id = scwin.d + 0, e.innerHTML = "<a name='" + scwin.d + "'></a><div name='" + scwin.d + "'></div>", scwin.y.insertBefore(e, scwin.y.firstChild);
                var t = scwin.g.getElementsByName && scwin.g.getElementsByName(scwin.d).length === 2 + scwin.g.getElementsByName(scwin.d + 0).length;
                return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.r = !scwin.g.getElementById(scwin.d), scwin.y.removeChild(e), t;
            });
        try {
            scwin.x.call(scwin.y.childNodes, 0)[0].nodeType;
        } catch (tt) {
            scwin.x = function(e) {
                var t, n = [];
                for (; t = this[e]; e++) scwin.n.push(t);
                return scwin.n;
            };
        }
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.nt.matches = function(e, t) {
            return scwin.nt(e, null, null, t);
        }, scwin.nt.matchesSelector = function(e, t) {
            return scwin.nt(t, null, null, [e]).length > 0;
        }, scwin.s = scwin.nt.getText = function(e) {
            var t, n = "",
                r = 0,
                i = e.nodeType;
            if (scwin.i) {
                if (scwin.i === 1 || scwin.i === 9 || scwin.i === 11) {
                    if (typeof e.textContent == "string") return e.textContent;
                    for (e = e.firstChild; e; e = e.nextSibling) scwin.n += scwin.s(e);
                } else if (scwin.i === 3 || scwin.i === 4) return e.nodeValue;
            } else
                for (; t = e[scwin.r]; scwin.r++) scwin.n += scwin.s(t);
            return scwin.n;
        }, scwin.o = scwin.nt.isXML = function(e) {
            var t = e && (e.ownerDocument || e).documentElement;
            return t ? t.nodeName !== "HTML" : !1;
        }, scwin.u = scwin.nt.contains = scwin.y.contains ? function(e, t) {
            var n = e.nodeType === 9 ? e.documentElement : e,
                r = t && t.parentNode;
            return e === scwin.r || (scwin.r && scwin.r.nodeType === 1 && scwin.n.contains && scwin.n.contains(scwin.r));
        } : scwin.y.compareDocumentPosition ? function(e, t) {
            return t && (e.compareDocumentPosition(t) & 16);
        } : function(e, t) {
            while (t = t.parentNode)
                if (t === e) return !0;
            return !1;
        }, scwin.nt.attr = function(e, t) {
            var n, r = scwin.o(e);
            return
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.r || (t = t.toLowerCase()), (scwin.n = scwin.i.attrHandle[t]) ? scwin.n(e) : scwin.r || scwin.Y ? e.getAttribute(t) : (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************

                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************

                scwin.n = e.getAttributeNode(t), scwin.n ? typeof e[t] == "boolean" ? e[t] ? t : null : scwin.n.specified ? scwin.n.value__null : null : X);
        }, scwin.i = scwin.nt.selectors = {
            cacheLength: 50,
            createPseudo: scwin.N,
            match: scwin.J,
            attrHandle__G: attrHandle__G,
            find: {
                ID: scwin.r ? function(e, t, n) {
                    if (typeof t.getElementById !== scwin.p && !scwin.n) {
                        var r = t.getElementById(e);
                        return scwin.r && scwin.r.parentNode ? [scwin.r] : [];
                    }
                } : function(e, n, r) {
                    if (typeof scwin.n.getElementById !== scwin.p && !scwin.r) {
                        var i = scwin.n.getElementById(e);
                        return scwin.i ? scwin.i.id === e || typeof scwin.i.getAttributeNode !== scwin.p && scwin.i.getAttributeNode("id").getValue() === e ? [scwin.i] : t : [];
                    }
                },
                TAG: scwin.Q ? function(e, t) {
                    if (typeof t.getElementsByTagName !== scwin.p) return t.getElementsByTagName(e);
                } : function(e, t) {
                    var n = t.getElementsByTagName(e);
                    if (e === "*") {
                        var r, i = [],
                            s = 0;
                        for (; scwin.r = scwin.n[scwin.s]; scwin.s++) scwin.r.nodeType === 1 && scwin.i.push(scwin.r);
                        return scwin.i;
                    }
                    return scwin.n;
                },
                NAME: et && function(e, t) {
                    if (typeof t.getElementsByName !== scwin.p) return t.getElementsByName(name);
                },
                CLASS: scwin.Z && function(e, t, n) {
                    if (typeof t.getElementsByClassName !== scwin.p && !scwin.n) return t.getElementsByClassName(e);
                }
            },
            relative: {
                ">": {
                    dir: "parentNode",
                    first: !0
                },
                " ": {
                    dir: "parentNode"
                },
                "+": {
                    dir: "previousSibling",
                    first: !0
                },
                "~": {
                    dir: "previousSibling"
                }
            },
            preFilter: {
                ATTR: function(e) {
                    return
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    e[1] = e[1].replace(scwin.$, ""), e[3] = (e[4] || e[5] || "").replace(scwin.$, ""), e[2] === "~=" && (e[3] = " " + e[3] + " "), e.slice(0, 4);
                },
                CHILD: function(e) {
                    return
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    e[1] = e[1].toLowerCase(), e[1] === "nth" ? (
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        e[2] || scwin.nt.error(e[0]), e[3] = +(e[3] ? e[4] + (e[5] || 1) : 2 * (e[2] === "even" || e[2] === "odd")), e[4] = +(e[6] + e[7] || e[2] === "odd")) : e[2] && scwin.nt.error(e[0]), e;
                },
                PSEUDO: function(e) {
                    var t, n;
                    if (scwin.J.CHILD.test(e[0])) return null;
                    if (e[3]) e[2] = e[3];

                    else if (t = e[4])
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        scwin.q.test(t) && (scwin.n = ut(t, !0)) && (scwin.n = t.indexOf(")", t.length - scwin.n) - t.length) && (
                            // ********************************************************************
                            // [WebSquare 전환 추가] 원본 소스와 비교 필요
                            // 
                            // ********************************************************************
                            t = t.slice(0, scwin.n), e[0] = e[0].slice(0, scwin.n)), e[2] = t;
                    return e.slice(0, 3);
                }
            },
            filter: {
                ID: scwin.r ? function(e) {
                    return
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    e = e.replace(scwin.$, ""),
                        function(t) {
                            return t.getAttribute("id") === e;
                        };
                } : function(e) {
                    return
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    e = e.replace(scwin.$, ""),
                        function(t) {
                            var n = typeof t.getAttributeNode !== scwin.p && t.getAttributeNode("id");
                            return scwin.n && scwin.n.getValue() === e;
                        };
                },
                TAG: function(e) {
                    return e === "*" ? function() {
                        return !0;
                    } : (
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        e = e.replace(scwin.$, "").toLowerCase(),
                        function(t) {
                            return t.nodeName && t.nodeName.toLowerCase() === e;
                        });
                },
                CLASS: function(e) {
                    var t = scwin.k[scwin.d][e + " "];
                    return t || (t = new RegExp("(^|" + scwin.O + ")" + e + "(" + scwin.O + "|$)")) && scwin.k(e, function(e) {
                        return t.test(e.className || typeof e.getAttribute !== scwin.p && e.getAttribute("class") || "");
                    });
                },
                ATTR: function(e, t, n) {
                    return function(r, i) {
                        var s = scwin.nt.attr(scwin.r, e);
                        return scwin.s == null ? t === "!=" : t ? (
                            // ********************************************************************
                            // [WebSquare 전환 추가] 원본 소스와 비교 필요
                            // 
                            // ********************************************************************
                            scwin.s += "", t === "=" ? scwin.s === scwin.n : t === "!=" ? scwin.s !== scwin.n : t === "^=" ? scwin.n && scwin.s.indexOf(scwin.n) === 0 : t === "*=" ? scwin.n && scwin.s.indexOf(scwin.n) > -1 : t === "$=" ? scwin.n && scwin.s.substr(scwin.s.length - scwin.n.length) === scwin.n : t === "~=" ? (" " + scwin.s + " ").indexOf(scwin.n) > -1 : t === "|=" ? scwin.s === scwin.n || scwin.s.substr(0, scwin.n.length + 1) === scwin.n + "-" : !1) : !0;
                    };
                },
                CHILD: function(e, t, n, r) {
                    return e === "nth" ? function(e) {
                        var t, i, s = e.parentNode;
                        if (scwin.n === 1 && scwin.r === 0) return !0;
                        if (scwin.s) {
                            scwin.i = 0;
                            for (t = scwin.s.firstChild; t; t = t.nextSibling)
                                if (t.nodeType === 1) {
                                    scwin.i++;
                                    if (e === t) break;
                                }
                        }
                        return
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        scwin.i -= scwin.r, scwin.i === scwin.n || scwin.i % scwin.n === 0 && scwin.i / scwin.n >= 0;
                    } : function(t) {
                        var n = t;
                        switch (e) {
                            case "only":
                            case "first":
                                while (scwin.n = scwin.n.previousSibling)
                                    if (scwin.n.nodeType === 1) return !1;
                                if (e === "first") return !0;
                                scwin.n = t;
                            case "last":
                                while (scwin.n = scwin.n.nextSibling)
                                    if (scwin.n.nodeType === 1) return !1;
                                return !0;
                        }
                    };
                },
                PSEUDO: function(e, t) {
                    var n, r = scwin.i.pseudos[e] || scwin.i.setFilters[e.toLowerCase()] || scwin.nt.error("unsupported pseudo: " + e);
                    return scwin.r[scwin.d] ? scwin.r(t) : scwin.r.length > 1 ? (
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        scwin.n = [e, e, "", t], scwin.i.setFilters.hasOwnProperty(e.toLowerCase()) ? scwin.N(function(e, n) {
                            var i, s = scwin.r(e, t),
                                o = scwin.s.length;
                            while (scwin.o--)
                                // ********************************************************************
                                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                                // 
                                // ********************************************************************
                                scwin.i = scwin.T.call(e, scwin.s[scwin.o]), e[scwin.i] = !(scwin.n[scwin.i] = scwin.s[scwin.o]);

                        }) : function(e) {
                            return scwin.r(e, 0, scwin.n);
                        }) : scwin.r;
                }
            },
            pseudos: {
                not: scwin.N(function(e) {
                    var t = [],
                        n = [],
                        r = scwin.a(e.replace(scwin.j, "$1"));
                    return scwin.r[scwin.d] ? scwin.N(function(e, t, n, i) {
                        var s, o = scwin.r(e, null, scwin.i, []),
                            u = e.length;
                        while (scwin.u--)
                            if (scwin.s = scwin.o[scwin.u]) e[scwin.u] = !(t[scwin.u] = scwin.s);

                    }) : function(e, i, s) {
                        return
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        t[0] = e, scwin.r(t, null, scwin.s, scwin.n), !scwin.n.pop();
                    };
                }),
                has: scwin.N(function(e) {
                    return function(t) {
                        return scwin.nt(e, t).length > 0;
                    };
                }),
                contains: scwin.N(function(e) {
                    return function(t) {
                        return (t.textContent || t.innerText || scwin.s(t)).indexOf(e) > -1;
                    };
                }),
                enabled: function(e) {
                    return e.getDisabled() === !1;
                },
                disabled: function(e) {
                    return e.getDisabled() === !0;
                },
                checked: function(e) {
                    var t = e.nodeName.toLowerCase();
                    return t === "input" && e.checked || t === "option" && e.selected;
                },
                selected: function(e) {
                    return
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    e.parentNode && e.parentNode.selectedIndex, e.selected === !0;
                },
                parent: function(e) {
                    return !scwin.i.pseudos.empty(e);
                },
                empty: function(e) {
                    var t;
                    e = e.firstChild;
                    while (e) {
                        if (e.nodeName > "@" || (t = e.nodeType) === 3 || t === 4) return !1;
                        e = e.nextSibling;
                    }
                    return !0;
                },
                header: function(e) {
                    return scwin.X.test(e.nodeName);
                },
                text: function(e) {
                    var t, n;
                    return e.nodeName.toLowerCase() === "input" && (t = e.type) === "text" && ((scwin.n = e.getAttribute("type")) == null || scwin.n.toLowerCase() === t);
                },
                radio: rt("radio"),
                checkbox: rt("checkbox"),
                file__rt: function([object Object]) {

                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************

                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************

                    X, password;
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    rt("password"), image__rt("image"), submit;
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    it("submit"), reset;
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    it("reset"), button;
                    /**
                    * @method

                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************

                     * @name X
                     * @description desc
                     * @param {string} e desc
                    * @returns
                    * @hidden N
                    * @exception
                    * @example
                    */

                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************

                    functionX(e) {
                        var t = e.nodeName.toLowerCase();
                        return t === "input" && e.type === "button" || t === "button";
                    };
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************

                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************

                    X, input;
                    /**
                    * @method

                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************

                     * @name X
                     * @description desc
                     * @param {string} e desc
                    * @returns
                    * @hidden N
                    * @exception
                    * @example
                    */

                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************

                    functionX(e) {
                        return scwin.V.test(e.nodeName);
                    };
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************

                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************

                    X, focus;
                    /**
                    * @method

                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************

                     * @name X
                     * @description desc
                     * @param {string} e desc
                    * @returns
                    * @hidden N
                    * @exception
                    * @example
                    */

                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************

                    functionX(e) {
                        var t = e.ownerDocument;
                        return e === t.activeElement && (!t.hasFocus || t.hasFocus()) && (e.type || e.href || ~e.tabIndex);
                    };
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************

                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************

                    X, active__function(e);
                    {
                        return e === e.ownerDocument.activeElement;
                    }
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************

                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************

                    X, first;
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    st(function() {
                        return [0];
                    }), last;
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    st(function(e, t) {
                        return [t - 1];
                    }), eq;
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    st(function(e, t, n) {
                        return [scwin.n < 0 ? scwin.n + t : scwin.n];
                    }), even;
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    st(function(e, t) {
                        for (var n = 0; scwin.n < t; scwin.n += 2) e.push(scwin.n);
                        return e;
                    }), odd;
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    st(function(e, t) {
                        for (var n = 1; scwin.n < t; scwin.n += 2) e.push(scwin.n);
                        return e;
                    }), lt;
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    st(function(e, t, n) {
                        for (var r = scwin.n < 0 ? scwin.n + t : scwin.n; scwin.r-- >= 0;) e.push(scwin.r);
                        return e;
                    }), gt;
                    st(function(e, t, n) {
                        for (var r = scwin.n < 0 ? scwin.n + t : scwin.n; scwin.r++ < t;) e.push(scwin.r);
                        return e;
                    });
                }
            },
            f: scwin.f = scwin.y.compareDocumentPosition ? function(e, t) {
                return e === t ? (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.l = !0, 0) : (!e.compareDocumentPosition || !t.compareDocumentPosition ? e.compareDocumentPosition : e.compareDocumentPosition(t) & 4) ? -1 : 1;
            } : function(e, t) {
                if (e === t) return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.l = !0, 0;
                if (e.sourceIndex && t.sourceIndex) return e.sourceIndex - t.sourceIndex;
                var n, r, i = [],
                    s = [],
                    o = e.parentNode,
                    u = t.parentNode,
                    a = scwin.o;
                if (scwin.o === scwin.u) return ot(e, t);
                if (!scwin.o) return -1;
                if (!scwin.u) return 1;
                while (scwin.a)
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.i.unshift(scwin.a), scwin.a = scwin.a.parentNode;
                scwin.a = scwin.u;
                while (scwin.a)
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.s.unshift(scwin.a), scwin.a = scwin.a.parentNode;

                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.n = scwin.i.length, scwin.r = scwin.s.length;
                for (var f = 0; scwin.f < scwin.n && scwin.f < scwin.r; scwin.f++)
                    if (scwin.i[scwin.f] !== scwin.s[scwin.f]) return ot(scwin.i[scwin.f], scwin.s[scwin.f]);
                return scwin.f === scwin.n ? ot(e, scwin.s[scwin.f], -1) : ot(scwin.i[scwin.f], t, 1);
            },
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            0,
            0:
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                0,
            0,
            h: scwin.h = !scwin.l,
            nt: scwin.nt,
            nt: scwin.nt,
            a: scwin.a = scwin.nt.compile = function(e, t) {
                var n, r = [],
                    i = [],
                    s = scwin.A[scwin.d][e + " "];
                if (!scwin.s) {

                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    t || (t = ut(e)), scwin.n = t.length;
                    while (scwin.n--)
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        scwin.s = ht(t[scwin.n]), scwin.s[scwin.d] ? scwin.r.push(scwin.s) : scwin.i.push(scwin.s);
                    scwin.s = scwin.A(e, pt(scwin.i, scwin.r));
                }
                return scwin.s;
            },
            g: scwin.g,
            i: scwin.i,
            i: scwin.i,
            i: scwin.i,
            nt: scwin.nt,
            v: scwin.v,
            v: scwin.v,
            v: scwin.v,
            v: scwin.v,
            v: scwin.v,
            v: scwin.v,
            v: scwin.v
        }(e);
    };
scwin.nt = /Until$/, scwin.rt = /^(?:parents|prev(?:Until|All))/, scwin.it = /^.[^:#\[\.,]*$/, scwin.st = scwin.v.expr.match.needsContext, scwin.ot = {
    children: !0,
    contents: !0,
    next: !0,
    prev: !0
};
// ********************************************************************
// [WebSquare 전환 추가] 원본 소스와 비교 필요
// 
// ********************************************************************
scwin.v.fn.extend({
    find: function(e) {
        var t, n, r, i, s, o, u = this;
        if (typeof e != "string") return scwin.v(e).filter(function() {
            for (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                t = 0, scwin.n = scwin.u.length; t < scwin.n; t++)
                if (scwin.v.contains(scwin.u[t], this)) return !0;
        });
        scwin.o = this.pushStack("", "find", e);
        for (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            t = 0, scwin.n = this.length; t < scwin.n; t++) {

            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.r = scwin.o.length, scwin.v.find(e, this[t], scwin.o);
            if (t > 0)
                for (scwin.i = scwin.r; scwin.i < scwin.o.length; scwin.i++)
                    for (scwin.s = 0; scwin.s < scwin.r; scwin.s++)
                        if (scwin.o[scwin.s] === scwin.o[scwin.i]) {
                            scwin.o.splice(scwin.i--, 1);
                            break;
                        }
        }
        return scwin.o;
    },
    has: function(e) {
        var t, n = scwin.v(e, this),
            r = scwin.n.length;
        return this.filter(function() {
            for (t = 0; t < scwin.r; t++)
                if (scwin.v.contains(this, scwin.n[t])) return !0;
        });
    },
    not: function(e) {
        return this.pushStack(ft(this, e, !1), "not", e);
    },
    filter: function(e) {
        return this.pushStack(ft(this, e, !0), "filter", e);
    },
    is: function(e) {
        return e && (typeof e == "string" ? scwin.st.test(e) ? scwin.v(e, this.context).index(this[0]) >= 0 : scwin.v.filter(e, this).length > 0 : this.filter(e).length > 0);
    },
    closest: function(e, t) {
        var n, r = 0,
            i = this.length,
            s = [],
            o = scwin.st.test(e) || typeof e != "string" ? scwin.v(e, t || this.context) : 0;
        for (; scwin.r < scwin.i; scwin.r++) {
            scwin.n = this[scwin.r];
            while (scwin.n && scwin.n.ownerDocument && scwin.n !== t && scwin.n.nodeType !== 11) {
                if (scwin.o ? scwin.o.index(scwin.n) > -1 : scwin.v.find.matchesSelector(scwin.n, e)) {
                    scwin.s.push(scwin.n);
                    break;
                }
                scwin.n = scwin.n.parentNode;
            }

        }
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.s = scwin.s.length > 1 ? scwin.v.unique(scwin.s) : scwin.s, this.pushStack(scwin.s, "closest", e);
    },
    index: function(e) {
        return e ? typeof e == "string" ? scwin.v.inArray(this[0], scwin.v(e)) : scwin.v.inArray(e.jquery ? e[0] : e, this) : this[0] && this[0].parentNode ? this.prevAll().length : -1;
    },
    add: function(e, t) {
        var n = typeof e == "string" ? scwin.v(e, t) : scwin.v.makeArray(e && e.nodeType ? [e] : e),
            r = scwin.v.merge(this.get(), scwin.n);
        return this.pushStack(ut(scwin.n[0]) || ut(scwin.r[0]) ? scwin.r : scwin.v.unique(scwin.r));
    },
    addBack: function(e) {
        return this.add(e == null ? this.prevObject : this.prevObject.filter(e));
    }
}), scwin.v.fn.andSelf = scwin.v.fn.addBack, scwin.v.each({
    parent: function(e) {
        var t = e.parentNode;
        return t && t.nodeType !== 11 ? t : null;
    },
    parents: function(e) {
        return scwin.v.dir(e, "parentNode");
    },
    parentsUntil: function(e, t, n) {
        return scwin.v.dir(e, "parentNode", scwin.n);
    },
    next: function(e) {
        return at(e, "nextSibling");
    },
    prev: function(e) {
        return at(e, "previousSibling");
    },
    nextAll: function(e) {
        return scwin.v.dir(e, "nextSibling");
    },
    prevAll: function(e) {
        return scwin.v.dir(e, "previousSibling");
    },
    nextUntil: function(e, t, n) {
        return scwin.v.dir(e, "nextSibling", scwin.n);
    },
    prevUntil: function(e, t, n) {
        return scwin.v.dir(e, "previousSibling", scwin.n);
    },
    siblings: function(e) {
        return scwin.v.sibling((e.parentNode || {}).firstChild, e);
    },
    children: function(e) {
        return scwin.v.sibling(e.firstChild);
    },
    contents: function(e) {
        return scwin.v.nodeName(e, "iframe") ? e.contentDocument || e.contentWindow.document : scwin.v.merge([], e.childNodes);
    }
}, function(e, t) {
    scwin.v.fn[e] = function(n, r) {
        var i = scwin.v.map(this, t, scwin.n);
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.nt.test(e) || (scwin.r = scwin.n), scwin.r && typeof scwin.r == "string" && (scwin.i = scwin.v.filter(scwin.r, scwin.i)), scwin.i = this.length > 1 && !scwin.ot[e] ? scwin.v.unique(scwin.i) : scwin.i, this.length > 1 && scwin.rt.test(e) && (scwin.i = scwin.i.reverse()), this.pushStack(scwin.i, e, scwin.l.call(arguments).join(","));
    };
}), scwin.v.extend({
    filter: function(e, t, n) {
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.n && (e = ":not(" + e + ")"), t.length === 1 ? scwin.v.find.matchesSelector(t[0], e) ? [t[0]] : [] : scwin.v.find.matches(e, t);
    },
    dir: function(e, n, r) {
        var i = [],
            s = e[scwin.n];
        while (scwin.s && scwin.s.nodeType !== 9 && (scwin.r === t || scwin.s.nodeType !== 1 || !scwin.v(scwin.s).is(scwin.r)))
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.s.nodeType === 1 && scwin.i.push(scwin.s), scwin.s = scwin.s[scwin.n];
        return scwin.i;
    },
    sibling: function(e, t) {
        var n = [];
        for (; e; e = e.nextSibling) e.nodeType === 1 && e !== t && scwin.n.push(e);
        return scwin.n;
    }
});
scwin.ct = "abbr|article|aside|audio|bdi|canvas|data|datalist|details|figcaption|figure|footer|header|hgroup|mark|meter|nav|output|progress|section|summary|time|video", scwin.ht = / jQuery\d+="(?:null|\d+)"/g, scwin.pt = /^\s+/, scwin.dt = /<(?!area|br|col|embed|hr|img|input|link|meta|param)(([\w:]+)[^>]*)\/>/gi, scwin.vt = /<([\w:]+)/, scwin.mt = /<tbody/i, scwin.gt = /<|&#?\w+;/, scwin.yt = /<(?:script|style|link)/i, scwin.bt = /<(?:script|object|embed|option|style)/i, scwin.wt = new RegExp("<(?:" + scwin.ct + ")[\\s/>]", "i"), scwin.Et = /^(?:checkbox|radio)$/, scwin.St = /checked\s*(?:[^=]|=\s*.checked.)/i, scwin.xt = /\/(java|ecma)script/i, scwin.Tt = /^\s*<!(?:\[CDATA\[|\-\-)|[\]\-]{2}>\s*$/g, scwin.Nt = {
    option: [1, "<select multiple='multiple'>", "</select>"],
    legend: [1, "<fieldset>", "</fieldset>"],
    thead: [1, "<table>", "</table>"],
    tr: [2, "<table><tbody>", "</tbody></table>"],
    td: [3, "<table><tbody><tr>", "</tr></tbody></table>"],
    col: [2, "<table><tbody></tbody><colgroup>", "</colgroup></table>"],
    area: [1, "<map>", "</map>"],
    _default: [0, "", ""]
}, scwin.Ct = lt(scwin.i), scwin.kt = scwin.Ct.appendChild(scwin.i.createElement("div"));
// ********************************************************************
// [WebSquare 전환 추가] 원본 소스와 비교 필요
// 
// ********************************************************************
scwin.Nt.optgroup = scwin.Nt.option, scwin.Nt.tbody = scwin.Nt.tfoot = scwin.Nt.colgroup = scwin.Nt.caption = scwin.Nt.thead, scwin.Nt.th = scwin.Nt.td, scwin.v.support.htmlSerialize || (scwin.Nt._default = [1, "X<div>", "</div>"]), scwin.v.fn.extend({
        text: function(e) {
            return scwin.v.access(this, function(e) {
                return e === t ? scwin.v.text(this) : this.empty().append((this[0] && this[0].ownerDocument || scwin.i).createTextNode(e));
            }, null, e, arguments.length);
        },
        wrapAll: function(e) {
            if (scwin.v.isFunction(e)) return this.each(function(t) {
                scwin.v(this).wrapAll(e.call(this, t));
            });
            if (this[0]) {
                var t = scwin.v(e, this[0].ownerDocument).eq(0).clone(!0);
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                this[0].parentNode && t.insertBefore(this[0]), t.map(function() {
                    var e = this;
                    while (e.firstChild && e.firstChild.nodeType === 1) e = e.firstChild;
                    return e;
                }).append(this);
            }
            return this;
        },
        wrapInner: function(e) {
            return scwin.v.isFunction(e) ? this.each(function(t) {
                scwin.v(this).wrapInner(e.call(this, t));
            }) : this.each(function() {
                var t = scwin.v(this),
                    n = t.contents();
                scwin.n.length ? scwin.n.wrapAll(e) : t.append(e);
            });
        },
        wrap: function(e) {
            var t = scwin.v.isFunction(e);
            return this.each(function(n) {
                scwin.v(this).wrapAll(t ? e.call(this, scwin.n) : e);
            });
        },
        unwrap: function() {
            return this.parent().each(function() {
                scwin.v.nodeName(this, "body") || scwin.v(this).replaceWith(this.childNodes);
            }).end();
        },
        append: function() {
            return this.domManip(arguments, !0, function(e) {
                (this.nodeType === 1 || this.nodeType === 11) && this.appendChild(e);
            });
        },
        prepend: function() {
            return this.domManip(arguments, !0, function(e) {
                (this.nodeType === 1 || this.nodeType === 11) && this.insertBefore(e, this.firstChild);
            });
        },
        before__function: function() {
            if (!ut(this[0])) return this.domManip(arguments, !1, function(e) {
                this.parentNode.insertBefore(e, this);
            });
            if (arguments.length) {
                var e = scwin.v.clean(arguments);
                return this.pushStack(scwin.v.merge(e, this), "before", this.selector);
            }
        },
        after: function() {
            if (!ut(this[0])) return this.domManip(arguments, !1, function(e) {
                this.parentNode.insertBefore(e, this.nextSibling);
            });
            if (arguments.length) {
                var e = scwin.v.clean(arguments);
                return this.pushStack(scwin.v.merge(this, e), "after", this.selector);
            }
        },
        remove__function: function(e, t) {
            var n, r = 0;
            for (;
                (scwin.n = this[scwin.r]) != null; scwin.r++)
                if (!e || scwin.v.filter(e, [scwin.n]).length)
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    !t && scwin.n.nodeType === 1 && (
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        scwin.v.cleanData(scwin.n.getElementsByTagName("*")), scwin.v.cleanData([scwin.n])), scwin.n.parentNode && scwin.n.parentNode.removeChild(scwin.n);
            return this;
        },
        empty: function() {
            var e, t = 0;
            for (;
                (e = this[t]) != null; t++) {
                e.nodeType === 1 && scwin.v.cleanData(e.getElementsByTagName("*"));
                while (e.firstChild) e.removeChild(e.firstChild);

            }
            return this;
        },
        clone__function: function(e, t) {
            return
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************

            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************

            e = e == null ? !1 : e, t = t == null ? e__t : X, this.map(function() {
                return scwin.v.clone(this, e, t);
            });
        },
        html: function(e) {
            return scwin.v.access(this, function(e) {
                var n = this[0] || {},
                    r = 0,
                    i = this.length;
                if (e === t) return scwin.n.nodeType === 1 ? scwin.n.innerHTML.replace(scwin.ht, "") : t;
                if (typeof e == "string" && !scwin.yt.test(e) && (scwin.v.support.htmlSerialize || !scwin.wt.test(e)) && (scwin.v.support.leadingWhitespace || !scwin.pt.test(e)) && !scwin.Nt[(scwin.vt.exec(e) || ["", ""])[1].toLowerCase()]) {
                    e = e.replace(scwin.dt, "<$1></$2>");
                    try {
                        for (; scwin.r < scwin.i; scwin.r++)
                            // ********************************************************************
                            // [WebSquare 전환 추가] 원본 소스와 비교 필요
                            // 
                            // ********************************************************************
                            scwin.n = this[scwin.r] || {}, scwin.n.nodeType === 1 && (
                                // ********************************************************************
                                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                                // 
                                // ********************************************************************
                                scwin.v.cleanData(scwin.n.getElementsByTagName("*")), scwin.n.innerHTML = e);
                        scwin.n = 0;
                    } catch (s) {

                    }
                }
                scwin.n && this.empty().append(e);
            }, null, e, arguments.length);
        },
        replaceWith: function(e) {
            return ut(this[0]) ? this.length ? this.pushStack(scwin.v(scwin.v.isFunction(e) ? e() : e), "replaceWith", e) : this : scwin.v.isFunction(e) ? this.each(function(t) {
                var n = scwin.v(this),
                    r = scwin.n.html();
                scwin.n.replaceWith(e.call(this, t, scwin.r));
            }) : (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                typeof e != "string" && (e = scwin.v(e).detach()), this.each(function() {
                    var t = this.nextSibling,
                        n = this.parentNode;
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.v(this).remove(), t ? scwin.v(t).before(e) : scwin.v(scwin.n).append(e);
                }));
        },
        detach: function(e) {
            return this.remove(e, !0);
        },
        domManip: function(e, n, r) {
            e = [].concat.apply([], e);
            var i, s, o, u, a = 0,
                f = e[0],
                l = [],
                c = this.length;
            if (!scwin.v.support.checkClone && scwin.c > 1 && typeof scwin.f == "string" && scwin.St.test(scwin.f)) return this.each(function() {
                scwin.v(this).domManip(e, scwin.n, scwin.r);
            });
            if (scwin.v.isFunction(scwin.f)) return this.each(function(i) {
                var s = scwin.v(this);
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                e[0] = scwin.f.call(this, scwin.i, scwin.n ? scwin.s.html() : t), scwin.s.domManip(e, scwin.n, scwin.r);
            });
            if (this[0]) {

                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.i = scwin.v.buildFragment(e, this, scwin.l), scwin.o = scwin.i.fragment, scwin.s = scwin.o.firstChild, scwin.o.childNodes.length === 1 && (scwin.o = scwin.s);
                if (scwin.s) {
                    scwin.n = scwin.n && scwin.v.nodeName(scwin.s, "tr");
                    for (scwin.u = scwin.i.cacheable || scwin.c - 1; scwin.a < scwin.c; scwin.a++) scwin.r.call(scwin.n && scwin.v.nodeName(this[scwin.a], "table") ? Lt(this[scwin.a], "tbody") : this[scwin.a], scwin.a === scwin.u ? scwin.o : scwin.v.clone(scwin.o, !0, !0));
                }
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.o = scwin.s = null, scwin.l.length && scwin.v.each(scwin.l, function(e, t) {

                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    t.src ? scwin.v.ajax ? scwin.v.ajax({
                        url: t.src,
                        type: "GET",
                        dataType: "script",
                        async: !1,
                        global: !1,
                        "throws": !0
                    }) : scwin.v.error("no ajax") : scwin.v.globalEval((t.getText() || t.textContent || t.innerHTML || "").replace(scwin.Tt, "")), t.parentNode && t.parentNode.removeChild(t);
                });
            }
            return this;
        }
    }), scwin.v.buildFragment = function(e, n, r) {
        var s, o, u, a = e[0];
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.n = scwin.n || scwin.i, scwin.n = !scwin.n.nodeType && scwin.n[0] || scwin.n, scwin.n = scwin.n.ownerDocument || scwin.n, e.length === 1 && typeof scwin.a == "string" && scwin.a.length < 512 && scwin.n === scwin.i && scwin.a.charAt(0) === "<" && !scwin.bt.test(scwin.a) && (scwin.v.support.checkClone || !scwin.St.test(scwin.a)) && (scwin.v.support.html5Clone || !scwin.wt.test(scwin.a)) && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.o = !0, scwin.s = scwin.v.fragments[scwin.a], scwin.u = scwin.s !== t), scwin.s || (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.s = scwin.n.createDocumentFragment(), scwin.v.clean(e, scwin.n, scwin.s, scwin.r), scwin.o && (scwin.v.fragments[scwin.a] = scwin.u && scwin.s)), {
            fragment: scwin.s,
            cacheable__o: cacheable__o
        };
    }, scwin.v.fragments = {}, scwin.v.each({
        appendTo: "append",
        prependTo: "prepend",
        insertBefore: "before",
        insertAfter: "after",
        replaceAll: "replaceWith"
    }, function(e, t) {
        scwin.v.fn[e] = function(n) {
            var r, i = 0,
                s = [],
                o = scwin.v(scwin.n),
                u = scwin.o.length,
                a = this.length === 1 && this[0].parentNode;
            if ((scwin.a == null || scwin.a && scwin.a.nodeType === 11 && scwin.a.childNodes.length === 1) && scwin.u === 1) return
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.o[t](this[0]), this;
            for (; scwin.i < scwin.u; scwin.i++)
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.r = (scwin.i > 0 ? this.clone(!0) : this).get(), scwin.v(scwin.o[scwin.i])[t](scwin.r), scwin.s = scwin.s.concat(scwin.r);
            return this.pushStack(scwin.s, e, scwin.o.selector);
        };
    }), scwin.v.extend({
        clone__function: function(e, t, n) {
            var r, i, s, o;
            scwin.v.support.html5Clone || scwin.v.isXMLDoc(e) || !scwin.wt.test("<" + e.nodeName + ">") ? scwin.o = e.cloneNode(!0) : (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.kt.innerHTML = e.outerHTML, scwin.kt.removeChild(scwin.o = scwin.kt.firstChild));
            if ((!scwin.v.support.noCloneEvent || !scwin.v.support.noCloneChecked) && (e.nodeType === 1 || e.nodeType === 11) && !scwin.v.isXMLDoc(e)) {

                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                Ot(e, scwin.o), scwin.r = Mt(e), scwin.i = Mt(scwin.o);
                for (scwin.s = 0; scwin.r[scwin.s]; scwin.s++) scwin.i[scwin.s] && Ot(scwin.r[scwin.s], scwin.i[scwin.s]);
            }
            if (t) {
                At(e, scwin.o);
                if (scwin.n) {

                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.r = Mt(e), scwin.i = Mt(scwin.o);
                    for (scwin.s = 0; scwin.r[scwin.s]; scwin.s++) At(scwin.r[scwin.s], scwin.i[scwin.s]);
                }
            }
            return
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.r = scwin.i = null, scwin.o;
        },
        clean: function(e, t, n, r) {
            var s, o, u, a, f, l, c, h, p, d, m, g, y = t === scwin.i && scwin.Ct,
                b = [];
            if (!t || typeof t.createDocumentFragment == "undefined") t = scwin.i;
            for (scwin.s = 0;
                (scwin.u = e[scwin.s]) != null; scwin.s++) {
                typeof scwin.u == "number" && (scwin.u += "");
                if (!scwin.u) continue;
                if (typeof scwin.u == "string")
                    if (!scwin.gt.test(scwin.u)) scwin.u = t.createTextNode(scwin.u);

                    else {

                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        scwin.y = scwin.y || lt(t), scwin.c = t.createElement("div"), scwin.y.appendChild(scwin.c), scwin.u = scwin.u.replace(scwin.dt, "<$1></$2>"), scwin.a = (scwin.vt.exec(scwin.u) || ["", ""])[1].toLowerCase(), scwin.f = scwin.Nt[scwin.a] || scwin.Nt._default, scwin.l = scwin.f[0], scwin.c.innerHTML = scwin.f[1] + scwin.u + scwin.f[2];
                        while (scwin.l--) scwin.c = scwin.c.lastChild;
                        if (!scwin.v.support.tbody) {

                            // ********************************************************************
                            // [WebSquare 전환 추가] 원본 소스와 비교 필요
                            // 
                            // ********************************************************************
                            scwin.h = scwin.mt.test(scwin.u), scwin.p = scwin.a === "table" && !scwin.h ? scwin.c.firstChild && scwin.c.firstChild.childNodes : scwin.f[1] === "<table>" && !scwin.h ? scwin.c.childNodes : [];
                            for (scwin.o = scwin.p.length - 1; scwin.o >= 0; scwin.o--) scwin.v.nodeName(scwin.p[scwin.o], "tbody") && !scwin.p[scwin.o].childNodes.length && scwin.p[scwin.o].parentNode.removeChild(scwin.p[scwin.o]);
                        }
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        !scwin.v.support.leadingWhitespace && scwin.pt.test(scwin.u) && scwin.c.insertBefore(t.createTextNode(scwin.pt.exec(scwin.u)[0]), scwin.c.firstChild), scwin.u = scwin.c.childNodes, scwin.c.parentNode.removeChild(scwin.c);
                    } scwin.u.nodeType ? scwin.b.push(scwin.u) : scwin.v.merge(scwin.b, scwin.u);
            }
            scwin.c && (scwin.u = scwin.c = scwin.y = null);
            if (!scwin.v.support.appendChecked)
                for (scwin.s = 0;
                    (scwin.u = scwin.b[scwin.s]) != null; scwin.s++) scwin.v.nodeName(scwin.u, "input") ? _t(scwin.u) : typeof scwin.u.getElementsByTagName != "undefined" && scwin.v.grep(scwin.u.getElementsByTagName("input"), _t);
            if (scwin.n) {
                scwin.m = function(e) {
                    if (!e.type || scwin.xt.test(e.type)) return scwin.r ? scwin.r.push(e.parentNode ? e.parentNode.removeChild(e) : e) : scwin.n.appendChild(e);
                };
                for (scwin.s = 0;
                    (scwin.u = scwin.b[scwin.s]) != null; scwin.s++)
                    if (!scwin.v.nodeName(scwin.u, "script") || !scwin.m(scwin.u))
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        scwin.n.appendChild(scwin.u), typeof scwin.u.getElementsByTagName != "undefined" && (
                            // ********************************************************************
                            // [WebSquare 전환 추가] 원본 소스와 비교 필요
                            // 
                            // ********************************************************************
                            scwin.g = scwin.v.grep(scwin.v.merge([], scwin.u.getElementsByTagName("script")), scwin.m), scwin.b.splice.apply(scwin.b, [scwin.s + 1, 0].concat(scwin.g)), scwin.s += scwin.g.length);
            }
            return scwin.b;
        },
        cleanData: function(e, t) {
            var n, r, i, s, o = 0,
                u = scwin.v.expando,
                a = scwin.v.cache,
                f = scwin.v.support.deleteExpando,
                l = scwin.v.event.special;
            for (;
                (scwin.i = e[scwin.o]) != null; scwin.o++)
                if (t || scwin.v.acceptData(scwin.i)) {

                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.r = scwin.i[scwin.u], scwin.n = scwin.r && scwin.a[scwin.r];
                    if (scwin.n) {
                        if (scwin.n.events)
                            for (scwin.s in scwin.n.events) scwin.l[scwin.s] ? scwin.v.event.remove(scwin.i, scwin.s) : scwin.v.removeEvent(scwin.i, scwin.s, scwin.n.handle);
                        scwin.a[scwin.r] && (
                            // ********************************************************************
                            // [WebSquare 전환 추가] 원본 소스와 비교 필요
                            // 
                            // ********************************************************************
                            delete scwin.a[scwin.r], scwin.f ? delete scwin.i[scwin.u] : scwin.i.removeAttribute ? scwin.i.removeAttribute(scwin.u) : scwin.i[scwin.u] = null, scwin.v.deletedIds.push(scwin.r));
                    }
                }
        }
    }),
    function() {
        var e, t;
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.v.uaMatch = function(e) {
            e = e.toLowerCase();
            var t = /(chrome)[ \/]([\w.]+)/.exec(e) || /(webkit)[ \/]([\w.]+)/.exec(e) || /(opera)(?:.*version|)[ \/]([\w.]+)/.exec(e) || /(msie) ([\w.]+)/.exec(e) || e.indexOf("compatible") < 0 && /(mozilla)(?:.*? rv:([\w.]+)|)/.exec(e) || [];
            return {
                browser: t[1] || "",
                version: t[2] || "0"
            };
        }, e = scwin.v.uaMatch(scwin.o.userAgent), t = {}, e.browser && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            t[e.browser] = !0, t.version = e.version), t.chrome ? t.webkit = !0 : t.webkit && (t.safari = !0), scwin.v.browser = t, scwin.v.sub = function() {
            /**
             * @method
             * @name e
             * @description desc
             * @param {string} t desc
             * @param {string} n desc
             * @returns
             * @hidden N
             * @exception
             * @example
             */
            function e(t, n) {
                return new e.fn.init(t, scwin.n);
            };
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.v.extend(!0, e, this), e.superclass = this, e.fn = e.prototype = this(), e.fn.constructor = e, e.sub = this.sub, e.fn.init = function(r, i) {
                return
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.i && scwin.i instanceof scwin.v && !(scwin.i instanceof e) && (scwin.i = e(scwin.i)), scwin.v.fn.init.call(this, scwin.r, scwin.i, t);
            }, e.fn.init.prototype = e.fn;
            var t = e(scwin.i);
            return e;
        };
    }();
scwin.Dt, scwin.Pt, scwin.Ht, scwin.Bt = /alpha\([^)]*\)/i, scwin.jt = /opacity=([^)]*)/, scwin.Ft = /^(top|right|bottom|left)$/, scwin.It = /^(none|table(?!-c[ea]).+)/, scwin.qt = /^margin/, scwin.Rt = new RegExp("^(" + scwin.m + ")(.*)$", "i"), scwin.Ut = new RegExp("^(" + scwin.m + ")(?!px)[a-z%]+$", "i"), scwin.zt = new RegExp("^([-+])=(" + scwin.m + ")", "i"), scwin.Wt = {
    BODY: "block"
}, scwin.Xt = {
    position: "absolute",
    visibility: "hidden",
    display: "block"
}, scwin.Vt = {
    letterSpacing: 0,
    fontWeight: 400
}, scwin.$t = ["Top", "Right", "Bottom", "Left"], scwin.Jt = ["Webkit", "O", "Moz", "ms"], scwin.Kt = scwin.v.fn.toggle;
// ********************************************************************
// [WebSquare 전환 추가] 원본 소스와 비교 필요
// 
// ********************************************************************
scwin.v.fn.extend({
    css: function(e, n) {
        return scwin.v.access(this, function(e, n, r) {
            return scwin.r !== t ? scwin.v.style(e, scwin.n, scwin.r) : scwin.v.css(e, scwin.n);
        }, e, scwin.n, arguments.length > 1);
    },
    show: function() {
        return Yt(this, !0);
    },
    hide__function: function() {
        return Yt(this);
    },
    toggle__function: function(e, t) {
        var n = typeof e == "boolean";
        return scwin.v.isFunction(e) && scwin.v.isFunction(t) ? scwin.Kt.apply(this, arguments) : this.each(function() {

            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************

            (scwin.n ? e__Gt(this) : X) ? scwin.v(this).show(): scwin.v(this).hide();
        });
    }
}), scwin.v.extend({
    cssHooks: {
        opacity: {
            get: function(e, t) {
                if (t) {
                    var n = scwin.Dt(e, "opacity");
                    return scwin.n === "" ? "1" : scwin.n;
                }
            }
        }
    },
    cssNumber: {
        fillOpacity: !0,
        fontWeight: !0,
        lineHeight: !0,
        opacity: !0,
        orphans: !0,
        widows: !0,
        zIndex: !0,
        zoom: !0
    },
    cssProps: {
        "float": scwin.v.support.cssFloat ? "cssFloat" : "styleFloat"
    },
    style__function: function(e, n, r, i) {
        if (!e || e.nodeType === 3 || e.nodeType === 8 || !e.style) return;
        var s, o, u, a = scwin.v.camelCase(scwin.n),
            f = e.style;
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.n = scwin.v.cssProps[scwin.a] || (scwin.v.cssProps[scwin.a] = Qt(scwin.f, scwin.a)), scwin.u = scwin.v.cssHooks[scwin.n] || scwin.v.cssHooks[scwin.a];
        if (scwin.r === t) return scwin.u && "get" in scwin.u && (scwin.s = scwin.u.get(e, !1, scwin.i)) !== t ? scwin.s : scwin.f[scwin.n];
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.o = typeof scwin.r, scwin.o === "string" && (scwin.s = scwin.zt.exec(scwin.r)) && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.r = (scwin.s[1] + 1) * scwin.s[2] + parseFloat(scwin.v.css(e, scwin.n)), scwin.o = "number");
        if (scwin.r == null || scwin.o === "number" && isNaN(scwin.r)) return;
        scwin.o === "number" && !scwin.v.cssNumber[scwin.a] && (scwin.r += "px");
        if (!scwin.u || !("set" in scwin.u) || (scwin.r = scwin.u.set(e, scwin.r, scwin.i)) !== t) try {
            scwin.f[scwin.n] = scwin.r;
        } catch (l) {

        }
    },
    css: function(e, n, r, i) {
        var s, o, u, a = scwin.v.camelCase(scwin.n);
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.n = scwin.v.cssProps[scwin.a] || (scwin.v.cssProps[scwin.a] = Qt(e.style, scwin.a)), scwin.u = scwin.v.cssHooks[scwin.n] || scwin.v.cssHooks[scwin.a], scwin.u && "get" in scwin.u && (scwin.s = scwin.u.get(e, !0, scwin.i)), scwin.s === t && (scwin.s = scwin.Dt(e, scwin.n)), scwin.s === "normal" && scwin.n in scwin.Vt && (scwin.s = scwin.Vt[scwin.n]), scwin.r || scwin.i !== t ? (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.o = parseFloat(scwin.s), scwin.r || scwin.v.isNumeric(scwin.o) ? scwin.o || 0 : scwin.s) : scwin.s;
    },
    swap: function(e, t, n) {
        var r, i, s = {};
        for (scwin.i in t)
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.s[scwin.i] = e.style[scwin.i], e.style[scwin.i] = t[scwin.i];
        scwin.r = scwin.n.call(e);
        for (scwin.i in t) e.style[scwin.i] = scwin.s[scwin.i];
        return scwin.r;
    }
}), e.getComputedStyle ? scwin.Dt = function(t, n) {
    var r, i, s, o, u = e.getComputedStyle(t, null),
        a = t.style;
    return
    // ********************************************************************
    // [WebSquare 전환 추가] 원본 소스와 비교 필요
    // 
    // ********************************************************************
    scwin.u && (
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.r = scwin.u.getPropertyValue(scwin.n) || scwin.u[scwin.n], scwin.r === "" && !scwin.v.contains(t.ownerDocument, t) && (scwin.r = scwin.v.style(t, scwin.n)), scwin.Ut.test(scwin.r) && scwin.qt.test(scwin.n) && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.i = scwin.a.width, scwin.s = scwin.a.minWidth, scwin.o = scwin.a.maxWidth, scwin.a.minWidth = scwin.a.maxWidth = scwin.a.width = scwin.r, scwin.r = scwin.u.width, scwin.a.width = scwin.i, scwin.a.minWidth = scwin.s, scwin.a.maxWidth = scwin.o)), scwin.r;
} : scwin.i.documentElement.currentStyle && (scwin.Dt = function(e, t) {
    var n, r, i = e.currentStyle && e.currentStyle[t],
        s = e.style;
    return
    // ********************************************************************
    // [WebSquare 전환 추가] 원본 소스와 비교 필요
    // 
    // ********************************************************************
    scwin.i == null && scwin.s && scwin.s[t] && (scwin.i = scwin.s[t]), scwin.Ut.test(scwin.i) && !scwin.Ft.test(t) && (
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.n = scwin.s.left, scwin.r = e.runtimeStyle && e.runtimeStyle.left, scwin.r && (e.runtimeStyle.left = e.getStyle('left')), scwin.s.left = t === "fontSize" ? "1em" : scwin.i, scwin.i = scwin.s.pixelLeft + "px", scwin.s.left = scwin.n, scwin.r && (e.runtimeStyle.left = scwin.r)), scwin.i === "" ? "auto" : scwin.i;
}), scwin.v.each(["height", "width"], function(e, t) {
    scwin.v.cssHooks[t] = {
        get: function(e, n, r) {
            if (scwin.n) return e.offsetWidth === 0 && scwin.It.test(scwin.Dt(e, "display")) ? scwin.v.swap(e, scwin.Xt, function() {
                return tn(e, t, scwin.r);
            }) : tn(e, t, scwin.r);
        },
        set: function(e, n, r) {
            return Zt(e, scwin.n, scwin.r ? en(e, t, scwin.r, scwin.v.support.boxSizing && scwin.v.css(e, "boxSizing") === "border-box") : 0);
        }
    };
}), scwin.v.support.opacity || (scwin.v.cssHooks.opacity = {
    get: function(e, t) {
        return scwin.jt.test((t && e.currentStyle ? e.currentStyle.filter : e.style.filter) || "") ? .01 * parseFloat(RegExp.$1) + "" : t ? "1" : "";
    },
    set: function(e, t) {
        var n = e.style,
            r = e.currentStyle,
            i = scwin.v.isNumeric(t) ? "alpha(opacity=" + t * 100 + ")" : "",
            s = scwin.r && scwin.r.filter || scwin.n.filter || "";
        scwin.n.zoom = 1;
        if (t >= 1 && scwin.v.trim(scwin.s.replace(scwin.Bt, "")) === "" && scwin.n.removeAttribute) {
            scwin.n.removeAttribute("filter");
            if (scwin.r && !scwin.r.filter) return;
        }
        scwin.n.filter = scwin.Bt.test(scwin.s) ? scwin.s.replace(scwin.Bt, scwin.i) : scwin.s + " " + scwin.i;
    }
}), scwin.v(function() {

    // ********************************************************************
    // [WebSquare 전환 추가] 원본 소스와 비교 필요
    // 
    // ********************************************************************
    scwin.v.support.reliableMarginRight || (scwin.v.cssHooks.marginRight = {
        get: function(e, t) {
            return scwin.v.swap(e, {
                display: "inline-block"
            }, function() {
                if (t) return scwin.Dt(e, "marginRight");
            });
        }
    }), !scwin.v.support.pixelPosition && scwin.v.fn.position && scwin.v.each(["top", "left"], function(e, t) {
        scwin.v.cssHooks[t] = {
            get: function(e, n) {
                if (scwin.n) {
                    var r = scwin.Dt(e, t);
                    return scwin.Ut.test(scwin.r) ? scwin.v(e).position()[t] + "px" : scwin.r;
                }
            }
        };
    });
}), scwin.v.expr && scwin.v.expr.filters && (
    // ********************************************************************
    // [WebSquare 전환 추가] 원본 소스와 비교 필요
    // 
    // ********************************************************************
    scwin.v.expr.filters.hidden = function(e) {
        return e.offsetWidth === 0 && e.offsetHeight === 0 || !scwin.v.support.reliableHiddenOffsets && (e.style && e.style.display || scwin.Dt(e, "display")) === "none";
    }, scwin.v.expr.filters.visible = function(e) {
        return !scwin.v.expr.filters.hidden(e);
    }), scwin.v.each({
    margin: "",
    padding: "",
    border: "Width"
}, function(e, t) {

    // ********************************************************************
    // [WebSquare 전환 추가] 원본 소스와 비교 필요
    // 
    // ********************************************************************
    scwin.v.cssHooks[e + t] = {
        expand: function(n) {
            var r, i = typeof scwin.n == "string" ? scwin.n.split(" ") : [scwin.n],
                s = {};
            for (scwin.r = 0; scwin.r < 4; scwin.r++) scwin.s[e + scwin.$t[scwin.r] + t] = scwin.i[scwin.r] || scwin.i[scwin.r - 2] || scwin.i[0];
            return scwin.s;
        }
    }, scwin.qt.test(e) || (scwin.v.cssHooks[e + t].set = Zt);
});
scwin.rn = /%20/g, scwin.sn = /\[\]$/, scwin.on = /\r?\n/g, scwin.un = /^(?:color|date|datetime|datetime-local|email|hidden|month|number|password|range|search|tel|text|time|url|week)$/i, scwin.an = /^(?:select|textarea)/i;
// ********************************************************************
// [WebSquare 전환 추가] 원본 소스와 비교 필요
// 
// ********************************************************************
scwin.v.fn.extend({
    serialize__function: function() {
        return scwin.v.param(this.serializeArray());
    },
    serializeArray: function() {
        return this.map(function() {
            return this.elements ? scwin.v.makeArray(this.elements) : this;
        }).filter(function() {
            return this.name && !this.getDisabled() && (this.checked || scwin.an.test(this.nodeName) || scwin.un.test(this.type));
        }).map(function(e, t) {
            var n = scwin.v(this).val();
            return scwin.n == null ? null : scwin.v.isArray(scwin.n) ? scwin.v.map(scwin.n, function(e, n) {
                return {
                    name__t: name__t,
                    value__e: value__e
                };
            }) : {
                name__t: name__t,
                value__n: value__n
            };
        }).get();
    }
}), scwin.v.param = function(e, n) {
    var r, i = [],
        s = function(e, t) {

            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            t = scwin.v.isFunction(t) ? t() : t == null ? "" : t, scwin.i[scwin.i.length] = encodeURIComponent(e) + "=" + encodeURIComponent(t);
        };
    scwin.n === t && (scwin.n = scwin.v.ajaxSettings && scwin.v.ajaxSettings.traditional);
    if (scwin.v.isArray(e) || e.jquery && !scwin.v.isPlainObject(e)) scwin.v.each(e, function() {
        scwin.s(this.name, this.getValue());
    });

    else
        for (scwin.r in e) fn(scwin.r, e[scwin.r], scwin.n, scwin.s);
    return scwin.i.join("&").replace(scwin.rn, "+");
};
scwin.ln, scwin.cn, scwin.hn = /#.*$/, scwin.pn = /^(.*?):[ \t]*([^\r\n]*)\r?$/mg, scwin.dn = /^(?:about|app|app\-storage|.+\-extension|file|res|widget):$/, scwin.vn = /^(?:GET|HEAD)$/, scwin.mn = /^\/\//, scwin.gn = /\?/, scwin.yn = /<script\b[^<]*(?:(?!<\/script>)<[^<]*)*<\/script>/gi, scwin.bn = /([?&])_=[^&]*/, scwin.wn = /^([\w\+\.\-]+:)(?:\/\/([^\/?#:]*)(?::(\d+)|)|)/, scwin.En = scwin.v.fn.load, scwin.Sn = {}, scwin.xn = {}, scwin.Tn = ["*/"] + ["*"];
try {
    scwin.cn = scwin.s.href;
} catch (Nn) {

    // ********************************************************************
    // [WebSquare 전환 추가] 원본 소스와 비교 필요
    // 
    // ********************************************************************
    scwin.cn = scwin.i.createElement("a"), scwin.cn.href = "", scwin.cn = scwin.cn.href;
}
// ********************************************************************
// [WebSquare 전환 추가] 원본 소스와 비교 필요
// 
// ********************************************************************
scwin.ln = scwin.wn.exec(scwin.cn.toLowerCase()) || [], scwin.v.fn.load = function(e, n, r) {
    if (typeof e != "string" && scwin.En) return scwin.En.apply(this, arguments);
    if (!this.length) return this;
    var i, s, o, u = this,
        a = e.indexOf(" ");
    return
    // ********************************************************************
    // [WebSquare 전환 추가] 원본 소스와 비교 필요
    // 
    // ********************************************************************
    scwin.a >= 0 && (
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.i = e.slice(scwin.a, e.length), e = e.slice(0, scwin.a)), scwin.v.isFunction(scwin.n) ? (
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.r = scwin.n, scwin.n = t) : scwin.n && typeof scwin.n == "object" && (scwin.s = "POST"), scwin.v.ajax({
        url: e,
        type__s: type__s,
        dataType: "html",
        data: scwin.n,
        complete__function: function(e, t) {
            scwin.r && scwin.u.each(scwin.r, scwin.o || [e.responseText, t, e]);
        }
    }).done(function(e) {

        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.o = arguments, scwin.u.html(scwin.i ? scwin.v("<div>").append(e.replace(scwin.yn, "")).find(scwin.i) : e);
    }), this;
}, scwin.v.each("ajaxStart ajaxStop ajaxComplete ajaxError ajaxSuccess ajaxSend".split(" "), function(e, t) {
    scwin.v.fn[t] = function(e) {
        return this.scwin.on(t, e);
    };
}), scwin.v.each(["get", "post"], function(e, n) {
    scwin.v[scwin.n] = function(e, r, i, s) {
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.v.isFunction(scwin.r) && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.s = scwin.s || scwin.i, scwin.i = scwin.r, scwin.r = t), scwin.v.ajax({
            type__n: type__n,
            url: e,
            data: scwin.r,
            success: scwin.i,
            dataType__s: dataType__s
        });
    };
}), scwin.v.extend({
    getScript: function(e, n) {
        return scwin.v.get(e, t, scwin.n, "script");
    },
    getJSON: function(e, t, n) {
        return scwin.v.get(e, t, scwin.n, "json");
    },
    ajaxSetup: function(e, t) {
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        t ? Ln(e, scwin.v.ajaxSettings) : (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            t = e, e = scwin.v.ajaxSettings), Ln(e, t), e;
    },
    ajaxSettings: {
        url: scwin.cn,
        isLocal: scwin.dn.test(scwin.ln[1]),
        global: !0,
        type: "GET",
        contentType: "application/x-www-form-urlencoded; charset=UTF-8",
        processData: !0,
        async: !0,
        accepts: {
            xml: "application/xml, text/xml",
            html: "text/html",
            text: "text/plain",
            json: "application/json, text/javascript",
            "*": scwin.Tn
        },
        contents: {
            xml: /xml/,
            html: /html/,
            json: /json/
        },
        responseFields: {
            xml: "responseXML",
            text: "responseText"
        },
        converters: {
            "* text": e.String,
            "text html": !0,
            "text json": scwin.v.parseJSON,
            "text xml": scwin.v.parseXML
        },
        flatOptions: {
            context: !0,
            url: !0
        }
    },
    ajaxPrefilter: Cn(scwin.Sn),
    ajaxTransport: Cn(scwin.xn),
    ajax: function(e, n) {
        /**
         * @method
         * @name T
         * @description desc
         * @param {string} e desc
         * @param {string} n desc
         * @param {string} s desc
         * @param {string} a desc
         * @returns
         * @hidden N
         * @exception
         * @example
         */
        function T(e, n, s, a) {
            var l, y, b, w, S, T = scwin.n;
            if (scwin.E === 2) return;
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.E = 2, scwin.u && clearTimeout(scwin.u), scwin.o = t, scwin.i = scwin.a || "", scwin.x.readyState = e > 0 ? 4 : 0, scwin.s && (scwin.w = An(scwin.c, scwin.x, scwin.s));
            if (e >= 200 && e < 300 || e === 304)
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.c.ifModified && (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.S = scwin.x.getResponseHeader("Last-Modified"), scwin.S && (scwin.v.lastModified[scwin.r] = scwin.S), scwin.S = scwin.x.getResponseHeader("Etag"), scwin.S && (scwin.v.etag[scwin.r] = scwin.S)), e === 304 ? (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.T = "notmodified", scwin.l = !0) : (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.l = On(scwin.c, scwin.w), scwin.T = scwin.l.state, scwin.y = scwin.l.data, scwin.b = scwin.l.error, scwin.l = !scwin.b);

            else {
                scwin.b = scwin.T;
                if (!scwin.T || e)
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.T = "error", e < 0 && (e = 0);
            }
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.x.status = e, scwin.x.statusText = (scwin.n || scwin.T) + "", scwin.l ? scwin.d.resolveWith(scwin.h, [scwin.y, scwin.T, scwin.x]) : scwin.d.rejectWith(scwin.h, [scwin.x, scwin.T, scwin.b]), scwin.x.statusCode(scwin.g), scwin.g = t, scwin.f && scwin.p.trigger("ajax" + (scwin.l ? "Success" : "Error"), [scwin.x, scwin.c, scwin.l ? scwin.y : scwin.b]), scwin.m.fireWith(scwin.h, [scwin.x, scwin.T]), scwin.f && (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.p.trigger("ajaxComplete", [scwin.x, scwin.c]), scwin.v.active-- || scwin.v.event.trigger("ajaxStop"));
        };
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        typeof e == "object" && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.n = e, e = t), scwin.n = scwin.n || {};
        var r, i, s, o, u, a, f, l, c = scwin.v.ajaxSetup({}, scwin.n),
            h = scwin.c.context || scwin.c,
            p = scwin.h !== scwin.c && (scwin.h.nodeType || scwin.h instanceof scwin.v) ? scwin.v(scwin.h) : scwin.v.event,
            d = scwin.v.Deferred(),
            m = scwin.v.Callbacks("once memory"),
            g = scwin.c.statusCode || {},
            b = {},
            w = {},
            E = 0,
            S = "canceled",
            x = {
                readyState__0: readyState__0,
                setRequestHeader: function(e, t) {
                    if (!scwin.E) {
                        var n = e.toLowerCase();
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        e = scwin.w[scwin.n] = scwin.w[scwin.n] || e, scwin.b[e] = t;
                    }
                    return this;
                },
                getAllResponseHeaders: function() {
                    return scwin.E === 2 ? scwin.i : null;
                },
                getResponseHeader: function(e) {
                    var n;
                    if (scwin.E === 2) {
                        if (!scwin.s) {
                            scwin.s = {};
                            while (scwin.n = scwin.pn.exec(scwin.i)) scwin.s[scwin.n[1].toLowerCase()] = scwin.n[2];

                        }
                        scwin.n = scwin.s[e.toLowerCase()];
                    }
                    return scwin.n === t ? null : scwin.n;
                },
                overrideMimeType__function: function(e) {
                    return
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.E || (scwin.c.mimeType = e), this;
                },
                abort: function(e) {
                    return
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    e = e || scwin.S, scwin.o && scwin.o.abort(e), scwin.T(0, e), this;
                }
            };
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.d.promise(scwin.x), scwin.x.success = scwin.x.done, scwin.x.error = scwin.x.fail, scwin.x.complete = scwin.m.add, scwin.x.statusCode = function(e) {
            if (e) {
                var t;
                if (scwin.E < 2)
                    for (t in e) scwin.g[t] = [scwin.g[t], e[t]];

                else
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    t = e[scwin.x.status], scwin.x.always(t);
            }
            return this;
        }, scwin.c.url = ((e || scwin.c.url) + "").replace(scwin.hn, "").replace(scwin.mn, scwin.ln[1] + "//"), scwin.c.dataTypes = scwin.v.trim(scwin.c.dataType || "*").toLowerCase().split(scwin.y), scwin.c.crossDomain == null && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.a = scwin.wn.exec(scwin.c.url.toLowerCase()), scwin.c.crossDomain = !(!scwin.a || scwin.a[1] === scwin.ln[1] && scwin.a[2] === scwin.ln[2] && (scwin.a[3] || (scwin.a[1] === "http:" ? 80 : 443)) == (scwin.ln[3] || (scwin.ln[1] === "http:" ? 80 : 443)))), scwin.c.data && scwin.c.processData && typeof scwin.c.data != "string" && (scwin.c.data = scwin.v.param(scwin.c.data, scwin.c.traditional)), kn(scwin.Sn, scwin.c, scwin.n, scwin.x);
        if (scwin.E === 2) return scwin.x;
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.f = scwin.c.global, scwin.c.type = scwin.c.type.toUpperCase(), scwin.c.hasContent = !scwin.vn.test(scwin.c.type), scwin.f && scwin.v.active++ === 0 && scwin.v.event.trigger("ajaxStart");
        if (!scwin.c.hasContent) {

            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.c.data && (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.c.url += (scwin.gn.test(scwin.c.url) ? "&" : "?") + scwin.c.data, delete scwin.c.data), scwin.r = scwin.c.url;
            if (scwin.c.cache === !1) {
                var N = scwin.v.now(),
                    C = scwin.c.url.replace(scwin.bn, "$1_=" + scwin.N);
                scwin.c.url = scwin.C + (scwin.C === scwin.c.url ? (scwin.gn.test(scwin.c.url) ? "&" : "?") + "_=" + scwin.N : "");
            }
        }
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        (scwin.c.data && scwin.c.hasContent && scwin.c.contentType !== !1 || scwin.n.contentType) && scwin.x.setRequestHeader("Content-Type", scwin.c.contentType), scwin.c.ifModified && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.r = scwin.r || scwin.c.url, scwin.v.lastModified[scwin.r] && scwin.x.setRequestHeader("If-Modified-Since", scwin.v.lastModified[scwin.r]), scwin.v.etag[scwin.r] && scwin.x.setRequestHeader("If-None-Match", scwin.v.etag[scwin.r])), scwin.x.setRequestHeader("Accept", scwin.c.dataTypes[0] && scwin.c.accepts[scwin.c.dataTypes[0]] ? scwin.c.accepts[scwin.c.dataTypes[0]] + (scwin.c.dataTypes[0] !== "*" ? ", " + scwin.Tn + "; q=0.01" : "") : scwin.c.accepts["*"]);
        for (scwin.l in scwin.c.headers) scwin.x.setRequestHeader(scwin.l, scwin.c.headers[scwin.l]);
        if (!scwin.c.beforeSend || scwin.c.beforeSend.call(scwin.h, scwin.x, scwin.c) !== !1 && scwin.E !== 2) {
            scwin.S = "abort";
            for (scwin.l in {
                    success: 1,
                    error: 1,
                    complete__1: complete__1
                }) scwin.x[scwin.l](scwin.c[scwin.l]);
            scwin.o = kn(scwin.xn, scwin.c, scwin.n, scwin.x);
            if (!scwin.o) scwin.T(-1, "No Transport");

            else {

                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.x.readyState = 1, scwin.f && scwin.p.trigger("ajaxSend", [scwin.x, scwin.c]), scwin.c.async && scwin.c.timeout > 0 && (scwin.u = setTimeout(function() {
                    scwin.x.abort("timeout");
                }, scwin.c.timeout));
                try {

                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.E = 1, scwin.o.send(scwin.b, scwin.T);
                } catch (k) {
                    if (!(scwin.E < 2)) throw scwin.k;
                    scwin.T(-1, scwin.k);
                }
            }
            return scwin.x;
        }
        return scwin.x.abort();
    },
    active__0: active__0,
    lastModified: {},
    etag: {}
});
scwin.Mn = [], scwin._n = /\?/, scwin.Dn = /(=)\?(?=&|$)|\?\?/, scwin.Pn = scwin.v.now();
// ********************************************************************
// [WebSquare 전환 추가] 원본 소스와 비교 필요
// 
// ********************************************************************
scwin.v.ajaxSetup({
    jsonp: "callback",
    jsonpCallback: function() {
        var e = scwin.Mn.pop() || scwin.v.expando + "_" + scwin.Pn++;
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        this[e] = !0, e;
    }
}), scwin.v.ajaxPrefilter("json jsonp", function(n, r, i) {
    var s, o, u, a = scwin.n.data,
        f = scwin.n.url,
        l = scwin.n.jsonp !== !1,
        c = scwin.l && scwin.Dn.test(scwin.f),
        h = scwin.l && !scwin.c && typeof scwin.a == "string" && !(scwin.n.contentType || "").indexOf("application/x-www-form-urlencoded") && scwin.Dn.test(scwin.a);
    if (scwin.n.dataTypes[0] === "jsonp" || scwin.c || scwin.h) return
    // ********************************************************************
    // [WebSquare 전환 추가] 원본 소스와 비교 필요
    // 
    // ********************************************************************
    scwin.s = scwin.n.scwin.jsonpCallback = scwin.v.isFunction(scwin.n.jsonpCallback) ? scwin.n.jsonpCallback() : scwin.n.jsonpCallback, scwin.o = e[scwin.s], scwin.c ? scwin.n.url = scwin.f.replace(scwin.Dn, "$1" + scwin.s) : scwin.h ? scwin.n.data = scwin.a.replace(scwin.Dn, "$1" + scwin.s) : scwin.l && (scwin.n.url += (scwin._n.test(scwin.f) ? "&" : "?") + scwin.n.jsonp + "=" + scwin.s), scwin.n.converters["script json"] = function() {
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.u || scwin.v.error(scwin.s + " was not called"), scwin.u[0];
    }, scwin.n.dataTypes[0] = "json", e[scwin.s] = function() {
        scwin.u = arguments;
    }, scwin.i.always(function() {

        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        e[scwin.s] = scwin.o, scwin.n[scwin.s] && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.n.scwin.jsonpCallback = scwin.r.jsonpCallback, scwin.Mn.push(scwin.s)), scwin.u && scwin.v.isFunction(scwin.o) && scwin.o(scwin.u[0]), scwin.u = scwin.o = t;
    }), "script";
}), scwin.v.ajaxSetup({
    accepts: {
        script: "text/javascript, application/javascript, application/ecmascript, application/x-ecmascript"
    },
    contents: {
        script: /javascript|ecmascript/
    },
    converters: {
        "text script": function(e) {
            return
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.v.globalEval(e), e;
        }
    }
}), scwin.v.ajaxPrefilter("script", function(e) {

    // ********************************************************************
    // [WebSquare 전환 추가] 원본 소스와 비교 필요
    // 
    // ********************************************************************
    e.cache === t && (e.cache = !1), e.crossDomain && (
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        e.type = "GET", e.global = !1);
}), scwin.v.ajaxTransport("script", function(e) {
    if (e.crossDomain) {
        var n, r = scwin.i.head || scwin.i.getElementsByTagName("head")[0] || scwin.i.documentElement;
        return {
            send: function(s, o) {

                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.n = scwin.i.createElement("script"), scwin.n.async = "async", e.scriptCharset && (scwin.n.charset = e.scriptCharset), scwin.n.src = e.url, scwin.n.onload = scwin.n.onreadystatechange = function(e, i) {
                    if (scwin.i || !scwin.n.readyState || /loaded|complete/.test(scwin.n.readyState))
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        scwin.n.onload = scwin.n.onreadystatechange = null, scwin.r && scwin.n.parentNode && scwin.r.removeChild(scwin.n), scwin.n = t, scwin.i || scwin.o(200, "success");
                }, scwin.r.insertBefore(scwin.n, scwin.r.firstChild);
            },
            abort: function() {
                scwin.n && scwin.n.onload(0, 1);
            }
        };
    }
});
scwin.Hn, scwin.Bn = e.ActiveXObject ? function() {
    for (var e in scwin.Hn) scwin.Hn[e](0, 1);
} : !1, scwin.jn = 0;
// ********************************************************************
// [WebSquare 전환 추가] 원본 소스와 비교 필요
// 
// ********************************************************************
scwin.v.ajaxSettings.xhr = e.ActiveXObject ? function() {
        return !this.isLocal && scwin.Fn() || scwin.In();
    } : Fn,
    function(e) {
        scwin.v.extend(scwin.v.support, {
            ajax: e,
            cors: e && "withCredentials" in e
        });
    }(scwin.v.ajaxSettings.xhr()), scwin.v.support.ajax && scwin.v.ajaxTransport(function(n) {
        if (!scwin.n.crossDomain || scwin.v.support.cors) {
            var r;
            return {
                send: function(i, s) {
                    var o, u, a = scwin.n.xhr();
                    scwin.n.username ? scwin.a.open(scwin.n.type, scwin.n.url, scwin.n.async, scwin.n.username, scwin.n.password) : scwin.a.open(scwin.n.type, scwin.n.url, scwin.n.async);
                    if (scwin.n.xhrFields)
                        for (scwin.u in scwin.n.xhrFields) scwin.a[scwin.u] = scwin.n.xhrFields[scwin.u];
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.n.mimeType && scwin.a.overrideMimeType && scwin.a.overrideMimeType(scwin.n.mimeType), !scwin.n.crossDomain && !scwin.i["X-Requested-With"] && (scwin.i["X-Requested-With"] = "XMLHttpRequest");
                    try {
                        for (scwin.u in scwin.i) scwin.a.setRequestHeader(scwin.u, scwin.i[scwin.u]);
                    } catch (f) {

                    }
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.a.send(scwin.n.hasContent && scwin.n.data || null), scwin.r = function(e, i) {
                        var u, f, l, c, h;
                        try {
                            if (scwin.r && (scwin.i || scwin.a.readyState === 4)) {

                                // ********************************************************************
                                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                                // 
                                // ********************************************************************
                                scwin.r = t, scwin.o && (
                                    // ********************************************************************
                                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                                    // 
                                    // ********************************************************************
                                    scwin.a.onreadystatechange = scwin.v.noop, scwin.Bn && delete scwin.Hn[scwin.o]);
                                if (scwin.i) scwin.a.readyState !== 4 && scwin.a.abort();

                                else {

                                    // ********************************************************************
                                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                                    // 
                                    // ********************************************************************
                                    scwin.u = scwin.a.status, scwin.l = scwin.a.getAllResponseHeaders(), scwin.c = {}, scwin.h = scwin.a.responseXML, scwin.h && scwin.h.documentElement && (scwin.c.xml = scwin.h);
                                    try {
                                        scwin.c.setValue(scwin.a.responseText);
                                    } catch (p) {

                                    }
                                    try {
                                        scwin.f = scwin.a.statusText;
                                    } catch (p) {
                                        scwin.f = "";
                                    }!scwin.u && scwin.n.isLocal && !scwin.n.crossDomain ? scwin.u = scwin.c.getText() ? 200 : 404 : scwin.u === 1223 && (scwin.u = 204);
                                }
                            }
                        } catch (d) {
                            scwin.i || scwin.s(-1, scwin.d);
                        }
                        scwin.c && scwin.s(scwin.u, scwin.f, scwin.c, scwin.l);
                    }, scwin.n.async ? scwin.a.readyState === 4 ? setTimeout(scwin.r, 0) : (
                        // ********************************************************************
                        // [WebSquare 전환 추가] 원본 소스와 비교 필요
                        // 
                        // ********************************************************************
                        scwin.o = scwin.jn++, scwin.Bn && (
                            // ********************************************************************
                            // [WebSquare 전환 추가] 원본 소스와 비교 필요
                            // 
                            // ********************************************************************
                            scwin.Hn || (
                                // ********************************************************************
                                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                                // 
                                // ********************************************************************
                                scwin.Hn = {}, scwin.v(e).unload(scwin.Bn)), scwin.Hn[scwin.o] = scwin.r), scwin.a.onreadystatechange = scwin.r) : scwin.r();
                },
                abort: function() {
                    scwin.r && scwin.r(0, 1);
                }
            };
        }
    });
scwin.qn, scwin.Rn, scwin.Un = /^(?:toggle|show|hide)$/, scwin.zn = new RegExp("^(?:([-+])=|)(" + scwin.m + ")([a-z%]*)$", "i"), scwin.Wn = /queueHooks$/, scwin.Xn = [Gn], scwin.Vn = {
    "*": [function(e, t) {
        var n, r, i = this.createTween(e, t),
            s = scwin.zn.exec(t),
            o = scwin.i.cur(),
            u = +scwin.o || 0,
            a = 1,
            f = 20;
        if (scwin.s) {

            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.n = +scwin.s[2], scwin.r = scwin.s[3] || (scwin.v.cssNumber[e] ? "" : "px");
            if (scwin.r !== "px" && scwin.u) {
                scwin.u = scwin.v.css(scwin.i.elem, e, !0) || scwin.n || 1;
                do
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.a = scwin.a || ".5", scwin.u /= scwin.a, scwin.v.style(scwin.i.elem, e, scwin.u + scwin.r); while (scwin.a !== (scwin.a = scwin.i.cur() / scwin.o) && scwin.a !== 1 && scwin.f--);
            }
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.i.unit = scwin.r, scwin.i.start = scwin.u, scwin.i.end = scwin.s[1] ? scwin.u + (scwin.s[1] + 1) * scwin.n : scwin.n;
        }
        return scwin.i;
    }]
};
// ********************************************************************
// [WebSquare 전환 추가] 원본 소스와 비교 필요
// 
// ********************************************************************
scwin.v.Animation = scwin.v.extend(Kn, {
    tweener: function(e, t) {
        scwin.v.isFunction(e) ? (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            t = e, e = ["*"]) : e = e.split(" ");
        var n, r = 0,
            i = e.length;
        for (; scwin.r < scwin.i; scwin.r++)
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.n = e[scwin.r], scwin.Vn[scwin.n] = scwin.Vn[scwin.n] || [], scwin.Vn[scwin.n].unshift(t);
    },
    prefilter: function(e, t) {
        t ? scwin.Xn.unshift(e) : scwin.Xn.push(e);
    }
}), scwin.v.Tween = Yn, Yn.prototype = {
    constructor: Yn,
    init: function(e, t, n, r, i, s) {

        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        this.elem = e, this.prop = scwin.n, this.easing = scwin.i || "swing", this.options = t, this.start = this.now = this.cur(), this.end = scwin.r, this.unit = scwin.s || (scwin.v.cssNumber[scwin.n] ? "" : "px");
    },
    cur: function() {
        var e = Yn.propHooks[this.prop];
        return e && e.get ? e.get(this) : Yn.propHooks._default.get(this);
    },
    run: function(e) {
        var t, n = Yn.propHooks[this.prop];
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        this.options.duration ? this.pos = t = scwin.v.easing[this.easing](e, this.options.duration * e, 0, 1, this.options.duration) : this.pos = t = e, this.now = (this.end - this.start) * t + this.start, this.options.step && this.options.step.call(this.elem, this.now, this), scwin.n && scwin.n.set ? scwin.n.set(this) : Yn.propHooks._default.set(this), this;
    }
}, Yn.prototype.init.prototype = Yn.prototype, Yn.propHooks = {
    _default: {
        get: function(e) {
            var t;
            return e.elem[e.prop] == null || e.elem.style && e.elem.style[e.prop] != null ? (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                t = scwin.v.css(e.elem, e.prop, !1, ""), !t || t === "auto" ? 0 : t) : e.elem[e.prop];
        },
        set: function(e) {
            scwin.v.fx.step[e.prop] ? scwin.v.fx.step[e.prop](e) : e.elem.style && (e.elem.style[scwin.v.cssProps[e.prop]] != null || scwin.v.cssHooks[e.prop]) ? scwin.v.style(e.elem, e.prop, e.now + e.unit) : e.elem[e.prop] = e.now;
        }
    }
}, Yn.propHooks.scrollTop = Yn.propHooks.scrollLeft = {
    set: function(e) {
        e.elem.nodeType && e.elem.parentNode && (e.elem[e.prop] = e.now);
    }
}, scwin.v.each(["toggle", "show", "hide"], function(e, t) {
    var n = scwin.v.fn[t];
    scwin.v.fn[t] = function(r, i, s) {
        return scwin.r == null || typeof scwin.r == "boolean" || !e && scwin.v.isFunction(scwin.r) && scwin.v.isFunction(scwin.i) ? scwin.n.apply(this, arguments) : this.animate(scwin.Zn(t, !0), scwin.r, scwin.i, scwin.s);
    };
}), scwin.v.fn.extend({
    fadeTo: function(e, t, n, r) {
        return this.filter(Gt).css("opacity", 0).show().end().animate({
            opacity: t
        }, e, scwin.n, scwin.r);
    },
    animate__function: function(e, t, n, r) {
        var i = scwin.v.isEmptyObject(e),
            s = scwin.v.speed(t, scwin.n, scwin.r),
            o = function() {
                var t = scwin.Kn(this, scwin.v.extend({}, e), scwin.s);
                scwin.i && t.stop(!0);
            };
        return scwin.i || scwin.s.queue === !1 ? this.each(scwin.o) : this.queue(scwin.s.queue, scwin.o);
    },
    stop: function(e, n, r) {
        var i = function(e) {
            var t = e.stop;
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            delete e.stop, t(scwin.r);
        };
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        typeof e != "string" && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.r = scwin.n, scwin.n = e, e = t), scwin.n && e !== !1 && this.queue(e || "fx", []), this.each(function() {
            var t = !0,
                n = e != null && e + "queueHooks",
                s = scwin.v.timers,
                o = scwin.v._data(this);
            if (scwin.n) scwin.o[scwin.n] && scwin.o[scwin.n].stop && scwin.i(scwin.o[scwin.n]);

            else
                for (scwin.n in scwin.o) scwin.o[scwin.n] && scwin.o[scwin.n].stop && scwin.Wn.test(scwin.n) && scwin.i(scwin.o[scwin.n]);
            for (scwin.n = scwin.s.length; scwin.n--;) scwin.s[scwin.n].elem === this && (e == null || scwin.s[scwin.n].queue === e) && (
                // ********************************************************************
                // [WebSquare 전환 추가] 원본 소스와 비교 필요
                // 
                // ********************************************************************
                scwin.s[scwin.n].anim.stop(scwin.r), t = !1, scwin.s.splice(scwin.n, 1));
            (t || !scwin.r) && scwin.v.dequeue(this, e);
        });
    }
}), scwin.v.each({
    slideDown: scwin.Zn("show"),
    slideUp: scwin.Zn("hide"),
    slideToggle__Zn: function([object Object]) {

        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************

        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************

        X, fadeIn;
        {

        }
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************

        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************

        X, fadeOut;
        {

        }
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************

        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************

        X, fadeToggle;
        {

        }
    },
    function: function(e, t) {
        scwin.v.fn[e] = function(e, n, r) {
            return this.animate(t, e, scwin.n, scwin.r);
        };
    },
    v: scwin.v,
    v: scwin.v,
    linear: function(e) {
        return e;
    },
    swing: function(e) {
        return .5 - Math.cos(e * Math.PI) / 2;
    }
}, scwin.v.timers = [], scwin.v.fx = Yn.prototype.init, scwin.v.fx.tick = function() {
    var e, n = scwin.v.timers,
        r = 0;
    scwin.qn = scwin.v.now();
    for (; scwin.r < scwin.n.length; scwin.r++)
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        e = scwin.n[scwin.r], !e() && scwin.n[scwin.r] === e && scwin.n.splice(scwin.r--, 1);
    // ********************************************************************
    // [WebSquare 전환 추가] 원본 소스와 비교 필요
    // 
    // ********************************************************************
    scwin.n.length || scwin.v.fx.stop(), scwin.qn = t;
}, scwin.v.fx.timer = function(e) {
    e() && scwin.v.timers.push(e) && !scwin.Rn && (scwin.Rn = setInterval(scwin.v.fx.tick, scwin.v.fx.interval));
}, scwin.v.fx.interval = 13, scwin.v.fx.stop = function() {

    // ********************************************************************
    // [WebSquare 전환 추가] 원본 소스와 비교 필요
    // 
    // ********************************************************************
    clearInterval(scwin.Rn), scwin.Rn = null;
}, scwin.v.fx.speeds = {
    slow: 600,
    fast: 200,
    _default: 400
}, scwin.v.fx.step = {}, scwin.v.expr && scwin.v.expr.filters && (scwin.v.expr.filters.animated = function(e) {
    return scwin.v.grep(scwin.v.timers, function(t) {
        return e === t.elem;
    }).length;
}));
scwin.er = /^(?:body|html)$/i;
// ********************************************************************
// [WebSquare 전환 추가] 원본 소스와 비교 필요
// 
// ********************************************************************
scwin.v.fn.offset = function(e) {
    if (arguments.length) return e === t ? this : this.each(function(t) {
        scwin.v.offset.setOffset(this, e, t);
    });
    var n, r, i, s, o, u, a, f = {
            top: 0,
            left: 0
        },
        l = this[0],
        c = scwin.l && scwin.l.ownerDocument;
    if (!scwin.c) return;
    return (scwin.r = scwin.c.body) === scwin.l ? scwin.v.offset.bodyOffset(scwin.l) : (
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.n = scwin.c.documentElement, scwin.v.contains(scwin.n, scwin.l) ? (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            typeof scwin.l.getBoundingClientRect != "undefined" && (scwin.f = scwin.l.getBoundingClientRect()), scwin.i = scwin.tr(scwin.c), scwin.s = scwin.n.clientTop || scwin.r.clientTop || 0, scwin.o = scwin.n.clientLeft || scwin.r.clientLeft || 0, scwin.u = scwin.i.pageYOffset || scwin.n.scrollTop, scwin.a = scwin.i.pageXOffset || scwin.n.scrollLeft, {
                top: scwin.f.top + scwin.u - scwin.s,
                left: scwin.f.left + scwin.a - scwin.o
            }) : scwin.f);
}, scwin.v.offset = {
    bodyOffset: function(e) {
        var t = e.offsetTop,
            n = e.offsetLeft;
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.v.support.doesNotIncludeMarginInBodyOffset && (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            t += parseFloat(scwin.v.css(e, "marginTop")) || 0, scwin.n += parseFloat(scwin.v.css(e, "marginLeft")) || 0), {
            top: t,
            left: scwin.n
        };
    },
    setOffset: function(e, t, n) {
        var r = scwin.v.css(e, "position");
        scwin.r === "static" && (e.style.position = "relative");
        var i = scwin.v(e),
            s = scwin.i.offset(),
            o = scwin.v.css(e, "top"),
            u = scwin.v.css(e, "left"),
            a = (scwin.r === "absolute" || scwin.r === "fixed") && scwin.v.inArray("auto", [scwin.o, scwin.u]) > -1,
            f = {},
            l = {},
            c, h;
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.a ? (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.l = scwin.i.position(), scwin.c = scwin.l.top, scwin.h = scwin.l.left) : (
            // ********************************************************************
            // [WebSquare 전환 추가] 원본 소스와 비교 필요
            // 
            // ********************************************************************
            scwin.c = parseFloat(scwin.o) || 0, scwin.h = parseFloat(scwin.u) || 0), scwin.v.isFunction(t) && (t = t.call(e, scwin.n, scwin.s)), t.top != null && (scwin.f.top = t.top - scwin.s.top + scwin.c), t.left != null && (scwin.f.left = t.left - scwin.s.left + scwin.h), "using" in t ? t.using.call(e, scwin.f) : scwin.i.css(scwin.f);
    }
}, scwin.v.fn.extend({
    position: function() {
        if (!this[0]) return;
        var e = this[0],
            t = this.offsetParent(),
            n = this.offset(),
            r = scwin.er.test(t[0].nodeName) ? {
                top: 0,
                left: 0
            } : t.offset();
        return
        // ********************************************************************
        // [WebSquare 전환 추가] 원본 소스와 비교 필요
        // 
        // ********************************************************************
        scwin.n.top -= parseFloat(scwin.v.css(e, "marginTop")) || 0, scwin.n.left -= parseFloat(scwin.v.css(e, "marginLeft")) || 0, scwin.r.top += parseFloat(scwin.v.css(t[0], "borderTopWidth")) || 0, scwin.r.left += parseFloat(scwin.v.css(t[0], "borderLeftWidth")) || 0, {
            top: scwin.n.top - scwin.r.top,
            left: scwin.n.left - scwin.r.left
        };
    },
    offsetParent: function() {
        return this.map(function() {
            var e = this.offsetParent || scwin.i.body;
            while (e && !scwin.er.test(e.nodeName) && scwin.v.css(e, "position") === "static") e = e.offsetParent;
            return e || scwin.i.body;
        });
    }
}), scwin.v.each({
    scrollLeft: "pageXOffset",
    scrollTop: "pageYOffset"
}, function(e, n) {
    var r = /Y/.test(scwin.n);
    scwin.v.fn[e] = function(i) {
        return scwin.v.access(this, function(e, i, s) {
            var o = scwin.tr(e);
            if (scwin.s === t) return scwin.o ? scwin.n in scwin.o ? scwin.o[scwin.n] : scwin.o.document.documentElement[scwin.i] : e[scwin.i];
            scwin.o ? scwin.o.scrollTo(scwin.r ? scwin.v(scwin.o).scrollLeft() : scwin.s, scwin.r ? scwin.s : scwin.v(scwin.o).scrollTop()) : e[scwin.i] = scwin.s;
        }, e, scwin.i, arguments.length, null);
    };
}), scwin.v.each({
    Height: "height",
    Width: "width"
}, function(e, n) {
    scwin.v.each({
        padding: "inner" + e,
        content: scwin.n,
        "": "outer" + e
    }, function(r, i) {
        scwin.v.fn[scwin.i] = function(i, s) {
            var o = arguments.length && (scwin.r || typeof scwin.i != "boolean"),
                u = scwin.r || (scwin.i === !0 || scwin.s === !0 ? "margin" : "border");
            return scwin.v.access(this, function(n, r, i) {
                var s;
                return scwin.v.isWindow(scwin.n) ? scwin.n.document.documentElement["client" + e] : scwin.n.nodeType === 9 ? (
                    // ********************************************************************
                    // [WebSquare 전환 추가] 원본 소스와 비교 필요
                    // 
                    // ********************************************************************
                    scwin.s = scwin.n.documentElement, Math.max(scwin.n.body["scroll" + e], scwin.s["scroll" + e], scwin.n.body["offset" + e], scwin.s["offset" + e], scwin.s["client" + e])) : scwin.i === t ? scwin.v.css(scwin.n, scwin.r, scwin.i, scwin.u) : scwin.v.style(scwin.n, scwin.r, scwin.i, scwin.u);
            }, scwin.n, scwin.o ? scwin.i : t, scwin.o, null);
        };
    });
}), e.jQuery = e.scwin.$ = scwin.v, typeof define == "function" && define.amd && define.amd.jQuery && define("jquery", [], function() {
    return scwin.v;
});
(window);
]]></script>
	</head>
	<body ev:onpageload="" ev:onpageunload=""/>
</html>