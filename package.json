{
  "name": "krx-craft-websquare",
  "version": "0.1.0",
  "private": true,
  "description": "KRX_Craft WebSquare (Inswave RIA) pages — gcc/ins/mgt/stf XML tree — with ESLint + Jest + wsxml_lint toolchain.",
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "lint:xml": "npm run lint:xml:gcc && npm run lint:xml:legacy",
    "lint:xml:gcc": "python -m wsxml_lint gcc",
    "lint:xml:legacy": "python -m wsxml_lint ins mgt stf --ignore WS111,WS112,WS113",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "ci": "npm run lint && npm run test:coverage"
  },
  "devDependencies": {
    "eslint": "^10.4.1",
    "globals": "^17.6.0",
    "jest": "^29.7.0"
  }
}
