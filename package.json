{
  "name": "krx-craft-websquare",
  "version": "0.1.0",
  "private": true,
  "description": "KRX_Craft WebSquare (Inswave RIA) pages — gcc/ins/mgt/stf XML tree — with ESLint + Jest + wsxml_lint toolchain.",
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "lint:xml": "npm run lint:xml:gcc && npm run lint:xml:legacy",
    "lint:xml:gcc": "python -m wsxml_lint src/gcc",
    "lint:xml:legacy": "python -m wsxml_lint src/as-is/ins src/as-is/mgt src/as-is/stf src/as-is/fil --ignore WS111,WS112,WS113",
    "docs:gcc": "python -m wsxml_lint.docgen src/gcc -o src/docs/api/gcc --title \"gcc API 문서\"",
    "docs:transfer": "python src/conversion/tools/gen_index_transfer.py",
    "test:transfer": "pytest src/conversion/tools/test_gen_index_transfer.py",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "install:wsxml": "pip install \"./tools/wsxml_lint[test]\"",
    "test:wsxml": "npm run install:wsxml && pytest tools/wsxml_lint",
    "ci": "npm run lint && npm run test:coverage && npm run test:wsxml && npm run lint:xml"
  },
  "devDependencies": {
    "eslint": "^10.4.1",
    "globals": "^17.6.0",
    "jest": "^29.7.0"
  }
}
